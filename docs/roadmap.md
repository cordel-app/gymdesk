# Gymdesk Roadmap

Phased implementation roadmap derived from the target ER model, adapted to the existing
multi-tenant architecture (every table gets `gym_id`; the diagram's monolithic `User` maps
onto the existing `members` + `gym_memberships` split — no new users table).

Each ticket below is a GitHub issue in `cordel-app/gymdesk`. Full scope and acceptance
criteria live in the issue bodies; this doc is the map.

Engineering quality backlog (tests, typing, helpers, member-app gap analysis) lives in
`docs/tech-debt.md` as a **map** of GitHub issues (#81–#86) — same model as this roadmap.
Agent session prompts: `docs/agent-prompts.md`. Always implement via the GitHub issue body.

## Status (2026-09-07)

- **Done**: Phase M (#45–#49, MySQL cutover 2026-07-04), P0.1–P0.3, P1.1–P1.8, P2.1–P2.8, P3.1–P3.4, P4.1–P4.5, P5.1–P5.6, P6.1–P6.3, #114, #117, #120, #121, #123, #124, #127, #129, #131, #132, #135, #144, #154, #156, #262 (Split-view calendar editor with inline delete: CalendarEventModal replaced by EventDetailsPanel right-side panel; inline delete confirmation; metadata section (created/updated/deleted by+at); backend SELECT extended with created_by_name/modified_by_name/deleted_by_name JOINs) (RBAC permission matrix — 7 granular roles + module gates), #157 (Membership Plans inline editing, Details modal, Duplicate action, Created By/At columns, paused enrollment status), #159 (move Payment Providers to Financials), #160 (Training Plans UX: Conclude Plan action, list columns, expired validation, Details dialog fixes), #161 (Specialities UX alignment), #164 (Training Plan workout builder parity with Workout Templates), #158 (Promotions management UX: Edit action in context menu, read-only rows, restructured list columns with Type/Value/Period/Created By, expanded Details modal with full audit info), #177 (pino structured logging), #178 (promtail config files — deploy to VPS to activate), #162 (Meals sub-entities UX alignment), #163 (Nutrition Library + redesigned Nutrition Plan Templates), #188 (Theme Model v2 + Editor UX: color token hard-switch to v2 shape with 26 grouped colors + dropdownBackground + modalBackground advanced field; inactive lifecycle status; StatusBadge in table row + context menu for status changes; auto-save for Colors/Typography/Advanced; explicit Save/Cancel only for Branding section; protection guard blocks draft/inactive/delete when assigned; assign/set-default gated to active themes only; renamed org_default → gym_default), #189 (User Impersonation UX v2: header Impersonate button + search dialog in admin and member apps; full member app support with AdminBar, ImpersonationBanner, MemberImpersonationDialog; account action lockout during impersonation; gym selector restricted to impersonated user's gyms via gymIds; all /me/* handlers use effectiveUserId from TenantContext; GET /platform/impersonation/candidates endpoint; impersonation.test.ts with 19 tests), #192 (Calendar Event Bookings & Waiting List: new `event_bookings` table + FIFO waitlist with auto-promote on cancel and on capacity increase; admin roster UI with expandable rows, confirmed/waitlist split, Add Member, Remove, Promote actions, FULL badge; member schedule page shows Calendar Events inline with Book/Join Waitlist/Cancel/Leave Waitlist buttons; cancellation policy: allowed if event starts >24h from now OR booking made <4h ago; waitlisted always cancel freely; GET /me/events, POST /me/event-bookings, DELETE /me/event-bookings/:id; event-bookings.test.ts with 23 tests), #190 (Calendar Foundation & Calendar Events: new `calendar_events` table with full audit/soft-delete; `activity_types` extended with `default_space_id`, `default_trainer_membership_id`, `color`; `/calendar-events` router with CRUD, space/trainer conflict detection (409), drag-and-drop and resize via PUT; FullCalendar v6 admin UI with day/week/month views, four filter modes (all/space/activity_type/trainer), CalendarEventModal with activity-type auto-populate; `/events` admin page removed; calendar-events.test.ts with 15 tests), #191 (Recurring Calendar Events: new `calendar_event_series` table stores recurrence template; `calendar_events` gains `series_id` FK + `series_occurrence_date` DATE; pure-TS `recurrenceEngine.ts` generates occurrence dates (daily/weekdays/weekends/weekly/monthly/yearly, interval, end by date/count/never, 13-month cap); "exploded" model — each occurrence is an independent DB row; scope-based edits (this / this_and_following / entire_series) via `scope` query param on PUT/DELETE; `this_and_following` splits series at cutDate; `entire_series` updates template + regenerates future occurrences; standalone cancellation uses `status='cancelled'`; series delete cancels future + soft-deletes series row; scope picker dialog in admin UI before editing/deleting recurring events; RecurrenceEditor component in CalendarEventModal; GET/DELETE `/calendar-event-series` router; `toDateStr()` normalizer for MySQL DATE↔JS Date driver discrepancy; calendar-event-series.test.ts with 13 tests), #193 (Attendance Management for Calendar Events / Class Sessions: new `attendance_status VARCHAR(20) DEFAULT 'pending'` CHECK ('pending','present','absent') column on `bookings` — separate from the booking lifecycle `status` column; backfill migrated old 'attended'→'present' and 'no_show'→'absent' then reset those rows' status to 'booked'; `bookings.status` CHECK reduced to ('booked','waitlisted','cancelled'); `attendance_recorded_at DATETIME` + `attendance_recorded_by_membership_id FK→gym_memberships` audit fields on `bookings`; `effective_trainer_membership_id FK→gym_memberships` + `effective_trainer_confirmed_at DATETIME` on `class_sessions` to record who actually delivered the session; new endpoints: `POST /bookings/:id/attendance` (requireRole admin/front_desk/trainer), `POST /class-sessions/:id/bulk-present` (marks all pending booked→present), `PUT /class-sessions/:id/effective-trainer`, `POST /class-sessions/:id/complete` (hard-blocked until all booked bookings have non-pending attendance_status AND at least one trainer field is set — returns 400 with {pending_count, missing_trainer}); `POST /bookings` gains `force=true` to add walk-ins over capacity (returns over_capacity:true flag); admin SessionRosterPanel rewritten with attendance progress header (present/pending/absent pills), per-booking attendance toggle buttons, bulk-present, effective trainer selector, complete session flow with ConfirmDialog and error feedback; attendance.test.ts with 21 tests), #210 (Theme Token Mapping + Editor Organization: `--gd-card-bg` applied to all expandable card containers across Themes, Spaces, Specialities, Centers, Promotions, Staff, Plans, Training Plans, Training Plan Templates, Workout Template blocks; sidebar hover background wired via `onMouseEnter`/`onMouseLeave` in `NavGroup` using `--gd-sidebar-hover-bg`; selected item text and background use `--gd-sidebar-selected-text`/`--gd-sidebar-selected-bg` CSS vars instead of hardcoded values; `Separator Height (px)` moved into the Header section of the Colors editor in both gym themes and base themes pages), #194 (Calendar Notifications & Member Experience: new `member_notifications` table (migration 087) with fire-and-forget `sendNotification`/`sendBulkNotification` helpers; 7 notification types: booking_confirmed, waitlist_joined, promoted_from_waitlist, event_cancelled, event_updated, booking_reminder_24h, booking_reminder_1h; notifications written on book/cancel/session-cancel flows; GET /me/notifications (paginated + unread count), GET /me/notifications/count, PUT /me/notifications/:id/read, PUT /me/notifications/read-all; GET /me/upcoming (next 30-day confirmed bookings, sessions + events combined), GET /me/activity-history (past bookings paginated); GET /me/schedule fixed to join activity_types + spaces instead of class_types + rooms, adds trainer_name + can_cancel; member app: Notifications page with mark-all-read and per-item read tracking, bell tab in BottomNav with unread badge, unreadNotifications count in AppContext), #211 (Plan Lifecycle & Enrollment Status refinement: lifecycle_status values changed to draft/active/paused/inactive — `archived` renamed to `inactive`; enrollment_status values changed to public/staff_only/closed — `open` renamed to `public`, enrollment-`paused` merged into `closed`, `staff_only` added for internal-only plans; data migration 088; backend validation updated; archive endpoint sets lifecycle=inactive; i18n adds `public`/`staff_only` keys to en/ca/es; frontend types, constants, and badge mappings updated; membership-plans.test.ts added), #212 (Plan Description persistence: added `cache: 'no-store'` to the Next.js proxy `fetch` so the data cache no longer serves stale GET responses after a PUT; Details modal now always shows Description, Last Modified At, and Last Modified By — each falls back to '—' instead of hiding the field), #213 (Plans UX Redesign: `+ Add Plan` creates an inline-editable row; Edit from context menu opens expanded area in edit mode; Duplicate creates a new editable row inline; new Charge Benefits section with all 4 charge types (no_benefit/waive/percentage_discount/fixed_discount); new Period Benefits section (all gym activity_types with enabled checkbox + quantity + frequency); Centers shown as checkbox grid in expanded area; Details modal gains `deleted_by_name`; new `plan_charge_benefits` table (migration 089) with `GET /:id/charge-benefits` + `PUT /:id/charge-benefits`; duplicate endpoint migrated to `db.transaction()`; enriched plan response includes `charge_benefits` array; 14 new API tests in `plan-charge-benefits.test.ts`), #218 (Spaces UX redesign: `+ New Space` creates an inline editable row without pre-calling API; clicking row toggles read-only accordion with General/Availability/Activity Types/Notes sections; Edit action in context menu opens inline edit form; StatusBadge replaces inline status selector; Details modal shows full audit info including `created_by_name`, `modified_by_name`, `deleted_by_name`; API SELECT extended to LEFT JOIN `gym_memberships` three times for those names), #215 (Gym Charges Configuration: new `gym_charges` table (migration 088) — per-gym configuration of 6 predefined charge types (registration_fee, insurance_fee, locker_rental, parking_fee, access_key, premium_fitness_app); `charge_types` extended with `name VARCHAR(100)` and `is_gym_charge TINYINT(1)` flag; 4 new charge type rows seeded; `gym_charges` rows auto-created at gym creation via INSERT IGNORE; Finance → Gym Charges page with expandable-row list, context menu (Details / Edit), read-only Details modal with full audit info, explicit inline edit mode with Save/Cancel; no Add or Delete actions; `GET /gym-charges`, `GET /gym-charges/:id`, `PUT /gym-charges/:id` (admin-only) under FINANCIALS module gate), #216 (Gym Charges in Plans & Promotions Benefits: migrations 091–092 replace `plan_charge_benefits.charge_type_id` and `promotion_charge_benefits.(charge_type_id + action_type_id)` with `gym_charge_id FK→gym_charges` and `action VARCHAR(30)`; Plans accordion gains inline Charge Benefits section (show-all-gym-charges grid, replace-all PUT); Promotions inline editor replaces old per-row POST/DELETE with same show-all-gym-charges pattern; `promotion-details.ts` drops POST/PUT/:cbId/DELETE endpoints, adds replace-all `PUT /charge-benefits`; `promotions.ts` GET queries drop `action_types` join, read `pcb.action` directly; `plan-charge-benefits.test.ts` updated with `gym_charge_id` fixtures and `gym_charge_code` assertions), #219 (Remove Speciality concept: dropped `specialities` and `trainer_specialities` tables, removed `speciality_id` from `class_types` and `activity_types`; deleted `/specialities` router, test file, and admin page; trainers router no longer fetches or assigns specialities; activity-types and class-types forms/tables no longer show speciality field; nav item removed; i18n keys cleaned up across en/es/ca), #214 (Promotions UX Redesign: expandable-row list, temp-row inline creation (no DB write until Save), read-only collapsed row (Name/Description/Created By/Created At/Start/End/StatusBadge/ContextMenu), Edit-only via context menu, Save writes all sub-resources atomically (plans + charge-benefits + period-benefits); soft delete (lifecycle_status='deleted', deleted_at, deleted_by_membership_id); Applicable Plans checkbox list; Charge Benefits grid (all gym charges, action dropdown); Period Benefits table (charge_type_id + quantity + frequency_interval + frequency_unit + enabled); Free Period fields (paid_months + bonus_months); Stackable checkbox in General section; Details modal shows lifecycle_status, free period, deleted audit fields; Duplicate → auto-opens duplicate in edit mode; migration 093 adds soft-delete columns, 4 new charge_types seeds, drops+recreates promotion_period_benefits with new schema; `membership-promotions.ts` updated to use `lifecycle_status` and gym_charges→charge_types join), #217 (Gym Charges in Plans & Promotions — availability filtering: `GET /gym-charges?availability=available|unavailable` filter with 400 validation; `gym_charge_availability` field added to all charge-benefit GET responses (plans + promotions); `PUT /charge-benefits` validates `availability='available'` before inserting; Plans and Promotions edit forms fetch only available gym charges; view mode shows "Unavailable" badge on saved benefits whose charge was later deactivated; `gym-charges.test.ts` with 12 tests), #221 (Remove Event Entity and Events Module: dropped `events` and `event_bookings` tables (migration 095); removed `events.ts` and `event-bookings.ts` routers; cleaned `me.ts` (removed `/me/events`, `/me/event-bookings`, event_bookings UNION legs from `/me/upcoming` and `/me/activity-history`); removed `event` entry from audit-registry; deleted `event-bookings.test.ts`; removed event keys from admin and member locales; member schedule page now session-only), #220 (Class Packages UX and Lifecycle Management: migration 094 adds `description` TEXT, `notes` TEXT, `created_by_membership_id`, `modified_at`, `modified_by_membership_id`, `deleted_at`, `deleted_by_membership_id` with SET NULL FK to gym_memberships; API updated with 3-way JOIN SELECT for names, soft-delete on DELETE, `POST /:id/duplicate` endpoint, price/validity_days default to 0 at creation; admin page fully rewritten to Spaces-style expandable-row UX — `+ New Class Package` inline creation (Name + Description + Sessions), accordion with General/Availability/Restrictions/Notes sections, context menu Details/Edit/Duplicate/Delete, inline edit form with General + Configuration + placeholder Availability/Restrictions + Notes, Details modal with full audit info; i18n updated in en/ca/es; `class-packages.test.ts` with 27 tests), #222 (Exercises Management UX and Lifecycle Management: migration 096 adds `created_by`, `modified_at`, `modified_by`, `deleted_by` audit columns with SET NULL FK to gym_memberships; API extended with audit field JOINs on all SELECTs, `getCallerMembershipId` helper on POST/PUT/DELETE, `?q=` name search on GET /, `POST /:id/duplicate` endpoint (copies muscles + result types); admin page fully rewritten to expandable-row UX — standard collapsed header (Name/Description/Primary Muscles/Created At/Created By/Status/ContextMenu), expanded view with Configuration/Muscles/Media sections, context menu Details/Edit/Duplicate/Delete, inline edit mode with General+Configuration+Muscles+Media sections, Save/Cancel; creation modal unchanged; ExerciseDetailModal with full audit info; name search + StatusFilter above list; i18n updated in en/ca/es; `exercises.test.ts` with 38 tests; fixed bug in `094_drop_specialities.js` where `hasColumn` on non-existent `class_types` threw instead of skipping), #224 (Training Plan Templates UX and Lifecycle Management: modal-based creation replaced with inline new row (`+ New Training Plan Template`); collapsed header now shows Name/Description/Workout Count/Created At/Created By/Status/ContextMenu; edit form moved inside expanded area (General section with Name/Description/Status + Save/Cancel) alongside the existing `TrainingPlanTree`; Duplicate calls `POST /:id/duplicate` then auto-opens the new record in edit mode (matches promotions pattern); context menu order: Details/Edit/Duplicate/Assign to Member (active only)/Delete; no DB or API changes — all audit columns and endpoints were already complete; i18n updated in en/ca/es with `assign_to_member`, `section_general`, `placeholder_name`, `col_description` keys), #223 (Workout Templates UX and Lifecycle Management: migration 097 adds `modified_at`, `modified_by_membership_id`, `deleted_by_membership_id` (SET NULL FK → gym_memberships), `notes TEXT` to `workout_templates`; `GET /workout-templates` now LEFT JOINs `gym_memberships` three times for `created_by_name`/`modified_by_name`/`deleted_by_name` and includes a `blocks_count` subquery; `POST /` and `POST /:id/duplicate` stamp `modified_at`/`modified_by_membership_id` on creation; `PUT /:id` stamps them on update; `DELETE /:id` stamps `deleted_by_membership_id`; `POST /:id/duplicate` race-condition fixed by moving `res.json()` outside the transaction so the commit completes before the client reads the DB; admin page fully rewritten to Spaces-style expandable-row UX — `+ New Workout Template` inline creation row, card-based list with `TemplateDropTarget` for cross-template DnD, collapsed row shows Name/blocks_count/Created By/Created At/StatusBadge/ContextMenu (Details/Edit/Duplicate/Delete), expanded area shows General/Workout Structure (`WorkoutTemplateTree`)/Notes sections; `TemplateHeader` component removed from `WorkoutTemplateTree` (metadata now lives in the parent page); Details modal shows full audit info (created/modified/deleted at+by); i18n updated in en/ca/es; `workout-templates.test.ts` extended with 5 new tests for audit stamping and `blocks_count`/`notes` in paginated GET), #226 (Nutrition Plan Templates Meal Configuration UX: migration 098 replaces 4 FK columns on meals (`main_dish_id`, `side_id`, `sauce_id`, `drink_id`) with `nutrition_plan_template_meal_items` junction table (`meal_id`, `nutrition_library_item_id`, `component_type` CHECK main_dish/side/sauce/additional, `quantity`, `unit`, `position`); renames `name` → `display_name` on meals; adds `meal_type VARCHAR(50)` CHECK (7 predefined values: recien_levantado/breakfast/media_manana/lunch/snack/dinner/antes_de_dormir) and `notes TEXT`; router rewritten: POST meals requires `meal_type`, defaults `display_name` to `meal_type`; new CRUD endpoints `GET/POST /:id/days/:dayId/meals/:mealId/items` and `PUT/DELETE …/items/:itemId`; goals POST/PUT validate `item_name` against 13-value whitelist; duplicate deep-copies meal_items; admin Add modal replaced with inline pending-new row; `NutritionPlanTree` MealRow shows compact summary `display_name · item1 qty · item2 qty` with Edit toggle; DayRow add-meal changes to meal_type dropdown; GoalsSection item_name changes to predefined select; i18n updated in en/ca/es; `nutrition-plan-templates.test.ts` fully rewritten), #229 (Base Themes UX Redesign: `t.tokens` added to `GET /platform/themes` SELECT — fixes color-preview bug where all themes showed identical swatches; `POST /platform/themes` now accepts `status` field (draft/active, default draft); `GET /platform/themes/:id` returns `deleted_by_name` from audit_logs; admin page rewritten to standard entity pattern — `+ New Theme` creates a temp inline row (Name + Status only), context menu: Details/Edit/Duplicate/Delete, Edit via context menu only with explicit Save/Cancel (no autosave), read-only expanded view shows color-group swatches, Duplicate calls clone endpoint, Details modal gains Primary Color/Background Primary/Secondary/Tertiary swatches and Deleted By/At fields; i18n adds 10 keys (col_color_preview, details_primary_color, details_bg_primary/secondary/tertiary, details_deleted_by/at, label_status, section_general, new_theme_placeholder) in en/es/ca; themes.test.ts gains 4 new cases: tokens in list, status on POST, deleted_by_name in GET /:id), #246 (Theme Text Color: added `textColor` to the Application section of the Theme token model (migration 099, tokens bumped to v3); default `#111827`; `--gd-text` CSS variable set in `applyTokens` (admin) and member ThemeProvider; `body` in both app layouts consumes `var(--gd-text, #111827)`; `textColor` field added after Page Background in the Application group of both the Base Themes editor and the Customer Themes editor; `validateTokens` in `themes.ts` and `gym-themes.ts` updated to include `textColor`; i18n `label_text_color` key added to en/es/ca), #247 (Move Calendar to Root Navigation: Calendar removed from Organization nav group; new top-level `CALENDAR` nav group and `CALENDAR` permission module added; all roles get RW except `accountant` (Finance) which gets NONE; `canWrite` in calendar page corrected from `TRAINING` to `CALENDAR`; `nav.groups.calendar` key added to en/es/ca locales; no functional changes to Calendar itself), #228 (Gyms Management UX and Lifecycle: migration 095 adds `description`, `status` CHECK ('active'/'inactive'/'deleted'), `created_by_name`, `modified_at`, `modified_by_name`, `deleted_at`, `deleted_by_name` to `gyms`; `requireSuperadmin` extended to attach `req.superadminName`; slug is now optional on POST (auto-generated from name via `uniqueSlug()`); new endpoints: `GET /platform/gyms/:id` (single gym including deleted), `PUT /platform/gyms/:id` (name/description/status/theme_id with audit stamp), `DELETE /platform/gyms/:id` (soft-delete), `POST /platform/gyms/:id/duplicate`; `GET /platform/gyms` gains `?status=active|inactive` filter; admin Cordel → Gyms page rewritten to expandable-row UX — `+ New Gym` inline creation (Name + Description), collapsed header (Name/Description/Created At/Created By/Status/ContextMenu), expanded read-only sections (General/Configuration/Notes), context menu Details/Edit/Duplicate/Delete, inline edit form with Save/Cancel, Details modal with full audit info; i18n updated in en/ca/es; `gyms.test.ts` added), #248 (Global Recycle Bin: new `GET /recycle-bin`, `GET /recycle-bin/:entityType/:id`, `POST /recycle-bin/:entityType/:id/recover` router under SYSTEM module gate; 10-way UNION query covers membership_plans, promotions, centers, spaces, class_packages, exercises, workout_templates, training_plan_templates, nutrition_plan_templates, themes; entity-specific recovery UPs clear soft-delete columns + set appropriate status; two runtime fixes applied — MySQL 8.4 LIMIT/OFFSET parameter bug (interpolated as integer literals) and utf8mb4 collation mismatch in themes UNION branch (CONVERT columns); admin System → Recycle Bin page at `/recycle-bin` with type/name/deleted-by/date-range filters, sortable table, Details modal with entity-specific fields, Recover button gated to canWrite; nav entry + i18n in en/ca/es; `recycle-bin.test.ts` with 18 tests covering auth, tenant isolation, list/detail/recover happy paths, invalid entity type 400, and 404 for non-deleted entities), #273 (Rename "System" → "Configuration": updated `nav.system` and `nav.groups.system` keys in en/es/ca locales; no code, permission, or route changes), #265 (Refactor User Impersonation to Use Application Users: `/candidates` endpoint renamed to `/targets`; `clerk_user_id IS NOT NULL` filter removed so all active members appear as targets; `effectiveType: 'member'|'staff'|null` added to `TenantContext`; wire format `x-impersonate-as: member:<id>` for member impersonation; `POST /:targetId` now requires `{ targetType }` body param; `resolveMemberId(gymId, ctx)` helper in `me.ts` routes all `/me/*` handlers through either `WHERE members.id = ?` or `WHERE clerk_user_id = ?` depending on `effectiveType`; admin and member `ImpersonationDialog` updated; 23-test `impersonation.test.ts`; no migrations), #266 (Clerk Status in Member and Staff Details: new `GET /members/:id/clerk-status` and `GET /staff/:id/clerk-status` endpoints — resolve Clerk enrollment state (not_enrolled/invited/active/suspended/error) on demand without storing it in the DB; members endpoint reads `clerk_user_id`/`invitation_id` directly; staff endpoint joins `gym_memberships` via `gym_membership_id`; admin Members edit modal gains a read-only Clerk section that loads on open; admin Staff inline editor gains a `clerk` tab that loads on row expand; `clerkClient` added to `staff.ts`; i18n keys added in en/es/ca; `member-clerk-status.test.ts` and `staff-clerk-status.test.ts` with 14 tests), #268 (Space Creation Center Selection: inline new-space form now includes a Center selector — required when the gym has multiple centers, auto-selected when there is exactly one center, blocked with an error message when no centers exist or when center loading fails; center_id included in POST body; i18n keys `error_center_required`, `no_centers_available`, `error_centers_load`, `placeholder_center` added in en/es/ca; no backend or migration changes — `resolveCenterId` already handled all cases), #270 (Fix Recycle Bin Audit Metadata: migration 101 adds `deleted_by_name VARCHAR(255)` to all 10 entity tables; each soft-delete endpoint stores `actorName` (Clerk-derived, always set for all roles including Superadmin) in `deleted_by_name`; `branchFor` UNION updated to COALESCE new column with FK join name for backward compat, add `created_at` + `created_by_name` columns; `fetchDeletedEntity` detail view also COALESCEs `deleted_by_name`; themes UNION branch uses `CONVERT(t.deleted_by_name USING utf8mb4)` for collation parity; frontend `RecycleBinItem` gains `created_at`/`created_by_name` fields displayed as sub-line under Name in the list), #259 (Base Nutrition Library + Base Nutrition Plan Templates + Clone/Assign Workflow + Member Nutrition Plans: migration 103 makes `nutrition_library_items.gym_id` nullable (drops FK, modifies column, re-adds FK + functional unique index `COALESCE(gym_id,'')` for NULL-safe uniqueness), makes `nutrition_plan_templates.gym_id` nullable (same FK-drop pattern); adds `status VARCHAR(20) DEFAULT 'active'` + `modified_at DATETIME` to `nutrition_library_items`; adds `cloned_from_id INT UNSIGNED FK→nutrition_plan_templates` to track lineage; creates 6 `member_nutrition_plan_*` tables (member_nutrition_plans → days → meals + meal_items + restrictions + goals, all with gym_id); new routers: `platform-nutrition-library.ts` (superadmin CRUD, `gym_id IS NULL`, GET /platform/nutrition-library + POST/PUT/:id/DELETE/:id), `platform-nutrition-plan-templates.ts` (superadmin CRUD for base templates, full day/meal/item/restriction/goal sub-routes at `/platform/nutrition-plan-templates`), `member-nutrition-plans.ts` (gym-level member plans: GET + GET /:id + GET /:id/hierarchy + DELETE /:id soft-delete); `nutrition-plan-templates.ts` extended with `?type=base|gym|all` filter on GET /, `owner_type` field on all rows, cross-scope GET/:id (base or gym), `POST /:id/clone` (deep-copies any template into a gym-owned draft with `cloned_from_id`), `POST /:id/assign` (deep-clones template → member_nutrition_plans in one transaction); `NutritionPlanTree.tsx` gains `apiBase?: string` prop (default `/nutrition-plan-templates`) for reuse from platform context; Cordel admin pages: Cordel → Base Nutrition Library (CRUD, category-grouped table), Cordel → Base Nutrition Plan Templates (expandable rows + NutritionPlanTree); nav keys `nav.base_nutrition_library` + `nav.base_nutrition_plan_templates` added to en/es/ca), #272 (Promotions Comprehensive Redesign: migration 102 renames `free_period_paid_months` → `paid_months` and `free_period_bonus_months` → `bonus_months` on `promotions`, adds `free_months` (initial free period before billing), adds `duration_months` to `promotion_period_benefits`, expands action CHECK on `promotion_charge_benefits` to include `fixed_price`, and creates new `promotion_included_benefits` table for one-time Sellable Item grants; API updated to use new field names in GET/POST/PUT/duplicate, period-benefits accept `duration_months`, charge-benefits accept `fixed_price` action, new `GET/PUT /included-benefits` endpoints; frontend edit form redesigned: "Billing & Duration" section with three independent inputs (Free Period / Paid Duration / Bonus Duration), dynamic example timeline table with hypothetical enrollment date and row-per-period breakdown, Included Benefits section, Duration column in Period Benefits, Fixed Price option in Charge Benefits; i18n updated in en/ca/es; `promotions.test.ts` with 18 tests), #260 (Base Training Catalog: migration 106 makes `gym_id` nullable on 7 training tables (`exercises`, `exercise_muscles`, `workout_templates`, `workout_template_blocks`, `workout_template_exercises`, `training_plan_templates`, `training_plan_template_workouts`) + adds `cloned_from_id` to 3 of them; new platform routers `platform-exercises.ts`, `platform-workout-templates.ts`, `platform-training-plan-templates.ts` (all behind `requireSuperadmin`) for Cordel-managed base content (`gym_id IS NULL`); gym-level routers updated: GET / and GET /:id now surface base resources alongside gym-owned ones, PUT/DELETE return 403 on base resources, new `POST /:id/clone` deep-clones a base resource into a gym-owned copy with `cloned_from_id`; `training-plan-creation.ts` updated to match `(gym_id IS NULL OR gym_id = ?)` for base TPT assign; `POST /training-plan-templates/:id/assign` endpoint wraps existing `createTrainingPlanTx`; admin gym-level Exercises/Workout Templates/Training Plan Templates pages gain Type column (Base/Gym badge) and Clone action; 3 new Cordel platform pages; nav entries `base_exercises`, `base_workout_templates`, `base_training_plan_templates` in Cordel group; i18n updated in en/es/ca; `platform-training.test.ts` + `training-base-gym.test.ts` with ~90 tests), #269 (Activity Types Redesign + Schedule Engine: migrations 107–109 add audit columns to `activity_types` (`created_by_membership_id`, `modified_at`, `modified_by_membership_id`, `deleted_at`, `deleted_by_membership_id`) and `default_center_id FK→centers`; new `activity_type_schedule_rules` table (type one_off/weekly/monthly, start_date, end_date, weekday, ordinal, start_time, end_time); `calendar_events.schedule_rule_id FK→activity_type_schedule_rules`; `gyms.timezone VARCHAR(64) DEFAULT 'Europe/Madrid'`; `scheduleEngine.ts` — pure-TS occurrence generator using Luxon for IANA timezone conversion, `materializeScheduleRule` bulk-inserts occurrences as calendar_events, `cancelFutureOccurrences` preserves past events; `/activity-types` router rewritten with soft-delete, `POST /:id/duplicate` (deep copies rules + materializes), `POST /:id/restore`; new `/activity-types/:id/schedule-rules` sub-router (GET/POST/PUT/DELETE) with full validation (end_date required for recurring, max 1-year horizon, ordinal for monthly); `recycle-bin.ts` extended with `activity_type` entity type; admin page rewritten to expandable-row UX — `+ Create Activity Type` inline creation (Name/Duration/Capacity), collapsed header (Name/Description/Duration·Capacity/Default Center/Status/ContextMenu), expanded General + Schedule (rule list with inline add/edit/delete) + Metadata sections; center→space linked dropdowns in edit form; context menu: Details/Edit/Duplicate/Delete; i18n updated in en/es/ca; `luxon` added to API dependencies), #290 (Theme Settings UX: moved Card Border color picker from Colors section to Advanced section in both Customer Themes and Base Themes editors; removed hardcoded `background: '#fafafa'` overrides from expanded card areas in themes/page.tsx, system/themes/page.tsx, and centers/page.tsx so the configured Card Background token applies to the full card container), #370 (Sellable Item Status/Enrollment Status split: `enrollment_status ENUM(public/staff_only)` added to `gym_charges` (migration 122), mirroring the `membership_plans` status/enrollment_status separation; `status='inactive'` always overrides `enrollment_status`; admin CRUD (`sellable-items.ts`) validates and filters `?enrollment_status=`, defaults new items to `public`; Sellable Items admin UI shows Status and Enrollment Status as separate badges/fields in the row, inline edit form, and Details modal)., #408 (Plans billing policy UX: per-field descriptions for Initial/Recurring Billing and Initial/Recurring Service explaining their effect on the subscription cycle, shown in both the read-only accordion and the inline edit form; `DEFAULT_BILLING_POLICY` named default applied to every new plan, surfaced as a notice in the inline create form), #366 (Assign Members When Creating a CalendarEvent: staff can assign Members directly on `POST /class-sessions` and on recurring `activity_type_schedule_rules`, auto-booked via the existing `bookMemberOnSession(force=true)` mechanism so capacity is a warning, never a hard block, for staff), #441 (Nutrition Plans parity with Training Plans: Edit/Duplicate/Details/Complete actions on assigned member plans — `PUT`/`POST :id/complete`/`POST :id/duplicate` on `member-nutrition-plans.ts`, plus full day/meal/item/restriction/goal CRUD mirroring `nutrition-plan-templates.ts`; plan mutable only while `status='active'`; migration 136 adds the missing `(member_nutrition_plan_id, weekday)` unique constraint; admin UI reuses `NutritionPlanTree.tsx`'s expand-in-place editor via its `apiBase` prop, now threaded through every child component instead of only the top level), #443 (Nutrition Plans: "+ New Nutrition Plan" creation flow mirroring Training Plans — scratch-vs-template modal, `POST /member-nutrition-plans`, and a shared `createNutritionPlanTx` helper also used by `/nutrition-plan-templates/:id/assign`)

- **Done (continued)**: #293 (Nutritional Qualities for Nutrition Library Foods: migration 110 adds `nutritional_qualities` catalogue table (slug-keyed, global) + `nutrition_library_item_qualities` junction table (item↔quality M2M, both ON DELETE CASCADE); seeds protein + carbohydrate; `platform-nutrition-library.ts` extended: GET / includes `qualities:[{id,slug}]` per item, POST/PUT accept optional `quality_ids` to set qualities atomically, new `PUT /:id/qualities` sub-resource for standalone quality replacement; `GET /platform/nutrition-library/nutritional-qualities` lists the catalogue; gym-level `GET /nutrition-library` includes `quality_slugs: string[]` per item; Cordel admin nutrition-library page shows quality chips in the item table and multi-select checkboxes in the Create/Edit modal; gym-level nutrition-library page shows translated quality badges; i18n keys `nutritional_qualities_label`, `quality_protein`, `quality_carbohydrate`, `no_qualities` added to en/es/ca; 9 new tests in `platform-nutrition-library.test.ts`), #291 (Card Background Theme Fix: replaced undefined `--gd-surface`/`--gd-border` CSS vars with `--gd-card-bg`/`--gd-card-border` in Promotions and Exercises pages; extended the same `--gd-border` → `--gd-card-border` fix to all other admin pages that referenced the undefined token: Spaces, Calendar EventDetailsPanel, System Gyms, Activity Types, Class Packages, Training Plan Templates, Workout Templates, Recycle Bin), #271 (Sellable Items: migrations 102–103 extend `gym_charges` with `name`, `type` (fee/service/sessions/merchandise/other), `units`, `status` (active/inactive), `is_system`, soft-delete columns, `package_information`, `validity_days`, `billing_frequency` extended with 'per_session'; migration 103 adds `class_package_id` FK and migrates all class_packages rows as type='sessions' custom items; `gym-charges.ts` fully rewritten — POST create custom, PUT system/custom split (system edits limited to description/amount/frequency/notes/status), POST /:id/activate, POST /:id/deactivate, DELETE soft-delete with 403 guard on is_system; GET / gains `?type`, `?status`, `?q` filters; `membership-plans.ts` and `promotion-details.ts` updated to LEFT JOIN charge_types + COALESCE name + filter by status/deleted_at; `recycle-bin.ts` entity type `class_package` → `sellable_item`, queries gym_charges WHERE is_system=0; admin `/financials/sellable-items` page with inline creation, inline editing, type/status filters, search, system/custom context menus, soft-delete confirm dialog; `/financials/gym-charges` + `/class-packages` redirect permanently to `/financials/sellable-items`; Class Packages removed from Organization nav; nav renamed to Sellable Items; i18n `sellable_items` namespace added in en/es/ca; `gym-charges.test.ts` updated with new filter, POST, and DELETE test blocks)., #292 (Feature Flags for System Navigation: migration 110 makes `audit_logs.gym_id` nullable for platform-scoped audit rows; creates `feature_flags` table with stable dot-separated keys and seeds all 30 released nav nodes as enabled=1; `featureFlags.ts` with 30s in-memory cache + `requireFeatureEnabled(key)` middleware (ancestor-key check, superadmin bypass); `platform-feature-flags.ts`: superadmin `GET/PUT /platform/feature-flags` + public `GET /feature-flags` returning `Record<string, boolean>`; `requireFeatureEnabled` wired to all domain routers in `app.ts` and to gated `/me/*` routes; `feature_flags` entity added to audit-registry; `navigationGroups.ts` extended with `featureKey` on every group and item, `filterNavGroups` accepts flags param; `FeatureFlagsContext` provider fetches flags on mount; layout wraps with `FeatureFlagsProvider`; Sidebar uses `useFeatureFlags()` + passes flags to filter; Cordel → Feature Flags management page with toggle tree; i18n updated in en/es/ca).
- **Done (continued)**: #344 (Move Deleted Members to Recycle Bin: migration 119 adds `deleted_by_name VARCHAR(255)` to `members`; `DELETE /members/:id` now stores `actorName` in `deleted_by_name`; `GET /members/deleted` and `POST /members/:id/restore` removed; `recycle-bin.ts` extended with `member` entity type (UNION branch, detail fetch, recover); admin `/members/deleted` page deleted; nav child removed from Members; recycle-bin frontend adds `member` to entity type list + detail modal + badge colors; `entity_member` i18n key added to en/es/ca; `members.test.ts` extended with DELETE tests; `recycle-bin.test.ts` extended with 10 member tests).
- **Done (continued)**: #323 (Shared Training Slots: `activity_types.shareable BOOLEAN DEFAULT false`; `spaces.max_concurrent_groups TINYINT DEFAULT 1`; `gym_memberships.max_concurrent_groups TINYINT DEFAULT 1`; `class_sessions.sharing_authorized BOOLEAN DEFAULT false`; new `shared_training_requests` table (host_session_id, requested_activity_type_id, requesting_member_id, status pending/approved/rejected, resolved_class_session_id); `POST /class-sessions` validates concurrent-group capacity atomically via `SELECT FOR UPDATE` when trainer+space both set — returns 409 codes slot_not_shareable/activity_not_shareable/slot_fully_occupied/sharing_not_authorized; `PUT /class-sessions/:id` revalidates on slot changes; new `PUT /class-sessions/:id/sharing-authorized`; new `/shared-training-requests` router (GET/GET:id/POST + POST/:id/approve + POST/:id/reject); approve is atomic: SELECT FOR UPDATE → create session → SET sharing_authorized=1 on host → bookMemberOnSession inside same transaction; trainers router includes max_concurrent_groups; spaces POST/PUT accept max_concurrent_groups; activity-types POST/PUT accept shareable; `/me/schedule` returns shareable/sharing_authorized/can_request_sharing; `POST /me/shared-training-requests` and `GET /me/shared-training-requests` for member-facing request flow; migrations 115–116; TypeScript clean; shared-training-requests.test.ts).
- **Done (continued)**: #326 (Calendar — Manage Individual Activity Instance: class sessions now rendered on the Calendar alongside calendar events; clicking a class session opens `ClassSessionDetailPanel` (instance management) instead of the event editor; panel shows enrolled members, waiting list, add member (with capacity warning + override), add to waiting list, remove member (with auto-promote confirmation), change event time, cancel event; `GET /class-sessions` extended with `activity_type_id`, `space_id`, `trainer_membership_id` filter params so the calendar filter bar works for sessions; `GET /members?q=` search param added (LIKE on name/email, max 20 results); `POST /bookings` gains `waitlist=true` flag for explicit staff-managed waitlist addition even when capacity is available; `MemberSearchInput` debounced combo component; `bookMemberOnSession` extended with `forceWaitlist` parameter; no migrations; shareable training deferred — no backend domain exists yet; `members.test.ts` with 6 tests; bookings.test.ts extended with 3 tests for explicit waitlist).
- **Done (continued)**: #325 (Member App — Membership Details, Status and Billing Information: `GET /me/membership` extended with `um.next_billing_date` in SELECT and new `upcoming_payments: [{date, amount, status:'scheduled'}]` field computed server-side via `computeUpcomingPayments` + `advanceDate` helpers (exported for unit testing); member app `membership/page.tsx` updated — `Membership` interface gains `upcoming_payments: UpcomingPayment[]` and `next_billing_date`, "Starts" label renamed to "Member since", new "Upcoming payments" section renders the server-computed array with `EventStatusPill` badge; new `EventStatusPill` component matches existing `StatusPill` design; locales en/es/ca extended with `member_since`, `upcoming_heading`, `upcoming_empty`, `event.recurring_payment`, `event.failed_billing`, `upcoming_status.scheduled`; `me-upcoming-payments.test.ts` — 13 unit tests covering `advanceDate` (days/weeks/months/years) and `computeUpcomingPayments` (null guards, far-future dates, stale past dates, amount formatting, Date object input); no migrations, no Admin changes).
- **Done (continued)**: #321 (Multi-day weekly schedule rules: migration 115 adds `weekdays JSON NULL` to `activity_type_schedule_rules`; backfills existing weekly rules from `weekday` → `weekdays` JSON array; clears `weekday` for weekly type (retained for monthly); API `validateRule` accepts `weekdays: number[]` with backward-compat shim for legacy `weekday` integer; `formatRule` parses JSON to array; schedule engine `occurrenceDatesForRule` iterates over `weekdays[]`, deduplicates + sorts; frontend toggle-button day-picker for weekly type using existing `weekday.N` i18n keys; no new i18n keys required).
- **Done (continued)**: #324 (Member Calendar: Free/Busy Availability and Booking Actions: migration 115 adds `activity_types.is_shareable TINYINT DEFAULT 0`, `class_sessions.allows_shared_booking TINYINT DEFAULT 0`, and new `shared_training_requests` table (pending/approved/rejected/cancelled lifecycle, UNIQUE on gym+session+member, `reviewed_by_membership_id FK→gym_memberships`); seeds `calendar.member_calendar` feature flag; new `/shared-training-requests` router with `GET /?status&class_session_id`, `POST /:id/approve` (transactional capacity re-check + booking INSERT + notification), `POST /:id/reject` (notification), under TRAINING module + `calendar.member_calendar` flag; `GET /me/schedule` extended with `activity_type_id` filter, 6 new fields (`allows_shared_booking`, `is_shareable`, `my_shared_request_id`, `my_shared_request_status`, and computed `availability_state` enum with 8 states: UNAVAILABLE/BOOKED_BY_MEMBER/AVAILABLE/WAITLISTED_BY_MEMBER/SHARED_REQUESTED_BY_MEMBER/SHARED_REQUEST_AVAILABLE/WAITLIST_AVAILABLE/FULL); new `POST /me/shared-training-requests` and `DELETE /me/shared-training-requests/:id` under `calendar.member_calendar` flag; `PUT /class-sessions/:id` accepts `allows_shared_booking`; member app gains `/calendar` page (FullCalendar v6 month/week/day, activity-type filter chips, per-state event colors, bottom-sheet action panel for all 8 states), new BottomNav "Calendar" tab, `member_calendar` locale namespace in en/es/ca; admin schedule page gains "Toggle Sharing" button (PUT class-sessions), "Shared" button → `SharedTrainingPanel` drawer with approve/reject actions; FullCalendar v6 added to member app package.json; 2 new test files).
- **Done (continued)**: #322 (Activity Audit Metadata: backend already correctly set `created_by_membership_id`/`modified_by_membership_id` in all CRUD operations — no code changes needed; added 6 audit metadata tests to `activity-types.test.ts` covering create, update-by-same-user, update-by-different-user, multiple-updates, failed-update, and client-supplied-fields-ignored scenarios).
- **Done (continued)**: #294 (Nutrition Plan Template Meal & Food Item Editor Improvements: migration 111 extends `nutrition_plan_template_meal_items.component_type` CHECK to include `drink`, `dessert`, `other` (keeps `additional` for backward compat); `COMPONENT_TYPES` constant updated in `nutrition-plan-templates.ts` and `platform-nutrition-plan-templates.ts`; `NutritionPlanTree.tsx` — `LibraryItem` interface gains `quality_slugs: string[]`; `MealRow` is now expandable/collapsible (▶/▼ toggle on header click, auto-expands on Edit); expanded non-edit mode shows read-only items list with quality badges (color-coded per quality slug) and OR separator between consecutive items of the same `component_type`; `MealItemsEditor` redesigned with Food Type selector (categories derived from loaded library items, not hardcoded) → filtered Food selector → quality badges shown beneath the selected food → Qty + Unit row; per-item inline edit button opens a one-row form (Food Type | Food | Qty | Unit | Save | Cancel) pre-populated from the library item's category; changing Food Type clears the food selection; `PUT …/items/:itemId` used to save edits; `ItemsReadView` extracted for use in non-edit expanded mode; i18n keys added in en/es/ca: `tree_label_food_type`, `tree_label_food`, `tree_food_type_or`, `tree_component_type_drink`, `tree_component_type_dessert`, `tree_component_type_other`; food names displayed as stored in DB (single-language); OR localized as OR/O/O).
- **Done (continued)**: #320 (Calendar double-click to Day view: in Month and Week views, double-clicking a day navigates directly to Day view for that date using FullCalendar's `api.changeView('timeGridDay', date)`; single-click behavior preserved via 300ms timer in `handleDateClick`; no backend changes, no new routes, no library changes — pure FullCalendar API; one file changed: `apps/admin/src/app/[locale]/calendar/page.tsx`).
- **Done (continued)**: #319 (Rename "Activity Types" → "Activities" in UI: updated `nav.activity_types` in en/es/ca locales; updated all user-facing strings in `activity_types.*`, `spaces.section_activity_types`, `spaces.no_activity_types`, `calendar.filter_activity_type`, `calendar.event_activity_type` across en/es/ca; fixed hardcoded `<FormLabel>Activity Type</FormLabel>` in `plans/page.tsx`; technical identifiers (`activity_types` table, `/activity-types` API paths, TypeScript types, variable names) unchanged).
- **Done (continued)**: #318 (Admin Navigation Audit + Cleanup: added `/memberships` to the Membership nav group (MEMBERS module) and `/schedule` to the Calendar nav group (CALENDAR module) — both pages existed but were unreachable from nav; confirmed no stale Specialities, Class packages, or workoutsRouter references remain in `navigationGroups.ts`; all i18n keys were already present in en/es/ca), #311 (Tax Rates + Sellable Items refactor: new `tax_rates` table (migration 112) — per-gym `is_system=1` default "Standard VAT 21%" auto-seeded at gym creation + custom CRUD with soft-delete; migration 113 adds `tax_behavior VARCHAR(20) CHECK ('inclusive','exclusive')` and `tax_rate_id FK→tax_rates ON DELETE SET NULL` to `gym_charges`, and `tax_rate_id FK→tax_rates ON DELETE SET NULL` to `membership_plans`, both backfilled from each gym's system default; `/taxes` router: GET/GET:id/POST/PUT/POST:id/activate/POST:id/deactivate/DELETE (403 on is_system=1, soft-delete otherwise); `/sellable-items` router replaces `/gym-charges` — SELECT LEFT JOINs `tax_rates` for `tax_rate_name`/`tax_rate_percent`, `computePriceFields()` returns `amount_excl_tax`/`amount_incl_tax`/`applied_tax_rate` based on `tax_behavior`; POST/PUT accept `tax_rate_id` and `tax_behavior`; `gyms.ts` seeds system tax_rate on gym creation; all 5 frontend callers migrated from `/gym-charges` to `/sellable-items`; `plans/page.tsx` and `promotions/page.tsx` updated; new admin Taxes page (`/financials/taxes/`); Sellable Items page extended with tax rate column and `amount_incl_tax` display; nav entry for Taxes added; i18n `taxes` namespace + tax keys in `sellable_items` namespace in en/es/ca; `gym-charges.test.ts` updated for new path + computed price field tests).
- **Done (continued)**: #349 (Admin Payments — Billing Events Management: migration 119 adds `billing_event_id INT UNSIGNED NULL FK→billing_events ON DELETE SET NULL` to `payment_requests` (with index); `billing.ts` rewritten so billing_events rows are inserted first and `payment_requests` rows reference them via `billing_event_id` — establishes the invariant "every payment is linked to a billing event"; `webhooks.ts` likewise: on MONEI payment_recorded event, inserts the billing_event then UPDATEs `payment_requests.billing_event_id`; two new endpoints in `payments.ts`: `GET /payments/billing-events` (combined past/future list — real rows from `billing_events` DB, virtual future rows computed from `user_memberships.next_billing_date` with a rolling 5-date window per active membership, merged and paginated in JS; filters: `member_id`, `from`, `to`, `limit`, `offset`) and `GET /payments/billing-events/:id/transactions` (payment_requests linked to a billing_event, JOINs payment_methods for card info); admin page at `/payments/billing-events` with inline member search filter, date-range filters, URL-persisted filter state, expandable rows (lazy-fetches transactions on first open), virtual future rows shown with "Scheduled" badge; nav item added to Payments group; i18n in en/es/ca; `payments-billing-events.test.ts` with coverage for auth, tenant isolation, happy path, member filter, date range, transactions endpoint), #347 (Training Plans deep-link from Member view: clicking Edit on a Training Plan in the expanded Member row now navigates to `/training-plans?open=<plan_id>&member_id=<member_id>`; the Training Plans page reads these params on mount — pre-sets the member filter and auto-expands the target plan (enters edit mode for non-completed plans, read-only expand for completed ones) and scrolls it into view; invalid/missing plan IDs are ignored gracefully; no API or DB changes), #341 (Member App — Gym and Centre Selection: new `GET /me/gyms` endpoint mounted before `tenantContext` returns all gyms the caller belongs to (via `gym_memberships`) with theme data, ordered alphabetically; superadmin variant returns all non-deleted gyms; `GymSwitcher` component renders a `<select>` hidden when ≤1 gym; `AppContext` extended with `gyms: GymOption[]` and `switchGym(id)` — persists selection to localStorage, switches theme client-side from cached list; `activeGymId` key in localStorage; i18n in en/es/ca; 5-test `me-gyms.test.ts`), #327 (Admin Members — Expandable Row with Membership, Plans & Billing Details: `GET /members` extended with `account_status` (active/invited/not_enrolled, derived from clerk_user_id/invitation_id DB fields) and `membership_status` (most-recent user_memberships status, subquery) — no extra round-trips; `StatusBadge` gains `invited`, `not_enrolled`, `suspended` color entries; Members page rewritten to use `DataTable` with `renderExpanded` — collapsed row shows Name+email, Account Status badge, Membership Status badge, ContextMenu; `MemberExpandedRow` component lazy-loads on first expand: Account section (Clerk status via GET /members/:id/clerk-status), Membership section (plan name/status/rate/dates + allowances via GET /membership-plans/:id/allowances), Training Plans section (active/inactive grouped, Edit → /training-plans list), Nutrition Plans section (read-only list, no editor), Billing Events section (flat list from GET /billing-events/member/:id, each event independently expandable showing charge type/notes/source/receipt); existing modals (MemberTrainingPlansModal, MemberPaymentsModal) preserved via ContextMenu; `members.test.ts` with 12 tests covering auth, tenant isolation, happy path, account_status derivation, and membership_status derivation; no migrations).
- **Done (continued)**: #352 (Event Colour, Booking Count, Session Status and Day View Details: `class-sessions` SELECT extended with `at.color AS activity_type_color`; calendar `eventContent` renderer branched on `arg.view.type` — Month: compact single-line title + status, Week: title / booking-count·status two-line, Day: two-line max with time·title·STATUS / trainer·space·booked/cap; STATUS_COLORS status-derived colour fallback removed; colours now use `activity_type_color || event_color_override || default` for calendar events and `activity_type_color || default` for sessions; "FULL" derived label shown when `booked_count >= effective_capacity && status === 'scheduled'`; no migrations, no new endpoints, no i18n changes). Reopened and fixed follow-up: month view events were only tinting a small dot (FullCalendar's default `list-item` display for timed events in `dayGridMonth`) instead of a coloured block — fixed with `eventDisplay="block"`; Week view was missing the start–end time line from the ticket's spec — added; Day view's trainer now falls back to the assigned `trainer_name` when no `effective_trainer_name` is confirmed yet (`class-sessions` SELECT gained `gm.name AS trainer_name`); newly created Activity Types via the inline "+ Add" row had no colour picker, so new activities never showed their colour bullet until edited — added a colour input to the inline row; `EventDetailsPanel` now shows the selected Activity Type's configured capacity (calendar events have no booking model, so this is the activity type's `max_capacity`, not a live booked count) — new `calendar.event_capacity` i18n key in en/es/ca.
- **Done (continued)**: #346 (Improve Members Management View: migrations 120–121 add profile fields (`date_of_birth DATE`, `gender VARCHAR(20)`, `address VARCHAR(500)`, `emergency_contact VARCHAR(255)`, `notes VARCHAR(3000)`) and audit columns (`created_by FK→gym_memberships`, `modified_at DATETIME`, `modified_by FK→gym_memberships`) to `members`; `GET /members` renames `membership_status` → `enrollment_status`, adds `payment_status` (most-recent `payment_requests.status` subquery), supports `?payment_status=` and `?enrollment_status=` filters via HAVING; `GET /members/:id` JOINs `gym_memberships` twice for `created_by_name`/`modified_by_name`; `POST /members` stores `created_by`; `PUT /members/:id` accepts profile fields, stamps `modified_at`/`modified_by`, intentionally excludes email (Clerk sync out of scope); admin Members page rewritten — row header shows Name + Payment Status badge + Enrollment Status badge; Clerk account_status moved to lazy-loaded expanded row; action menu: Invite/Edit/Details/Delete (Training Plans and Payments shortcuts removed); new `MemberDetailModal` (follows ExerciseDetailModal pattern, Profile + Audit sections); edit form adds profile fields with email shown read-only; toolbar: name search (`?q=`) + Payment Status + Enrollment Status dropdowns; URL params stay in sync; i18n en/es/ca updated with all new keys; `members.test.ts` updated: `membership_status` → `enrollment_status` rename, added tests for enrollment_status filter, payment_status subquery/filter, combined filter, GET /:id audit fields, PUT profile fields).
- **Done (continued)**: #351 (Assign Nutrition Plan Template to Member: `POST /nutrition-plan-templates/:id/assign` extended with optional `name` body param (falls back to template name when omitted); "Assign plan to member" context menu item added to nutrition plan templates page (gated to `status === 'active'`); new `AssignNutritionPlanDialog.tsx` with template name (read-only), member selector, plan name field, POSTs to the assign endpoint; new `/nutrition/nutrition-plans/page.tsx` lists member nutrition plans with member filter and delete action; `navigationGroups.ts` gains `nutrition.nutrition_plans` nav item with `separatorAbove: true`; i18n keys in en/es/ca; 1 new test case in `nutrition-plan-templates.test.ts` for custom name; no migration).
- **Done (continued)**: #353 (Calendar — Resizable Event Editor: hardcoded `PANEL_WIDTH = 380` replaced with `MIN_PANEL_WIDTH = 280` / `MAX_PANEL_WIDTH = 800` constants and stateful `panelWidth` (sessionStorage-backed, key `calendar-panel-width`); 6px drag handle `<div>` inserted between calendar and panel flex items — `onMouseDown` captures `startX`/`startWidth`, adds `window` `mousemove`/`mouseup` listeners that clamp and update width, persists to sessionStorage on release; `onKeyDown` on the handle — `ArrowLeft` expands panel +20px, `ArrowRight` shrinks −20px; handle has `role="separator"`, `aria-orientation="vertical"`, `aria-label`, `aria-valuenow/min/max`, `tabIndex=0`; applied to both EventDetailsPanel and ClassSessionDetailPanel panel slots; `panel_resize_handle` i18n key added to en/es/ca; no backend changes).
- **Done (continued)**: #342 (Fix Staff Impersonation Targets: GET /platform/impersonation/targets staff query no longer filters by `status = 'active'`; `gm.status` added to SELECT and returned in response so UI can show `Staff · role · status`; POST /:targetId staff path makes Clerk lookup best-effort — superadmin check skipped if user not found in Clerk rather than returning 404; both DB queries in POST (membership validation + gymIds) drop the `status = 'active'` filter; `Target` interface in ImpersonationDialog gains optional `status` field; subtitle displays `Staff · role · status`; 2 new tests: invited staff appears in GET targets, invited staff can be impersonated via POST; 27 tests pass).
- **Done (continued)**: #368 (Allow Editing Taxes on Sellable Items: `gym_charges.tax_rate_id`/`tax_behavior` already existed from #311's tax rates work but had no edit UI — Sellable Items inline-new and edit forms now expose a Tax dropdown populated from `GET /taxes`, defaulting to "No tax"; `POST`/`PUT /sellable-items` now validate `tax_rate_id` server-side against `tax_rates` for the current gym before saving, rejecting unknown or cross-gym ids with 400; Details view and expanded read-only row now show the applied tax rate; no migration needed, no new API surface — reused the existing pricing computation (`amount_excl_tax`/`amount_incl_tax`) already returned by the router; new tests cover valid tax_rate_id assignment on create/edit, rejection of a non-existent id, and cross-gym tenant isolation).
- **Done (continued)**: #361 (Redesign Members App Home and Navigation: removed the bottom tab bar (`BottomNav.tsx` deleted) — Home is now the primary navigation surface; new `TopBar.tsx` gives every non-Home page a "← Home" link plus a persistent Profile entry point; Home rewritten as a personalized dashboard — time-of-day greeting, Alerts card (shown only when an unread notification exists, hidden otherwise), Next Booking, Today's Nutrition Plan (new, see below), a 2×2 nav tile grid (Calendar/My Training Plan/My Bookings/My Nutrition), and a My Membership summary card; Packages and Payments are no longer top-level nav items — both were already surfaced inside `/membership`, which is now the single "My Membership" destination; new `GET /me/nutrition-plan` endpoint (mirrors staff-facing `member-nutrition-plans.ts` `/:id/hierarchy`, scoped to the caller via `resolveMemberId`) returns the caller's own active nutrition plan with full days/meals/items/goals — feature-flagged `nutrition.nutrition_plans`; new `/nutrition` ("My Nutrition") read-only page consumes the same endpoint for a full-week view, Home's card filters it client-side to `weekday === today || weekday === 7` (the "All Days" sentinel); `/schedule` renamed "My Bookings", `/training` renamed "My Training Plan", `/notifications` renamed "Alerts" in the UI (routes unchanged); `/calendar` `initialView` changed to `timeGridDay` so it defaults to today; Profile page extended with default gym/center switching (reuses existing `AppContext` state, no new backend), enrollment status (from `/me/membership`) and a payment-status summary (derived from `/me/payment-requests`); i18n `nav` namespace repurposed for the new tile/entry-point labels, new `nutrition` namespace added, in en/es/ca; `me-nutrition-plan.test.ts` — 6 tests covering auth, tenant isolation, empty state, happy path, and member isolation within the same gym; no migrations — everything reuses the existing `member_nutrition_plans` schema from #259).
- **Done (continued)**: #365 (Replace Member Edit Modal with Inline Editing: the Members admin page's Edit action no longer opens a modal — it now expands the member's row and renders an inline `MemberEditForm` (new component) with the same fields the old edit modal exposed (Name/Phone/Date of birth/Gender/Address/Emergency contact/Notes + center assignment when the gym has multiple centers), followed by Save/Cancel; the Add-Member modal is unchanged and now only handles creation; inline edit follows the Training Plans (#112) pattern — `editingId`/`editForm`/`editError`/`editSaving` state, `guardUnsaved()` + `pendingAction` + a `beforeunload` listener warn about discarding unsaved changes when navigating away mid-edit, a save error keeps the user in edit mode with their entered values preserved; `members.unsaved_changes`/`unsaved_discard` i18n keys added to en/es/ca; no API or migration changes — reuses the existing `PUT /members/:id` and `PUT /members/:id/centers` endpoints; frontend-only change, no test file added (no existing test infra for `apps/admin` pages in this repo).
- **Done (continued)**: #370 (Align Sellable Item Status and Enrollment Status: migration 122 adds `gym_charges.enrollment_status ENUM(public/staff_only)` — mirrors the `status`/`enrollment_status` split already used by `membership_plans`; `status='inactive'` always overrides `enrollment_status`; `VALID_ENROLLMENT_STATUSES` added to `sellable-items.ts`, `GET /` gains `?enrollment_status` filter, POST/PUT accept and validate it, system items may edit it, Recycle Bin restore leaves it as-is; admin Sellable Items page shows Status and Enrollment Status as separate badges/fields in row, inline edit, and Details; `gym-charges.test.ts` extended).
- **Done (continued)**: #369 (4-Week Billing/Duration Option: investigation found Membership Plans already support an arbitrary 4-week period today via the existing generic `billing_policies.recurring_billing_interval`/`recurring_billing_unit` fields — `advanceBillingDate()` in `billing.ts` already advances `unit='week'` by `interval * 7` days, so interval=4/unit='week' correctly yields 28 days and is never conflated with a calendar month; no Plan-side schema or code change was needed. Sellable Items' `billing_frequency` had no such interval multiplier — it's a fixed single-value enum used only as a descriptive label (actual entitlement length is tracked separately via `validity_days`) — so migration 123 widens the `gym_charges_billing_frequency_check` CHECK constraint to add `'four_weeks'` alongside the existing `once/per_session/week/month/year` (renumbered from 122 to 123 after merging #370, which independently claimed migration 122 for `enrollment_status`); `VALID_FREQUENCIES` in `sellable-items.ts` and `FREQUENCIES` in the admin Sellable Items page updated to match; `frequency_four_weeks` i18n key added to en/es/ca under the `sellable_items` namespace; `advanceBillingDate` exported and covered by a new unit-test file `billing-date-math.test.ts` (5 tests, no DB) confirming 4×week = 28 days and that it never equals a 1-month advance; `gym-charges.test.ts` extended with POST/PUT `billing_frequency=four_weeks` acceptance tests and an invalid-value rejection test).
- **Done (continued)**: #371 (System Sellable Item — Personal Training Class Package (10 Sessions): data/setup-only migration 124 backfills one system Sellable Item per existing gym into `gym_charges` — `type='sessions'`, `units=10`, `is_system=1`, `status='active'`, `enrollment_status='staff_only'` (maps the ticket's "Active — Only Internal" onto the split introduced by #370: staff can sell it internally, Members cannot purchase it directly), `validity_days=182` (≈6 calendar months — `gym_charges` has no month-based validity field, only a fixed day count), `tax_rate_id` set to each gym's own system default tax rate (same lookup migration 113 used); `amount` left `NULL` so price stays configurable via the normal Sellable Item admin flow; no API, UI, or model changes — `gym_charges.name` has no localization mechanism today so no translated-name column was added; idempotent via `NOT EXISTS` guard, `down` deletes the seeded rows; migration renumbered from 123 to 124 after merging #369, which independently claimed migration 123 for the four-week billing frequency).
- **Done (continued)**: #370 amendment (Align Plan Enrollment Status with Sellable Items: migration 125 removes `closed` from `membership_plans.enrollment_status` — only `public`/`staff_only` remain, mirroring `gym_charges.enrollment_status`; existing `closed` plans migrated to `public` per the ticket's explicit mapping; column default changed from `closed` to `staff_only`; new plans, duplicated plans, archived plans, and soft-deleted plans now default/reset to `staff_only` instead of `closed`; `VALID_ENROLLMENT`/`ENROLLMENT_STATUSES` in `membership-plans.ts` and the admin Plans page drop `closed`; `membership-plans.test.ts` and the plan fixtures in `billing-run.test.ts`/`payment-page.test.ts`/`payment-requests.test.ts`/`payments-billing-events.test.ts`/`plan-charge-benefits.test.ts`/`recycle-bin.test.ts` updated to use `staff_only`).
- **Done (continued)**: #388 (Inline Tax Management, Descriptions, Audit Information & Impact Warning: migration 126 (renumbered from 125 after merging #370's amendment, which independently claimed migration 125 for the `enrollment_status` change) adds `tax_rates.description` (nullable) plus `created_by_name`/`created_by_type` and `modified_by_name`/`modified_by_type` — `created_by_type`/`modified_by_type` is a CHECK-constrained `'staff'`/`'superadmin'` column snapshotting the actor at write time, fixing a real bug where `created_by_membership_id`/`modified_by_membership_id` (joined to `gym_memberships` for display) silently showed no actor at all for a superadmin acting directly, since superadmins have no `gym_memberships` row; backfilled for existing rows resolvable via the old membership-id join, left unattributed otherwise; `POST /taxes`, `PUT /taxes/:id`, `POST /taxes/:id/activate` and `POST /taxes/:id/deactivate` all populate the new columns going forward. `PUT /taxes/:id` now computes the current count of non-deleted `gym_charges` (Sellable Items) and `membership_plans` referencing the tax before persisting an edit — Membership instances are deliberately not counted, since Taxes are never snapshotted onto a Membership at instantiation; if either count is non-zero and the request omits `confirmImpact: true`, it returns `409 { error: 'confirmation_required', impact }` without writing, so a direct API call can't bypass the warning. Admin Taxes page (`apps/admin/.../financials/taxes/`) rebuilt to match the `sellable-items` page's standard pattern — inline "+ Add" row and inline row-level Edit (no create/edit modal), expand/collapse row with the description truncated in the collapsed header and shown in full when expanded, Details modal shows the actor's Staff/Superadmin label; Save on Edit now surfaces the impact warning as a confirm dialog (with the affected counts) before resubmitting with `confirmImpact: true`. New `api/src/test/taxes.test.ts` (this router had no prior test file) covers tenant isolation, auth/role gating, the confirmation-required flow (both triggered and skipped), audit actor population for staff vs. superadmin, is_system delete protection, and soft-delete visibility.).
- **Done (continued)**: #367 (Member Web Feature Flags — sub-issue of #362: extends the `feature_flags` infrastructure with a `member_web.*` namespace, separate from the Admin App's own flags. Migration 127 seeds `member_web.my_training_plan`/`my_nutrition`/`my_bookings`/`my_membership`/`profile`, all `enabled=1`. Backend: each flag additionally gates the `/me/*` endpoints specific to that section (chained after any existing flag on the same route); superadmins bypass unconditionally, including while impersonating a member. `GET /me/schedule` (shared with the un-flagged `/calendar` page), `GET /me/payment-requests` (shared between `/membership` and `/profile`), and `GET /me/profile` (the app's core `isLinked` bootstrap call) are deliberately left ungated at the API layer to avoid one section's flag blocking another — those three rely on the frontend route guard alone. Frontend: new `apps/member/src/context/FeatureFlagsContext.tsx` (mirrors the admin app's), Home hides the disabled nav tiles/My Membership card, and each of `/training`/`/nutrition`/`/schedule`/`/membership`/`/profile` redirects to Home when its flag is off for a non-superadmin. The existing generic Admin Feature Flags tree UI needed no changes — it renders `member_web` as its own section automatically. New `api/src/test/member-web-feature-flags.test.ts`).
- **#362 (in progress — Slice 1 of Option A)**: Members App audit — fixed Home's Next Booking / Membership sections staying stuck on `Loading...` forever under admin impersonation. Root cause was two compounding bugs: (1) `apps/member/src/app/api/proxy/[...path]/route.ts` stripped the `x-impersonate-as`/`x-center-id` headers before forwarding requests to the API, so the backend never saw the impersonation target even though `tenantContext`/`resolveMemberId` already handled it correctly; (2) `AppContext`'s data-loading effect only depended on `[isSignedIn, user?.id]` and read `sessionStorage` once inline, so it never re-ran when impersonation started/stopped, leaving `isLinked` stuck at its pre-impersonation value forever. Fixed the proxy allow-list, reordered `ImpersonationProvider`/`AppProvider` nesting so `AppContext` consumes `useImpersonation()` reactively, and added `x-impersonate-as` to every `/me/*` call `AppContext` makes (centers, notifications count). Added `api/src/test/me-impersonation-context.test.ts` covering `/me/schedule` and `/me/membership` under impersonation. Calendar, Bookings, Training, Nutrition, Payments, Alerts, and Profile audits remain as follow-up PRs against #362.
- **#362 (in progress — Slice 2 of Option A)**: found and fixed a related latent bug left over after Slice 1: `centerContext` (the middleware resolving a `member` role's assigned centers, runs after `tenantContext` on every `/me/*` route) matched `member_centers` against the impersonating superadmin's raw `userId`/`clerk_user_id` instead of the effective identity, so any `/me/*` request carrying `x-center-id` while impersonating a member with `member_centers` rows always resolved to zero allowed centers and 403'd — silently, since `page.tsx`'s outer `catch` degrades to empty state rather than surfacing the error. `centerContext` now mirrors `resolveMemberId`'s `effectiveType`-based branch: `members.id = effectiveUserId` under member impersonation, `members.clerk_user_id = userId` otherwise. Extended `api/src/test/me-impersonation-context.test.ts` with center-scoped impersonation cases (own center allowed, other center 403).
- **Done (continued)**: #350 (Nutrition Library — flat list, expandable rows, search + multi-select filters: replaced the category-grouped layout on both `/cordel/nutrition-library` and `/nutrition/nutrition-library` with a flat, paginated `DataTable` list ordered by name; new `MultiSelectFilter` component (`apps/admin/src/components/MultiSelectFilter.tsx`) provides Excel-like checkbox dropdowns for Category (OR semantics) and Nutrition Properties (AND semantics — item must have every selected quality), combined with a debounced name search and an existing-pattern prev/next pager. Shared query-building/pagination helpers extracted to `api/src/domain/nutritionLibrary.ts` (`buildListWhere`, `loadQualitiesMap`, `replaceQualities`, `validateQualityIds`, `clampLimit`/`clampOffset`) and reused by both `platform-nutrition-library.ts` and `nutrition-library.ts`; `GET` on both routers now returns `{ items, total, limit, offset }` (was a bare array) and accepts `?search=`, repeatable `?category=`, repeatable `?quality_id=`, `?limit=`/`?offset=`. Per the issue's answered clarifying questions: the gym-facing router gained `POST /nutrition-library` and `PUT /nutrition-library/:id` (both `requireModuleWrite('NUTRITION')`) so gym staff with write access can create and edit their own gym-owned items (`gym_id` = their gym); system items (`gym_id IS NULL`, created via `/cordel/nutrition-library`) stay read-only there — editing one returns 403, editing another gym's item returns 404. Nutrition-data-quality review (data model stayed qualitative-only per the issue's Q2 answer — no numeric columns added): migration 127 backfills the `protein`/`carbohydrate` quality tags for the unambiguous seeded system items (chicken/turkey/salmon/etc. → protein; rice/potatoes/oats/etc. → carbohydrate), since the original #163/#293 seed data had never been tagged. Tests extended in `platform-nutrition-library.test.ts` and `nutrition-library.test.ts` for search/filter/pagination, the gym-write permission boundary, and tenant isolation on edit.).
- **Done (continued)**: #410 (Financials — Assigned Plans view: new read-only `GET /user-memberships` field `lifecycle_status` — a date-aware projection of the stored `status` column computed in SQL (`pending` when `starts_at` is in the future, `expired` when an otherwise-active row's `ends_at` has passed, `active`/`paused`/`cancelled`/the explicit `expired` value passed through unchanged otherwise); no migration, no change to the stored `status` values or existing business logic. New admin page `financials/assigned-plans` lists every Membership assignment across the gym (Member Name/Plan Name/Start Date/End Date — "Open-ended" when `ends_at` is null/Status badge), reusing the existing `/user-memberships` endpoint; nav entry added to the Financials group (`financials.assigned_plans` feature key, defaults enabled — not yet seeded in `feature_flags`); `status.pending` i18n key added alongside the existing `status.*` badge labels in en/es/ca. `user-memberships.test.ts` extended with `lifecycle_status` coverage for all five states.
- **Done (continued)**: #411 (Financials — advanced filtering on the Assigned Plans view, building on #410's `lifecycle_status` field. `GET /user-memberships` gains three additive, backward-compatible filters: repeatable/comma-separated `lifecycle_status` (multi-select over `pending`/`active`/`paused`/`expired`/`cancelled` — filtered via an outer query over a derived table, since `lifecycle_status` is a `CASE`-expression SELECT alias and can't be reused directly in the same query's `WHERE`), and `start_date`/`end_date` (date-range overlap against `starts_at`/`ends_at`). The existing single-value `status` param (raw stored column) and `member_id` are unchanged — `lifecycle_status` is a separate param rather than repurposing `status`, since `pending` has no equivalent raw stored value and existing callers (`memberships/page.tsx`, `MemberPaymentsModal.tsx`) rely on `status` matching the raw column. `financials/assigned-plans` page gets a filter bar: a `MultiSelectFilter` (existing component, #350) for status, a debounced member-search filter (mirroring `payments/billing-events`' inline `MemberFilter`), and `start`/`end` date inputs, with a "Clear filters" action and a distinct empty-state message when filters exclude all rows. New `filter_status`/`filter_member`/`filter_from`/`filter_to`/`filter_clear`/`no_matches` i18n keys in en/es/ca. `user-memberships.test.ts` extended with coverage for single/multi/comma-separated `lifecycle_status` filtering, invalid-value 400s, combining `member_id` with `lifecycle_status`, the date-range overlap semantics, and malformed-date 400s.).
- **Done (continued)**: #412 (Members — plan history sorting + Cancel/Assign New Plan transition workflows; no migration, existing `status` enum already had `cancelled`/`expired`). New `POST /user-memberships/:id/assign-new-plan` (admin-only, mirrors the admin-only gate on the existing cancel `DELETE`) atomically supersedes a Membership: in one transaction it flips the current row to `expired` only if it's still `active`/`paused` (a row already `cancelled`/`expired` is left untouched), inserts the new `active` `user_memberships` row for the same Member via the existing `effectivePrice()` snapshot logic (same discount-override/`discount_reason` validation as `POST /`), and adds the owner to `user_membership_members` — avoiding the `active_member_key` unique-index collision by expiring the old row before inserting the new one within the same transaction. Admin `MemberExpandedRow.tsx` (Members page) now lists a Member's **full** plan history (the `GET /user-memberships?member_id=` list was already sorted `starts_at DESC` from #410/#411) instead of only the latest row, with a per-row admin-only `ContextMenu`: "Cancel Plan" (reuses the existing admin-only `DELETE /user-memberships/:id`) and "Assign New Plan" (new `AssignNewPlanModal.tsx`, plan-picker + start date, calling the new endpoint). Benefits/next-billing-date display stay scoped to the most recent row. New locale keys in `members.*` (en complete; es/ca only for the new keys, since `members.*` in those two locales was already significantly behind en before this change). `user-memberships.test.ts` extended with tenant-isolation/auth/happy-path/invariant/validation coverage for the new endpoint.
- **Done (continued)**: #413 (Membership Plans — applicable tax, aligned with Sellable Items' financial config: `membership_plans.tax_rate_id FK→tax_rates` was already added in #311's migration 112/113 and backfilled to each gym's system tax rate, but no `tax_behavior` column existed alongside it and neither field was ever surfaced by any endpoint or the admin UI — `taxes.ts`'s `getTaxImpact()` already counted `membership_plans` referencing a tax rate, confirming the field was meant to be live. This ticket wires it up rather than duplicating Plans' existing price (`membership_plan_prices`, a validity-windowed price history) or renewal-frequency (`billing_policies.recurring_billing_interval`/`recurring_billing_unit`, already `day`/`week`/`month`/`year` — no `once` option, so "excluding one-shot" already held) mechanisms, which are richer than Sellable Items' flat `amount`/`billing_frequency` and are already exposed in `enrichPlan`/the Plans page's Billing and Prices sections. Migration 135 adds `membership_plans.tax_behavior VARCHAR(20) CHECK ('inclusive','exclusive') DEFAULT 'inclusive'`, mirroring `gym_charges.tax_behavior` from #311 (column add and CHECK constraint guarded independently per `126_tax_rates_description_and_audit_actor.js`'s pattern, so a retry after a partial failure can resume safely). `sellable-items.ts` now exports `computePriceFields`/`validateTaxRateId` for reuse. `membership-plans.ts`: POST validates `tax_rate_id`/`tax_behavior` and defaults `tax_rate_id` to the gym's system tax rate when omitted (mirroring the #311 backfill), PUT updates both fields, duplicate copies them; `enrichPlan` LEFT JOINs `tax_rates` and reuses `computePriceFields` against the plan's `current_price` to return `tax_rate_name`/`tax_rate_percent`/`amount_excl_tax`/`amount_incl_tax`. Admin Plans page (`plans/page.tsx`): Tax Rate + Tax Behavior selects added to the inline create and edit forms (fetches `GET /taxes`, same `taxRateOptions()` pattern as the Sellable Items page); expanded Details' Prices section shows the resolved tax rate and the excl/incl-tax breakdown of the current price. New `Applicable tax on membership plans` describe block in `membership-plans.test.ts` (system-tax-rate default, explicit tax rate + computed fields, cross-gym tax_rate_id 400, invalid tax_behavior 400, PUT update, price-with-tax computation, duplicate copies tax fields).
- **#416 (done)**: Multi-select status filtering on the admin Billing Events view (`payments/billing-events`). `status` on this endpoint is a derived value (`paid`/`failed`/`recorded`/`scheduled`, computed from `billing_events.event_type`, or hardcoded `scheduled` for projected future rows) rather than a stored column, so `GET /payments/billing-events` accepts `status` as a multi-value query param (repeated `status=a&status=b` or comma-separated, same `z.preprocess` normalizer pattern as #411's `lifecycle_status`) and filters the already-merged past+future array in JS before pagination; when the selected statuses exclude `scheduled`, the future-row DB query is skipped entirely rather than run and discarded. Admin UI: a `MultiSelectFilter` (#350) added to the existing filter bar, synced to a `?status=` URL param via the page's existing `pushParams`/`useSearchParams` pattern, so the filtered view is shareable/survives navigation. New `filter_status` i18n key in en/es/ca. `payments-billing-events.test.ts` extended with single/multi (comma and repeated-param)/invalid-value/tenant-isolation coverage for the new filter, plus a case confirming `status=scheduled` returns only virtual rows.
- **#446 (done)**: Expanding a nutrition template crashed with `TypeError: o.map is not a function`. #350 changed `GET /nutrition-library` from a bare array to `{ items, total, limit, offset }` (and `quality_slugs` → `qualities[]`); `NutritionPlanTree` still stored the paginated object and called `.map()` on it in `RestrictionsSection`. Tree now unwraps `items`, maps `qualities[].slug` → `quality_slugs`, and fetches `?limit=200`. No API/proxy change. Follow-up: admin and member `FeatureFlagsProvider` now wait for Clerk and send `Authorization` on `GET /api/proxy/feature-flags`, fixing the layout 401 (bare `fetch` with no token). Follow-up (this ticket): the `/class-types` 404 was the Schedule page (`[locale]/schedule/page.tsx`) still calling the pre-#360 `class-types` router (`class_type_id`/`class-types?status=active`), which was renamed to `activity-types` and now returns 404. Its `Session`/`ActivityType` types, form state, and API calls (fetch, `onActivityTypeChange`, session create/edit body) are now consistently `activity_type_id`/`/activity-types`, matching what `GET /class-sessions` actually returns and `POST/PUT /class-sessions` actually accept (both already `activity_type_id`, per the #360 stage 3 consolidation) — session create/edit was silently broken (400) on top of the dropdown 404. A separate, unreachable `[locale]/class-types/page.tsx` (not linked from any nav group, fully superseded by `[locale]/activity-types/`) still calls the same dead `/class-types` CRUD endpoints; left untouched as out-of-scope dead-code cleanup.
- **#362 (in progress — Slice 2 of Option A)**: Calendar functional audit found the page/data flow itself already correct (`apps/member/.../calendar/page.tsx` calls `GET /me/schedule` via the shared `apiClient` `buildHeaders()`-equivalent helper, defaults to `timeGridDay` (today), supports prev/next/today navigation, and has bounded loading + a `.catch` on fetch failures — no infinite-spinner bug here), but surfaced a real multi-center scoping gap: `GET /me/schedule` never applied any center restriction, so a member could see every center's class sessions in their gym instead of only their assigned center(s)'s. Root cause traced to `api/src/infra/centerContext.ts` resolving a `member`-role caller's `allowedCenterIds` via `member_centers JOIN members ON clerk_user_id = ctx.userId` — under member impersonation `userId` is the impersonating superadmin's own Clerk ID, never the impersonated member's, silently collapsing `allowedCenterIds` to `[]`. This underlying `centerContext` bug was independently fixed by #396; this PR wires `GET /me/schedule` in `api/src/api/me.ts` to call `getCenterContext(req)` and filter by `cs.center_id` (explicit `x-center-id` header, or restricted to `allowedCenterIds` for a plain member), mirroring the pattern already used by `centers.ts`/`class-sessions.ts`. Extended `api/src/test/me-impersonation-context.test.ts` with center-scoping regression coverage under impersonation. Bookings, Training, Nutrition, Membership/Payments, Alerts, and Profile audits remain as follow-up PRs against #362.
- **#360 (in progress — Stage 3: API consolidation)**: Unify Sessions and Events under `CalendarEvent`. Migration 134 closes schema gaps (adds `kind ENUM`, `center_id`, `modified_at`/`modified_by_membership_id` to `calendar_event_bookings`, fixes FK `fk_cestr_event` to CASCADE, adds `calendar_event_booking_id` to `class_package_transactions` and makes `booking_id` nullable). `class-sessions.ts` merged into `calendar-events.ts` (now exports both `classSessionsRouter` filtering `kind='session'` and `calendarEventsRouter` filtering `kind='event'`). `bookings.ts`, `package-credits.ts`, `plan-allowances.ts`, `shared-training-requests.ts`, and all 9 `/me/*` calendar/booking/shared-training endpoints repointed from `class_sessions`+`bookings`+`shared_training_requests` to `calendar_events`+`calendar_event_bookings`+`calendar_event_shared_training_requests`. `scheduleEngine.ts` extended to write `kind='session'`, `capacity`, and `center_id` on materialized events. `app.ts` import updated; old `class-sessions.ts` deleted. TypeScript clean; DB tests blocked by local env (ER_ACCESS_DENIED). Stages 4 (frontend) and 5 (cleanup: drop old tables, remove legacy columns) remain.
- **#362 (in progress — Slice 3 of Option A)**: Bookings functional audit. `apps/member/.../schedule/page.tsx` ("My Bookings") only ever fetched `/me/schedule` bounded to the next 14 days, so it could show upcoming bookable/booked sessions but had no way to show past bookings at all, and no way to open/view a booking's fuller details — despite the backend already having purpose-built `GET /me/upcoming`/`GET /me/activity-history` endpoints from #194 that nothing in the frontend ever called. Wired a new "Past bookings" section into the page using `GET /me/activity-history` (own loading/empty/error state, tap-to-expand showing attendance/cancellation detail); added the same tap-to-expand to the existing upcoming list (showing description/capacity) — this is the "open/view a booking" the ticket asks for, since there's no member-facing single-booking-by-id backend route to build a separate detail page against. Also fixed the page's error/empty-state conflation: a failed `/me/schedule` fetch used to set the same `message` state as a successful booking action, so an error banner and the redundant "No upcoming sessions" empty text could show together — `loadError` is now tracked separately and suppresses the empty-state text. Backend bugs found and fixed alongside: `GET /me/bookings` joined the pre-migration-059 `class_types`/`cs.class_type_id` columns (dropped on a fully-migrated DB) and 500'd on every call — rewritten to join `activity_types`/`activity_type_id` like every other `/me/*` handler; `GET /me/bookings`, `GET /me/upcoming`, and `GET /me/activity-history` never applied any center scoping (unlike the already-fixed `/me/schedule`) — all three now call `getCenterContext(req)` and filter by `cs.center_id`, matching the pattern fixed for `/me/schedule` in Slice 2. Cancellation (`DELETE /me/bookings/:id`) was already correctly implemented and member-reachable — confirmed working, not rebuilt. New `api/src/test/me-bookings.test.ts` covers auth, impersonation identity resolution, and center scoping for all three endpoints, plus the fixed `activity_types` join. Training, Nutrition, Membership/Payments, Alerts, and Profile audits remain as follow-up PRs against #362.
- **#364 (done)**: Made Admin staff impersonation fully database-driven — `api/src/api/impersonation.ts` no longer imports/instantiates `createClerkClient` at all. `GET /platform/impersonation/targets` builds the staff target list straight from `gym_memberships` (no per-row `clerkClient.users.getUser()` lookup, so a staff member missing from Clerk still appears). `POST /platform/impersonation/:targetId` validates staff targets against `gym_memberships` only (no Clerk superadmin check on the target). Per the ticket's answered clarification, no new `superadmins` DB table was introduced: superadmins are excluded implicitly because they carry no `gym_memberships` row (`TenantContext.gymMembershipId` is already documented as `null` for superadmins) — impersonating a superadmin's Clerk ID fails the same "no membership in this gym" check as any other non-staff ID. `requireSuperadmin` (gating who may *call* these endpoints) is unchanged and still Clerk-based. `impersonation.test.ts` updated: replaced the old "excludes staff who are superadmins" mock-driven tests with tests proving staff discovery/validation succeed even when the Clerk `getUser` mock throws for that user.
- **#372 (done)**: Member detail "Session Packages" section + attendance/cancellation-driven package-credit consumption, per the ticket's answered business-rule table. `MemberExpandedRow.tsx` (admin Members page) now renders each of a member's `user_class_packages` rows (name, purchased date, total/used/remaining sessions, expiration, status) via the existing `GET /members/:memberId/class-packages`; PAYMENTS-write staff get an inline "Extend expiration" action against a new `PUT /members/:memberId/class-packages/:id/extend-expiration` (400 unless the new date is strictly later, audited via `recordAudit` + new `user_class_packages.modified_at`/`modified_by_membership_id` columns, migration 128). Credit-consumption logic in `package-credits.ts`/`bookings.ts` changed: `cancelBooking` now only auto-refunds a spent credit when the member cancels **at least 1 day before** the session (`DATEDIFF(session.starts_at, UTC_TIMESTAMP()) >= 1`) — a same-day cancellation keeps the credit consumed by default, matching the confirmed rule that a same-day cancel or a booked-then-absent no-show still counts as consumption. New `POST /bookings/:id/refund-credit` (requireRole admin/front_desk/trainer_performance/trainer_perf_nutrition) lets a trainer explicitly refund a same-day-cancelled booking's credit — gated on `bookings.user_class_package_id` still being set, so calling it twice 400s instead of double-refunding. New `POST /class-sessions/:id/walk-in` (same roles) covers the "no booking, but attended" case from the rule table: books the member (`bookMemberOnSession(force=true)`, reusing the existing booking-time debit) and marks them present in one transaction, rejecting with 409 if they already have an active booking for that session. `refundPackageCredit` gained `reason`/`actorUserId` params so the ledger (`class_package_transactions`) distinguishes an automatic advance-cancellation refund from a manual same-day one. New `api/src/test/package-credits.test.ts` covers all of the above plus tenant isolation and role gating.
- **#374 (done)**: Support Multi-Member Membership Plans. Migration 129 adds `membership_plans.member_limit` (`'1'`/`'2'`/`'family'`, named CHECK, default `'1'`) and a new `user_membership_members` join table (gym_id, `user_membership_id`/`member_id` FKs both `ON DELETE CASCADE`, `is_owner`, UNIQUE on the pair) recording every Member covered by a Membership; existing Memberships are backfilled so their current owner becomes their sole covered Member (equivalent to `member_limit='1'`), so no manual data migration is needed. `membership-plans.ts` validates `member_limit` on create/update, copies it on duplicate, and rejects (400) shrinking it below the covered-Member count of any active Membership already on that plan. `user-memberships.ts`: `POST /` now inserts the paying Member as `is_owner=1` into `user_membership_members` in the same transaction as the Membership row; new `GET/POST /:id/members` and `DELETE /:id/members/:memberId` (all `requireModuleWrite('PAYMENTS')` for writes) manage the covered-Member list, enforcing the plan's cap server-side and protecting the owner from removal. Per the ticket's "benefits apply to all covered Members" requirement, the plan's booking-time entitlement checks (`plan-allowances.ts` and `plan-class-types.ts`'s `registerBookingAccessHook`s) and `GET /me/schedule`'s `access_locked` flag were extended so a covered Member gets the same access as the Membership's owner, not just the paying Member. Admin UI: Plans page gets a "Members per Membership" select (Individual/Couple/Family) in the add/edit/details views; Memberships page gets a new "Members" action opening `MembershipMembersModal.tsx` (owner-first list, add via a Member picker respecting the cap, remove for non-owners). Bug fix found and fixed along the way (pre-existing, unrelated to this ticket): `user-memberships.ts`'s `effectivePrice()` selected `membership_plans.base_price`, a column dropped in migration 058 and never restored — every `POST /user-memberships` call was failing with a MySQL "Unknown column" error, apparently uncaught because this router had zero test coverage before now. New `api/src/test/user-memberships.test.ts` (the router's first test file — auth/role/tenant-isolation guards, full CRUD happy paths, and the whole `/:id/members` sub-resource including cap enforcement for each `member_limit` value, the owner-removal guard, and the duplicate-member 409) and extensions to `membership-plans.test.ts` for `member_limit` validation and the shrink-guard.
- **#375 (done)**: Aligned Membership Plans with the standard Admin Inline row CRUD pattern (frontend-only; no migration, no API changes). `apps/admin/src/app/[locale]/plans/page.tsx` no longer imports `CrudModal` — the Add Plan, Details, Price, Allowance and Centers modals were all removed. `+ Add Plan` now opens an inline creation row (`inlineNew` state, `renderInlineNewRow()`, capturing Name/Description/Status/Enrollment as in the ticket's mock; a default `billing_policies` row is created alongside it, same as before). `Details` from the row's `ContextMenu` now just expands the row read-only (`showDetails()`) instead of opening a separate modal, satisfying the ticket's "Details and Edit use the same expanded context" requirement. Billing Policy, Centers, Included Services (allowances) and Price History each gained a per-section inline `Edit`/`+ Add` toggle within the expanded row (`billingEditForPlanId`, `centersForPlanId`, `allowanceForPlanId`, `priceForPlanId`), matching the pre-existing inline Charge Benefits editor already on this page — this also newly makes Billing Policy editable after creation (previously only settable via the removed Add modal). `docs/feature-patterns.md` and `CLAUDE.md` updated: Plans is now the **Inline row CRUD** reference (alongside Sellable Items/Taxes) instead of **Modal CRUD**; Class Types is the new Modal CRUD reference. Also carries forward #374's "Members per Membership" select and Members-action wiring on the Plans/Memberships pages, reconciled with the inline-row rewrite during the merge with #374.
- **#436 (done)**: Fixed "Delete Center" (`DELETE /centers/:id`), which crashed with a 500 on almost every real-world delete. Root cause: `firstDependentTable()`'s pre-delete dependency guard queried a table called `events`, permanently dropped in migration `095_drop_events.js` (#221) — the guard only reaches that entry when a center has no dependents in the tables checked before it, so it fired on the common case (deleting a center with no remaining dependents), not the rare one. Also modernized two other stale entries left over from the #360 stage 3 consolidation: `class_sessions` → `calendar_events` (scoped to `kind = 'session'`, so one-off Activities/CalendarEvents don't block a delete) and `bookings` → `calendar_event_bookings`. Follow-up after CI on #433: `rooms` → `spaces` (migration 067 rename), which #436 had left in place and which still 500'd every clean delete; session test fixtures now set required `calendar_events.title`; `POST /me/shared-training-requests` aliases `class_session_id` again. Soft delete (`deleted_at`/`deleted_by_membership_id`/`deleted_by_name`), audit logging (`recordAudit`, entity type `center`), and recycle-bin integration (`center` was already a registered `EntityType` in `recycle-bin.ts`) were all already correct — they just needed the crash removed so the DELETE handler could reach them. First test coverage for this router: new `api/src/test/centers.test.ts` (auth/role/tenant-isolation guards, full CRUD happy paths, and the dependency-check/soft-delete/audit/recycle-bin-restore regression this fix addresses).
- **#376 (done)**: Assign Plan to Member(s) and Create Membership & Billing Events, building on #374's multi-member model. New `POST /membership-plans/:id/assign` (`api/src/api/membership-plans.ts`, admin-only) takes `{ member_ids[], owner_member_id, starts_at }`, validates the count against the plan's `member_limit` (exactly 1/2, unlimited for `family`) and that every member exists in the gym, then in one transaction: inserts the `user_memberships` row (owner as `member_id`, reusing `effectivePrice()` from `user-memberships.ts`), writes the P1.6 `status_changed` billing ledger row via `recordStatusChange`, inserts one `user_membership_members` row per selected member (owner flagged, per #374), and snapshots the plan's current `plan_charge_benefits` onto a new `user_membership_charge_benefits` table (migration 130 — same shape as `plan_charge_benefits`, `no_benefit` rows excluded, unique on `(user_membership_id, gym_charge_id)`) so a later edit to the Plan's benefits never retroactively changes an already-created Membership. After the transaction commits, any Promotion currently targeting the plan is auto-applied best-effort via the existing `applyPromotionToMembership()` (a non-stackable conflict is skipped, not fatal). Reuses the existing `ER_DUP_ENTRY` → 409 handling for "member already has an active membership." Admin Plans page gained an "Assign Plan to Member" context-menu action opening `AssignPlanModal.tsx` (member search/multi-select via the existing `MemberSearchInput`, owner radio-select, capacity warning, starts-on date).
- **#438 (done)**: Fixed feature-flag/Home sync in `apps/member/`. The "Next Booking" section on Home (`page.tsx`) rendered unconditionally regardless of the `member_web.my_bookings` flag, because it's backed by `GET /me/schedule` — deliberately left ungated at the API layer (#367) since it's shared with the un-flagged `/calendar` page — so disabling the flag left the widget (and its backend-gated `DELETE /me/bookings/:id` cancel button) visibly stuck instead of hidden; now explicitly wrapped in the same `featureEnabled('member_web.my_bookings')` check (superadmin-bypassing) already used by the nav tiles and My Membership card. Also fixed client-side staleness: `FeatureFlagsProvider` fetched flags once on mount and never again, so an admin's toggle wasn't reflected in an already-open member session without a full reload — it now polls `GET /feature-flags` every 30s, matching the backend's in-memory cache TTL (`api/src/infra/featureFlags.ts`). Scoped to `apps/member/` per the ticket; the identical staleness pattern in `apps/admin/`'s `FeatureFlagsContext.tsx`, plus impersonation-scoped flag evaluation, is covered by #439.
- **#360 (in progress — stage 3 of 5, API consolidation)**: Unify Sessions and Events under `CalendarEvent`, staged per the agreed rollout (design → schema+booking support → API consolidation → frontend consolidation → cleanup; stages 1–2 landed via #406/#407). This stage repoints every booking/session/shared-training router at the stage-2 schema with no behavior or field-name change visible to any frontend: `class-sessions.ts` merged into `calendar-events.ts` (still mounted at `/class-sessions` and `/calendar-events`, discriminated by a new `calendar_events.kind ENUM('session','event')` column so the admin Calendar page's existing dual-fetch, #326, still renders each occurrence exactly once); `bookings.ts`, `me.ts`, `shared-training-requests.ts`, `plan-allowances.ts`, and `package-credits.ts` now read/write `calendar_events`/`calendar_event_bookings`/`calendar_event_shared_training_requests` instead of `class_sessions`/`bookings`/`shared_training_requests` (now frozen, dropped in stage 5). Migration 134 closes schema gaps found while scoping this stage: `calendar_events.center_id` (was never added despite docs assuming it existed), `calendar_event_bookings.center_id`/`modified_at`/`modified_by_membership_id`, `calendar_event_shared_training_requests`'s FK recreated `ON DELETE CASCADE`, and `class_package_transactions.calendar_event_booking_id` (a mutual-exclusivity CHECK against the now-legacy `booking_id`). Also fixed a pre-existing bug found along the way: `domain/scheduleEngine.ts`'s schedule-rule materializer fetched `activity_type.max_capacity` but never wrote it to the generated `calendar_events.capacity` column — now backfilled for existing rows and fixed going forward, and the materializer also resolves `center_id` (activity type's `default_center_id`, falling back to the gym's sole center) and stamps `kind='session'`. Per the ticket's answered scoping questions (issue comment thread), this was a hard cutover with no dual-write/backfill (no production data to preserve) and no literal router-file merge deferral to stage 4. See `docs/architecture.md`'s "Planned: CalendarEvent Unification" section and `docs/decisions.md` #10. Frontend consolidation (stage 4) and dropping the old tables (stage 5) remain.
- **#417 (done — Stage 3: nutrition meal images)**: Cloudflare R2 image storage integration, per the agreed 3-stage plan on the issue thread (Stage 1: R2 client + bucket-init action, no upload yet; Stage 2: generic upload endpoint + rewire `exercises.image_url`; Stage 3: add `image_url` to nutrition meals). Credentials are platform-wide env vars only (`CLOUDFLARE_R2_ENDPOINT`/`CLOUDFLARE_R2_ACCESS_KEY_ID`/`CLOUDFLARE_R2_SECRET_ACCESS_KEY`/`CLOUDFLARE_R2_BUCKET`) — one shared bucket, folder-scoped per gym — not per-gym DB-stored secrets, per the issue's confirmed resolution to CLAUDE.md's "no hardcoded values"/"no additional stores" constraints. New `api/src/infra/storage.ts` lazily constructs an `@aws-sdk/client-s3` client (mirrors `payments/index.ts`'s `getPaymentProvider()` pattern) and exposes `isStorageConfigured()`, `sanitizeGymFolderName()`/`buildGymFolderPrefix()`, and `initializeGymBucket()` (creates the `Nutrition/Images`, `Exercises/Images`, `Exercises/Videos`, `Branding/Logo`, `Branding/Images`, `Members` folder markers). Migration 135 adds `gyms.storage_folder_prefix` (nullable, unique) and `gyms.storage_initialized_at` (nullable). New `POST /platform/gyms/:id/storage/initialize` (superadmin) — 503 if the deployment isn't configured, 502 on an R2 failure, idempotent (reuses the captured prefix on re-run). Admin **Cordel → Gyms** page gets a read-only Storage section + "Initialize Cloudflare Bucket" button. Follow-up fix: the GitHub Environment `dev` secrets/vars for R2 existed but `deploy.yml` never forwarded them to the VPS, so `isStorageConfigured()` was always false in production and the button never rendered (only the "not configured" warning did) — `deploy.yml` now passes all four `CLOUDFLARE_R2_*` values through. This also came with a deploy-mechanism change: the deploy script now writes the API's whole runtime env — DB creds, Clerk keys, and the R2 values — as inline `Environment=` lines directly in the `fitness-api.container` quadlet unit (regenerated every deploy, GitHub stays the source of truth), not a separate `fitness-api.env` file as before. **Stage 2**: new `api/src/api/storage.ts` (`storageRouter`, mounted at `/storage`) exposes `POST /storage/uploads/exercise-image` (`requireModuleWrite('TRAINING')` + `requireFeatureEnabled('training.exercises')`) — one route per upload target so each target keeps its own module/feature guard, rather than one shared gate. Raw-body upload via `express.raw({ type: image/*, limit: '6mb' })`, same pattern as the existing theme-logo upload in `themes.ts`; validates mime (png/jpeg/webp/gif, else 415) and a 5MB size cap (413); 503 if the deployment has no R2 configured; 409 with the ticket's exact copy ("Cloudflare storage has not been initialized for this gym, therefore images cannot be uploaded.") if the calling gym's `storage_folder_prefix` is still null; 502 on an R2 failure. New `infra/storage.ts` `uploadGymImage()` builds the object key as `<folderPrefix>/<folder>/<uuid>.<ext>` and returns the URL as `${CLOUDFLARE_R2_ENDPOINT}/${CLOUDFLARE_R2_BUCKET}/<key>`, per the issue thread's confirmed URL-construction answer. Frontend: new reusable `apps/admin/src/components/ImageUploadField.tsx` (thumbnail preview + Upload/Replace/Remove buttons + inline not-configured/not-initialized warning, reading two new fields — `storage_configured`/`storage_folder_prefix` — added to `GymContext`'s `GymOption`) replaces the plain URL `<input>` for `image_url` in both the Exercises inline-edit row and the Add-Exercise modal; the read-only Exercise Details modal and the expanded-row view now render the image as a thumbnail instead of a raw URL string. **Stage 3**: migration 138 adds nullable `nutrition_library_items.image_url` (`VARCHAR(500)`, mirrors `exercises.image_url`) — gym-owned items only, since system (`gym_id IS NULL`) catalog items have no per-gym storage folder to upload into. New sibling route `POST /storage/uploads/nutrition-image` (`requireModuleWrite('NUTRITION')` + `requireFeatureEnabled('nutrition.nutrition_library')`) shares `handleImageUpload()` with the exercise-image route, uploading into `Nutrition/Images`. `nutrition-library.ts`'s create/update endpoints now accept/return `image_url` (the platform/system-item router, `platform-nutrition-library.ts`, is untouched — system items stay image-less). Admin **Nutrition → Nutrition Library** page's Add/Edit modals use the shared `ImageUploadField` (`uploadPath="/storage/uploads/nutrition-image"`), and the expanded row's detail view renders the stored image as a thumbnail when set.
- **#439 (done)**: Feature flag toggles didn't reach an already-open admin/member session (fetched once on mount, no polling), and a superadmin's flag-bypass incorrectly carried over while impersonating, so an impersonated user saw the superadmin's unrestricted view instead of their own. Fix: `requireFeatureEnabled` (`api/src/infra/featureFlags.ts`) now bypasses only in native capacity — `tenantCtx.isSuperadmin && !tenantCtx.impersonatedUserId` — `tenantCtx.isSuperadmin` itself is unchanged (still `true` under impersonation for other permission checks). Both `FeatureFlagsContext.tsx` (admin and member) now poll every 30s, matching the backend's in-memory cache TTL. Frontend bypass checks were updated the same way: admin's `Sidebar.tsx` computes `filterNavGroups`'s role as `(isSuperadmin && !isImpersonating) ? 'superadmin' : activeGym?.role`; member's Home/`/training`/`/nutrition`/`/schedule`/`/membership`/`/profile` use `isSuperadmin && !isImpersonating` in place of raw `isSuperadmin`, `isImpersonating` from `useImpersonation()`. `AdminBar`/`TopHeader`'s own impersonate/exit-impersonation controls and `GymSwitcher`'s pre-impersonation gym picker intentionally keep raw `isSuperadmin` — they must render in native capacity regardless of feature flags. Extended `member-web-feature-flags.test.ts` to assert the corrected precedence (impersonated superadmin gets 403 when the flag is off; native superadmin still bypasses).
- **Training module redesign (#60–#63, done)**: dynamic Workout Block form driven by
  `blockFieldConfig.ts` (#60); tree-grid Training Plan Template editor (#61); dependency
  awareness for shared catalog entities (#62); tree-grid Workout Template editor with
  in-place block/exercise CRUD, paginated/filtered list (`created_by_membership_id`,
  migration 053) and cross-template block drag-and-drop via `PUT …/blocks/:id/move` (#63).
  Patterns documented in `docs/feature-patterns.md` (Tree-Grid Editor, Config-Driven Fields,
  Dependency Awareness).
- **Personalized Training Plans module (#67, done)**: gym-level **Training → Training
  Plans** page (list, filters by member, source template, status, created by, and date
  ranges) with a two-step "New Training Plan" dialog (From Template / From Scratch) that
  posts to `POST /training-plans`. Existing-active-plan collisions return `409`; the UI
  swaps to a keep-vs-expire prompt whose "Expire" branch closes prior actives
  (`status='expired'`, `end_date` = new plan's start) and their `member_training_plans`
  rows atomically before the new plan is created. Editor page at
  `/training-plans/[id]` with a header form (name/desc/dates/status), drag-reorder for
  workouts, cross-parent move + duplicate for blocks and exercises via new endpoints on
  `training-plans.ts` (`…/blocks/:id/move`, `…/exercises/:id/move`,
  `…/duplicate` at each level). Templates page gains an **Assign Plan to Member** action
  on active templates that opens the same dialog with the template preselected.
  Migration 054 adds `training_plans.start_date` (backfilled from the latest
  `member_training_plans.valid_from`) and `end_date`, and updates the status CHECK to
  `draft/active/expired/deleted` (existing `inactive` rows remapped to `expired`).
  `training-plan-creation.ts` extracts the clone/write-history transaction so both
  `POST /training-plans` and the legacy `POST /members/:id/member-training-plans` share
  one implementation.
- **Training Plans UX Redesign — Inline Editing (#112, done)**: replaced the
  `/training-plans/[id]` editor page with an inline expandable card list matching the
  Training Plan Templates UX. Each card expands to show workouts/blocks/exercises with
  the existing drag-reorder and block/exercise CRUD modals. New context menu actions:
  **Edit** (inline metadata), **Details** (read-only modal with full audit info),
  **Duplicate** (clones the full plan tree to a draft), and **Complete** (transitions
  status to `completed`, stamps `end_date`, makes plan read-only). Migration 066 adds
  `completed` to the status CHECK constraint. The old `/training-plans/[id]` route now
  redirects to the list. All write endpoints on `training-plans.ts` reject completed plans
  with 403.
- **Training Plan Workout Builder Parity (#164, done)**: Training Plan workout cards now have
  full UX parity with the Workout Template builder. Extracted `WorkoutBlockBuilder.tsx`
  (`apps/admin/src/app/[locale]/workout-templates/`) — a self-contained component with its own
  dnd-kit `DndContext` that handles block reorder, exercise reorder within a block, and
  cross-block exercise moves via `PUT …/blocks/:srcBlockId/exercises/:exId/move`. Props:
  `workoutKey` (namespaces drag IDs to avoid collision across multiple open workout cards),
  `blocksUrl`, `blocks: HierBlock[]`, `canWrite`, `onChanged`. Training Plans page updated:
  block/exercise CRUD modals removed from the workout tree; each workout card now renders
  `WorkoutBlockBuilder` inline with a searchable `ExerciseCombobox`, always-editable fields
  (inline block name + type + config, inline exercise rows, result_type/target_value/unit),
  block Duplicate/Delete context menus, and drag-and-drop at both levels. Workout name
  always-editable inline (on-blur PUT, reverts on error). `+ Add Workout` replaces
  `window.prompt()` with an inline form. `canWrite` now uses `canWriteModule(role, 'TRAINING')`
  (matches Training Plan Templates pattern). No API or DB changes — backend already supported
  all required endpoints (`/blocks`, `/blocks/:id/exercises`, cross-block `/move`, etc.).
- **Billing ledger member history endpoint (#114, done)**: `GET /billing-events/member/:memberId`
  convenience alias added to `billing-events.ts` (paginated, tenant-isolated, 404 on unknown
  member). First test suite for the billing ledger (`billing-events.test.ts`): 14 tests
  covering auth, happy paths, new endpoint, and cross-gym tenant isolation.
- **Cordel platform menu + two-scope audit log (#66, done)**: superadmin-only **Cordel**
  sidebar group (Gyms, Users, platform Audit Log at `/cordel/audit`); **System** reduced to
  gym-scoped Audit Log + Customize and opened to gym admins (`requiredRole: 'admin'`).
  `GET /audit-logs?scope=all` (platform superadmins only, `tenantCtx.isSuperadmin`) lifts
  the tenant gym filter and joins `gyms.name`; shared `AuditLogView` component renders both
  scopes, adding a Gym column in platform mode.
- **Audit Log UX Improvements (#69, done)**: each audit row is now a self-contained
  historical snapshot. `actor_name` and `entity_name` are captured at write time and never
  recalculated (migration 055; existing rows truncated). Actor name comes from the Clerk user
  object already fetched in `tenantContext` (zero extra queries). Entity name is resolved by
  `infra/audit-registry.ts`: named entities use a `name`-column lookup; M-N/link entities
  get a composed label from their FK parents (e.g. `user_membership` → member name + plan
  name). FK fields in `previous_values`/`new_values` are enriched at write time from plain
  `foo_id: value` to `foo: { id, name }` objects. `GET /audit-logs/meta` returns the full
  entity-type list + action list for dropdown population. New filter params: `entity_name`
  (LIKE), `actor` (name LIKE or Clerk ID exact match), `action`, `source`. `AuditLogView`
  gains dropdowns for entity type, action and source; actor now searched by stored name;
  entity column shows `type#id` + name beneath it; Clerk uid surfaced only in the expanded
  row. `class-types.ts` and `promotions.ts` gain `recordAudit` coverage.
- **Done outside the phase plan**:
  - **Assign Members When Creating a CalendarEvent (#366)**: staff can assign one or more Members directly when creating a session or a recurring activity, instead of booking them one at a time afterward. `POST /class-sessions` accepts an optional `member_ids: number[]`, validated and booked (via the existing `bookMemberOnSession(force=true)` — same mechanism as a manual staff "Add member") inside the same transaction as the session's own INSERT. New `activity_type_schedule_rule_members` table (migration 135) lets staff assign Members to a recurring `activity_type_schedule_rules` row; `materializeScheduleRule` auto-books every assigned Member on each freshly generated `calendar_events` occurrence — including occurrences generated by a later `PUT` re-materialization — so the assignment isn't a one-time snapshot. Capacity is always advisory for this staff-driven path (never blocks create/save); Member-facing privacy needed no changes since `/me/bookings` and the member calendar were already scoped to the caller's own bookings. New `MemberMultiSelect` component (`[locale]/calendar/MemberMultiSelect.tsx`) wraps the existing `MemberSearchInput` with removable chips and a lightweight over-capacity warning, used on both the Schedule page's session-creation form and the Activity Types page's schedule-rule form.
  - **Per-gym theming (#51, extended by #68)**: `gyms.theme_key` (migration 030, presets `indigo/emerald/crimson/amber`), `ThemeProvider`, and the superadmin **System → Customize** editor (now replaced by #68).
  - **Centers UI Redesign (#74)**: Centers list now shows Email, truncated Address, Active Members count, and a ContextMenu (⋮) replacing Edit/Delete buttons. Menu items: Edit (existing modal, unchanged), Details (new read-only dialog with General / Statistics / Audit sections including `created_by`/`modified_by`/`deleted_by` resolved names), View Members (navigates to `/members?centerId=X&status=active`), Delete. Migration 064 adds `created_by_membership_id` and `deleted_by_membership_id` to `centers`. `GET /centers` and `GET /centers/:id` return `active_member_count` (subquery) and resolved modifier names via LEFT JOINs. `GET /members` gains `?centerId` filter. Members page reads `centerId` from URL params on load and shows a center dropdown filter (multi-center gyms only). `ContextMenu` component updated to `position: fixed` dropdown so it escapes `overflow: hidden` table containers.
  - **Membership Plans Redesign (#70)**: `membership_plans` gains `lifecycle_status` ENUM(`draft`/`active`/`archived`) + `enrollment_status` ENUM(`open`/`closed`) + soft-delete + audit fields (migrations 058–062). `class_types` renamed to `activity_types` (migration 059, all FK references migrated). New tables: `billing_policies` (1-per-plan billing/service cycle config), `plan_allowances` (replaces `class_type_user_memberships` + `membership_plan_benefits`; supports `unlimited` and `session_count` with recurrence), `membership_plan_centers` (optional per-plan center restriction). API: enriched `GET /membership-plans` (nested `billing_policy`, `allowances`, `centers`, `price_history`, `current_price`, `member_count`); new actions `duplicate`, `archive`, `PUT enrollment`. Booking enforcement (`plan-allowances.ts` side-effect hook) checks center restriction then allowance type/quota. Admin UI replaces DataTable with a per-plan accordion showing all five sections inline; context menu with Edit / Duplicate / Open‑Close Enrollment / Archive / Delete.
  - **Theme Management (#68)**: first-class `themes` entity (migrations 056–057 — table + gym FK). `tokens` JSON carries header/sidebar/typography design tokens. Superadmin **Cordel → Themes** CRUD (draft/active/deleted lifecycle, logo upload up to 512 KB via `POST /platform/themes/:id/logo`, `GET /themes/:id/logo` public caching endpoint). Gyms assigned a theme via `theme_id` FK replacing old `theme_key`. Admin `ThemeProvider` reads `activeGym.theme.tokens` from the gym list response (LEFT JOIN) and writes `--gd-*` CSS variables live on gym switch. Member app introduces `GET /me/gym` (no tenant context needed) so `AppContext` self-resolves gymId + theme; member-side `ThemeProvider` applies the same variables. Both `TopHeader` and `Sidebar` consume `--gd-*` vars; `BottomNav` in the member app follows suit.
  - **Separate Base Themes from Org Themes / Fix Default Logic (#144)**: `GET|PUT|DELETE /platform/themes` now filter to `gym_id IS NULL` — org-created custom themes no longer appear in the Base Themes screen. New `is_system_default TINYINT(1)` column on `themes` (migration 072, Black seeded as initial default) marks the single Base Theme used as factory default when a new organization is created. New endpoint `PUT /platform/themes/:id/set-system-default` atomically flips `is_system_default` to the target theme and clears all others; 404 if the target is not a base theme. Base Themes list response drops `is_default`/`gym_name` in favour of `is_system_default`. UI: Type column always shows "System" badge; Default badge reflects `is_system_default`; "Set as System Default" added to the context menu for non-default base themes. Org default theme logic (`gyms.theme_id`) and center inheritance are unchanged.
  - **Immediate Theme Application on Default Change (#143)**: After a successful `PUT /system/themes/:id/set-default`, the Themes page now calls `refreshGyms()` in parallel with `loadAssignments()`. `GymProvider` re-fetches the gym list (which includes the updated `theme.tokens`), causing `ThemeProvider` to re-apply `--gd-*` CSS variables immediately — no browser refresh required. No backend or migration changes.
  - **Instant Theme Reload on Any Change (#209)**: Any token modification (colors, typography, advanced) is now applied to the DOM immediately via `applyTokens()` when the edited theme is the currently active gym or center theme — no waiting for the debounced save. After the save succeeds, `refreshGyms()` + `refreshCenters()` sync the context so the `ThemeProvider` confirms the new state; on failure the pre-edit tokens are restored. Assignment changes (Assign Centers, Restore Inheritance, Set as Org Default) also call `refreshGyms()` + `refreshCenters()` so the new theme assignment propagates instantly. `CenterOption` in `CenterContext` gains `theme_id: string | null` (already in the API response via `c.*`) to let `isExpandedThemeActive()` detect when the active center has a direct theme override. No backend or migration changes.
  - **Theme Assignment Management (#120)**: Gym admins can now assign themes to the Organization and individual Centers from within the inline Theme editor. System → Themes page (`/themes`) rebuilt as the proper gym-admin page using `GET /system/themes` (was accidentally overwritten in #117 with Base Themes content). New Assignments tab in the inline editor for every theme (base or custom): org-default checkbox (`gyms.theme_id`), center list with Inherited/Assigned badges, Restore Inheritance (sets `centers.theme_id = NULL`), "+ Assign Centers…" multi-select picker, search filter, show-all when > 10. Five new endpoints on `gym-themes.ts`: `GET/:id/assignments`, `PUT/:id/set-default`, `GET/:id/unassigned-centers`, `POST/:id/assign-centers`, `DELETE/:id/centers/:centerId`. Delete guard unified to single message: "This theme is currently in use. Remove all assignments before deleting it." Theme resolution order: `centers.theme_id` → `gyms.theme_id` → first alphabetically.
  - **Base Themes rich list (#121)**: Cordel → Base Themes page gains a rich summary row: Logo (32×32), Name, Description (first line truncated), Type badges (System/Custom + Default when `gyms.theme_id` points to the theme), Usage count (distinct orgs for system themes; centers direct or inherited for custom themes), and Status. The ⋮ context menu now includes a Details action opening a read-only modal with full description, type, status, usage, owner (Cordel / gym name), org-default flag, and audit fields (Created/Modified by + at, resolved from `audit_logs`). New `description TEXT` column added to `themes` (migration 068). `GET /platform/themes` now returns all themes (system + custom) in one list; `GET /platform/themes/:id` includes `created_by_name` / `modified_by_name`.
  - **Meals Sub-Entities UX Alignment (#162)**: Dishes, Sides, and Sauces now follow the platform entity standard. Migration 077 adds `status`, `created_by_membership_id`, `modified_by_membership_id`, `deleted_at`, `deleted_by_membership_id` to all three tables. `DELETE` changed from hard-delete to soft-delete (`status='deleted'`). New `POST /:id/duplicate` endpoint. `GET /` filters out deleted items; `GET /:id` returns full audit fields. Admin UI replaced with `DataTable` with columns: Name, Description (truncated), Created At, Created By, Status badge, Actions. Context menu: Details (read-only modal with audit fields) / Edit / Duplicate / Delete. 65 new tests in `meals-catalog.test.ts`. **Note**: dishes/sides/sauces are fully removed by #163 — `meals-catalog.test.ts` and the three routers are deleted.
  - **Nutrition Library + Nutrition Plan Templates Redesign (#163)**: Replaces the gym-scoped dishes/sides/sauces catalog with a global `nutrition_library_items` table (no `gym_id`, seeded with 32 items across 6 categories: main_dish, side, sauce, drink, dessert, other — same pattern as `benefit_types`). Migration 075 drops `nutrition_plan_template_meal_dishes`, `dishes`, `sides`, `sauces`; creates `nutrition_library_items`, `nutrition_plan_template_restrictions`, `nutrition_plan_template_goals`; adds `main_dish_id`, `side_id`, `sauce_id`, `drink_id` FK columns to `nutrition_plan_template_meals`; extends weekday CHECK to 0–7 (7 = "All Days"). New `nutrition-library.ts` router: `GET /nutrition-library` (global read, optional `?category=` filter, no write endpoints). `nutrition-plan-templates.ts` rewritten: meals now have 4 direct library FK selectors instead of a sortable dish list; new CRUD endpoints for restrictions (`POST/GET/PUT/DELETE /:id/restrictions`) and goals (`POST/GET/PUT/DELETE /:id/goals`); `POST /:id/duplicate` deep-clones days→meals + restrictions + goals in one transaction. `GET /:id/hierarchy` returns `{ days, restrictions, goals }`. Admin: Nutrition Library page (read-only category view), navigation entry renamed from "Meals" to "Nutrition Library", `meals/page.tsx` redirects to `nutrition-library`. `NutritionPlanTree.tsx` rewritten: `MealRow` renders 4 library dropdowns, `RestrictionsSection` and `GoalsSection` appended below the days list. `meals-catalog.ts` router and `meals-catalog.test.ts` deleted. New test files: `nutrition-library.test.ts` (4 tests), `nutrition-plan-templates.test.ts` fully rewritten (removing all dish references).
  - **Nutrition Plan Templates (#132)**: Inline expandable nutrition module. Shared gym-scoped catalogs: `dishes`, `sides`, `sauces` (each a full CRUD router: `GET/POST/PUT/DELETE /dishes|sides|sauces`; `DELETE /dishes/:id` returns 409 if the dish is referenced in any template). Template hierarchy: `nutrition_plan_templates` → days (weekday 0–6, one per weekday per template) → meals (N per day, drag-reorder) → meal_dishes (N per meal, each with optional side + sauce, drag-reorder). New routers: `nutrition-plan-templates.ts` (template CRUD + duplicate deep-clone + hierarchy endpoint + day/meal/meal-dish CRUD + per-level reorder), `meals-catalog.ts` (factory for dishes/sides/sauces). Admin UI: **Nutrition → Meals** page (three expandable catalog sections), **Nutrition → Nutrition Plan Templates** page (expandable cards with inline tree editor using @dnd-kit at three levels). Migration 071 creates 7 new tables. 38 new tests; `cleanupTestGyms` extended with nutrition table deletions.
  - **Centers UX Redesign (#131)**: Centers page replaces the DataTable + modal editor with the standard Cordel inline expandable row pattern. Collapsed row shows Name, Email, Theme (with "(Inherited)" suffix when `center.theme_id` is null), Status inline dropdown, Created At, and a ContextMenu (Details / Delete). Expanding a row opens four inline sections: General Information (Name, Code, Status), Contact Information (Email, Phone), Location (Address), and Theme assignment. All text fields auto-save on blur/Enter; dropdowns (Status, Theme) auto-save on change. No Save/Cancel buttons. "+ Add Center" creates a record via POST then auto-expands + focuses the Name field. Theme section: dropdown of all gym-accessible themes (base + custom); when a direct theme is assigned, a "Restore inheritance" link appears to reset `center.theme_id` to null. Details modal (audit only: Created At/By, Modified At/By, Deleted At/By). The legacy "Edit Center" modal is removed. `GET /centers` and `GET /centers/:id` now join `themes` and `gyms` to return `theme_name` and `gym_theme_name`.
  - **Workout Templates Fully Inline Editing (#130)**: Replaces modal-based block/exercise editing with always-editable inline fields — no save/cancel buttons, no modals. Block header is a single row (name input + type select + rounds input) that auto-saves on blur/change. Exercise table rows are always editable; exercise selection uses a custom filter-as-you-type combobox (position: absolute listbox, closes on outside click). Type-adaptive columns: `reps` → Sets/Min–Max reps/Rest, `time` → Sets/Duration/Rest, `distance` → Distance/Unit/Rest. Rest field displayed in minutes (÷60 display, ×60 on save). New endpoints: `POST /:id/blocks/:blockId/duplicate` (deep-copies block + exercises), `POST /:id/blocks/:blockId/exercises/:exId/duplicate`. Context menus: Duplicate + Delete (no Edit). `BlockModal` and `ExerciseModal` removed. Migration 071 adds `exercise_type VARCHAR(20) NOT NULL DEFAULT 'reps'` (CHECK: reps/time/distance) to `exercises`; adds `duration_seconds INT UNSIGNED`, `distance_value DECIMAL(8,2) UNSIGNED`, `distance_unit VARCHAR(20)` to `workout_template_exercises`. `exerciseSummary()` in `summaries.ts` updated for type adaptation and rest-in-minutes.
  - **Promotions UX redesign (#127)**: Promotions page replaces the DataTable + modal editor with the standard inline expandable row pattern. Collapsed row shows Name, Description, Created, Start Date, End Date, Status badge, and a ContextMenu (Details / Duplicate / Delete). Expanding a row opens an inline form for all promotion fields plus two embedded child collections: **Charge Benefits** (charge_type, action, value) and **Period Benefits** (membership_plan, action, value, duration_months). Both collections display as compact tables with inline add-row forms and per-row delete. "+ New Promotion" creates a record and auto-expands + focuses the Name field. Search by name/description with 300ms debounce; status filter; `created_by_membership_id` tracking (migration 069). `promotion_period_benefits` table redesigned from "pay X get Y free" model to "plan + action + value + duration" model (migration 070). New API: `GET /promotions` adds `q`, `created_by`, `sort`, `dir`; `GET /promotions/created-by-options`; `POST /promotions/:id/duplicate` (deep-copies charge + period benefits); `PUT /charge-benefits/:cbId`; `PUT /period-benefits/:pbId`; `POST /period-benefits` updated to new shape. Details modal simplified to audit info only (created_by, created_at). Refined by #158: Edit moved to context menu (rows no longer expand on click), list columns restructured (Type + Value derived from first charge_benefit, Period combines starts/ends, Created By added), Details modal expanded with full promo fields + modified_at/by from audit_logs, `GET /promotions` and `GET /promotions/:id` extended with primary_action_type, primary_value, modified_at, modified_by_name.
  - **Base Themes UX unification (#117)**: Cordel → Base Themes page replaces the DataTable + modal editor with the same inline expandable row pattern used by System → Themes. Each row shows a color swatch, name, status badge, and a context menu (Clone, Details, Delete). Expanding a row opens the branding/typography/colors inline editor. New `POST /platform/themes/clone/:id` clones a base theme into a new base theme (superadmin only). A read-only Details modal surfaces name, created_at, and modified_at. Deleted themes are non-expandable.
  - **Theme Management v2 (#97)**: Separates platform-owned Base Themes from customer-owned Customer Themes. Migration 065 adds `gym_id` to `themes` (NULL = base, non-NULL = customer theme for that gym) and `theme_id` to `centers` (optional per-center override). Seeds "Black" base theme. New `POST /system/themes/clone/:id` (clone any theme to customer theme), `PUT/DELETE /system/themes/:id` (edit/soft-delete customer themes with gym/center conflict guard), logo endpoints. Cordel nav entry renamed to **Base Themes** (`nav.base_themes`); new **System → Themes** page (`/themes`) for gym admins shows System Themes (read-only, expandable) and My Themes (inline editable with ContextMenu: Clone / Activate / Edit / Delete) in separate sections.
  - **Team management (#53)**: gym-scoped admin/coach/staff CRUD via `gym-users.ts` + the **Organization → Team** page. Clerk-invitation flow with an `invited` placeholder row that links on first sign-in; self-edit and last-admin guards; audited. Added `gym_memberships` columns `status`, `email`, `name`, `invitation_id` (migrations 031–034).
  - **Platform superadmin management**: `platform/superadmins` + **System → Users**.
  - **Grouped, role-gated sidebar**: `config/navigationGroups.ts` (Membership / Organization / Training / Nutrition / Financials / System / Cordel).
  - **Staff nav rename (#77)**: Navigation-only refactor. **Team** entry renamed to **Staff** and now points to the existing Trainers page (`/trainers`). The standalone **Trainers** nav entry removed. `trainers.title` i18n key updated to "Staff" across `en`/`es`/`ca` locales. No API, DB, or business logic changes.
- **Placeholder shells (built, content pending)**: per-group Dashboard pages (`organization`, `training`, `nutrition`, `financials`, singular `membership`). Nutrition has no backend yet.
- **Phase 9 — Centers (#59, supersedes Phase 7)**: `Center` (migration 043) as the single
  location concept going forward — `gym_locations` was never built. `member_centers`
  junction (044) gives each Member one or more Centers with a default; `center_id` +
  audit-column backfill added to `rooms`/`class_sessions`/`bookings` (045–047); every
  existing Gym auto-got one default Center named after the gym, every existing Member
  defaulted onto it (046), and new gyms get the same default Center at creation. New
  `resources`/`trainer_availability`/`events` tables (048–050), each `center_id`-scoped
  from creation. `centerContext` middleware (`x-center-id` header) mirrors `tenantContext`;
  `resolveCenterId()` falls back to a gym's sole active Center so single-center gyms need
  no UI changes. Admin: **Organization → Centers/Resources/Events** pages, Trainer
  Availability inline on the Trainers page, Member-edit Assigned Centers/Default Center
  (hidden until a gym has >1 center). Member app: `GET /me/centers` + an optional
  center switcher, hidden for the single-center case. **Phase 7 (#39–#41) is superseded
  and closed** — its `gym_locations`/nullable-FK design was never implemented.
- **Phase 8 — MONEI payments (active, replacing deferred Stripe plan)**: ~~provider abstraction (#179)~~, ~~DB migrations (#180)~~, ~~API routers + isolated payment page token endpoint (#181)~~, ~~admin MemberPaymentsModal (#182)~~, ~~member payment banner + success/error pages (#183)~~, ~~isolated payment page app at `pay.vdicube.com` (#204)~~, ~~infra/Docker for the payment container (#205)~~, ~~recurring MIT billing run (#184)~~, ~~cash payment recording + fiscal receipt (#185)~~, ~~nginx IP allowlist for /billing/ endpoints (#306)~~.
- Platform naming migrated gymdesk → fitness (2026-07-10/11): DB schema `fitness`, containers/images
  `fitness-*`, VPS user `podman`, env vars `CORDEL_FITNESS_*`. See `docs/architecture.md` § Deployment.
- Legacy cleanup complete: `/fares` and `/subscriptions` routers + the old
  Subscriptions page deleted, `user_memberships.plan` column dropped
  (migrations 004, 007, 009). The member app's `/subscriptions` route was
  renamed to `/membership` (P1.8).
- **#440 (done)**: Staff Management — consolidated the expanded staff profile view in
  `apps/admin/src/app/[locale]/staff/page.tsx` from tabbed sections (General/Employment/
  Schedule/Notes/Clerk) into a single continuous scroll (tab bar + `activeSection` state
  removed). Added multi-center association for Staff, mirroring #59's `member_centers`
  pattern: migration 136 creates `staff_centers` (gym_id/staff_id/center_id, `is_default`,
  same generated-column one-default-per-staff unique index as `member_centers`); migration
  137 backfills it from the old single `staff.assigned_center_id` column (falling back to
  a gym's sole center) and then drops that column — nothing outside `staff.ts` read it.
  New `api/src/api/staff-centers.ts` (`GET`/`PUT /staff/:staffId/centers`, admin-only
  write) mirrors `member-centers.ts`, except a staff member may have zero centers (no
  access-control path depends on staff having one, unlike members). `staff.ts`'s
  create/duplicate now accept/propagate `center_ids`/`default_center_id` via
  `resolveStaffCenters()` (members' `resolveMemberCenters()` equivalent). Frontend adds a
  Centers subsection (checkboxes + default `<select>`, hidden for single-center gyms,
  matching the Members page's `showCenters` pattern) to the consolidated view.

## Decisions

- **Database (updated 2026-07-03)**: migrate from Neon PostgreSQL to **Oracle HeatWave MySQL**
  (paid tier, ~€50/mo) for predictable pricing and single-vendor infra. Tracked as
  **Phase M ([#45](https://github.com/cordel-app/gymdesk/issues/45)–[#49](https://github.com/cordel-app/gymdesk/issues/49))**, which **blocks Phase 1**. MySQL consequences for later tickets:
  partial unique indexes (P1.5, P2.5) become generated column + unique index; `jsonb`/`inet`
  (P6.1) become `JSON`/`VARCHAR(45)`; `RETURNING` is replaced by insert + select helpers.
- **Multi-location (updated 2026-07-15)**: shipped as **Phase 9 — Centers (#59)**, superseding
  the deferred Phase 7 `gym_locations` design (never built). `Center` is the single location
  concept; `Gym` remains the tenant boundary.
- **Payments**: internal `billing_events` ledger first (staff-recorded); MONEI is the active PSP for Phase 8 (replaced Stripe/Paycomet). Gymdesk owns all subscription logic; MONEI only tokenizes and charges. PCI scope minimized via isolated `fitness-pay` container at `pay.vdicube.com`.
- `fares` → `membership_plans` and `subscriptions` → `user_memberships` **evolve in place**
  with data-carrying migrations; old routes/pages are replaced.
- Trainers are existing `coach`-role rows in `gym_memberships`; trainer data (specialities)
  attaches there.
- Lookup vocabularies (`benefit_types`, `charge_types`, `action_types`) are global tables
  (no `gym_id`), seeded in their migrations. Statuses are `text` + CHECK constraints.
- Exercises/muscles catalog is **per-gym**, with an idempotent `import-defaults` seed endpoint.
- "Replaces" tickets keep the old route mounted until the phase's frontend ticket lands,
  then delete it.

## Conventions

Every feature ticket follows `docs/feature-patterns.md`: migration → Express router
(`requireRole` guards, `gym_id` filter, `ER_DUP_ENTRY`→409) → register in `api/src/index.ts` →
admin page (Members page as staff-level/soft-delete template, Plans page as admin-only
template) → Sidebar → i18n (en/es/ca).

## Phases

### Phase M — MySQL migration (blocks Phase 1)
| Ticket | Issue | Size | Depends on |
|---|---|---|---|
| M1 Provision HeatWave MySQL on OCI (backups + PITR) | [#45](https://github.com/cordel-app/gymdesk/issues/45) | S | — |
| M2 Replace node-pg-migrate with Knex targeting MySQL | [#46](https://github.com/cordel-app/gymdesk/issues/46) | M | — |
| M3 Port backend data layer and queries to mysql2 | [#47](https://github.com/cordel-app/gymdesk/issues/47) | L | #46 |
| M4 Migrate data from Neon to HeatWave | [#48](https://github.com/cordel-app/gymdesk/issues/48) | M | #45 #47 |
| M5 Cutover deploy, CI on MySQL, roadmap amendments | [#49](https://github.com/cordel-app/gymdesk/issues/49) | M | #48 |

### Phase 0 — Foundation
| Ticket | Issue | Size | Depends on |
|---|---|---|---|
| P0.1 Extract shared DataTable, CrudModal, ConfirmDialog | [#2](https://github.com/cordel-app/gymdesk/issues/2) | M | — |
| P0.2 StatusBadge + status-filter convention | [#3](https://github.com/cordel-app/gymdesk/issues/3) | S | #2 |
| P0.3 Member app navigation shell | [#4](https://github.com/cordel-app/gymdesk/issues/4) | M | — |

### Phase 0a — Structured logging (pino) ✅
| Ticket | Issue | Size | Depends on |
|---|---|---|---|
| Install pino structured logging in the API | [#177](https://github.com/cordel-app/gymdesk/issues/177) | S | — |

### Phase 0b — Log shipping (Grafana Alloy → Grafana Cloud Loki) ✅
| Ticket | Issue | Size | Depends on |
|---|---|---|---|
| Set up Alloy on VPS → Grafana Cloud Loki | [#178](https://github.com/cordel-app/gymdesk/issues/178) | M | #177 |

Config files: `infra/alloy/config-corback.alloy`, `infra/alloy/config-corfront.alloy`, `infra/alloy/alloy.service`.
Deploy: push to `main` triggers `.github/workflows/deploy-alloy.yml`; write `GRAFANA_CLOUD_API_KEY=<token>` to `~/.config/alloy/secrets` on each VPS on first install.
Grafana Cloud: Loki instance `xavieregea-logs`, user `969010`, push URL `https://logs-prod-012.grafana.net/loki/api/v1/push`.

### Phase 1 — Membership plans & billing core (replaces fares, subscriptions)
| Ticket | Issue | Size | Depends on |
|---|---|---|---|
| P1.1 ⚠️ Migrate fares → membership_plans | [#5](https://github.com/cordel-app/gymdesk/issues/5) | M | — |
| P1.2 Replace Fares page with Plans page | [#6](https://github.com/cordel-app/gymdesk/issues/6) | M | #5 #2 #3 |
| P1.3 membership_plan_prices with validity windows | [#7](https://github.com/cordel-app/gymdesk/issues/7) | M | #6 |
| P1.4 benefit_types + membership_plan_benefits | [#8](https://github.com/cordel-app/gymdesk/issues/8) | M | #7 |
| P1.5 ⚠️ Migrate subscriptions → user_memberships | [#9](https://github.com/cordel-app/gymdesk/issues/9) | L | #5 |
| P1.6 charge_types + billing_events ledger | [#10](https://github.com/cordel-app/gymdesk/issues/10) | M | #9 |
| P1.7 Replace Subscriptions page with Memberships page | [#11](https://github.com/cordel-app/gymdesk/issues/11) | L | #9 #10 #2 |
| P1.8 Member app: my membership + payment history | [#12](https://github.com/cordel-app/gymdesk/issues/12) | M | #11 #4 |

### Phase 2 — Classes v2 (replaces classes, bookings v1)
| Ticket | Issue | Size | Depends on |
|---|---|---|---|
| P2.1 Rooms CRUD | [#13](https://github.com/cordel-app/gymdesk/issues/13) | S | #2 |
| ~~P2.2 Specialities + trainer_specialities~~ | [#14](https://github.com/cordel-app/gymdesk/issues/14) | M | #13 | _(removed in #219)_ |
| P2.3 class_types CRUD | [#15](https://github.com/cordel-app/gymdesk/issues/15) | M | #14 |
| P2.4 ⚠️ Migrate classes → class_sessions | [#16](https://github.com/cordel-app/gymdesk/issues/16) | L | #15 |
| P2.5 ⚠️ Bookings v2: waitlist, capacity, attendance | [#17](https://github.com/cordel-app/gymdesk/issues/17) | L | #16 |
| P2.6 Replace Classes/Bookings pages with Schedule | [#18](https://github.com/cordel-app/gymdesk/issues/18) | L | #17 #2 |
| P2.7 class_type_user_memberships plan-access control | [#19](https://github.com/cordel-app/gymdesk/issues/19) | M | #17 #9 |
| P2.8 Member app: schedule, booking, waitlist | [#20](https://github.com/cordel-app/gymdesk/issues/20) | L | #19 #4 |

### Phase 3 — Class packages
| Ticket | Issue | Size | Depends on |
|---|---|---|---|
| P3.1 class_packages catalog CRUD | [#21](https://github.com/cordel-app/gymdesk/issues/21) | S | #2 |
| P3.2 user_class_packages + credit transactions | [#22](https://github.com/cordel-app/gymdesk/issues/22) | L | #21 #10 |
| P3.3 Consume/refund credits on booking lifecycle | [#23](https://github.com/cordel-app/gymdesk/issues/23) | L | #22 #19 |
| P3.4 Member app: my packages + credit balance | [#24](https://github.com/cordel-app/gymdesk/issues/24) | M | #23 #12 |

### Phase 4 — Promotions
| Ticket | Issue | Size | Depends on |
|---|---|---|---|
| P4.1 action_types + promotions CRUD | [#25](https://github.com/cordel-app/gymdesk/issues/25) | M | #2 |
| P4.2 Promotion plan targeting + charge benefits | [#26](https://github.com/cordel-app/gymdesk/issues/26) | M | #25 #10 |
| P4.3 promotion_period_benefits | [#27](https://github.com/cordel-app/gymdesk/issues/27) | S | #25 |
| P4.4 Apply promotions to memberships (price calc) | [#28](https://github.com/cordel-app/gymdesk/issues/28) | L | #26 #11 |
| P4.5 Backoffice + member app promotion surfaces | [#29](https://github.com/cordel-app/gymdesk/issues/29) | M | #28 #12 |

### Phase 5 — Workouts & training
| Ticket | Issue | Size | Depends on |
|---|---|---|---|
| P5.1 muscles, exercises, exercise_muscles | [#30](https://github.com/cordel-app/gymdesk/issues/30) | L | #2 |
| P5.2 workouts + workout_exercises builder | [#31](https://github.com/cordel-app/gymdesk/issues/31) | L | #30 |
| P5.3 training_plan_templates | [#32](https://github.com/cordel-app/gymdesk/issues/32) | M | #31 |
| P5.4 Assign training_plans to members | [#33](https://github.com/cordel-app/gymdesk/issues/33) | M | #32 |
| P5.5 workout_logs + member training endpoints | [#34](https://github.com/cordel-app/gymdesk/issues/34) | M | #33 |
| P5.6 Member app: Training tab + set logging | [#35](https://github.com/cordel-app/gymdesk/issues/35) | L | #34 #4 |

### Phase 6 — Audit log
| Ticket | Issue | Size | Depends on |
|---|---|---|---|
| P6.1 audit_logs table + recordAudit helper | [#36](https://github.com/cordel-app/gymdesk/issues/36) | M | — |
| P6.2 Instrument high-value mutation routes | [#37](https://github.com/cordel-app/gymdesk/issues/37) | M | #36 + phases 1–4 |
| P6.3 Backoffice audit viewer | [#38](https://github.com/cordel-app/gymdesk/issues/38) | M | #37 #2 |

### Phase 7 — Multi-location (superseded by Phase 9 / #59, 2026-07-15)
`gym_locations` was never built — closed in favor of `Center` (see Phase 9 above).

| Ticket | Issue | Size | Depends on |
|---|---|---|---|
| ~~P7.1 gym_locations CRUD~~ | [#39](https://github.com/cordel-app/gymdesk/issues/39) (closed) | S | — |
| ~~P7.2 Locations on rooms, trainers, class types~~ | [#40](https://github.com/cordel-app/gymdesk/issues/40) (closed) | M | #39 |
| ~~P7.3 Per-location plan pricing + member filter~~ | [#41](https://github.com/cordel-app/gymdesk/issues/41) (closed) | M | #40 #7 |

### Phase 9 — Centers (#59)
| Ticket | Issue | Size | Depends on |
|---|---|---|---|
| Center + member_centers + center_id backfill (backend) | [#59](https://github.com/cordel-app/gymdesk/issues/59) | L | — |
| Centers/Resources/Events admin UI + Member center assignment | [#59](https://github.com/cordel-app/gymdesk/issues/59) | L | backend |
| Member app center plumbing | [#59](https://github.com/cordel-app/gymdesk/issues/59) | S | backend |

### Phase 8 — MONEI payments
| Ticket | Issue | Size | Depends on |
|---|---|---|---|
| ~~P8.1 Payment provider abstraction layer (MoneiProvider)~~ | [#179](https://github.com/cordel-app/gymdesk/issues/179) | M | — | ✅ done |
| ~~P8.2 DB migrations — payment_methods + payment_requests~~ | [#180](https://github.com/cordel-app/gymdesk/issues/180) | S | #179 | ✅ done |
| ~~P8.3 payment-requests + payment-page token + webhooks API~~ | [#181](https://github.com/cordel-app/gymdesk/issues/181) | M | #179 #180 | ✅ done |
| ~~P8.4 Isolated payment page app (vanilla JS + Monei iframe)~~ | [#204](https://github.com/cordel-app/gymdesk/issues/204) | M | #179 #181 | ✅ done |
| ~~P8.5 Infra — Docker + deploy for fitness-pay container~~ | [#205](https://github.com/cordel-app/gymdesk/issues/205) | S | #204 | ✅ done |
| ~~P8.6 Admin app — MemberPaymentsModal + payment-providers page~~ | [#182](https://github.com/cordel-app/gymdesk/issues/182) | M | #181 | ✅ done |
| ~~P8.7 Member app — payment banner, success and error pages~~ | [#183](https://github.com/cordel-app/gymdesk/issues/183) | M | #181 | ✅ done |
| ~~P8.8 Recurring billing run — MIT charges~~ | [#184](https://github.com/cordel-app/gymdesk/issues/184) | M | #179 #180 #181 | ✅ done |
| ~~P8.9 Cash payment recording + fiscal receipt generation~~ | [#185](https://github.com/cordel-app/gymdesk/issues/185) | M | #182 | ✅ done |
| ~~P8.10 nginx IP allowlist for /billing/ endpoints~~ | [#306](https://github.com/cordel-app/gymdesk/issues/306) | S | #184 | ✅ done |

_(Old Stripe tickets #42–#44 are closed and superseded by this phase.)_

## Member app follow-ups (spike #85, 2026-07-19)

**Already shipped** (member app parity with roadmap items): P2.8 schedule/booking (#20 closed), P1.8 membership + billing history (#12 closed), P5.6 training tab + set logging (#35 closed), P3.4 my packages (#24 closed).

**Gaps identified and new issues created** (prioritised by member value vs. backend readiness):

| Ticket | Issue | Size | Depends on |
|---|---|---|---|
| Member home dashboard (upcoming booking + membership snapshot) | [#105](https://github.com/cordel-app/gymdesk/issues/105) | S | — |
| Member app: my class packages & credit balance | [#106](https://github.com/cordel-app/gymdesk/issues/106) | M | — |
| Member app: profile page (view & edit personal data) | [#107](https://github.com/cordel-app/gymdesk/issues/107) | M | — |

## Critical path

- **Billing**: P1.1 → P1.5 → P1.6 — the ledger unblocks packages (Phase 3), promotions (Phase 4), and MONEI payments (Phase 8).
- **Classes**: P2.3 → P2.4 → P2.5 — the session model unblocks everything class-related.
- Phases 3, 4, and 5 are parallelizable once their Phase 1/2 dependencies land.
