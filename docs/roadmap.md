# Gymdesk Roadmap

Phased implementation roadmap derived from the target ER model, adapted to the existing
multi-tenant architecture (every table gets `gym_id`; the diagram's monolithic `User` maps
onto the existing `members` + `gym_memberships` split — no new users table).

Each ticket below is a GitHub issue in `cordel-app/gymdesk`. Full scope and acceptance
criteria live in the issue bodies; this doc is the map.

Engineering quality backlog (tests, typing, helpers, member-app gap analysis) lives in
`docs/tech-debt.md` as a **map** of GitHub issues (#81–#86) — same model as this roadmap.
Agent session prompts: `docs/agent-prompts.md`. Always implement via the GitHub issue body.

## Status (2026-09-07)

- **Done**: Phase M (#45–#49, MySQL cutover 2026-07-04), P0.1–P0.3, P1.1–P1.8, P2.1–P2.8, P3.1–P3.4, P4.1–P4.5, P5.1–P5.6, P6.1–P6.3, #114, #117, #120, #121, #123, #124, #127, #129, #131, #132, #135, #144, #154, #156, #262 (Split-view calendar editor with inline delete: CalendarEventModal replaced by EventDetailsPanel right-side panel; inline delete confirmation; metadata section (created/updated/deleted by+at); backend SELECT extended with created_by_name/modified_by_name/deleted_by_name JOINs) (RBAC permission matrix — 7 granular roles + module gates), #157 (Membership Plans inline editing, Details modal, Duplicate action, Created By/At columns, paused enrollment status), #159 (move Payment Providers to Financials), #160 (Training Plans UX: Conclude Plan action, list columns, expired validation, Details dialog fixes), #161 (Specialities UX alignment), #164 (Training Plan workout builder parity with Workout Templates), #158 (Promotions management UX: Edit action in context menu, read-only rows, restructured list columns with Type/Value/Period/Created By, expanded Details modal with full audit info), #177 (pino structured logging), #178 (promtail config files — deploy to VPS to activate), #162 (Meals sub-entities UX alignment), #163 (Nutrition Library + redesigned Nutrition Plan Templates), #188 (Theme Model v2 + Editor UX: color token hard-switch to v2 shape with 26 grouped colors + dropdownBackground + modalBackground advanced field; inactive lifecycle status; StatusBadge in table row + context menu for status changes; auto-save for Colors/Typography/Advanced; explicit Save/Cancel only for Branding section; protection guard blocks draft/inactive/delete when assigned; assign/set-default gated to active themes only; renamed org_default → gym_default), #189 (User Impersonation UX v2: header Impersonate button + search dialog in admin and member apps; full member app support with AdminBar, ImpersonationBanner, MemberImpersonationDialog; account action lockout during impersonation; gym selector restricted to impersonated user's gyms via gymIds; all /me/* handlers use effectiveUserId from TenantContext; GET /platform/impersonation/candidates endpoint; impersonation.test.ts with 19 tests), #192 (Calendar Event Bookings & Waiting List: new `event_bookings` table + FIFO waitlist with auto-promote on cancel and on capacity increase; admin roster UI with expandable rows, confirmed/waitlist split, Add Member, Remove, Promote actions, FULL badge; member schedule page shows Calendar Events inline with Book/Join Waitlist/Cancel/Leave Waitlist buttons; cancellation policy: allowed if event starts >24h from now OR booking made <4h ago; waitlisted always cancel freely; GET /me/events, POST /me/event-bookings, DELETE /me/event-bookings/:id; event-bookings.test.ts with 23 tests), #190 (Calendar Foundation & Calendar Events: new `calendar_events` table with full audit/soft-delete; `activity_types` extended with `default_space_id`, `default_trainer_membership_id`, `color`; `/calendar-events` router with CRUD, space/trainer conflict detection (409), drag-and-drop and resize via PUT; FullCalendar v6 admin UI with day/week/month views, four filter modes (all/space/activity_type/trainer), CalendarEventModal with activity-type auto-populate; `/events` admin page removed; calendar-events.test.ts with 15 tests), #191 (Recurring Calendar Events: new `calendar_event_series` table stores recurrence template; `calendar_events` gains `series_id` FK + `series_occurrence_date` DATE; pure-TS `recurrenceEngine.ts` generates occurrence dates (daily/weekdays/weekends/weekly/monthly/yearly, interval, end by date/count/never, 13-month cap); "exploded" model — each occurrence is an independent DB row; scope-based edits (this / this_and_following / entire_series) via `scope` query param on PUT/DELETE; `this_and_following` splits series at cutDate; `entire_series` updates template + regenerates future occurrences; standalone cancellation uses `status='cancelled'`; series delete cancels future + soft-deletes series row; scope picker dialog in admin UI before editing/deleting recurring events; RecurrenceEditor component in CalendarEventModal; GET/DELETE `/calendar-event-series` router; `toDateStr()` normalizer for MySQL DATE↔JS Date driver discrepancy; calendar-event-series.test.ts with 13 tests), #193 (Attendance Management for Calendar Events / Class Sessions: new `attendance_status VARCHAR(20) DEFAULT 'pending'` CHECK ('pending','present','absent') column on `bookings` — separate from the booking lifecycle `status` column; backfill migrated old 'attended'→'present' and 'no_show'→'absent' then reset those rows' status to 'booked'; `bookings.status` CHECK reduced to ('booked','waitlisted','cancelled'); `attendance_recorded_at DATETIME` + `attendance_recorded_by_membership_id FK→gym_memberships` audit fields on `bookings`; `effective_trainer_membership_id FK→gym_memberships` + `effective_trainer_confirmed_at DATETIME` on `class_sessions` to record who actually delivered the session; new endpoints: `POST /bookings/:id/attendance` (requireRole admin/front_desk/trainer), `POST /class-sessions/:id/bulk-present` (marks all pending booked→present), `PUT /class-sessions/:id/effective-trainer`, `POST /class-sessions/:id/complete` (hard-blocked until all booked bookings have non-pending attendance_status AND at least one trainer field is set — returns 400 with {pending_count, missing_trainer}); `POST /bookings` gains `force=true` to add walk-ins over capacity (returns over_capacity:true flag); admin SessionRosterPanel rewritten with attendance progress header (present/pending/absent pills), per-booking attendance toggle buttons, bulk-present, effective trainer selector, complete session flow with ConfirmDialog and error feedback; attendance.test.ts with 21 tests), #210 (Theme Token Mapping + Editor Organization: `--gd-card-bg` applied to all expandable card containers across Themes, Spaces, Specialities, Centers, Promotions, Staff, Plans, Training Plans, Training Plan Templates, Workout Template blocks; sidebar hover background wired via `onMouseEnter`/`onMouseLeave` in `NavGroup` using `--gd-sidebar-hover-bg`; selected item text and background use `--gd-sidebar-selected-text`/`--gd-sidebar-selected-bg` CSS vars instead of hardcoded values; `Separator Height (px)` moved into the Header section of the Colors editor in both gym themes and base themes pages), #194 (Calendar Notifications & Member Experience: new `member_notifications` table (migration 087) with fire-and-forget `sendNotification`/`sendBulkNotification` helpers; 7 notification types: booking_confirmed, waitlist_joined, promoted_from_waitlist, event_cancelled, event_updated, booking_reminder_24h, booking_reminder_1h; notifications written on book/cancel/session-cancel flows; GET /me/notifications (paginated + unread count), GET /me/notifications/count, PUT /me/notifications/:id/read, PUT /me/notifications/read-all; GET /me/upcoming (next 30-day confirmed bookings, sessions + events combined), GET /me/activity-history (past bookings paginated); GET /me/schedule fixed to join activity_types + spaces instead of class_types + rooms, adds trainer_name + can_cancel; member app: Notifications page with mark-all-read and per-item read tracking, bell tab in BottomNav with unread badge, unreadNotifications count in AppContext), #211 (Plan Lifecycle & Enrollment Status refinement: lifecycle_status values changed to draft/active/paused/inactive — `archived` renamed to `inactive`; enrollment_status values changed to public/staff_only/closed — `open` renamed to `public`, enrollment-`paused` merged into `closed`, `staff_only` added for internal-only plans; data migration 088; backend validation updated; archive endpoint sets lifecycle=inactive; i18n adds `public`/`staff_only` keys to en/ca/es; frontend types, constants, and badge mappings updated; membership-plans.test.ts added), #212 (Plan Description persistence: added `cache: 'no-store'` to the Next.js proxy `fetch` so the data cache no longer serves stale GET responses after a PUT; Details modal now always shows Description, Last Modified At, and Last Modified By — each falls back to '—' instead of hiding the field), #213 (Plans UX Redesign: `+ Add Plan` creates an inline-editable row; Edit from context menu opens expanded area in edit mode; Duplicate creates a new editable row inline; new Charge Benefits section with all 4 charge types (no_benefit/waive/percentage_discount/fixed_discount); new Period Benefits section (all gym activity_types with enabled checkbox + quantity + frequency); Centers shown as checkbox grid in expanded area; Details modal gains `deleted_by_name`; new `plan_charge_benefits` table (migration 089) with `GET /:id/charge-benefits` + `PUT /:id/charge-benefits`; duplicate endpoint migrated to `db.transaction()`; enriched plan response includes `charge_benefits` array; 14 new API tests in `plan-charge-benefits.test.ts`), #218 (Spaces UX redesign: `+ New Space` creates an inline editable row without pre-calling API; clicking row toggles read-only accordion with General/Availability/Activity Types/Notes sections; Edit action in context menu opens inline edit form; StatusBadge replaces inline status selector; Details modal shows full audit info including `created_by_name`, `modified_by_name`, `deleted_by_name`; API SELECT extended to LEFT JOIN `gym_memberships` three times for those names), #215 (Gym Charges Configuration: new `gym_charges` table (migration 088) — per-gym configuration of 6 predefined charge types (registration_fee, insurance_fee, locker_rental, parking_fee, access_key, premium_fitness_app); `charge_types` extended with `name VARCHAR(100)` and `is_gym_charge TINYINT(1)` flag; 4 new charge type rows seeded; `gym_charges` rows auto-created at gym creation via INSERT IGNORE; Finance → Gym Charges page with expandable-row list, context menu (Details / Edit), read-only Details modal with full audit info, explicit inline edit mode with Save/Cancel; no Add or Delete actions; `GET /gym-charges`, `GET /gym-charges/:id`, `PUT /gym-charges/:id` (admin-only) under FINANCIALS module gate), #216 (Gym Charges in Plans & Promotions Benefits: migrations 091–092 replace `plan_charge_benefits.charge_type_id` and `promotion_charge_benefits.(charge_type_id + action_type_id)` with `gym_charge_id FK→gym_charges` and `action VARCHAR(30)`; Plans accordion gains inline Charge Benefits section (show-all-gym-charges grid, replace-all PUT); Promotions inline editor replaces old per-row POST/DELETE with same show-all-gym-charges pattern; `promotion-details.ts` drops POST/PUT/:cbId/DELETE endpoints, adds replace-all `PUT /charge-benefits`; `promotions.ts` GET queries drop `action_types` join, read `pcb.action` directly; `plan-charge-benefits.test.ts` updated with `gym_charge_id` fixtures and `gym_charge_code` assertions), #219 (Remove Speciality concept: dropped `specialities` and `trainer_specialities` tables, removed `speciality_id` from `class_types` and `activity_types`; deleted `/specialities` router, test file, and admin page; trainers router no longer fetches or assigns specialities; activity-types and class-types forms/tables no longer show speciality field; nav item removed; i18n keys cleaned up across en/es/ca), #214 (Promotions UX Redesign: expandable-row list, temp-row inline creation (no DB write until Save), read-only collapsed row (Name/Description/Created By/Created At/Start/End/StatusBadge/ContextMenu), Edit-only via context menu, Save writes all sub-resources atomically (plans + charge-benefits + period-benefits); soft delete (lifecycle_status='deleted', deleted_at, deleted_by_membership_id); Applicable Plans checkbox list; Charge Benefits grid (all gym charges, action dropdown); Period Benefits table (charge_type_id + quantity + frequency_interval + frequency_unit + enabled); Free Period fields (paid_months + bonus_months); Stackable checkbox in General section; Details modal shows lifecycle_status, free period, deleted audit fields; Duplicate → auto-opens duplicate in edit mode; migration 093 adds soft-delete columns, 4 new charge_types seeds, drops+recreates promotion_period_benefits with new schema; `membership-promotions.ts` updated to use `lifecycle_status` and gym_charges→charge_types join), #217 (Gym Charges in Plans & Promotions — availability filtering: `GET /gym-charges?availability=available|unavailable` filter with 400 validation; `gym_charge_availability` field added to all charge-benefit GET responses (plans + promotions); `PUT /charge-benefits` validates `availability='available'` before inserting; Plans and Promotions edit forms fetch only available gym charges; view mode shows "Unavailable" badge on saved benefits whose charge was later deactivated; `gym-charges.test.ts` with 12 tests), #221 (Remove Event Entity and Events Module: dropped `events` and `event_bookings` tables (migration 095); removed `events.ts` and `event-bookings.ts` routers; cleaned `me.ts` (removed `/me/events`, `/me/event-bookings`, event_bookings UNION legs from `/me/upcoming` and `/me/activity-history`); removed `event` entry from audit-registry; deleted `event-bookings.test.ts`; removed event keys from admin and member locales; member schedule page now session-only), #220 (Class Packages UX and Lifecycle Management: migration 094 adds `description` TEXT, `notes` TEXT, `created_by_membership_id`, `modified_at`, `modified_by_membership_id`, `deleted_at`, `deleted_by_membership_id` with SET NULL FK to gym_memberships; API updated with 3-way JOIN SELECT for names, soft-delete on DELETE, `POST /:id/duplicate` endpoint, price/validity_days default to 0 at creation; admin page fully rewritten to Spaces-style expandable-row UX — `+ New Class Package` inline creation (Name + Description + Sessions), accordion with General/Availability/Restrictions/Notes sections, context menu Details/Edit/Duplicate/Delete, inline edit form with General + Configuration + placeholder Availability/Restrictions + Notes, Details modal with full audit info; i18n updated in en/ca/es; `class-packages.test.ts` with 27 tests), #222 (Exercises Management UX and Lifecycle Management: migration 096 adds `created_by`, `modified_at`, `modified_by`, `deleted_by` audit columns with SET NULL FK to gym_memberships; API extended with audit field JOINs on all SELECTs, `getCallerMembershipId` helper on POST/PUT/DELETE, `?q=` name search on GET /, `POST /:id/duplicate` endpoint (copies muscles + result types); admin page fully rewritten to expandable-row UX — standard collapsed header (Name/Description/Primary Muscles/Created At/Created By/Status/ContextMenu), expanded view with Configuration/Muscles/Media sections, context menu Details/Edit/Duplicate/Delete, inline edit mode with General+Configuration+Muscles+Media sections, Save/Cancel; creation modal unchanged; ExerciseDetailModal with full audit info; name search + StatusFilter above list; i18n updated in en/ca/es; `exercises.test.ts` with 38 tests; fixed bug in `094_drop_specialities.js` where `hasColumn` on non-existent `class_types` threw instead of skipping), #224 (Training Plan Templates UX and Lifecycle Management: modal-based creation replaced with inline new row (`+ New Training Plan Template`); collapsed header now shows Name/Description/Workout Count/Created At/Created By/Status/ContextMenu; edit form moved inside expanded area (General section with Name/Description/Status + Save/Cancel) alongside the existing `TrainingPlanTree`; Duplicate calls `POST /:id/duplicate` then auto-opens the new record in edit mode (matches promotions pattern); context menu order: Details/Edit/Duplicate/Assign to Member (active only)/Delete; no DB or API changes — all audit columns and endpoints were already complete; i18n updated in en/ca/es with `assign_to_member`, `section_general`, `placeholder_name`, `col_description` keys), #223 (Workout Templates UX and Lifecycle Management: migration 097 adds `modified_at`, `modified_by_membership_id`, `deleted_by_membership_id` (SET NULL FK → gym_memberships), `notes TEXT` to `workout_templates`; `GET /workout-templates` now LEFT JOINs `gym_memberships` three times for `created_by_name`/`modified_by_name`/`deleted_by_name` and includes a `blocks_count` subquery; `POST /` and `POST /:id/duplicate` stamp `modified_at`/`modified_by_membership_id` on creation; `PUT /:id` stamps them on update; `DELETE /:id` stamps `deleted_by_membership_id`; `POST /:id/duplicate` race-condition fixed by moving `res.json()` outside the transaction so the commit completes before the client reads the DB; admin page fully rewritten to Spaces-style expandable-row UX — `+ New Workout Template` inline creation row, card-based list with `TemplateDropTarget` for cross-template DnD, collapsed row shows Name/blocks_count/Created By/Created At/StatusBadge/ContextMenu (Details/Edit/Duplicate/Delete), expanded area shows General/Workout Structure (`WorkoutTemplateTree`)/Notes sections; `TemplateHeader` component removed from `WorkoutTemplateTree` (metadata now lives in the parent page); Details modal shows full audit info (created/modified/deleted at+by); i18n updated in en/ca/es; `workout-templates.test.ts` extended with 5 new tests for audit stamping and `blocks_count`/`notes` in paginated GET), #226 (Nutrition Plan Templates Meal Configuration UX: migration 098 replaces 4 FK columns on meals (`main_dish_id`, `side_id`, `sauce_id`, `drink_id`) with `nutrition_plan_template_meal_items` junction table (`meal_id`, `nutrition_library_item_id`, `component_type` CHECK main_dish/side/sauce/additional, `quantity`, `unit`, `position`); renames `name` → `display_name` on meals; adds `meal_type VARCHAR(50)` CHECK (7 predefined values: recien_levantado/breakfast/media_manana/lunch/snack/dinner/antes_de_dormir) and `notes TEXT`; router rewritten: POST meals requires `meal_type`, defaults `display_name` to `meal_type`; new CRUD endpoints `GET/POST /:id/days/:dayId/meals/:mealId/items` and `PUT/DELETE …/items/:itemId`; goals POST/PUT validate `item_name` against 13-value whitelist; duplicate deep-copies meal_items; admin Add modal replaced with inline pending-new row; `NutritionPlanTree` MealRow shows compact summary `display_name · item1 qty · item2 qty` with Edit toggle; DayRow add-meal changes to meal_type dropdown; GoalsSection item_name changes to predefined select; i18n updated in en/ca/es; `nutrition-plan-templates.test.ts` fully rewritten), #229 (Base Themes UX Redesign: `t.tokens` added to `GET /platform/themes` SELECT — fixes color-preview bug where all themes showed identical swatches; `POST /platform/themes` now accepts `status` field (draft/active, default draft); `GET /platform/themes/:id` returns `deleted_by_name` from audit_logs; admin page rewritten to standard entity pattern — `+ New Theme` creates a temp inline row (Name + Status only), context menu: Details/Edit/Duplicate/Delete, Edit via context menu only with explicit Save/Cancel (no autosave), read-only expanded view shows color-group swatches, Duplicate calls clone endpoint, Details modal gains Primary Color/Background Primary/Secondary/Tertiary swatches and Deleted By/At fields; i18n adds 10 keys (col_color_preview, details_primary_color, details_bg_primary/secondary/tertiary, details_deleted_by/at, label_status, section_general, new_theme_placeholder) in en/es/ca; themes.test.ts gains 4 new cases: tokens in list, status on POST, deleted_by_name in GET /:id), #246 (Theme Text Color: added `textColor` to the Application section of the Theme token model (migration 099, tokens bumped to v3); default `#111827`; `--gd-text` CSS variable set in `applyTokens` (admin) and member ThemeProvider; `body` in both app layouts consumes `var(--gd-text, #111827)`; `textColor` field added after Page Background in the Application group of both the Base Themes editor and the Customer Themes editor; `validateTokens` in `themes.ts` and `gym-themes.ts` updated to include `textColor`; i18n `label_text_color` key added to en/es/ca), #247 (Move Calendar to Root Navigation: Calendar removed from Organization nav group; new top-level `CALENDAR` nav group and `CALENDAR` permission module added; all roles get RW except `accountant` (Finance) which gets NONE; `canWrite` in calendar page corrected from `TRAINING` to `CALENDAR`; `nav.groups.calendar` key added to en/es/ca locales; no functional changes to Calendar itself), #228 (Gyms Management UX and Lifecycle: migration 095 adds `description`, `status` CHECK ('active'/'inactive'/'deleted'), `created_by_name`, `modified_at`, `modified_by_name`, `deleted_at`, `deleted_by_name` to `gyms`; `requireSuperadmin` extended to attach `req.superadminName`; slug is now optional on POST (auto-generated from name via `uniqueSlug()`); new endpoints: `GET /platform/gyms/:id` (single gym including deleted), `PUT /platform/gyms/:id` (name/description/status/theme_id with audit stamp), `DELETE /platform/gyms/:id` (soft-delete), `POST /platform/gyms/:id/duplicate`; `GET /platform/gyms` gains `?status=active|inactive` filter; admin Cordel → Gyms page rewritten to expandable-row UX — `+ New Gym` inline creation (Name + Description), collapsed header (Name/Description/Created At/Created By/Status/ContextMenu), expanded read-only sections (General/Configuration/Notes), context menu Details/Edit/Duplicate/Delete, inline edit form with Save/Cancel, Details modal with full audit info; i18n updated in en/ca/es; `gyms.test.ts` added), #248 (Global Recycle Bin: new `GET /recycle-bin`, `GET /recycle-bin/:entityType/:id`, `POST /recycle-bin/:entityType/:id/recover` router under SYSTEM module gate; 10-way UNION query covers membership_plans, promotions, centers, spaces, class_packages, exercises, workout_templates, training_plan_templates, nutrition_plan_templates, themes; entity-specific recovery UPs clear soft-delete columns + set appropriate status; two runtime fixes applied — MySQL 8.4 LIMIT/OFFSET parameter bug (interpolated as integer literals) and utf8mb4 collation mismatch in themes UNION branch (CONVERT columns); admin System → Recycle Bin page at `/recycle-bin` with type/name/deleted-by/date-range filters, sortable table, Details modal with entity-specific fields, Recover button gated to canWrite; nav entry + i18n in en/ca/es; `recycle-bin.test.ts` with 18 tests covering auth, tenant isolation, list/detail/recover happy paths, invalid entity type 400, and 404 for non-deleted entities), #273 (Rename "System" → "Configuration": updated `nav.system` and `nav.groups.system` keys in en/es/ca locales; no code, permission, or route changes), #265 (Refactor User Impersonation to Use Application Users: `/candidates` endpoint renamed to `/targets`; `clerk_user_id IS NOT NULL` filter removed so all active members appear as targets; `effectiveType: 'member'|'staff'|null` added to `TenantContext`; wire format `x-impersonate-as: member:<id>` for member impersonation; `POST /:targetId` now requires `{ targetType }` body param; `resolveMemberId(gymId, ctx)` helper in `me.ts` routes all `/me/*` handlers through either `WHERE members.id = ?` or `WHERE clerk_user_id = ?` depending on `effectiveType`; admin and member `ImpersonationDialog` updated; 23-test `impersonation.test.ts`; no migrations), #266 (Clerk Status in Member and Staff Details: new `GET /members/:id/clerk-status` and `GET /staff/:id/clerk-status` endpoints — resolve Clerk enrollment state (not_enrolled/invited/active/suspended/error) on demand without storing it in the DB; members endpoint reads `clerk_user_id`/`invitation_id` directly; staff endpoint joins `gym_memberships` via `gym_membership_id`; admin Members edit modal gains a read-only Clerk section that loads on open; admin Staff inline editor gains a `clerk` tab that loads on row expand; `clerkClient` added to `staff.ts`; i18n keys added in en/es/ca; `member-clerk-status.test.ts` and `staff-clerk-status.test.ts` with 14 tests), #268 (Space Creation Center Selection: inline new-space form now includes a Center selector — required when the gym has multiple centers, auto-selected when there is exactly one center, blocked with an error message when no centers exist or when center loading fails; center_id included in POST body; i18n keys `error_center_required`, `no_centers_available`, `error_centers_load`, `placeholder_center` added in en/es/ca; no backend or migration changes — `resolveCenterId` already handled all cases), #270 (Fix Recycle Bin Audit Metadata: migration 101 adds `deleted_by_name VARCHAR(255)` to all 10 entity tables; each soft-delete endpoint stores `actorName` (Clerk-derived, always set for all roles including Superadmin) in `deleted_by_name`; `branchFor` UNION updated to COALESCE new column with FK join name for backward compat, add `created_at` + `created_by_name` columns; `fetchDeletedEntity` detail view also COALESCEs `deleted_by_name`; themes UNION branch uses `CONVERT(t.deleted_by_name USING utf8mb4)` for collation parity; frontend `RecycleBinItem` gains `created_at`/`created_by_name` fields displayed as sub-line under Name in the list), #259 (Base Nutrition Library + Base Nutrition Plan Templates + Clone/Assign Workflow + Member Nutrition Plans: migration 103 makes `nutrition_library_items.gym_id` nullable (drops FK, modifies column, re-adds FK + functional unique index `COALESCE(gym_id,'')` for NULL-safe uniqueness), makes `nutrition_plan_templates.gym_id` nullable (same FK-drop pattern); adds `status VARCHAR(20) DEFAULT 'active'` + `modified_at DATETIME` to `nutrition_library_items`; adds `cloned_from_id INT UNSIGNED FK→nutrition_plan_templates` to track lineage; creates 6 `member_nutrition_plan_*` tables (member_nutrition_plans → days → meals + meal_items + restrictions + goals, all with gym_id); new routers: `platform-nutrition-library.ts` (superadmin CRUD, `gym_id IS NULL`, GET /platform/nutrition-library + POST/PUT/:id/DELETE/:id), `platform-nutrition-plan-templates.ts` (superadmin CRUD for base templates, full day/meal/item/restriction/goal sub-routes at `/platform/nutrition-plan-templates`), `member-nutrition-plans.ts` (gym-level member plans: GET + GET /:id + GET /:id/hierarchy + DELETE /:id soft-delete); `nutrition-plan-templates.ts` extended with `?type=base|gym|all` filter on GET /, `owner_type` field on all rows, cross-scope GET/:id (base or gym), `POST /:id/clone` (deep-copies any template into a gym-owned draft with `cloned_from_id`), `POST /:id/assign` (deep-clones template → member_nutrition_plans in one transaction); `NutritionPlanTree.tsx` gains `apiBase?: string` prop (default `/nutrition-plan-templates`) for reuse from platform context; Cordel admin pages: Cordel → Base Nutrition Library (CRUD, category-grouped table), Cordel → Base Nutrition Plan Templates (expandable rows + NutritionPlanTree); nav keys `nav.base_nutrition_library` + `nav.base_nutrition_plan_templates` added to en/es/ca), #272 (Promotions Comprehensive Redesign: migration 102 renames `free_period_paid_months` → `paid_months` and `free_period_bonus_months` → `bonus_months` on `promotions`, adds `free_months` (initial free period before billing), adds `duration_months` to `promotion_period_benefits`, expands action CHECK on `promotion_charge_benefits` to include `fixed_price`, and creates new `promotion_included_benefits` table for one-time Sellable Item grants; API updated to use new field names in GET/POST/PUT/duplicate, period-benefits accept `duration_months`, charge-benefits accept `fixed_price` action, new `GET/PUT /included-benefits` endpoints; frontend edit form redesigned: "Billing & Duration" section with three independent inputs (Free Period / Paid Duration / Bonus Duration), dynamic example timeline table with hypothetical enrollment date and row-per-period breakdown, Included Benefits section, Duration column in Period Benefits, Fixed Price option in Charge Benefits; i18n updated in en/ca/es; `promotions.test.ts` with 18 tests), #260 (Base Training Catalog: migration 106 makes `gym_id` nullable on 7 training tables (`exercises`, `exercise_muscles`, `workout_templates`, `workout_template_blocks`, `workout_template_exercises`, `training_plan_templates`, `training_plan_template_workouts`) + adds `cloned_from_id` to 3 of them; new platform routers `platform-exercises.ts`, `platform-workout-templates.ts`, `platform-training-plan-templates.ts` (all behind `requireSuperadmin`) for Cordel-managed base content (`gym_id IS NULL`); gym-level routers updated: GET / and GET /:id now surface base resources alongside gym-owned ones, PUT/DELETE return 403 on base resources, new `POST /:id/clone` deep-clones a base resource into a gym-owned copy with `cloned_from_id`; `training-plan-creation.ts` updated to match `(gym_id IS NULL OR gym_id = ?)` for base TPT assign; `POST /training-plan-templates/:id/assign` endpoint wraps existing `createTrainingPlanTx`; admin gym-level Exercises/Workout Templates/Training Plan Templates pages gain Type column (Base/Gym badge) and Clone action; 3 new Cordel platform pages; nav entries `base_exercises`, `base_workout_templates`, `base_training_plan_templates` in Cordel group; i18n updated in en/es/ca; `platform-training.test.ts` + `training-base-gym.test.ts` with ~90 tests), #269 (Activity Types Redesign + Schedule Engine: migrations 107–109 add audit columns to `activity_types` (`created_by_membership_id`, `modified_at`, `modified_by_membership_id`, `deleted_at`, `deleted_by_membership_id`) and `default_center_id FK→centers`; new `activity_type_schedule_rules` table (type one_off/weekly/monthly, start_date, end_date, weekday, ordinal, start_time, end_time); `calendar_events.schedule_rule_id FK→activity_type_schedule_rules`; `gyms.timezone VARCHAR(64) DEFAULT 'Europe/Madrid'`; `scheduleEngine.ts` — pure-TS occurrence generator using Luxon for IANA timezone conversion, `materializeScheduleRule` bulk-inserts occurrences as calendar_events, `cancelFutureOccurrences` preserves past events; `/activity-types` router rewritten with soft-delete, `POST /:id/duplicate` (deep copies rules + materializes), `POST /:id/restore`; new `/activity-types/:id/schedule-rules` sub-router (GET/POST/PUT/DELETE) with full validation (end_date required for recurring, max 1-year horizon, ordinal for monthly); `recycle-bin.ts` extended with `activity_type` entity type; admin page rewritten to expandable-row UX — `+ Create Activity Type` inline creation (Name/Duration/Capacity), collapsed header (Name/Description/Duration·Capacity/Default Center/Status/ContextMenu), expanded General + Schedule (rule list with inline add/edit/delete) + Metadata sections; center→space linked dropdowns in edit form; context menu: Details/Edit/Duplicate/Delete; i18n updated in en/es/ca; `luxon` added to API dependencies), #290 (Theme Settings UX: moved Card Border color picker from Colors section to Advanced section in both Customer Themes and Base Themes editors; removed hardcoded `background: '#fafafa'` overrides from expanded card areas in themes/page.tsx, system/themes/page.tsx, and centers/page.tsx so the configured Card Background token applies to the full card container), #370 (Sellable Item Status/Enrollment Status split: `enrollment_status ENUM(public/staff_only)` added to `gym_charges` (migration 122), mirroring the `membership_plans` status/enrollment_status separation; `status='inactive'` always overrides `enrollment_status`; admin CRUD (`sellable-items.ts`) validates and filters `?enrollment_status=`, defaults new items to `public`; Sellable Items admin UI shows Status and Enrollment Status as separate badges/fields in the row, inline edit form, and Details modal)., #408 (Plans billing policy UX: per-field descriptions for Initial/Recurring Billing and Initial/Recurring Service explaining their effect on the subscription cycle, shown in both the read-only accordion and the inline edit form; `DEFAULT_BILLING_POLICY` named default applied to every new plan, surfaced as a notice in the inline create form), #366 (Assign Members When Creating a CalendarEvent: staff can assign Members directly on `POST /class-sessions` and on recurring `activity_type_schedule_rules`, auto-booked via the existing `bookMemberOnSession(force=true)` mechanism so capacity is a warning, never a hard block, for staff), #441 (Nutrition Plans parity with Training Plans: Edit/Duplicate/Details/Complete actions on assigned member plans — `PUT`/`POST :id/complete`/`POST :id/duplicate` on `member-nutrition-plans.ts`, plus full day/meal/item/restriction/goal CRUD mirroring `nutrition-plan-templates.ts`; plan mutable only while `status='active'`; migration 136 adds the missing `(member_nutrition_plan_id, weekday)` unique constraint; admin UI reuses `NutritionPlanTree.tsx`'s expand-in-place editor via its `apiBase` prop, now threaded through every child component instead of only the top level), #443 (Nutrition Plans: "+ New Nutrition Plan" creation flow mirroring Training Plans — scratch-vs-template modal, `POST /member-nutrition-plans`, and a shared `createNutritionPlanTx` helper also used by `/nutrition-plan-templates/:id/assign`), #501 (Nutrition Library: multiple food categories per item via a `nutrition_library_categories` + `nutrition_library_item_categories` M2M pair mirroring the existing `nutritional_qualities` pattern, replacing the old single scalar `category` column; `fat`/`fiber` added as nutritional qualities; both the gym-facing and Cordel/platform Nutrition Library admin pages converted from Modal CRUD to Inline row CRUD, matching the Plans reference pattern), #512 (Membership Plan Details modal: contextual-menu "Details" now opens a read-only modal — following the Promotion Details modal pattern, with a compact summary section — instead of expanding the row; row expand/collapse is unaffected; `promotion_count` added to the enriched `GET /membership-plans` response), #541 (Apply status-based colors to calendar events: centralized status→color map in `apps/admin/src/lib/calendarEventColors.ts`, reused by the day/week/month FullCalendar rendering for both calendar_events and class_sessions, replacing the prior activity-type-based event fill), #545 (Sellable Items: Duplicate action — `POST /sellable-items/:id/duplicate`, admin-only, copies any visible item (system or custom) into a new custom, current-gym, current-user-created row named "Copy of <original>", preserving status/enrollment_status but dropping `charge_type_id`/`class_package_id` lineage; context-menu action, no confirmation step, matches the `membership-plans.ts` Duplicate convention), #546 (Link Session-type Sellable Items to Professional Services: many-to-many via new `sellable_item_professional_services` join table (`gym_id`, `sellable_item_id`, `professional_service_id`, unique on the pair) — mirrors the `nutrition_library_item_categories` M2M pattern (#501); `GET`/`POST`/`PUT`/`POST :id/duplicate` on `sellable-items.ts` extended to accept/return `professional_service_ids`/`professional_services`, validated against the requesting gym's own + global system Professional Services and persisted transactionally alongside the sellable item write; changing type away from `sessions` clears the relationship (no confirmation — it's a catalog association, not a booking/billing record); duplicate copies links for `sessions`-type items only; admin UI adds a type-gated multi-select checkbox field (Plans/Nutrition-Library chip-checkbox pattern) to the Sellable Items inline create/edit forms and read-only/Details views), #551 (Membership Fee Benefits split into its own Promotion category, reusing the Period Benefits mechanism via a new `GET`/`PUT /promotions/:id/membership-fee-benefit` singleton — item hardcoded server-side to Membership Fee, generic Period Benefits excludes/rejects it, no schema change), #552 (Promotion Billing & Duration UI: dropped the "(months)" suffix from the four field labels; Example Timeline moved below all other Promotion form sections with a new "assumes monthly billing period" disclaimer; the timeline's Billing column now reflects the promotion's (unsaved, draft) Membership Fee Benefit — `GET /promotions/timeline` gained optional `membership_fee_action`/`membership_fee_value`/`membership_fee_enabled`/`membership_fee_duration_months` query params and each period's response gained `billingAction`/`billingValue`, no schema change), #553 (Promotions card UX: Example Timeline simulation now renders whenever the card is expanded — view or edit — computed from the promotion's saved values in view mode and from the unsaved edit-form draft while editing, instead of only appearing once a Billing & Duration field is touched in edit mode; shows a `timeline_empty` hint instead of hiding the section when there isn't enough data yet; Save/Cancel buttons moved from just below General to the very bottom of the edit form, after every section including the Example Timeline; no schema or API change), #625 (Limit Membership Fee Benefit duration to the Promotion duration: a Promotion Period Benefit belongs to the Promotion lifecycle and can never outlast it, so `duration_months` is capped at `free_months + paid_months + bonus_months` — Pay Beforehand excluded. Backend rejects an over-long explicit duration on `PUT /promotions/:id/membership-fee-benefit`; the admin inline UI caps the input at the Promotion duration and re-constrains it live when the Promotion is shortened; the Example Timeline forecast uses the effective (clamped) duration so the benefit never reaches the Pay (regular) period even when configured too long or unbounded; migration 162 clamps existing over-long rows. Shared pure helpers `promotionDurationMonths` / `effectiveBenefitDurationMonths`), #626 (Remove Charge Benefits from Promotions: the Charge Benefits section is gone from the Promotion editor — both the edit form's per-Sellable-Item action/value grid and the read-only view list — along with the frontend state, handlers and `GET`/`PUT /promotions/:id/charge-benefits` calls that existed only for it, and the `promotions` namespace's Charge Benefit i18n keys. One-off / Session / Period / Membership Fee Benefits and the unrelated Membership Plans Charge Benefits section are untouched. No schema change and no backend removal: `promotion_charge_benefits` is still read at apply time by `membership-promotions.ts` (snapshot + `computeFinalPrice`) and surfaced on Assigned Plans, so the backend is not exclusively required by the removed UI), #627 (Split Promotion Editing by Section: the Promotion is no longer editable as a whole — the context-menu **Edit** action now only opens the main configuration (General, Suitable Membership Plans, Billing & Duration), and One-off / Session / Period / Membership Fee Benefits each get their own **Edit** button with their own independent Save/Cancel writing only that section's endpoint. A new `editingSection` discriminator alongside `editingId` allows exactly one section of one card to be editable at a time; every other section stays read-only and its Edit button is disabled while one is open. Creating a Promotion stays a single form (no id yet to hang per-section saves off). Frontend-only, inline/expandable UI preserved, no modal; #625's Membership-Fee-duration cap is preserved by re-clamping an over-long saved duration when the main save shortens the Promotion), #628 (Inline Assign Plan to Member with Promotion selection: the "Assign New Plan" action no longer opens a modal — `AssignNewPlanModal.tsx` is replaced by `AssignPlanInlineEditor.tsx`, rendered inline inside the plan card it supersedes, with plan picker, start date and a new **Promotions** section. Eligible Promotions come from the new `GET /promotions?membership_plan_id=` filter (`EXISTS` over `promotion_membership_plans`) so the list matches what the apply path accepts; the selection enforces the `stackable` rule live (a non-stackable pick disables every other Promotion, any pick disables the non-stackable ones, clearing re-enables all). `POST /user-memberships/:id/assign-new-plan` takes `promotion_ids` and re-validates the whole set server-side before the assignment transaction — shared pure helper `validatePromotionStacking` (`domain/promotionStacking.ts`) plus `validatePromotionSelection` for the per-Promotion eligibility checks — then applies each one through the existing `applyPromotionToMembership`. Membership Plan status workflow untouched; no schema change), #630 (Rename "Close" to "Cancel": the Assigned Plan context-menu action reads **Cancel** in every locale — label-only, the action still posts to `POST /user-memberships/:id/close`, still runs the two-step confirm (plain, then the unused-value warning) and still maps onto the `cancelled` status. Because the action's own name became "Cancel", the confirm dialogs no longer label their dismiss button with the generic `cancel` key — new `action_close_confirm` / `action_close_dismiss` keys give the two buttons distinct labels. Frontend-only, no API or schema change), #632 (Collapsible Theme color groups: every group in the Theme editors' **Colors** section (Application, Cards, Text, Separators, Inputs, Header, Sidebar, Navigation, Buttons, Status, Links, Tables, Calendar) now renders as a collapsible card — a full-width header `<button>` carrying the group's existing label, `aria-expanded` and a rotating `▾`, with the group's controls hidden while collapsed. All groups start collapsed. Open state is a local `Set` of group keys in `ThemeColorsEditor`, kept separate from the token draft, so groups toggle independently (not an accordion) and collapsing can never change a color value. Presentation-only: same color pickers, same inline `ThemeAdvancedSection`, same Calendar contrast report, same read-only handling for Base Themes, no new translation keys. Frontend-only, no API or schema change), #633 (Promotions — new `only_applicable_for_new_members` boolean on the main Promotion configuration: migration 163 adds `promotions.only_applicable_for_new_members tinyint(1) NOT NULL DEFAULT 1` after `stackable`, so existing Promotions are backfilled to `true` by the column default; `promotions.ts` carries it through the list SELECT, POST (defaults to `1` when omitted or null), PUT (`IF(?, ?, …)` partial-update semantics, matching `stackable`) and `POST /:id/duplicate`; the admin Promotions page renders it as a checkbox directly below "Stackable with other promotions", checked by default on create, and the Promotion detail modal shows it read-only. Store-and-display only — Promotion eligibility, stacking, the apply path and the Assign-Plan-to-Member flow are deliberately unchanged, and the `user_membership_promotions` snapshot does not capture it yet; a follow-up ticket implements the behaviour), #637 (Sellable Items column alignment: the list's column headers and every collapsed row now render from one shared `LIST_COLUMNS` definition in `financials/sellable-items/page.tsx` — a CSS-grid template (fixed px tracks, the Name column the only flexible one) used by both, replacing the flex rows whose per-cell `minWidth` let a long name or a long "created by" widen its cell and push every column after it out of line with the header. Values ellipsis inside their own track, the chevron and the ⋮ menu share one Actions cell (new `col_actions` label in en/es/ca, the only new key), and the header + rows sit in a single `overflow-x: auto` wrapper with a computed `LIST_MIN_WIDTH`, so a narrow viewport scrolls horizontally instead of dropping columns. Presentation-only: same columns, same order, same data, same actions, no API or schema change), #638 (Finance Dashboard — Membership Plan cards: the `Finance → Dashboard` page (`[locale]/financials/page.tsx`, until now a "coming soon" placeholder) renders one card per Membership Plan with its name, lifecycle status badge and the number of Assigned Members. New read-only router `api/src/api/financials-dashboard.ts` mounted at `/financials/dashboard` under the `financials` **group** flag (not `financials.plans`, so the Dashboard survives the Plans page being switched off); its single `GET /membership-plans` groups `user_memberships` per plan. A plan is shown when it is `lifecycle_status='active'` or still has at least one assignment, so retired plans members are still on stay visible while empty non-active plans disappear. An assignment counts while it is anything other than `cancelled`/`expired` under the #410 date-aware projection (so the count agrees with the Assigned Plans list), and assignments of soft-deleted members are excluded; each assignment counts once. No migration, no write path, no Membership Plan / Assigned Plan behaviour change. New `api/src/test/financials-dashboard.test.ts` (14 cases: auth, role, tenant isolation, visibility rules, counting invariants); new `financials_dashboard` i18n namespace in en/es/ca), #639 (Created and Next Payment on Billing Events: `GET /payments/billing-events` rows gain `created_at` (the ledger row's creation instant as a full ISO datetime; `null` on projected rows, which aren't persisted) and `next_payment_date` (`YYYY-MM-DD` of `user_memberships.next_billing_date` for the event's membership, `null` when the event has no membership, the membership is no longer `active`, or nothing is scheduled). The admin Billing Events table renders both as new `Created` / `Next Payment` columns in `DD/MM/YYYY HH:mm`, with `—` for missing data; `next_billing_date` is a DATE column so the scheduled day is rendered at `00:00` rather than parsed as an instant and shifted by the viewer's timezone. Also fixes the future-row projection, which seeded its rolling window with `String(next_billing_date).slice(0, 10)` — mysql2 returns DATE columns as JS `Date` objects, so scheduled rows carried a `billing_date` of `"Wed Oct 21"` instead of an ISO date (rendered as `Invalid Date`, and mis-sorted/mis-filtered against `from`/`to`); a new `toDateOnly()` helper normalises both representations. No migration, no billing-logic, payment-execution or event-status change; new `col_created`/`col_next_payment` i18n keys in en/es/ca; 6 new cases in `payments-billing-events.test.ts`), #642 (View Audit Log from Member Details: the Members → Details modal gains a **View Audit Log** button that closes the modal and navigates to Configuration → Audit Log already filtered to that member — `/{locale}/audit?entity_type=member&entity_id=<id>`. `AuditLogView` now seeds its `entity_type` / `entity_id` / `entity_name` filters from the query string, and a new **Entity ID** filter box makes the applied filter visible and clearable; `GET /audit-logs` gains a matching exact-match `entity_id` param (served by the existing `(gym_id, entity_type, entity_id)` index — no migration). The button is hidden for non-admins and when the `system.audit` feature flag is off, mirroring the sidebar's rule. New `audit.filter_entity_id*` and `members.action_view_audit_log` i18n keys in en/es/ca; new `audit-logs.test.ts` (8 cases: auth, entity_id filtering, exact match, tenant isolation)), #629 stage 1 (Billing Simulation engine: a read-only, never-persisted forecast of what a Member will actually be charged — `GET /user-memberships/member/:memberId/billing-simulation` returns the concrete dates, the regular price, the Promotion/Benefit that changed it and the resulting amount, consolidated across every simulated assignment of the Member. Member-level rather than per Assigned Plan, per the ticket thread's Q4 answer, so the engine lands once and renders where #634 wants it. Pure engine in `domain/billingSimulation.ts` (sections one-off / year / monthly / 4-week, plus weekly / session / other for cadences the catalogue can produce; Free and Bonus promotional periods waive the fee, Pay/Prepaid apply the Membership Fee Benefit via `computePromotionTimeline`, granted Sellable Items are projected at their own `billing_frequency` with 4 weeks as 28 days); loader + router in `api/src/api/billing-simulation.ts`; `loadPromotionApplications` is now exported from `user-memberships.ts` and also carries each Promotion's name and Free/Paid/Bonus/Pay-Beforehand duration. The horizon runs until every item has been charged once at its regular price, capped at 36 months (`truncated`), and charges a year or more out are flagged `price_may_change`. Plan Charge Benefits are deliberately excluded (being decommissioned); Additional Periodic Services (#631) plug into the same engine as stage 2. No migration, no writes — nothing is persisted. New `BILLING SIMULATION` section on the Member expanded row (`MemberBillingSimulation.tsx`), outside the Membership Plan cards; new `members.billing_simulation_*` i18n keys in en/es/ca; 23 unit tests in `billing-simulation.test.ts`, 19 integration cases in `user-memberships.test.ts`, 8 structural cases in the admin app's `member-billing-simulation.test.ts`), #635 (stage 1 — Membership Plans aligned with the Promotion benefit structure: migration 173 adds `free_months`/`paid_months`/`bonus_months` to `membership_plans` and the three Sellable-Item-keyed benefit tables `membership_plan_session`/`_oneoff`/`_periodical`, the same shape migration 155 gave Promotions; new `GET`/`PUT /membership-plans/:id/{session,oneoff,periodical}-benefits` with the Promotion endpoints' exact contract (replace-all `{items:[{gym_charge_id,quantity}]}`, category checked via `classifySellableItem()`, an already-attached item survives going inactive); `PUT /membership-plans/:id` accepts the three duration fields, clearing one back to NULL rather than 0; `/:id/duplicate` carries both; new `BILLING & DURATION`, `ONE-OFF`, `SESSION` and `PERIOD BENEFITS` sections on the Plans page, each with its own Edit/Save/Cancel and no modal, sharing the new `components/SellableItemBenefits.tsx` editor; no Membership Fee Benefits (Promotion-only) and no billing change — Included Services and Charge Benefits stay load-bearing until stage 4; 60 integration cases in `membership-plan-benefits.test.ts`, 17 structural cases in the admin app's `plans-benefit-sections.test.ts`)

- **Done**: #647 (Book Personal Training Class Slots — all four stages of the plan agreed on the issue thread). **Stage 1 done**: migration 168 adds a nullable `professional_service_id` (FK→`professional_services` `ON DELETE SET NULL`) to both `activity_types` and `calendar_events`, so an occurrence can say which Professional Service (#484) delivers it; the Activity Type is the default and `activity-types.ts` propagates a change to not-yet-started occurrences through the existing `PROPAGATABLE_FIELDS` / `confirm_propagate` flow, `scheduleEngine.materializeScheduleRule()` stamps it on every generated occurrence, and both calendar routers accept it on create/update (a session inherits the Activity Type's value unless the field is sent). New `domain/professionalServices.ts` validates the id against the services visible **and enabled** for the gym (`gym_professional_services.status = 'active'`). New `domain/memberProfessionalServices.ts` answers the Member side — how many sessions the Member holds per Professional Service, summed across purchased `user_class_packages`, `promotion_session` benefits on applied promotions of active assignments, and `user_membership_services` attachments of Session-type items, all resolved through `sellable_item_professional_services` (#546) — served read-only by `GET /members/:memberId/professional-services`. No UI and no booking behaviour change yet. **Stage 2 done**: no migration — `GET /members/:memberId/personal-training-slots` (`api/member-personal-training-slots.ts`) projects the next 2 months of `calendar_events` whose `professional_service_id` the Member holds into recurring Mon–Sun slots, grouped by *gym-local* weekday + start/end time + activity type + professional service (local, not UTC, so a slot survives a DST change). Each slot carries one entry per expected date with a status (`available` / `no_occurrence` / `not_scheduled` / `full` / `already_booked`) plus `available_count` and `fully_available`: per the thread's Q5 answer a taken or unscheduled date is reported, never a reason to drop the weekday from the grid. The grouping rules are pure in `domain/personalTrainingSlots.ts`; #481 eligibility is asked through the new exported `isActivityTypeEligibleForMember()` that the booking hook itself now calls, so the projection can't drift from `bookMemberOnSession`. Admin: display-only `Personal Training Class Slots` section in the expanded Member row (`members/MemberPersonalTrainingSlots.tsx`). **Stage 3 done**: migration 169 adds `member_recurring_slots` — the Member's *weekly pattern* (`iso_weekday` 1=Mon…7=Sun, gym-local `start_time`/`end_time`, activity type, professional service, nullable center), unique through a VIRTUAL `selection_key` that folds a NULL center to 0; `PUT /members/:memberId/personal-training-slots/selections` replaces the whole set (every identity must be a slot the projection currently offers) and `POST /members/:memberId/personal-training-slots/book` books the window for it, optionally replacing the set in the same call. Both are `requireModuleWrite('MEMBERS')`. Booking goes one occurrence at a time through `bookMemberOnSession` in its own transaction, so capacity, waitlist mode, #481 eligibility and package-credit debiting stay the booking path's business and one rejected date cannot roll back the rest; a date that filled up since the projection is rolled back out of the waitlist and reported as `full` (§3: never book a slot that became unavailable), and the report names every date it skipped or failed. A stored slot the grid no longer offers books nothing (§5) and existing bookings are never touched by deselecting. Admin: the same section is now selectable (checkbox per slot, Save selection / Book, per-date result list). **Stage 4 done**: migration 170 adds `recurring_booking_run_log` (a `billing_run_log`-shaped singleton) and extends `chk_member_notifications_type`; `POST /recurring-bookings/run` (`api/recurring-bookings.ts`, `X-Internal-Secret`, fired by `.github/workflows/recurring-booking-run.yml` at 03:00 UTC) maintains the rolling 2-month window. Per the thread's Q4 answer it is an endpoint an external scheduler calls, not an in-process timer — the `POST /billing/run` precedent, and the only shape that behaves when the API runs more than one replica. Each run re-projects every member with stored selections from *now* and books the free dates through the very same `bookSelectedSlots()` the Book button runs (extracted from the stage-3 route, so capacity, waitlist mode, #481 eligibility, credit debiting and the §5 service re-check exist once); the window advances because "now" moved, with no per-member bookkeeping that could drift, and §4's "creates only missing bookings, never duplicates" falls out of the projection. Dates it cannot take raise a `recurring_booking_skipped` member notification (the thread's Q5 answer — festivity, gym closed, or taken by another booking), deduped on (slot, date) over a 120-day lookback so a rolling window does not re-alert nightly; `already_booked` and internal failures never notify. An unscoped run takes the 23-hour lock; a run scoped to a `gym_id`/`member_id` skips it so an operator can re-run one gym after a fix. The job honours the `organization.professional_services` flag, and one member's failure is caught and reported without ending the night's run.

- **Done (continued)**: #677 (Fix Theme Card Border and Border Radius Settings: the Theme editors' **Cards** group — `cardBorder` (a colour) and `cardBorderRadius` (an `advanced` attribute) — now actually paints the cards, in both Base and Custom Themes. Two defects: `cardBorderRadius` was editable and persisted but never written to any CSS variable, so the Card Border Radius did nothing anywhere; and most card surfaces restated a border colour (`#e2e2e6`, `#ececf0`, `#e0e0e8`) and a `borderRadius` of their own, so even `--gd-card-border` — which `applyTokens()` has always written — only reached the handful of pages that used the variable. `applyTokens()` now emits `--gd-card-radius` through an exported `CARD_ADVANCED_VARS` map, mirroring `CALENDAR_ADVANCED_VARS` and falling back to `DEFAULT_ADVANCED` for a theme with no `advanced` map or a blank value (the calendar's length-fallback logic extracted as the shared `cssLengthValue()`); and every card surface in the admin app — 21 `cardStyle`/`cardSt` definitions plus the inline list cards on Themes, Staff and Centers — spreads one shared `cardSurfaceStyle` (`apps/admin/src/components/ui.tsx`) carrying `border` + `borderRadius` + `background` as `var()`s with their default token values as CSS fallbacks. A highlighted or being-edited card overrides only `border` on top of the spread and keeps the themed radius. Base ↔ Custom is unchanged: both are applied through the same `applyTokens()`, so a Base Theme's values paint when it is the applied theme and a Custom Theme's override when it is (there is still no live sparse inheritance — cloning copies the blob, as since #559). Frontend-only: no migration, no API change, no i18n change, and no other theme setting touched. One deliberate visual change: cards that hardcoded `borderRadius: 10` now follow the token, whose default is `8px`. New `apps/admin/src/test/theme-card-css.test.ts` (7 cases: the attribute → variable map, emission for a configured theme, for a legacy theme with no `advanced` map and for a blank value, the configured-over-default precedence, the Cards colour group's fields, the shared style's variables and its fallback matching the default token, and a source scan asserting no card style declares a border radius or a border colour of its own))

- **Done (continued)**: #678 (Unify Base Theme Editor with Custom Theme Editor: expanding a Base Theme row in Cordel → Base Themes (`apps/admin/src/app/[locale]/system/themes/page.tsx`) now opens the same structured editor the Custom Themes screen uses — collapsible **Branding / Colors / Typography** sections over a Save/Cancel footer — instead of a read-only grid of colour swatches that exposed nothing but colours and hid the rest behind a separate *Edit* step. `Assignments` is the one Custom section with no platform counterpart (a theme is assigned to a *gym's* centres). The section wrapper and the Branding block (name, description, logo upload/removal, `logo_contains_gym_name`) moved into a shared `apps/admin/src/components/ThemeSectionEditor.tsx` (`ThemeSection`, `ThemeBrandingEditor`), joining the Colors/Typography editors already shared through `ThemeTokensEditor.tsx`, so a future theme setting is implemented once. On the Custom Themes screen a Base Theme now shows all four sections instead of Assignments + Colours, read-only: read-only means disabled controls, never a different set of controls, so `ThemeAdvancedSection` renders disabled rather than being omitted and `ThemeTypographyEditor` gained the `readOnly` prop `ThemeColorsEditor` already had. No change to the theme engine, persistence, status workflow or Base↔Custom inheritance — same `PUT /platform/themes/:id`, same status select, same #492 draft/live-preview/dirty-guard. Frontend-only: no migration, no API change; `themes.label_description` added to `es.json`/`ca.json` where it was missing and rendered as its raw key. Covered by `apps/admin/src/test/theme-editor-unification.test.ts` (9 cases))
- **Done (continued)**: #694 (Website registration no longer fails for a gym with one active and one inactive center: every sole-center fallback now goes through `soleActiveCenterId()` (`api/src/infra/centerContext.ts`) and counts active centers only; for website registration an inactive center behaves exactly like a deleted one (never counts towards "more than one center", explicit inactive `center_id` → same 400 as an unknown id; the self-registration first sign-in falls back to the first active center); the missing-center error now reads `center_id is required as the gym has more than one center`. Staff-side explicit center choices unchanged. No migration.)
- **Done (continued)**: #696 (Member app `/link`: setting a password after an invitation no longer fails with "Something went wrong". The password step called `signUp.password({ password, emailAddress })`, and re-sending `emailAddress` un-verified the address the invitation ticket had verified; it now calls `signUp.create({ strategy: 'ticket', ticket, password })`, keeping the ticket-verified email. No API change.)
- **Done (continued)**: #698 (Docs: the invitation email is Clerk's single *Invitation* template per instance, with no per-invitation language in Clerk's API — the language is chosen inside the template, see #701. Documented in `docs/wordpress-integration.md`; `docs/go-to-production.md` gains an item to recreate the customised template on the Clerk production instance (a premium feature there). No code change.)
- **Done (continued)**: #701 (Invitation language: website registration defaults `locale` to `es`, and copies `ca`/`en` into the invitation's `publicMetadata.lang` so the Clerk *Invitation* template — which picks Catalan/English from `lang` and falls back to Spanish via Handlebars `#if` — and the `/{locale}/link` page always match. `lang` is cleared with `gym_signup` on first sign-in. No migration.)
- **Done (continued)**: #702 (Member app `/link`: after a successful `POST /me/link` the page does a full navigation to Home (`window.location.replace`) instead of `router.replace`, so `AppContext` — which loaded `/me/gyms` and `/me/profile` on mount, before the member existed — loads again and the greeting shows the member's name straight after sign-up. No API change.)

- **Done (continued)**: #675 (View Audit Log on every entity's Details view: the deep link #642 added to the Member Details modal is now offered by every Details view in the admin app, through one shared `apps/admin/src/components/ViewAuditLogButton.tsx`. The component owns the permission rule (admin or superadmin, and the `system` / `system.audit` flags, matching the sidebar) and the URL shape (`?entity_type=<canonical>&entity_id=<pk>`), so no Details view hand-rolls either; `scope="platform"` points the three platform-level entities — Gyms, base Themes and the Cordel Nutrition Library — at Cordel → Audit Log instead of the gym one. Filtering is always by id, never by display name. 23 Details views covered: Members, Membership Plans, Promotions, Exercises, Assigned Plans, Tax Rates, Sellable Items, Workout Templates, Training Plans and Templates, Activity Types, Professional Services, Centers, Spaces, Staff, Themes, Billing Events, Nutrition Plans and Templates, both Nutrition Libraries, Gyms and platform Themes. On the API side `AUDIT_ENTITY_REGISTRY` gains the nine entity types those views deep-link that `recordAudit()` already wrote but the registry did not know (`activity_type`, `theme`, `tax_rate`, `gym_charge`, `professional_service`, `nutrition_library_item`, `nutrition_plan_template`, `member_nutrition_plan`, `staff`) — without an entry they resolved no `entity_name` and were missing from the `GET /audit-logs/meta` dropdown the deep link preselects. No schema change and no audit-writing behaviour changed. Covered by `apps/admin/src/test/view-audit-log-everywhere.test.ts` and `api/src/test/audit-registry.test.ts`)

- **Done (continued)**: #667 (Fix Info Toast Styling: `toast(message, type?)` in `apps/admin/src/components/Toast.tsx` defaults `type` to `'error'`, so a confirmation that omits the second argument renders with the red border and the ✕ icon — which is what "Diagnostics copied to clipboard." did. Frontend only: no API, schema or i18n change, and the component's three variants are untouched. The three confirmations left on the default now pass `'success'` — the storage-diagnostics copy in Cordel → Gyms (`system/gyms/page.tsx`) and the manual-payment and successful-retry toasts in Payments → Billing Events. Genuine failures keep the error variant, including `retry_failed` / `retry_failed_paused` and the Nutrition Plan Templates tree's blocking client-side validation messages. New `apps/admin/src/test/toast-severity.test.ts` parses every `toast(` call in the admin app (balanced-paren argument split, so multi-line calls are read correctly) and fails when a message whose wording says it succeeded is left on the default variant, when a message whose wording says it failed is given `'success'`/`'info'`, or when an unknown `ToastType` literal is passed; `docs/feature-patterns.md` records the convention alongside the existing error-toast rules)

- **Done (continued)**: #670 (Rename “Amount” to “Price” in Sellable Items: the field the Sellable Items catalogue calls `amount` is the item's price, so every label that named it now reads **Price** / **Precio** / **Preu** — the `PRICE` list column (still between `UNITS` and `TAX RATE`) and the four form/detail sites that render the field: the inline create form, the inline edit form, the expanded row's `BILLING CONFIGURATION` section and the Details modal. i18n-only: `sellable_items.col_amount`/`label_amount` were renamed to `col_price`/`label_price` in en/es/ca — matching the `*_price` key convention the `plans`, `prices`, `class_packages` and `memberships` namespaces already use — and the page reads the new keys. Nothing behind the label moves: the API still returns and accepts `amount`, both forms still submit `amount`, `fmtAmount()` and the tax-inclusive list cell (`amount_incl_tax` + the `taxIncluded`/`taxExcluded` suffix) are untouched, and there is no migration and no change to the DB column, the billing path or the VAT computation. Per the ticket, `Amount` labels that mean a transaction amount rather than a catalogue price are deliberately left alone — Members → Payments, Payments → Billing Events, and the membership ledger — as is the dead `gym_charges` i18n namespace, whose page has been a redirect to Sellable Items since #613 and which no component reads. New `apps/admin/src/test/sellable-items-price-label.test.ts` (6 cases: the label in all three locales, the old keys gone from both the locale files and the page, no locale value reintroducing “Amount”/“Importe”/“Import” anywhere in the namespace, the four `label_price` call sites and the column's position, the payload/formatting left intact, and the unrelated `Amount` labels preserved); `sellable-items-column-alignment.test.ts` (#637) follows the key rename)

- **Done (continued)**: #668 (Move Gym Storage Root Under `gyms/`: `buildGymFolderPrefix()` in `api/src/infra/storage.ts` now returns `gyms/<gym_id>-<sanitized_gym_name>` instead of `<gym_id>-<sanitized_gym_name>`, so a gym's root inside the shared R2 bucket is `<bucket>/gyms/<gym_id>-<gym_name>/`. That one helper is the single place the root is built: `POST /platform/gyms/:id/storage/initialize` captures its result in `gyms.storage_folder_prefix`, and both `initializeGymBucket()`'s folder markers and every later `uploadGymImage()` key hang off the stored value — so the `gyms/` root propagates with no second concatenation site and nothing below the gym root changes. Purely an object-key change: bucket (`CLOUDFLARE_R2_BUCKET`), endpoint, credentials, the `{gym_id}-{gym_name}` naming convention, the folder list and the diagnostics/error paths are all untouched, and no migration runs. Per the ticket's explicit constraint, **existing gyms are not migrated** — a gym initialized before this keeps its old captured prefix and its objects (re-running Initialize Cloudflare Bucket deliberately reuses the captured prefix rather than recomputing it), so the bucket can hold both shapes; `docs/go-to-production.md` §1 carries the decision to copy-and-rewrite or accept the split before launch. Unit coverage in `api/src/test/storage.test.ts`: the new prefix shape, every folder marker under `gyms/`, and an upload key under `gyms/`)

- **Done (continued)**: #669 (Remove “Manage” Button from Gym Edit View: the expanded read-only row in Cordel → Gyms (`apps/admin/src/app/[locale]/system/gyms/page.tsx`) ended with a `Manage` button under `NOTES` that called `setActiveGymId(gym.id)` and pushed the superadmin to `/{locale}/members` — an impersonation shortcut the ticket retires. Frontend-only deletion: the button, its wrapper `div`, the now-orphaned `handleManage()` helper, and `setActiveGymId` from the page's `useGym()` destructure (it had no other caller in the file). `NOTES` now ends after the notes paragraph with no trailing gap. Nothing else moves — the notes field, the gym information rows, the `Initialize Cloudflare Bucket` button, the Details/Edit/Duplicate/Delete context menu, routing, permissions and the API are untouched, and `setActiveGymId` itself stays in the gym context for the gym switcher and its other callers. No migration, no API change, no i18n key — the label was a hardcoded string, not a translation)

- **Done (continued)**: #672 (Align Workout Block Configuration by Type: a Workout Block's block-level numeric configuration now follows its Block Type — Standard, Superset, Triset and Giant Set have none, Circuit has `Rounds`, EMOM and AMRAP have `Minutes`, Tabata has `Intervals` — instead of every type but EMOM/AMRAP offering `Rounds`. `blockFieldConfig.ts` replaces the per-field `BLOCK_TYPE_FIELDS`/`isBlockFieldVisible` list with `BLOCK_TYPE_CONFIG`, one entry per type naming the column it persists to, its label/summary i18n keys and the stored-per-displayed unit, plus `getBlockConfig()`, `blockConfigInput()` (stored → input) and `blockConfigPatch()` (input → block patch). The three editors read it and nothing else: the Workouts builder (`WorkoutBlockBuilder.tsx`, used by Training → Training Plans), the Workout Templates tree (`WorkoutTemplateTree.tsx`) and the member Training Plan blocks modal (`members/PlanWorkoutBlocksModal.tsx`), as does `blockSummary()` in `summaries.ts` (so the Training Plan Template tree's chips follow too). No schema and no API change: `Minutes` is a display unit over the existing `duration_seconds` column (÷60 in, ×60 out) and Tabata's `Intervals` are stored in `rounds`, so blocks written before this read back unchanged. `work_seconds` and `rest_seconds` are no longer offered by any type — the columns and any values already in them stay, unread and uncleared, per the “don't clear hidden fields” rule in `docs/feature-patterns.md`; the block-level `col_duration`/`col_work_seconds`/`col_rest_seconds` and `summary_work`/`summary_rest` labels are dropped from en/es/ca and `col_minutes`/`col_intervals`/`summary_intervals` added. Per the ticket, `Sets` stays an exercise-level field and Target/Unit/Rest on the exercise row are untouched. New `apps/admin/src/test/workout-block-config.test.ts` (20 cases: the per-type map, the minutes and rounds conversions and their round-trip, empty/NaN input → null, hidden columns left to the caller, the three editors routing every write through `blockConfigPatch`, Sets still exercise-level, and the label keys present in all three locales with the retired ones gone))

- **Done (continued)**: #674 (Payments → Dashboard: a read-only metric page under Payments with four Billing Event counters — Scheduled This Month, and Total / Failed / Successful Last Month. New router `api/src/api/payments-dashboard.ts` exposing a single `GET /payments/dashboard/summary`, mounted in `app.ts` **before** `/payments` so it is gated by its own `payments.dashboard` flag (migration 171 seeds the key enabled) rather than by `payments.transactions` — the Dashboard survives Transactions or Billing Events being switched off, and can be switched off on its own. Last month's three cards come from one grouped query over `billing_events` (`created_at` inside the previous calendar month, one `WHERE be.gym_id = ?`) whose groups are mapped through the shared `deriveBillingEventStatus()` (#640), so the status vocabulary is never re-spelled as SQL and the cards agree with the Billing Events page; `total_last_month` counts every ledger row, so it is not necessarily failed + successful (an `adjustment` or `status_changed` row is neither). Scheduled This Month reuses the page's projection from `user_memberships.next_billing_date` + `billing_policies.recurring_billing_interval`, counting the projected dates that fall inside the current month and skipping dates the nightly run has not caught up with, exactly as the `scheduled` rows on that page do. Month boundaries are derived in UTC in one exported `monthWindows()` so the SQL ranges and the JS projection share a clock, and the API returns the window it counted over so the page's period labels can't drift into the browser's time zone. Frontend `apps/admin/src/app/[locale]/payments/dashboard/page.tsx` follows the #638 Finance Dashboard card wall; the nav entry carries `featureKey: 'payments.dashboard'` and the page itself bounces a direct URL when the flag is off, applying the same superadmin-bypass-unless-impersonating rule as the sidebar. New `payments_dashboard` namespace in en/es/ca; no Billing Event or payment-processing behaviour changed. Covered by `api/src/test/payments-dashboard.test.ts`)

- **Done (continued)**: #673 (Activate/Deactivate Toggle in the Exercise Context Menu: a gym exercise's ⋮ menu now carries a status toggle between `Duplicate` and `Delete` — `Deactivate` when the row is active, `Activate` when it is inactive — so a status change no longer requires opening the inline editor. Frontend-only: `handleToggleStatus()` in `apps/admin/src/app/[locale]/exercises/page.tsx` reuses `PUT /exercises/:id` with a status-only body, which the router already applies on its own (`status = COALESCE(?, status)`, every other column guarded by `'field' in req.body`, muscles and result types left untouched when absent), then reloads the list so the `StatusBadge` and the status filter follow. No confirmation dialog, per the ticket — the delete confirmation is the only one on the page. The toggle carries the same `canWrite` gate and read-only tooltip as the other write actions, and an open inline editor has its form status synced so saving it cannot revert the toggle. Base exercises (`gym_id IS NULL`) keep their `Details` + `Clone to gym` menu — the router answers 403 for a gym-side write to them — and the Cordel → Base Exercises page is unchanged. New `exercises.activate`/`deactivate` keys in en/es/ca; no migration, no API change. Covered by `apps/admin/src/test/exercise-status-toggle.test.ts` (9 cases: the labels in all three locales, the status-conditional menu entry, the write gate, the base-exercise menu, the status-only payload, the editor sync and the absence of a second confirmation) plus a status-only `PUT` case in `api/src/test/exercises.test.ts`)

- **Done (continued)**: #643 (Translate Nutrition Library Content: migration 166 adds `nutrition_library_item_translations` (`item_id FK→nutrition_library_items` CASCADE, `locale`, `name`, PK `(item_id, locale)`, `KEY nlit_locale_name`) — the same catalogue+junction shape as categories (#501) and qualities (#293) — and seeds Spanish + Catalan for all 32 system items (`gym_id IS NULL`) via `INSERT IGNORE ... SELECT` matched on the base name, so a re-run, a renamed item or an already-edited translation is a no-op. `nutrition_library_items.name` stays the base (English) value, the uniqueness key and the fallback for any locale with no row. New `api/src/infra/locale.ts` resolves the request locale from `x-locale`, then `Accept-Language` (q-value ordered, `es-ES`→`es`), then `DEFAULT_LOCALE`, always narrowing to the `SUPPORTED_LOCALES` allowlist (env-configured, default `en,es,ca`); `domain/nutritionLibrary.ts` gains `localizedNameSql`/`localizedNameExpr` (a `COALESCE` subquery, a bare column for the base locale), `loadTranslationsMap`, `replaceTranslations` and `validateTranslations`. Every read surface resolves names per request — `nutrition-library.ts`, `platform-nutrition-library.ts`, `nutrition-plan-templates.ts`, `platform-nutrition-plan-templates.ts`, `member-nutrition-plans.ts` and `me.ts` (`GET /me/nutrition-plan`) — with library list rows now carrying `display_name` (localized) and `translations` next to the base `name`, and list search/ordering following the displayed locale. Superadmins author translations on Cordel → Base Nutrition Library (one field per translatable locale, blank = fall back) through `POST`/`PUT` `/platform/nutrition-library` plus a new `PUT /platform/nutrition-library/:id/translations` sub-resource and `GET /platform/nutrition-library/locales`. Gym-created items keep a single entered name shown in every locale. Category and quality names stay slug-keyed and translated in the apps' locale files, where en/es/ca were already complete. Both apps send `x-locale` from `useLocale()` (added to both proxies' header allowlists). 23 unit cases in `locale.test.ts`; integration coverage added to `platform-nutrition-library.test.ts`, `nutrition-library.test.ts` and `me-nutrition-plan.test.ts`)

- **Done (continued)**: #644 (Review and Classify Nutrition Library Nutritional Qualities: migration 167 completes the partial backfill migration 127 (#350) started. `fat` and `fiber` existed in the `nutritional_qualities` catalogue since #501 but had **zero** item assignments, and only 20 `protein`/`carbohydrate` links existed at all. `nutrition_library_items` stores no macro values — the row is `id, gym_id, name, status, image_url, created_at, modified_at` — so, per the ticket thread's answers, the classification is made by food from general nutritional composition and seeded in a migration, where the per-item decisions are reviewable in the diff and versioned with the schema (no numeric columns added; that stays a separate ticket if ever wanted). Stated criterion, indicative per 100 g of the food as normally served: protein ≥ 10 g, carbohydrate ≥ 10 g, fat ≥ 8 g, fiber ≥ 2 g, with wholegrain/whole-plant foods marked for fiber and refined-grain staples not, so the borderline cases are decided by a rule rather than by rounding. All 32 system items are in the table, including the seven that qualify for nothing (Water/Coffee/Tea/Mustard/Hot Sauce/Tomato Sauce/Yogurt Sauce) so the review is complete on its face; each row carries a one-line note. 19 new links: `fat` on Beef/Salmon/Eggs/Tofu/Olive Oil/Nuts/Dairy, `fiber` on Brown Rice/Vegetables/Salad/Sweet Potatoes/Quinoa/Fruit/Oats/Nuts, plus `protein` on Dairy and `carbohydrate` on Juice. Matching is by item name and covers **gym-created** items as well as system ones, but is **additive only** (`INSERT IGNORE`) — re-reviewing 127's 20 links found none that contradict the criterion, and a name-matched heuristic must not delete a tag a gym set by hand; non-active items are skipped, and it is a one-time review rather than a standing rule (an item created later is untouched). Note the `utf8mb4_0900_ai_ci` collation makes the name match case- and accent-insensitive, so a gym's "Salmón" is classified as "Salmon". `down()` is deliberately narrower than `up()` — system items only, and never 127's pairs — because `INSERT IGNORE` keeps no record of which links it actually created, so a name-and-slug delete cannot distinguish its own row from one an admin set by hand; leaving an extra tag on a gym item is recoverable from the UI, deleting a curated one is not. No schema, API, UI or i18n change — the `quality_*` labels and the read/write surface (`domain/nutritionLibrary.ts`, both Nutrition Library pages) already handled all four slugs. New `nutrition-library-quality-classification.test.ts` (6 DB-free unit cases: full system-item coverage, valid slugs only, a note per item, every quality used, no 127 assignment lost, the untagged set), plus 5 integration cases appended to `nutrition-library.test.ts` asserting the seeded result in the migrated database and through `GET /nutrition-library`)

- **Done (continued)**: #640 (Billing Events Context Menu, Details, Retry Payment and Manual payment: migration 165 adds `modified_at`/`modified_by_user_id` to both `billing_events` and `payment_requests` (Clerk-id actor column, matching `billing_events.actor_user_id`, resolved to a name through `gym_memberships.name`), `failure_code`/`failure_message`/`notes`/`attempt` to `payment_requests`, and widens `chk_payment_requests_source` to `admin|customer|billing_run|retry|manual`. A Billing Event's **status is now derived from its latest linked Payment Transaction** (`domain/billingEventStatus.ts` — pure, unit-tested: `completed`→paid, `failed`/`expired`→failed, `pending`→pending), falling back to the `event_type` mapping when the event has no transaction; `pending` joins the status vocabulary of the list filter. New routes on `paymentsRouter`: `GET /payments/billing-events/:id` (read-only Details — id, member, plan, amount, currency, status, charge type, source, Created/Modified At+By, Next Payment, failure reason, receipt, `can_retry`/`can_record_manual_payment`), `POST /payments/billing-events/:id/retry` and `POST /payments/billing-events/:id/manual-payment` (both `requireModuleWrite('PAYMENTS')`). Neither action ever appends a Billing Event: they add Payment Transactions against the existing `billing_event_id`, so the append-only ledger row is never rewritten. Retry (issue Q3 option B) fires the MIT charge and, on rejection, once more — two rejections pause the assigned plan through `recordStatusChange`, and every covered member's derived payment status follows because `GET /members` now resolves `payment_status` through `user_membership_members` as well as `pr.member_id` (two-person/family plans bill once, against the owner). A successful retry or manual payment advances `next_billing_date`/`last_billed_at` exactly like the nightly run, guarded by a 409 on an event that already has a completed transaction so one charge can never advance the schedule twice. `audit_logs` records the two manual interventions (`retry_payment`, `manual_payment`, added to `AUDIT_ACTIONS`) with previous/new status and the attempt results. The nightly run now also stores the provider's rejection reason on the failed `payment_requests` row, not only in the ledger row's notes. Admin UI: a `ContextMenu` per real row (Details always; Retry Payment + Manual payment only when the row is actionable), Details expanding the same row read-only and the Manual payment card rendered inline inside it — no modal; read-only PAYMENTS roles see the menu with the write items disabled)

- **Done (continued)**: #634 (Member Membership Configuration and Consolidated Billing Simulation — stage 3 of the #629 → #631 → #634 chain, whose engine and Additional Periodic Services shipped in the first two. Migration 172 narrows migration 007's one-active-Membership-per-Member unique index from `(active_member_key)` to `(active_member_key, membership_plan_id)`, so a Member may hold **several active Membership Plans in parallel, one per Plan** — the thread's answer to the blocking question. The index swap needs no data migration (the old guarantee implies the new one) and touches no row; the rollback refuses with an actionable message if any Member already holds two. Adding a Plan is purely additive (§14): `POST /user-memberships` no longer supersedes anything, and "Assign New Plan" stays as the separate, explicit supersede action. `POST /user-memberships` and `/:id/assign-new-plan` now reject a Plan that is not Active + Public (§2, `planAssignabilityError()`); `GET /membership-plans` gains an `enrollment_status` filter, and the Members page's plan fetch — previously `?status=active`, a parameter the endpoint does not read, so every plan came back — now asks for `lifecycle_status=active&enrollment_status=public`. New read-only `GET /user-memberships/member/:memberId/configuration` (`member-membership-configuration.ts`) returns `{plans, promotions, services}` in one call — every assignment newest-first with `is_live` and its `activity_allowances`, plus the live assignments' Promotions and Additional Services, each carrying `user_membership_id` and `plan_name`. It contains no business logic: every write stays on the existing per-Assigned-Plan routes. `MemberExpandedRow.tsx` is restructured into the ticket's four independent sections — `MEMBERSHIP PLANS` (`MemberMembershipPlans.tsx`, inline "+ Add Membership Plan" with radio-button Plan selection), `PROMOTIONS` (`MemberPromotions.tsx`, Member-level, each row naming its Plan, stacking rules live in the picker), `ADDITIONAL SERVICES` (`MemberAdditionalServices.tsx`, reusing #631's inline editor once per live Plan) and `BILLING SIMULATION` — none nested inside a plan card (§13) and no modal anywhere (§15). A change in any of the first three re-reads the configuration and remounts the simulation (§12), whose lines now name the Plan that generated each charge (§7). The simulation itself already consolidated every live assignment from #629/#631, so §5–§11 needed no engine change. Knock-on: `POST /me/payment-requests` stopped picking "the" active membership with `LIMIT 1` — it takes an optional `user_membership_id` and answers `409 multiple_active_memberships` otherwise. New `members.*` keys in en/es/ca; new `api/src/test/member-membership-configuration.test.ts`, plus cases in `user-memberships.test.ts`, `membership-plans.test.ts` and `membership-promotions` coverage for parallel plans, the Active+Public gate and the new-members rule. Deliberately **not** implemented: §3's "Promotion `Only applicable for new members` is respected". The flag has been stored since #633 (migration 163) with `NOT NULL DEFAULT 1`, so every existing Promotion carries it, and the ticket never says what makes a Member "new" — under one reading a Member adding a second parallel Plan is no longer new, under another only a lapsed-and-returned Member is not. Either guess would retroactively refuse most Promotions on most Members, including through #628's Assign New Plan picker, so the definition is asked for on the issue rather than chosen here. Also unchanged, and out of scope: the Assigned Plan snapshot architecture (#635).)
- **Done (continued)**: #631 (Additional Periodic Services on an Assigned Plan: migration 164 adds `user_membership_services` (gym_id, `user_membership_id FK→user_memberships` CASCADE, `gym_charge_id FK→gym_charges` with no cascade, quantity, `starts_at`, `ends_at`, CHECKs `quantity > 0` and `ends_at >= starts_at`, plus a VIRTUAL generated `open_service_key` under a unique index so two concurrent POSTs can't attach the same item twice), attaching recurring Sellable Items directly to the Assigned Plan — independent of the Membership Plan's included benefits and of Promotion benefits, which keep their own tables. The Sellable Item stays the source of truth for name/price/`billing_frequency` (read live, never snapshotted); only items `classifySellableItem()` classifies as `periodical` are attachable, and only while the plan still bills. New `api/src/api/user-membership-services.ts` router mounted at `/user-memberships/:id/services` (GET/POST/DELETE, PAYMENTS gate + `payments.transactions` flag); the services are also embedded in `GET /user-memberships/:id` as `additional_services`. Removal is future-only: DELETE stamps `ends_at = today` so charges already produced stand, and only deletes outright when billing hadn't started. The Billing Simulation (#629) now projects each service as its own stream at the item's own frequency and price × quantity, over its effective window, with no Promotion benefit ever applied to it — a service added after the plan started bills from its effective date, and a removed one disappears after its end date. New `ADDITIONAL PERIODIC SERVICES` inline section (no modal) in `AdditionalPeriodicServices.tsx`, rendered both in the Assigned Plan expanded row and in the Member page's current plan card, where an add/remove also remounts the Billing Simulation so it recalculates immediately; new `assigned_plans_page.services_*` / `section_additional_services` and `members.section_additional_services` i18n keys in en/es/ca; 9 new unit cases in `billing-simulation.test.ts`, a new integration file `user-membership-services.test.ts`, 2 new simulation cases in `user-memberships.test.ts`, and 9 structural cases in the admin app's `additional-periodic-services.test.ts`)

- **Done (continued)**: #645 (Registration endpoint keyed by gym id + health check: `POST /public/gyms/:slug/registrations` becomes `POST /public/gyms/:gymRef/registrations`, where `:gymRef` is `{gymId}-{gym-name}` — `parseGymRef` resolves the leading `CHAR(36)` UUID and ignores the readable half, so two gyms with the same name can no longer share an endpoint and a rename never breaks a configured website. A reference that doesn't start with a gym id still resolves as `gyms.slug`, so WordPress installs configured before this change keep registering; `GET /system/website-integration` now advertises the new form (`gym_ref` + `endpoint_path` / `endpoint_url`, name half URL-encoded). Second half: exactly `{ name: 'test', email: '' }` is a health check — after the key check and gym resolution it returns `200 { ok: true, health_check: true }` before validation, creating no member, no invitation and no email, and skipping the per-gym daily quota (per-IP limiter still applies); `isHealthCheckBody` can't collide with a real registration because the schema requires a non-empty email. No migration, no schema change. New unit test `public-registration-gym-ref.test.ts` (13 cases for both helpers), `public-registrations.test.ts` extended with gym-reference and health-check blocks (same-name gyms get non-interchangeable endpoints, legacy slug still 202, probe 401s without a valid key), `website-integration.test.ts` asserts the advertised `gym_ref`; `docs/wordpress-integration.md` documents the new URL format, the health check and a `gymdesk_registration_health_check()` plugin helper; new `website_integration.how_5` i18n key in en/es/ca)

- **Done (continued)**: #293 (Nutritional Qualities for Nutrition Library Foods: migration 110 adds `nutritional_qualities` catalogue table (slug-keyed, global) + `nutrition_library_item_qualities` junction table (item↔quality M2M, both ON DELETE CASCADE); seeds protein + carbohydrate; `platform-nutrition-library.ts` extended: GET / includes `qualities:[{id,slug}]` per item, POST/PUT accept optional `quality_ids` to set qualities atomically, new `PUT /:id/qualities` sub-resource for standalone quality replacement; `GET /platform/nutrition-library/nutritional-qualities` lists the catalogue; gym-level `GET /nutrition-library` includes `quality_slugs: string[]` per item; Cordel admin nutrition-library page shows quality chips in the item table and multi-select checkboxes in the Create/Edit modal; gym-level nutrition-library page shows translated quality badges; i18n keys `nutritional_qualities_label`, `quality_protein`, `quality_carbohydrate`, `no_qualities` added to en/es/ca; 9 new tests in `platform-nutrition-library.test.ts`), #291 (Card Background Theme Fix: replaced undefined `--gd-surface`/`--gd-border` CSS vars with `--gd-card-bg`/`--gd-card-border` in Promotions and Exercises pages; extended the same `--gd-border` → `--gd-card-border` fix to all other admin pages that referenced the undefined token: Spaces, Calendar EventDetailsPanel, System Gyms, Activity Types, Class Packages, Training Plan Templates, Workout Templates, Recycle Bin), #271 (Sellable Items: migrations 102–103 extend `gym_charges` with `name`, `type` (fee/service/sessions/merchandise/other), `units`, `status` (active/inactive), `is_system`, soft-delete columns, `package_information`, `validity_days`, `billing_frequency` extended with 'per_session'; migration 103 adds `class_package_id` FK and migrates all class_packages rows as type='sessions' custom items; `gym-charges.ts` fully rewritten — POST create custom, PUT system/custom split (system edits limited to description/amount/frequency/notes/status), POST /:id/activate, POST /:id/deactivate, DELETE soft-delete with 403 guard on is_system; GET / gains `?type`, `?status`, `?q` filters; `membership-plans.ts` and `promotion-details.ts` updated to LEFT JOIN charge_types + COALESCE name + filter by status/deleted_at; `recycle-bin.ts` entity type `class_package` → `sellable_item`, queries gym_charges WHERE is_system=0; admin `/financials/sellable-items` page with inline creation, inline editing, type/status filters, search, system/custom context menus, soft-delete confirm dialog; `/financials/gym-charges` + `/class-packages` redirect permanently to `/financials/sellable-items`; Class Packages removed from Organization nav; nav renamed to Sellable Items; i18n `sellable_items` namespace added in en/es/ca; `gym-charges.test.ts` updated with new filter, POST, and DELETE test blocks)., #292 (Feature Flags for System Navigation: migration 110 makes `audit_logs.gym_id` nullable for platform-scoped audit rows; creates `feature_flags` table with stable dot-separated keys and seeds all 30 released nav nodes as enabled=1; `featureFlags.ts` with 30s in-memory cache + `requireFeatureEnabled(key)` middleware (ancestor-key check, superadmin bypass); `platform-feature-flags.ts`: superadmin `GET/PUT /platform/feature-flags` + public `GET /feature-flags` returning `Record<string, boolean>`; `requireFeatureEnabled` wired to all domain routers in `app.ts` and to gated `/me/*` routes; `feature_flags` entity added to audit-registry; `navigationGroups.ts` extended with `featureKey` on every group and item, `filterNavGroups` accepts flags param; `FeatureFlagsContext` provider fetches flags on mount; layout wraps with `FeatureFlagsProvider`; Sidebar uses `useFeatureFlags()` + passes flags to filter; Cordel → Feature Flags management page with toggle tree; i18n updated in en/es/ca).
- **Done (continued)**: #344 (Move Deleted Members to Recycle Bin: migration 119 adds `deleted_by_name VARCHAR(255)` to `members`; `DELETE /members/:id` now stores `actorName` in `deleted_by_name`; `GET /members/deleted` and `POST /members/:id/restore` removed; `recycle-bin.ts` extended with `member` entity type (UNION branch, detail fetch, recover); admin `/members/deleted` page deleted; nav child removed from Members; recycle-bin frontend adds `member` to entity type list + detail modal + badge colors; `entity_member` i18n key added to en/es/ca; `members.test.ts` extended with DELETE tests; `recycle-bin.test.ts` extended with 10 member tests).
- **Done (continued)**: #323 (Shared Training Slots: `activity_types.shareable BOOLEAN DEFAULT false`; `spaces.max_concurrent_groups TINYINT DEFAULT 1`; `gym_memberships.max_concurrent_groups TINYINT DEFAULT 1`; `class_sessions.sharing_authorized BOOLEAN DEFAULT false`; new `shared_training_requests` table (host_session_id, requested_activity_type_id, requesting_member_id, status pending/approved/rejected, resolved_class_session_id); `POST /class-sessions` validates concurrent-group capacity atomically via `SELECT FOR UPDATE` when trainer+space both set — returns 409 codes slot_not_shareable/activity_not_shareable/slot_fully_occupied/sharing_not_authorized; `PUT /class-sessions/:id` revalidates on slot changes; new `PUT /class-sessions/:id/sharing-authorized`; new `/shared-training-requests` router (GET/GET:id/POST + POST/:id/approve + POST/:id/reject); approve is atomic: SELECT FOR UPDATE → create session → SET sharing_authorized=1 on host → bookMemberOnSession inside same transaction; trainers router includes max_concurrent_groups; spaces POST/PUT accept max_concurrent_groups; activity-types POST/PUT accept shareable; `/me/schedule` returns shareable/sharing_authorized/can_request_sharing; `POST /me/shared-training-requests` and `GET /me/shared-training-requests` for member-facing request flow; migrations 115–116; TypeScript clean; shared-training-requests.test.ts).
- **Done (continued)**: #326 (Calendar — Manage Individual Activity Instance: class sessions now rendered on the Calendar alongside calendar events; clicking a class session opens `ClassSessionDetailPanel` (instance management) instead of the event editor; panel shows enrolled members, waiting list, add member (with capacity warning + override), add to waiting list, remove member (with auto-promote confirmation), change event time, cancel event; `GET /class-sessions` extended with `activity_type_id`, `space_id`, `trainer_membership_id` filter params so the calendar filter bar works for sessions; `GET /members?q=` search param added (LIKE on name/email, max 20 results); `POST /bookings` gains `waitlist=true` flag for explicit staff-managed waitlist addition even when capacity is available; `MemberSearchInput` debounced combo component; `bookMemberOnSession` extended with `forceWaitlist` parameter; no migrations; shareable training deferred — no backend domain exists yet; `members.test.ts` with 6 tests; bookings.test.ts extended with 3 tests for explicit waitlist).
- **Done (continued)**: #325 (Member App — Membership Details, Status and Billing Information: `GET /me/membership` extended with `um.next_billing_date` in SELECT and new `upcoming_payments: [{date, amount, status:'scheduled'}]` field computed server-side via `computeUpcomingPayments` + `advanceDate` helpers (exported for unit testing); member app `membership/page.tsx` updated — `Membership` interface gains `upcoming_payments: UpcomingPayment[]` and `next_billing_date`, "Starts" label renamed to "Member since", new "Upcoming payments" section renders the server-computed array with `EventStatusPill` badge; new `EventStatusPill` component matches existing `StatusPill` design; locales en/es/ca extended with `member_since`, `upcoming_heading`, `upcoming_empty`, `event.recurring_payment`, `event.failed_billing`, `upcoming_status.scheduled`; `me-upcoming-payments.test.ts` — 13 unit tests covering `advanceDate` (days/weeks/months/years) and `computeUpcomingPayments` (null guards, far-future dates, stale past dates, amount formatting, Date object input); no migrations, no Admin changes).
- **Done (continued)**: #321 (Multi-day weekly schedule rules: migration 115 adds `weekdays JSON NULL` to `activity_type_schedule_rules`; backfills existing weekly rules from `weekday` → `weekdays` JSON array; clears `weekday` for weekly type (retained for monthly); API `validateRule` accepts `weekdays: number[]` with backward-compat shim for legacy `weekday` integer; `formatRule` parses JSON to array; schedule engine `occurrenceDatesForRule` iterates over `weekdays[]`, deduplicates + sorts; frontend toggle-button day-picker for weekly type using existing `weekday.N` i18n keys; no new i18n keys required).
- **Done (continued)**: #324 (Member Calendar: Free/Busy Availability and Booking Actions: migration 115 adds `activity_types.is_shareable TINYINT DEFAULT 0`, `class_sessions.allows_shared_booking TINYINT DEFAULT 0`, and new `shared_training_requests` table (pending/approved/rejected/cancelled lifecycle, UNIQUE on gym+session+member, `reviewed_by_membership_id FK→gym_memberships`); seeds `calendar.member_calendar` feature flag; new `/shared-training-requests` router with `GET /?status&class_session_id`, `POST /:id/approve` (transactional capacity re-check + booking INSERT + notification), `POST /:id/reject` (notification), under TRAINING module + `calendar.member_calendar` flag; `GET /me/schedule` extended with `activity_type_id` filter, 6 new fields (`allows_shared_booking`, `is_shareable`, `my_shared_request_id`, `my_shared_request_status`, and computed `availability_state` enum with 8 states: UNAVAILABLE/BOOKED_BY_MEMBER/AVAILABLE/WAITLISTED_BY_MEMBER/SHARED_REQUESTED_BY_MEMBER/SHARED_REQUEST_AVAILABLE/WAITLIST_AVAILABLE/FULL); new `POST /me/shared-training-requests` and `DELETE /me/shared-training-requests/:id` under `calendar.member_calendar` flag; `PUT /class-sessions/:id` accepts `allows_shared_booking`; member app gains `/calendar` page (FullCalendar v6 month/week/day, activity-type filter chips, per-state event colors, bottom-sheet action panel for all 8 states), new BottomNav "Calendar" tab, `member_calendar` locale namespace in en/es/ca; admin schedule page gains "Toggle Sharing" button (PUT class-sessions), "Shared" button → `SharedTrainingPanel` drawer with approve/reject actions; FullCalendar v6 added to member app package.json; 2 new test files).
- **Done (continued)**: #322 (Activity Audit Metadata: backend already correctly set `created_by_membership_id`/`modified_by_membership_id` in all CRUD operations — no code changes needed; added 6 audit metadata tests to `activity-types.test.ts` covering create, update-by-same-user, update-by-different-user, multiple-updates, failed-update, and client-supplied-fields-ignored scenarios).
- **Done (continued)**: #294 (Nutrition Plan Template Meal & Food Item Editor Improvements: migration 111 extends `nutrition_plan_template_meal_items.component_type` CHECK to include `drink`, `dessert`, `other` (keeps `additional` for backward compat); `COMPONENT_TYPES` constant updated in `nutrition-plan-templates.ts` and `platform-nutrition-plan-templates.ts`; `NutritionPlanTree.tsx` — `LibraryItem` interface gains `quality_slugs: string[]`; `MealRow` is now expandable/collapsible (▶/▼ toggle on header click, auto-expands on Edit); expanded non-edit mode shows read-only items list with quality badges (color-coded per quality slug) and OR separator between consecutive items of the same `component_type`; `MealItemsEditor` redesigned with Food Type selector (categories derived from loaded library items, not hardcoded) → filtered Food selector → quality badges shown beneath the selected food → Qty + Unit row; per-item inline edit button opens a one-row form (Food Type | Food | Qty | Unit | Save | Cancel) pre-populated from the library item's category; changing Food Type clears the food selection; `PUT …/items/:itemId` used to save edits; `ItemsReadView` extracted for use in non-edit expanded mode; i18n keys added in en/es/ca: `tree_label_food_type`, `tree_label_food`, `tree_food_type_or`, `tree_component_type_drink`, `tree_component_type_dessert`, `tree_component_type_other`; food names displayed as stored in DB (single-language); OR localized as OR/O/O).
- **Done (continued)**: #320 (Calendar double-click to Day view: in Month and Week views, double-clicking a day navigates directly to Day view for that date using FullCalendar's `api.changeView('timeGridDay', date)`; single-click behavior preserved via 300ms timer in `handleDateClick`; no backend changes, no new routes, no library changes — pure FullCalendar API; one file changed: `apps/admin/src/app/[locale]/calendar/page.tsx`).
- **Done (continued)**: #319 (Rename "Activity Types" → "Activities" in UI: updated `nav.activity_types` in en/es/ca locales; updated all user-facing strings in `activity_types.*`, `spaces.section_activity_types`, `spaces.no_activity_types`, `calendar.filter_activity_type`, `calendar.event_activity_type` across en/es/ca; fixed hardcoded `<FormLabel>Activity Type</FormLabel>` in `plans/page.tsx`; technical identifiers (`activity_types` table, `/activity-types` API paths, TypeScript types, variable names) unchanged).
- **Done (continued)**: #318 (Admin Navigation Audit + Cleanup: added `/memberships` to the Membership nav group (MEMBERS module) and `/schedule` to the Calendar nav group (CALENDAR module) — both pages existed but were unreachable from nav; confirmed no stale Specialities, Class packages, or workoutsRouter references remain in `navigationGroups.ts`; all i18n keys were already present in en/es/ca), #311 (Tax Rates + Sellable Items refactor: new `tax_rates` table (migration 112) — per-gym `is_system=1` default "Standard VAT 21%" auto-seeded at gym creation + custom CRUD with soft-delete; migration 113 adds `tax_behavior VARCHAR(20) CHECK ('inclusive','exclusive')` and `tax_rate_id FK→tax_rates ON DELETE SET NULL` to `gym_charges`, and `tax_rate_id FK→tax_rates ON DELETE SET NULL` to `membership_plans`, both backfilled from each gym's system default; `/taxes` router: GET/GET:id/POST/PUT/POST:id/activate/POST:id/deactivate/DELETE (403 on is_system=1, soft-delete otherwise); `/sellable-items` router replaces `/gym-charges` — SELECT LEFT JOINs `tax_rates` for `tax_rate_name`/`tax_rate_percent`, `computePriceFields()` returns `amount_excl_tax`/`amount_incl_tax`/`applied_tax_rate` based on `tax_behavior`; POST/PUT accept `tax_rate_id` and `tax_behavior`; `gyms.ts` seeds system tax_rate on gym creation; all 5 frontend callers migrated from `/gym-charges` to `/sellable-items`; `plans/page.tsx` and `promotions/page.tsx` updated; new admin Taxes page (`/financials/taxes/`); Sellable Items page extended with tax rate column and `amount_incl_tax` display; nav entry for Taxes added; i18n `taxes` namespace + tax keys in `sellable_items` namespace in en/es/ca; `gym-charges.test.ts` updated for new path + computed price field tests).
- **Done (continued)**: #349 (Admin Payments — Billing Events Management: migration 119 adds `billing_event_id INT UNSIGNED NULL FK→billing_events ON DELETE SET NULL` to `payment_requests` (with index); `billing.ts` rewritten so billing_events rows are inserted first and `payment_requests` rows reference them via `billing_event_id` — establishes the invariant "every payment is linked to a billing event"; `webhooks.ts` likewise: on MONEI payment_recorded event, inserts the billing_event then UPDATEs `payment_requests.billing_event_id`; two new endpoints in `payments.ts`: `GET /payments/billing-events` (combined past/future list — real rows from `billing_events` DB, virtual future rows computed from `user_memberships.next_billing_date` with a rolling 5-date window per active membership, merged and paginated in JS; filters: `member_id`, `from`, `to`, `limit`, `offset`) and `GET /payments/billing-events/:id/transactions` (payment_requests linked to a billing_event, JOINs payment_methods for card info); admin page at `/payments/billing-events` with inline member search filter, date-range filters, URL-persisted filter state, expandable rows (lazy-fetches transactions on first open), virtual future rows shown with "Scheduled" badge; nav item added to Payments group; i18n in en/es/ca; `payments-billing-events.test.ts` with coverage for auth, tenant isolation, happy path, member filter, date range, transactions endpoint), #347 (Training Plans deep-link from Member view: clicking Edit on a Training Plan in the expanded Member row now navigates to `/training-plans?open=<plan_id>&member_id=<member_id>`; the Training Plans page reads these params on mount — pre-sets the member filter and auto-expands the target plan (enters edit mode for non-completed plans, read-only expand for completed ones) and scrolls it into view; invalid/missing plan IDs are ignored gracefully; no API or DB changes), #341 (Member App — Gym and Centre Selection: new `GET /me/gyms` endpoint mounted before `tenantContext` returns all gyms the caller belongs to (via `gym_memberships`) with theme data, ordered alphabetically; superadmin variant returns all non-deleted gyms; `GymSwitcher` component renders a `<select>` hidden when ≤1 gym; `AppContext` extended with `gyms: GymOption[]` and `switchGym(id)` — persists selection to localStorage, switches theme client-side from cached list; `activeGymId` key in localStorage; i18n in en/es/ca; 5-test `me-gyms.test.ts`), #327 (Admin Members — Expandable Row with Membership, Plans & Billing Details: `GET /members` extended with `account_status` (active/invited/not_enrolled, derived from clerk_user_id/invitation_id DB fields) and `membership_status` (most-recent user_memberships status, subquery) — no extra round-trips; `StatusBadge` gains `invited`, `not_enrolled`, `suspended` color entries; Members page rewritten to use `DataTable` with `renderExpanded` — collapsed row shows Name+email, Account Status badge, Membership Status badge, ContextMenu; `MemberExpandedRow` component lazy-loads on first expand: Account section (Clerk status via GET /members/:id/clerk-status), Membership section (plan name/status/rate/dates + allowances via GET /membership-plans/:id/allowances), Training Plans section (active/inactive grouped, Edit → /training-plans list), Nutrition Plans section (read-only list, no editor), Billing Events section (flat list from GET /billing-events/member/:id, each event independently expandable showing charge type/notes/source/receipt); existing modals (MemberTrainingPlansModal, MemberPaymentsModal) preserved via ContextMenu; `members.test.ts` with 12 tests covering auth, tenant isolation, happy path, account_status derivation, and membership_status derivation; no migrations).
- **Done (continued)**: #352 (Event Colour, Booking Count, Session Status and Day View Details: `class-sessions` SELECT extended with `at.color AS activity_type_color`; calendar `eventContent` renderer branched on `arg.view.type` — Month: compact single-line title + status, Week: title / booking-count·status two-line, Day: two-line max with time·title·STATUS / trainer·space·booked/cap; STATUS_COLORS status-derived colour fallback removed; colours now use `activity_type_color || event_color_override || default` for calendar events and `activity_type_color || default` for sessions; "FULL" derived label shown when `booked_count >= effective_capacity && status === 'scheduled'`; no migrations, no new endpoints, no i18n changes). Reopened and fixed follow-up: month view events were only tinting a small dot (FullCalendar's default `list-item` display for timed events in `dayGridMonth`) instead of a coloured block — fixed with `eventDisplay="block"`; Week view was missing the start–end time line from the ticket's spec — added; Day view's trainer now falls back to the assigned `trainer_name` when no `effective_trainer_name` is confirmed yet (`class-sessions` SELECT gained `gm.name AS trainer_name`); newly created Activity Types via the inline "+ Add" row had no colour picker, so new activities never showed their colour bullet until edited — added a colour input to the inline row; `EventDetailsPanel` now shows the selected Activity Type's configured capacity (calendar events have no booking model, so this is the activity type's `max_capacity`, not a live booked count) — new `calendar.event_capacity` i18n key in en/es/ca.
- **Done (continued)**: #346 (Improve Members Management View: migrations 120–121 add profile fields (`date_of_birth DATE`, `gender VARCHAR(20)`, `address VARCHAR(500)`, `emergency_contact VARCHAR(255)`, `notes VARCHAR(3000)`) and audit columns (`created_by FK→gym_memberships`, `modified_at DATETIME`, `modified_by FK→gym_memberships`) to `members`; `GET /members` renames `membership_status` → `enrollment_status`, adds `payment_status` (most-recent `payment_requests.status` subquery), supports `?payment_status=` and `?enrollment_status=` filters via HAVING; `GET /members/:id` JOINs `gym_memberships` twice for `created_by_name`/`modified_by_name`; `POST /members` stores `created_by`; `PUT /members/:id` accepts profile fields, stamps `modified_at`/`modified_by`, intentionally excludes email (Clerk sync out of scope); admin Members page rewritten — row header shows Name + Payment Status badge + Enrollment Status badge; Clerk account_status moved to lazy-loaded expanded row; action menu: Invite/Edit/Details/Delete (Training Plans and Payments shortcuts removed); new `MemberDetailModal` (follows ExerciseDetailModal pattern, Profile + Audit sections); edit form adds profile fields with email shown read-only; toolbar: name search (`?q=`) + Payment Status + Enrollment Status dropdowns; URL params stay in sync; i18n en/es/ca updated with all new keys; `members.test.ts` updated: `membership_status` → `enrollment_status` rename, added tests for enrollment_status filter, payment_status subquery/filter, combined filter, GET /:id audit fields, PUT profile fields).
- **Done (continued)**: #351 (Assign Nutrition Plan Template to Member: `POST /nutrition-plan-templates/:id/assign` extended with optional `name` body param (falls back to template name when omitted); "Assign plan to member" context menu item added to nutrition plan templates page (gated to `status === 'active'`); new `AssignNutritionPlanDialog.tsx` with template name (read-only), member selector, plan name field, POSTs to the assign endpoint; new `/nutrition/nutrition-plans/page.tsx` lists member nutrition plans with member filter and delete action; `navigationGroups.ts` gains `nutrition.nutrition_plans` nav item with `separatorAbove: true`; i18n keys in en/es/ca; 1 new test case in `nutrition-plan-templates.test.ts` for custom name; no migration).
- **Done (continued)**: #353 (Calendar — Resizable Event Editor: hardcoded `PANEL_WIDTH = 380` replaced with `MIN_PANEL_WIDTH = 280` / `MAX_PANEL_WIDTH = 800` constants and stateful `panelWidth` (sessionStorage-backed, key `calendar-panel-width`); 6px drag handle `<div>` inserted between calendar and panel flex items — `onMouseDown` captures `startX`/`startWidth`, adds `window` `mousemove`/`mouseup` listeners that clamp and update width, persists to sessionStorage on release; `onKeyDown` on the handle — `ArrowLeft` expands panel +20px, `ArrowRight` shrinks −20px; handle has `role="separator"`, `aria-orientation="vertical"`, `aria-label`, `aria-valuenow/min/max`, `tabIndex=0`; applied to both EventDetailsPanel and ClassSessionDetailPanel panel slots; `panel_resize_handle` i18n key added to en/es/ca; no backend changes).
- **Done (continued)**: #342 (Fix Staff Impersonation Targets: GET /platform/impersonation/targets staff query no longer filters by `status = 'active'`; `gm.status` added to SELECT and returned in response so UI can show `Staff · role · status`; POST /:targetId staff path makes Clerk lookup best-effort — superadmin check skipped if user not found in Clerk rather than returning 404; both DB queries in POST (membership validation + gymIds) drop the `status = 'active'` filter; `Target` interface in ImpersonationDialog gains optional `status` field; subtitle displays `Staff · role · status`; 2 new tests: invited staff appears in GET targets, invited staff can be impersonated via POST; 27 tests pass).
- **Done (continued)**: #368 (Allow Editing Taxes on Sellable Items: `gym_charges.tax_rate_id`/`tax_behavior` already existed from #311's tax rates work but had no edit UI — Sellable Items inline-new and edit forms now expose a Tax dropdown populated from `GET /taxes`, defaulting to "No tax"; `POST`/`PUT /sellable-items` now validate `tax_rate_id` server-side against `tax_rates` for the current gym before saving, rejecting unknown or cross-gym ids with 400; Details view and expanded read-only row now show the applied tax rate; no migration needed, no new API surface — reused the existing pricing computation (`amount_excl_tax`/`amount_incl_tax`) already returned by the router; new tests cover valid tax_rate_id assignment on create/edit, rejection of a non-existent id, and cross-gym tenant isolation).
- **Done (continued)**: #361 (Redesign Members App Home and Navigation: removed the bottom tab bar (`BottomNav.tsx` deleted) — Home is now the primary navigation surface; new `TopBar.tsx` gives every non-Home page a "← Home" link plus a persistent Profile entry point; Home rewritten as a personalized dashboard — time-of-day greeting, Alerts card (shown only when an unread notification exists, hidden otherwise), Next Booking, Today's Nutrition Plan (new, see below), a 2×2 nav tile grid (Calendar/My Training Plan/My Bookings/My Nutrition), and a My Membership summary card; Packages and Payments are no longer top-level nav items — both were already surfaced inside `/membership`, which is now the single "My Membership" destination; new `GET /me/nutrition-plan` endpoint (mirrors staff-facing `member-nutrition-plans.ts` `/:id/hierarchy`, scoped to the caller via `resolveMemberId`) returns the caller's own active nutrition plan with full days/meals/items/goals — feature-flagged `nutrition.nutrition_plans`; new `/nutrition` ("My Nutrition") read-only page consumes the same endpoint for a full-week view, Home's card filters it client-side to `weekday === today || weekday === 7` (the "All Days" sentinel); `/schedule` renamed "My Bookings", `/training` renamed "My Training Plan", `/notifications` renamed "Alerts" in the UI (routes unchanged); `/calendar` `initialView` changed to `timeGridDay` so it defaults to today; Profile page extended with default gym/center switching (reuses existing `AppContext` state, no new backend), enrollment status (from `/me/membership`) and a payment-status summary (derived from `/me/payment-requests`); i18n `nav` namespace repurposed for the new tile/entry-point labels, new `nutrition` namespace added, in en/es/ca; `me-nutrition-plan.test.ts` — 6 tests covering auth, tenant isolation, empty state, happy path, and member isolation within the same gym; no migrations — everything reuses the existing `member_nutrition_plans` schema from #259).
- **Done (continued)**: #365 (Replace Member Edit Modal with Inline Editing: the Members admin page's Edit action no longer opens a modal — it now expands the member's row and renders an inline `MemberEditForm` (new component) with the same fields the old edit modal exposed (Name/Phone/Date of birth/Gender/Address/Emergency contact/Notes + center assignment when the gym has multiple centers), followed by Save/Cancel; the Add-Member modal is unchanged and now only handles creation; inline edit follows the Training Plans (#112) pattern — `editingId`/`editForm`/`editError`/`editSaving` state, `guardUnsaved()` + `pendingAction` + a `beforeunload` listener warn about discarding unsaved changes when navigating away mid-edit, a save error keeps the user in edit mode with their entered values preserved; `members.unsaved_changes`/`unsaved_discard` i18n keys added to en/es/ca; no API or migration changes — reuses the existing `PUT /members/:id` and `PUT /members/:id/centers` endpoints; frontend-only change, no test file added (no existing test infra for `apps/admin` pages in this repo).
- **Done (continued)**: #370 (Align Sellable Item Status and Enrollment Status: migration 122 adds `gym_charges.enrollment_status ENUM(public/staff_only)` — mirrors the `status`/`enrollment_status` split already used by `membership_plans`; `status='inactive'` always overrides `enrollment_status`; `VALID_ENROLLMENT_STATUSES` added to `sellable-items.ts`, `GET /` gains `?enrollment_status` filter, POST/PUT accept and validate it, system items may edit it, Recycle Bin restore leaves it as-is; admin Sellable Items page shows Status and Enrollment Status as separate badges/fields in row, inline edit, and Details; `gym-charges.test.ts` extended).
- **Done (continued)**: #369 (4-Week Billing/Duration Option: investigation found Membership Plans already support an arbitrary 4-week period today via the existing generic `billing_policies.recurring_billing_interval`/`recurring_billing_unit` fields — `advanceBillingDate()` in `billing.ts` already advances `unit='week'` by `interval * 7` days, so interval=4/unit='week' correctly yields 28 days and is never conflated with a calendar month; no Plan-side schema or code change was needed. Sellable Items' `billing_frequency` had no such interval multiplier — it's a fixed single-value enum used only as a descriptive label (actual entitlement length is tracked separately via `validity_days`) — so migration 123 widens the `gym_charges_billing_frequency_check` CHECK constraint to add `'four_weeks'` alongside the existing `once/per_session/week/month/year` (renumbered from 122 to 123 after merging #370, which independently claimed migration 122 for `enrollment_status`); `VALID_FREQUENCIES` in `sellable-items.ts` and `FREQUENCIES` in the admin Sellable Items page updated to match; `frequency_four_weeks` i18n key added to en/es/ca under the `sellable_items` namespace; `advanceBillingDate` exported and covered by a new unit-test file `billing-date-math.test.ts` (5 tests, no DB) confirming 4×week = 28 days and that it never equals a 1-month advance; `gym-charges.test.ts` extended with POST/PUT `billing_frequency=four_weeks` acceptance tests and an invalid-value rejection test).
- **Done (continued)**: #371 (System Sellable Item — Personal Training Class Package (10 Sessions): data/setup-only migration 124 backfills one system Sellable Item per existing gym into `gym_charges` — `type='sessions'`, `units=10`, `is_system=1`, `status='active'`, `enrollment_status='staff_only'` (maps the ticket's "Active — Only Internal" onto the split introduced by #370: staff can sell it internally, Members cannot purchase it directly), `validity_days=182` (≈6 calendar months — `gym_charges` has no month-based validity field, only a fixed day count), `tax_rate_id` set to each gym's own system default tax rate (same lookup migration 113 used); `amount` left `NULL` so price stays configurable via the normal Sellable Item admin flow; no API, UI, or model changes — `gym_charges.name` has no localization mechanism today so no translated-name column was added; idempotent via `NOT EXISTS` guard, `down` deletes the seeded rows; migration renumbered from 123 to 124 after merging #369, which independently claimed migration 123 for the four-week billing frequency).
- **Done (continued)**: #370 amendment (Align Plan Enrollment Status with Sellable Items: migration 125 removes `closed` from `membership_plans.enrollment_status` — only `public`/`staff_only` remain, mirroring `gym_charges.enrollment_status`; existing `closed` plans migrated to `public` per the ticket's explicit mapping; column default changed from `closed` to `staff_only`; new plans, duplicated plans, archived plans, and soft-deleted plans now default/reset to `staff_only` instead of `closed`; `VALID_ENROLLMENT`/`ENROLLMENT_STATUSES` in `membership-plans.ts` and the admin Plans page drop `closed`; `membership-plans.test.ts` and the plan fixtures in `billing-run.test.ts`/`payment-page.test.ts`/`payment-requests.test.ts`/`payments-billing-events.test.ts`/`plan-charge-benefits.test.ts`/`recycle-bin.test.ts` updated to use `staff_only`).
- **Done (continued)**: #388 (Inline Tax Management, Descriptions, Audit Information & Impact Warning: migration 126 (renumbered from 125 after merging #370's amendment, which independently claimed migration 125 for the `enrollment_status` change) adds `tax_rates.description` (nullable) plus `created_by_name`/`created_by_type` and `modified_by_name`/`modified_by_type` — `created_by_type`/`modified_by_type` is a CHECK-constrained `'staff'`/`'superadmin'` column snapshotting the actor at write time, fixing a real bug where `created_by_membership_id`/`modified_by_membership_id` (joined to `gym_memberships` for display) silently showed no actor at all for a superadmin acting directly, since superadmins have no `gym_memberships` row; backfilled for existing rows resolvable via the old membership-id join, left unattributed otherwise; `POST /taxes`, `PUT /taxes/:id`, `POST /taxes/:id/activate` and `POST /taxes/:id/deactivate` all populate the new columns going forward. `PUT /taxes/:id` now computes the current count of non-deleted `gym_charges` (Sellable Items) and `membership_plans` referencing the tax before persisting an edit — Membership instances are deliberately not counted, since Taxes are never snapshotted onto a Membership at instantiation; if either count is non-zero and the request omits `confirmImpact: true`, it returns `409 { error: 'confirmation_required', impact }` without writing, so a direct API call can't bypass the warning. Admin Taxes page (`apps/admin/.../financials/taxes/`) rebuilt to match the `sellable-items` page's standard pattern — inline "+ Add" row and inline row-level Edit (no create/edit modal), expand/collapse row with the description truncated in the collapsed header and shown in full when expanded, Details modal shows the actor's Staff/Superadmin label; Save on Edit now surfaces the impact warning as a confirm dialog (with the affected counts) before resubmitting with `confirmImpact: true`. New `api/src/test/taxes.test.ts` (this router had no prior test file) covers tenant isolation, auth/role gating, the confirmation-required flow (both triggered and skipped), audit actor population for staff vs. superadmin, is_system delete protection, and soft-delete visibility.).
- **Done (continued)**: #367 (Member Web Feature Flags — sub-issue of #362: extends the `feature_flags` infrastructure with a `member_web.*` namespace, separate from the Admin App's own flags. Migration 127 seeds `member_web.my_training_plan`/`my_nutrition`/`my_bookings`/`my_membership`/`profile`, all `enabled=1`. Backend: each flag additionally gates the `/me/*` endpoints specific to that section (chained after any existing flag on the same route); superadmins bypass unconditionally, including while impersonating a member. `GET /me/schedule` (shared with the un-flagged `/calendar` page), `GET /me/payment-requests` (shared between `/membership` and `/profile`), and `GET /me/profile` (the app's core `isLinked` bootstrap call) are deliberately left ungated at the API layer to avoid one section's flag blocking another — those three rely on the frontend route guard alone. Frontend: new `apps/member/src/context/FeatureFlagsContext.tsx` (mirrors the admin app's), Home hides the disabled nav tiles/My Membership card, and each of `/training`/`/nutrition`/`/schedule`/`/membership`/`/profile` redirects to Home when its flag is off for a non-superadmin. The existing generic Admin Feature Flags tree UI needed no changes — it renders `member_web` as its own section automatically. New `api/src/test/member-web-feature-flags.test.ts`).
- **#362 (in progress — Slice 1 of Option A)**: Members App audit — fixed Home's Next Booking / Membership sections staying stuck on `Loading...` forever under admin impersonation. Root cause was two compounding bugs: (1) `apps/member/src/app/api/proxy/[...path]/route.ts` stripped the `x-impersonate-as`/`x-center-id` headers before forwarding requests to the API, so the backend never saw the impersonation target even though `tenantContext`/`resolveMemberId` already handled it correctly; (2) `AppContext`'s data-loading effect only depended on `[isSignedIn, user?.id]` and read `sessionStorage` once inline, so it never re-ran when impersonation started/stopped, leaving `isLinked` stuck at its pre-impersonation value forever. Fixed the proxy allow-list, reordered `ImpersonationProvider`/`AppProvider` nesting so `AppContext` consumes `useImpersonation()` reactively, and added `x-impersonate-as` to every `/me/*` call `AppContext` makes (centers, notifications count). Added `api/src/test/me-impersonation-context.test.ts` covering `/me/schedule` and `/me/membership` under impersonation. Calendar, Bookings, Training, Nutrition, Payments, Alerts, and Profile audits remain as follow-up PRs against #362.
- **#362 (in progress — Slice 2 of Option A)**: found and fixed a related latent bug left over after Slice 1: `centerContext` (the middleware resolving a `member` role's assigned centers, runs after `tenantContext` on every `/me/*` route) matched `member_centers` against the impersonating superadmin's raw `userId`/`clerk_user_id` instead of the effective identity, so any `/me/*` request carrying `x-center-id` while impersonating a member with `member_centers` rows always resolved to zero allowed centers and 403'd — silently, since `page.tsx`'s outer `catch` degrades to empty state rather than surfacing the error. `centerContext` now mirrors `resolveMemberId`'s `effectiveType`-based branch: `members.id = effectiveUserId` under member impersonation, `members.clerk_user_id = userId` otherwise. Extended `api/src/test/me-impersonation-context.test.ts` with center-scoped impersonation cases (own center allowed, other center 403).
- **Done (continued)**: #350 (Nutrition Library — flat list, expandable rows, search + multi-select filters: replaced the category-grouped layout on both `/cordel/nutrition-library` and `/nutrition/nutrition-library` with a flat, paginated `DataTable` list ordered by name; new `MultiSelectFilter` component (`apps/admin/src/components/MultiSelectFilter.tsx`) provides Excel-like checkbox dropdowns for Category (OR semantics) and Nutrition Properties (AND semantics — item must have every selected quality), combined with a debounced name search and an existing-pattern prev/next pager. Shared query-building/pagination helpers extracted to `api/src/domain/nutritionLibrary.ts` (`buildListWhere`, `loadQualitiesMap`, `replaceQualities`, `validateQualityIds`, `clampLimit`/`clampOffset`) and reused by both `platform-nutrition-library.ts` and `nutrition-library.ts`; `GET` on both routers now returns `{ items, total, limit, offset }` (was a bare array) and accepts `?search=`, repeatable `?category=`, repeatable `?quality_id=`, `?limit=`/`?offset=`. Per the issue's answered clarifying questions: the gym-facing router gained `POST /nutrition-library` and `PUT /nutrition-library/:id` (both `requireModuleWrite('NUTRITION')`) so gym staff with write access can create and edit their own gym-owned items (`gym_id` = their gym); system items (`gym_id IS NULL`, created via `/cordel/nutrition-library`) stay read-only there — editing one returns 403, editing another gym's item returns 404. Nutrition-data-quality review (data model stayed qualitative-only per the issue's Q2 answer — no numeric columns added): migration 127 backfills the `protein`/`carbohydrate` quality tags for the unambiguous seeded system items (chicken/turkey/salmon/etc. → protein; rice/potatoes/oats/etc. → carbohydrate), since the original #163/#293 seed data had never been tagged. Tests extended in `platform-nutrition-library.test.ts` and `nutrition-library.test.ts` for search/filter/pagination, the gym-write permission boundary, and tenant isolation on edit.).
- **Done (continued)**: #410 (Financials — Assigned Plans view: new read-only `GET /user-memberships` field `lifecycle_status` — a date-aware projection of the stored `status` column computed in SQL (`pending` when `starts_at` is in the future, `expired` when an otherwise-active row's `ends_at` has passed, `active`/`paused`/`cancelled`/the explicit `expired` value passed through unchanged otherwise); no migration, no change to the stored `status` values or existing business logic. New admin page `financials/assigned-plans` lists every Membership assignment across the gym (Member Name/Plan Name/Start Date/End Date — "Open-ended" when `ends_at` is null/Status badge), reusing the existing `/user-memberships` endpoint; nav entry added to the Financials group (`financials.assigned_plans` feature key, defaults enabled — not yet seeded in `feature_flags`); `status.pending` i18n key added alongside the existing `status.*` badge labels in en/es/ca. `user-memberships.test.ts` extended with `lifecycle_status` coverage for all five states.
- **Done (continued)**: #411 (Financials — advanced filtering on the Assigned Plans view, building on #410's `lifecycle_status` field. `GET /user-memberships` gains three additive, backward-compatible filters: repeatable/comma-separated `lifecycle_status` (multi-select over `pending`/`active`/`paused`/`expired`/`cancelled` — filtered via an outer query over a derived table, since `lifecycle_status` is a `CASE`-expression SELECT alias and can't be reused directly in the same query's `WHERE`), and `start_date`/`end_date` (date-range overlap against `starts_at`/`ends_at`). The existing single-value `status` param (raw stored column) and `member_id` are unchanged — `lifecycle_status` is a separate param rather than repurposing `status`, since `pending` has no equivalent raw stored value and existing callers (`memberships/page.tsx`, `MemberPaymentsModal.tsx`) rely on `status` matching the raw column. `financials/assigned-plans` page gets a filter bar: a `MultiSelectFilter` (existing component, #350) for status, a debounced member-search filter (mirroring `payments/billing-events`' inline `MemberFilter`), and `start`/`end` date inputs, with a "Clear filters" action and a distinct empty-state message when filters exclude all rows. New `filter_status`/`filter_member`/`filter_from`/`filter_to`/`filter_clear`/`no_matches` i18n keys in en/es/ca. `user-memberships.test.ts` extended with coverage for single/multi/comma-separated `lifecycle_status` filtering, invalid-value 400s, combining `member_id` with `lifecycle_status`, the date-range overlap semantics, and malformed-date 400s.).
- **Done (continued)**: #412 (Members — plan history sorting + Cancel/Assign New Plan transition workflows; no migration, existing `status` enum already had `cancelled`/`expired`). New `POST /user-memberships/:id/assign-new-plan` (admin-only, mirrors the admin-only gate on the existing cancel `DELETE`) atomically supersedes a Membership: in one transaction it flips the current row to `expired` only if it's still `active`/`paused` (a row already `cancelled`/`expired` is left untouched), inserts the new `active` `user_memberships` row for the same Member via the existing `effectivePrice()` snapshot logic (same discount-override/`discount_reason` validation as `POST /`), and adds the owner to `user_membership_members` — avoiding the `active_member_key` unique-index collision by expiring the old row before inserting the new one within the same transaction (that index is per-(Member, Plan) since #634 / migration 172, and the ordering still matters for a same-Plan supersede). Admin `MemberExpandedRow.tsx` (Members page) now lists a Member's **full** plan history (the `GET /user-memberships?member_id=` list was already sorted `starts_at DESC` from #410/#411) instead of only the latest row, with a per-row admin-only `ContextMenu`: "Cancel Plan" (reuses the existing admin-only `DELETE /user-memberships/:id`) and "Assign New Plan" (new `AssignNewPlanModal.tsx`, plan-picker + start date, calling the new endpoint). Benefits/next-billing-date display stay scoped to the most recent row. New locale keys in `members.*` (en complete; es/ca only for the new keys, since `members.*` in those two locales was already significantly behind en before this change). `user-memberships.test.ts` extended with tenant-isolation/auth/happy-path/invariant/validation coverage for the new endpoint.
- **Done (continued)**: #413 (Membership Plans — applicable tax, aligned with Sellable Items' financial config: `membership_plans.tax_rate_id FK→tax_rates` was already added in #311's migration 112/113 and backfilled to each gym's system tax rate, but no `tax_behavior` column existed alongside it and neither field was ever surfaced by any endpoint or the admin UI — `taxes.ts`'s `getTaxImpact()` already counted `membership_plans` referencing a tax rate, confirming the field was meant to be live. This ticket wires it up rather than duplicating Plans' existing price (`membership_plan_prices`, a validity-windowed price history) or renewal-frequency (`billing_policies.recurring_billing_interval`/`recurring_billing_unit`, already `day`/`week`/`month`/`year` — no `once` option, so "excluding one-shot" already held) mechanisms, which are richer than Sellable Items' flat `amount`/`billing_frequency` and are already exposed in `enrichPlan`/the Plans page's Billing and Prices sections. Migration 135 adds `membership_plans.tax_behavior VARCHAR(20) CHECK ('inclusive','exclusive') DEFAULT 'inclusive'`, mirroring `gym_charges.tax_behavior` from #311 (column add and CHECK constraint guarded independently per `126_tax_rates_description_and_audit_actor.js`'s pattern, so a retry after a partial failure can resume safely). `sellable-items.ts` now exports `computePriceFields`/`validateTaxRateId` for reuse. `membership-plans.ts`: POST validates `tax_rate_id`/`tax_behavior` and defaults `tax_rate_id` to the gym's system tax rate when omitted (mirroring the #311 backfill), PUT updates both fields, duplicate copies them; `enrichPlan` LEFT JOINs `tax_rates` and reuses `computePriceFields` against the plan's `current_price` to return `tax_rate_name`/`tax_rate_percent`/`amount_excl_tax`/`amount_incl_tax`. Admin Plans page (`plans/page.tsx`): Tax Rate + Tax Behavior selects added to the inline create and edit forms (fetches `GET /taxes`, same `taxRateOptions()` pattern as the Sellable Items page); expanded Details' Prices section shows the resolved tax rate and the excl/incl-tax breakdown of the current price. New `Applicable tax on membership plans` describe block in `membership-plans.test.ts` (system-tax-rate default, explicit tax rate + computed fields, cross-gym tax_rate_id 400, invalid tax_behavior 400, PUT update, price-with-tax computation, duplicate copies tax fields).
- **Done (continued)**: #479 (Allow changing Center and Trainer on Calendar Events — Trainer editing already existed end-to-end; only `center_id` was missing. `calendarEventsRouter`'s `EVENT_SELECT` gains a `LEFT JOIN centers` for `center_name`; `POST`/`PUT /calendar-events` accept `center_id` independently of `space_id`/`trainer_membership_id` via the same `IF('center_id' in req.body, ?, center_id)` idiom already used for those fields; a new `isValidCenterId()` helper rejects a `center_id` outside the caller's gym with 400 — `resolveCenterId()`/`centerContext` aren't used here since `calendarEventsRouter` isn't mounted with the `centerContext` middleware (unlike `classSessionsRouter`), and unlike sessions a plain event's center stays optional with no default auto-assignment and no conflict rule. No migration — `calendar_events.center_id` already existed (migration 134). Admin `EventDetailsPanel`/`CalendarEventModal` (`CalendarEventForm`, `EMPTY_FORM`) gain a `center_id` field and a Center `<select>` next to Trainer/Space; `page.tsx` fetches `GET /centers` in its own non-fatal call (kept out of the page's required `Promise.all` since `organization.centers` is a feature flag some gyms don't have enabled) and threads `center_id` through `openEdit`/`handleSave`. `event_center` i18n key added to en/es/ca. `calendar-events.test.ts` extended with 5 tests: create/read with center, cross-gym center_id 400 on POST and PUT, set/clear center_id independently, and updating trainer+center together).
- **Done (continued)**: #504 (Fix Admin Staff User Impersonation — the shared `GET /platform/impersonation/targets` endpoint always returned both staff and members with no way to filter, so the Admin app's `ImpersonationDialog` showed Members mixed in among Staff even though it only ever wanted Staff; added an optional `type=staff|member` query filter, enforced server-side, defaulting to the original combined behavior when omitted so the Member app's `MemberImpersonationDialog` — which legitimately needs `type=member` via the same endpoint — is unaffected; Admin's dialog now always requests `type=staff`. Separately, the Team page's own per-row Impersonate button was silently broken — it POSTed `/platform/impersonation/:targetId` without the required `targetType` body field, so every attempt 400'd; fixed by sending `targetType: 'staff'`. Nameless staff (granted via an existing Clerk user, no name/email) fall back to their raw Clerk user ID as `name` for search matching — the dialog now detects that fallback and shows a translated "Unnamed staff member" placeholder instead of the technical ID, plus email as secondary disambiguating info when available. No migration. `impersonation.test.ts` extended with 3 tests for the new `type` filter. **Follow-up (reopened):** the ticket persisted after the above landed — root cause was a frontend race in `ImpersonationDialog`'s two separate search-triggering `useEffect`s (one immediate on mount/gym-change, one debounced per keystroke) with no guard against out-of-order responses, so a slower unfiltered request could resolve after a faster filtered one and silently overwrite it, making search appear broken and showing unrelated staff. Fixed by consolidating into a single effect with a request-sequence guard (`useRef` counter) that only ever applies the most recent response. Backend `gym_id` filtering was re-verified correct and locked in with a new cross-gym isolation test on `GET /targets`; no backend change was needed). **Follow-up (reopened again):** still showing wrong/unnamed staff after the above. Root cause was in `gym-users.ts`, not `impersonation.ts`: `POST /gym-users`'s "existing Clerk user" grant path (the common case — the admin picks a Team role for a user Clerk already knows) inserted only `user_id, gym_id, role` into `gym_memberships`, silently dropping the `name` the admin had just typed into the Add Team Member form. That row then had no `name` *or* `email`, so `impersonation.ts`'s existing `COALESCE(gm.name, gm.email, gm.user_id)` fallback (from #364) resolved to the raw Clerk user ID for every search and display — exactly the "technical-looking identifiers instead of the real Staff member" symptom reported. Fixed by persisting `name` on both the insert and the re-grant/role-change update paths (`COALESCE(?, name)` on update, so re-granting a different role without retyping the name never clobbers one saved earlier). Also fixed a related i18n gap while in this dialog: `ImpersonationDialog` was rendering the raw `role` enum value (e.g. `front_desk`) as secondary text instead of a translated label — added `role_*` keys to the `impersonation` namespace in en/es/ca (mirroring `team`'s existing `role_*` keys) and switched the dialog to use them. New `api/src/test/gym-users.test.ts` (this router's first test file — auth/role guards, tenant isolation on `GET /gym-users`, and the name-persistence regression for both the fresh-grant and re-grant paths, plus the existing new-invitation path for contrast).
- **Done (continued)**: #592 (Link Staff records to auth identities; Impersonate lists Staff; remove Team — root cause of the empty Admin Impersonate dialog: `GET /platform/impersonation/targets?type=staff` reads `gym_memberships`, but the Staff page reads the `staff` table and nothing ever wrote `staff.gym_membership_id` (present since #123), while the only UI that created `gym_memberships` rows — the Team page — had been dropped from navigation in #77 and was orphaned. No migration. `api/src/infra/staff-access.ts` now holds the Clerk invite/grant/revoke/link mechanics moved from the deleted `gym-users.ts`; `POST /staff` grants access on create with the role fixed by `PROFILE_ROLE_MAP` (profile → RBAC role, `infra/permissions.ts`, mirrored in `apps/admin/src/config/permissions.ts`) and stores the link; `PUT` syncs role/name (self + last-admin guards); deactivate revokes the login (Clerk user kept); delete revokes and removes the Clerk user when it was their last gym; new `POST/DELETE /staff/:id/access` for retry/resend/revoke; `POST /staff/link` replaces `/gym-users/link` (`GymContext` self-heal repointed; webhook import repointed). Impersonation staff targets are `gym_memberships LEFT JOIN staff`, returning the Staff name + `profile`; gym-owner admin rows (no Staff record) stay impersonable. Admin: `/team` + `/link-team` pages and the `team` i18n namespace removed; Staff row's Clerk section became **App access** (status, role, Send/Resend invitation, Revoke) with a role hint under the Profile select; `ImpersonationDialog` shows the profile. Tests: new `staff-access.test.ts` (auth/tenant guards, grant-existing/invite paths, unknown profile 400, inactive-on-create, Clerk failure keeps the row, resend overwrites `invitation_id`, PUT role/name sync, last-admin + self guards, deactivate/delete/revoke semantics), `impersonation.test.ts` extended for the join, `require-role.test.ts` retargeted to `/staff`, `gym-users.test.ts` deleted.)
- **Done (continued)**: #594 (Refuse member/staff email collision within a gym — surfaced while verifying #592: granting staff access to an email that is already a member of the same gym silently `UPDATE`d the single `gym_memberships` row's role to the staff role, breaking the member app for that person (`requireRole('member')` 403s) with nothing visible in the admin UI, and a later revoke deleted the row with no way back (`/me/link` requires `clerk_user_id IS NULL`). Intended model written down: staff log in with their work account, membership lives on a personal account. Both sides now refuse with 409: `assertNotMemberEmail` (staff side, runs before `POST /staff` inserts anything, on `PUT` email change, and inside `grantAccess`) and `isStaffLoginEmail` (member side, `POST /members` + `POST /members/:id/invite`), plus `POST /me/link` refuses when the account already holds a non-member row in the gym. No migration, no UI change beyond the error message. New `api/src/test/member-staff-collision.test.ts` (10 cases, both directions incl. case-insensitive match, existing-account member row, pending staff invite, and the `/me/link` guard).)
- **Done (continued)**: #596 (Admin impersonation — role/gym list stayed stale until reload after start/stop: `GymContext` derived the effective role by reading `sessionStorage` during render instead of subscribing to `ImpersonationContext`, and `GymProvider` gets `children` as a stable prop so the parent's `setSession` never re-rendered it; right after starting a `front_desk` session the Staff page still showed **+ New Staff Member** / **Revoke access** until a reload (UI-only — the API always enforced the real role via `x-impersonate-as`). `GymProvider` now consumes `useImpersonation()`; `ImpersonationContext` gained a `ready` flag (mirrors the Member app, #415) with a synchronous storage fallback before rehydration so a mid-session reload never flashes the superadmin role. Frontend-only, no API/migration. Investigated and ruled out in the same ticket: the session does **not** survive *Stop* — storage is cleared synchronously and stays cleared across navigations; the `ERR_ABORTED` next to `/stop → 204` is just how a bodiless 204 is logged.)
- **Done (continued)**: #609 + #610 (Feature flag coverage — found by running the real `filterNavGroups` once per production flag: Professional Services and Assigned Plans were gated on keys never seeded into `feature_flags` (missing key = enabled → never hideable, absent from Cordel → Feature Flags; `/professional-services` also checked the missing key), and Taxes shared `financials.gym_charges` with Sellable Items. Migration 160 seeds `organization.professional_services`, `financials.assigned_plans` and `financials.taxes` (copying gym_charges' current value). Taxes menu entry uses `financials.taxes`; `/taxes` reads now depend only on the `financials` group flag (shared tax dropdown in Plans/Sellable Items — hiding Sellable Items used to break it) and `financials.taxes` gates its five write routes. New `feature-flag-coverage.test.ts`. Permission-matrix mismatch between the admin app and the API split out as #611.)
- **Done (continued)**: #614 (Calendar permission module in the API — #247 made Calendar an independent permission area with front desk read/write, but only the admin app got it; the API still gated `/calendar-events` + `/class-sessions` on TRAINING, where front desk is `R`, so front desk saw create/edit controls that 403'd. Added `CALENDAR` to the API `PERMISSION_MATRIX` (admin/trainers/front desk RW, nutritionist R, accountant/member NONE) and switched both mounts and all 9 write routes to it. Admin app's nutritionist cell corrected RW → R so UI = API. No migration. New `calendar-permissions.test.ts`; `calendar-events.test.ts` front-desk assertion flipped 403 → 201.)
- **Done (continued)**: #613 + #611 (Read-only roles. Part 1/3 (Organization + Calendar pages): new `useModuleAccess(module)` hook (impersonation-aware), `ContextMenuItem.disabled`, `readOnlyStyle`, `common.read_only_hint`. Centers, Spaces, Activity Types (incl. schedule-rule buttons), Professional Services, Operating Hours (weekly grid + holidays), Staff (controls shown disabled instead of hidden; editor Save gated) and Calendar now disable write controls for roles without write access. New `read-only-writes.test.ts` (51 cases across MEMBERS/ORGANIZATION/TRAINING/NUTRITION/FINANCIALS/PAYMENTS). Client redirects stay until #611. Part 2/3 (Financials + Payments, done): Plans (incl. every in-row billing/centers/allowance/price edit button; *Assign to member* follows PAYMENTS, so front desk keeps it), Promotions, Sellable Items, Taxes, and the Assigned Plan row (status actions shown disabled instead of hidden; *Close* admin-only via the hook's new `isAdmin`). Transactions, Billing Events and Payment Providers have no write controls. Part 3/3 (Training + Nutrition, this PR): the seven Training/Nutrition pages switch to the hook, show row actions disabled instead of hidden, and gate Add/New/Import-defaults, Clone and Assign-to-member; hook gains `useReadOnlyTitle`.) **#611**: admin `PERMISSION_MATRIX` aligned cell for cell with the API (= #156 spec): trainers + nutritionist gain Organization, nutritionist gains Training, front desk gains Financials (read), accountant loses Members and is read-only on Financials/Payments. The 15 module pages redirect/return-null/load on `canRead` instead of admin/write checks, so read-only roles open them with write controls disabled. New admin tests `permission-matrix-parity.test.ts` (imports the API table) and `nav-by-role.test.ts`; `apps/admin/vitest.config.ts` adds the `@` alias.)
- **Done (continued)**: #607 (Staff logins — Impersonate was empty in every gym but Claude MONEI Test because Staff records created before #592 had no login and saving them never created one, and invited staff looked impersonable but weren't. `PUT /staff/:id` now grants a login to an active record without one (same as create, #594 guard included) and removes it when the record is saved inactive (matches `PATCH /deactivate`); response carries `access`, the form warns an invitation email may be sent. Impersonate dialog lists invited staff greyed out as "Invitation pending"; `POST /platform/impersonation/:targetId` refuses them — reverses the POST half of #358, whose test only passed because the Clerk mock resolves any id (`tenantContext` looks the target up in Clerk on every request). No migration. Tests: 5 new PUT cases in `staff-access.test.ts`, pending-target refusal in `impersonation.test.ts`.)
- **Done (continued)**: #599 (Website self-registration — a gym's WordPress site can now send the member invite email. New `POST /public/gyms/:slug/registrations` (`{ name, email, center_id?, locale? }`), called server-to-server and authenticated by a per-gym API key in `x-api-key`; only a salted scrypt digest is stored, with a prefix pre-filter so junk never reaches scrypt (migration 159: `gyms.website_api_key_hash/_prefix/_created_at` + flag `system.website_integration`). It only creates a Clerk invitation carrying `publicMetadata.gym_signup`; the `members` row is created on first sign-in by the extended `POST /me/link` (201), so fake requests never reach the roster. Always `202` once authenticated (no enumeration; Clerk `422` swallowed = per-email throttle), one shared `401` for bad key/unknown slug, per-IP limiter before DB work + per-gym limiter after the key check (env-tunable). Admin: new **System → Website Integration** page (`GET /system/website-integration`, `POST`/`DELETE …/key` — key shown once, rotate/revoke audited). Also fixes a pre-existing bug: `trust proxy` was never set behind nginx, so every per-IP limiter (incl. the global 500/15 min) was one bucket for the whole platform (`TRUST_PROXY_HOPS`, default 1). `gyms.ts` reads `g.*`, so `stripGymSecrets` keeps the hash out of responses and audit payloads. WordPress setup: `docs/wordpress-integration.md`. Tests: `public-registrations.test.ts`, `website-integration.test.ts`, `me-link-self-registration.test.ts`, unit `website-api-key.test.ts`.)
- **Done (continued)**: #600 (Removed the leftover one-time `POST /dev/seed-gym` route from `api/src/app.ts` — commented "remove after use" and never removed; it sat outside `requireAuth()`. Nothing referenced it: local seeding is `api/src/infra/seed.ts`, gym creation is the superadmin `POST /platform/gyms`. No migration, no UI change. New `api/src/test/dev-seed-gym.test.ts` (3 cases) pins the route as gone and asserts no gym/membership row is written. Convention going forward: no route is mounted in `app.ts` without `requireAuth()` unless it is deliberately public (`/health`, `/docs`, `/public`, `/payment-page`, `/billing`, `/themes`, webhooks).)
- **Done (continued)**: #605 (Docs only — new `docs/go-to-production.md`: single checklist of go-live requirements (GitHub `production` environment, Clerk Production instance, first-superadmin bootstrap via Clerk Dashboard `publicMetadata.platform_role` — not `npm run db:seed`, which also creates a placeholder gym —, public-route audit, Monei/PCI items from `decisions.md`). Linked from `README.md`, `docs/architecture.md` and the `CLAUDE.md` doc-update checklist.)
- **#554 (done)**: Suitable Membership Plans section for Promotions. The
  association (`promotion_membership_plans`, migration 020), its
  `GET`/`PUT /promotions/:id/plans` endpoints, and the apply-time eligibility
  check (`applyPromotionToMembership`/`POST /user-memberships/:id/promotions`
  in `membership-promotions.ts`, both already rejecting a promotion that
  doesn't target the membership's plan) all predate this ticket (#26, #511) —
  no new table or enforcement plumbing was needed. This ticket: (1) tightens
  `PUT /plans` server-side validation — a *newly added* plan id must belong
  to the gym and be active (`lifecycle_status='active'`, `deleted_at IS
  NULL`); an id the promotion was *already* associated with is let through
  even if that plan has since gone inactive, so an unrelated re-save of the
  promotion never drops a pre-existing association (Historical Integrity);
  duplicate ids in the request are deduped instead of hitting the table's
  unique constraint; (2) fixes a frontend bug where the read-only view mode
  resolved an associated plan's name against the *active-only* plans list,
  so an inactive-but-associated plan rendered as `#<id>`, by rendering
  directly off `GET /plans`' own `{id, name}` rows instead; (3) renames the
  "Applicable Plans" section to "Suitable Membership Plans" and adds a
  Select All checkbox (checked/unchecked/indeterminate, only ever acting on
  the *displayed* active plans — pure logic in
  `apps/admin/src/lib/suitablePlansSelection.ts`, unit tested), dedicated
  loading/empty/error states for the active-plans fetch (split out of the
  page's other, best-effort lookups), and secondary info (current price or
  enrollment status) next to each plan name. `promotions.test.ts` gains a
  "Suitable Membership Plans" describe block (active-only new selections,
  cross-tenant rejection, dedup, removal, historical-preservation,
  role/tenant guards); no migration.
- **#416 (done)**: Multi-select status filtering on the admin Billing Events view (`payments/billing-events`). `status` on this endpoint is a derived value (`paid`/`failed`/`recorded`/`scheduled`, computed from `billing_events.event_type`, or hardcoded `scheduled` for projected future rows) rather than a stored column, so `GET /payments/billing-events` accepts `status` as a multi-value query param (repeated `status=a&status=b` or comma-separated, same `z.preprocess` normalizer pattern as #411's `lifecycle_status`) and filters the already-merged past+future array in JS before pagination; when the selected statuses exclude `scheduled`, the future-row DB query is skipped entirely rather than run and discarded. Admin UI: a `MultiSelectFilter` (#350) added to the existing filter bar, synced to a `?status=` URL param via the page's existing `pushParams`/`useSearchParams` pattern, so the filtered view is shareable/survives navigation. New `filter_status` i18n key in en/es/ca. `payments-billing-events.test.ts` extended with single/multi (comma and repeated-param)/invalid-value/tenant-isolation coverage for the new filter, plus a case confirming `status=scheduled` returns only virtual rows.
- **#446 (done)**: Expanding a nutrition template crashed with `TypeError: o.map is not a function`. #350 changed `GET /nutrition-library` from a bare array to `{ items, total, limit, offset }` (and `quality_slugs` → `qualities[]`); `NutritionPlanTree` still stored the paginated object and called `.map()` on it in `RestrictionsSection`. Tree now unwraps `items`, maps `qualities[].slug` → `quality_slugs`, and fetches `?limit=200`. No API/proxy change. Follow-up: admin and member `FeatureFlagsProvider` now wait for Clerk and send `Authorization` on `GET /api/proxy/feature-flags`, fixing the layout 401 (bare `fetch` with no token). Follow-up (this ticket): the `/class-types` 404 was the Schedule page (`[locale]/schedule/page.tsx`) still calling the pre-#360 `class-types` router (`class_type_id`/`class-types?status=active`), which was renamed to `activity-types` and now returns 404. Its `Session`/`ActivityType` types, form state, and API calls (fetch, `onActivityTypeChange`, session create/edit body) are now consistently `activity_type_id`/`/activity-types`, matching what `GET /class-sessions` actually returns and `POST/PUT /class-sessions` actually accept (both already `activity_type_id`, per the #360 stage 3 consolidation) — session create/edit was silently broken (400) on top of the dropdown 404. A separate, unreachable `[locale]/class-types/page.tsx` (not linked from any nav group, fully superseded by `[locale]/activity-types/`) still calls the same dead `/class-types` CRUD endpoints; left untouched as out-of-scope dead-code cleanup.
- **#476 (done)**: Activities expandable-row header aligned with the admin UI information hierarchy used elsewhere (Spaces reference pattern). Frontend-only — `activity-types.ts`'s `SELECT` already returned every field needed. `activity-types/page.tsx`: collapsed row header now shows Name/Created At/Created By/Status (`StatusBadge`)/Center/Trainer instead of Name/Description/Duration·Capacity/Center/Status; Duration/Capacity stay in the expanded General section (already present there). The expanded read-only view's `Metadata` section (Created/Modified By+At) is removed — `Created At`/`By` moved to the header, `Modified At`/`By` moved into the `Details` modal, which now also surfaces `Deleted At`/`By` and renders `status` via `StatusBadge`. No migration, no i18n changes (`col_created_at`/`col_created_by`/`col_default_trainer`/`meta_deleted_by`/`meta_deleted_at` already existed in en/es/ca, just unused). `docs/feature-patterns.md` updated to note Spaces and Activity Types as the documented exception to the "Details just expands the row" Inline-row-CRUD rule.
- **#362 (in progress — Slice 2 of Option A)**: Calendar functional audit found the page/data flow itself already correct (`apps/member/.../calendar/page.tsx` calls `GET /me/schedule` via the shared `apiClient` `buildHeaders()`-equivalent helper, defaults to `timeGridDay` (today), supports prev/next/today navigation, and has bounded loading + a `.catch` on fetch failures — no infinite-spinner bug here), but surfaced a real multi-center scoping gap: `GET /me/schedule` never applied any center restriction, so a member could see every center's class sessions in their gym instead of only their assigned center(s)'s. Root cause traced to `api/src/infra/centerContext.ts` resolving a `member`-role caller's `allowedCenterIds` via `member_centers JOIN members ON clerk_user_id = ctx.userId` — under member impersonation `userId` is the impersonating superadmin's own Clerk ID, never the impersonated member's, silently collapsing `allowedCenterIds` to `[]`. This underlying `centerContext` bug was independently fixed by #396; this PR wires `GET /me/schedule` in `api/src/api/me.ts` to call `getCenterContext(req)` and filter by `cs.center_id` (explicit `x-center-id` header, or restricted to `allowedCenterIds` for a plain member), mirroring the pattern already used by `centers.ts`/`class-sessions.ts`. Extended `api/src/test/me-impersonation-context.test.ts` with center-scoping regression coverage under impersonation. Bookings, Training, Nutrition, Membership/Payments, Alerts, and Profile audits remain as follow-up PRs against #362.
- **#360 (in progress — Stage 3: API consolidation)**: Unify Sessions and Events under `CalendarEvent`. Migration 134 closes schema gaps (adds `kind ENUM`, `center_id`, `modified_at`/`modified_by_membership_id` to `calendar_event_bookings`, fixes FK `fk_cestr_event` to CASCADE, adds `calendar_event_booking_id` to `class_package_transactions` and makes `booking_id` nullable). `class-sessions.ts` merged into `calendar-events.ts` (now exports both `classSessionsRouter` filtering `kind='session'` and `calendarEventsRouter` filtering `kind='event'`). `bookings.ts`, `package-credits.ts`, `plan-allowances.ts`, `shared-training-requests.ts`, and all 9 `/me/*` calendar/booking/shared-training endpoints repointed from `class_sessions`+`bookings`+`shared_training_requests` to `calendar_events`+`calendar_event_bookings`+`calendar_event_shared_training_requests`. `scheduleEngine.ts` extended to write `kind='session'`, `capacity`, and `center_id` on materialized events. `app.ts` import updated; old `class-sessions.ts` deleted. TypeScript clean; DB tests blocked by local env (ER_ACCESS_DENIED). Stages 4 (frontend) and 5 (cleanup: drop old tables, remove legacy columns) remain.
- **#362 (in progress — Slice 3 of Option A)**: Bookings functional audit. `apps/member/.../schedule/page.tsx` ("My Bookings") only ever fetched `/me/schedule` bounded to the next 14 days, so it could show upcoming bookable/booked sessions but had no way to show past bookings at all, and no way to open/view a booking's fuller details — despite the backend already having purpose-built `GET /me/upcoming`/`GET /me/activity-history` endpoints from #194 that nothing in the frontend ever called. Wired a new "Past bookings" section into the page using `GET /me/activity-history` (own loading/empty/error state, tap-to-expand showing attendance/cancellation detail); added the same tap-to-expand to the existing upcoming list (showing description/capacity) — this is the "open/view a booking" the ticket asks for, since there's no member-facing single-booking-by-id backend route to build a separate detail page against. Also fixed the page's error/empty-state conflation: a failed `/me/schedule` fetch used to set the same `message` state as a successful booking action, so an error banner and the redundant "No upcoming sessions" empty text could show together — `loadError` is now tracked separately and suppresses the empty-state text. Backend bugs found and fixed alongside: `GET /me/bookings` joined the pre-migration-059 `class_types`/`cs.class_type_id` columns (dropped on a fully-migrated DB) and 500'd on every call — rewritten to join `activity_types`/`activity_type_id` like every other `/me/*` handler; `GET /me/bookings`, `GET /me/upcoming`, and `GET /me/activity-history` never applied any center scoping (unlike the already-fixed `/me/schedule`) — all three now call `getCenterContext(req)` and filter by `cs.center_id`, matching the pattern fixed for `/me/schedule` in Slice 2. Cancellation (`DELETE /me/bookings/:id`) was already correctly implemented and member-reachable — confirmed working, not rebuilt. New `api/src/test/me-bookings.test.ts` covers auth, impersonation identity resolution, and center scoping for all three endpoints, plus the fixed `activity_types` join. Training, Nutrition, Membership/Payments, Alerts, and Profile audits remain as follow-up PRs against #362.
- **#364 (done)**: Made Admin staff impersonation fully database-driven — `api/src/api/impersonation.ts` no longer imports/instantiates `createClerkClient` at all. `GET /platform/impersonation/targets` builds the staff target list straight from `gym_memberships` (no per-row `clerkClient.users.getUser()` lookup, so a staff member missing from Clerk still appears). `POST /platform/impersonation/:targetId` validates staff targets against `gym_memberships` only (no Clerk superadmin check on the target). Per the ticket's answered clarification, no new `superadmins` DB table was introduced: superadmins are excluded implicitly because they carry no `gym_memberships` row (`TenantContext.gymMembershipId` is already documented as `null` for superadmins) — impersonating a superadmin's Clerk ID fails the same "no membership in this gym" check as any other non-staff ID. `requireSuperadmin` (gating who may *call* these endpoints) is unchanged and still Clerk-based. `impersonation.test.ts` updated: replaced the old "excludes staff who are superadmins" mock-driven tests with tests proving staff discovery/validation succeed even when the Clerk `getUser` mock throws for that user.
- **#372 (done)**: Member detail "Session Packages" section + attendance/cancellation-driven package-credit consumption, per the ticket's answered business-rule table. `MemberExpandedRow.tsx` (admin Members page) now renders each of a member's `user_class_packages` rows (name, purchased date, total/used/remaining sessions, expiration, status) via the existing `GET /members/:memberId/class-packages`; PAYMENTS-write staff get an inline "Extend expiration" action against a new `PUT /members/:memberId/class-packages/:id/extend-expiration` (400 unless the new date is strictly later, audited via `recordAudit` + new `user_class_packages.modified_at`/`modified_by_membership_id` columns, migration 128). Credit-consumption logic in `package-credits.ts`/`bookings.ts` changed: `cancelBooking` now only auto-refunds a spent credit when the member cancels **at least 1 day before** the session (`DATEDIFF(session.starts_at, UTC_TIMESTAMP()) >= 1`) — a same-day cancellation keeps the credit consumed by default, matching the confirmed rule that a same-day cancel or a booked-then-absent no-show still counts as consumption. New `POST /bookings/:id/refund-credit` (requireRole admin/front_desk/trainer_performance/trainer_perf_nutrition) lets a trainer explicitly refund a same-day-cancelled booking's credit — gated on `bookings.user_class_package_id` still being set, so calling it twice 400s instead of double-refunding. New `POST /class-sessions/:id/walk-in` (same roles) covers the "no booking, but attended" case from the rule table: books the member (`bookMemberOnSession(force=true)`, reusing the existing booking-time debit) and marks them present in one transaction, rejecting with 409 if they already have an active booking for that session. `refundPackageCredit` gained `reason`/`actorUserId` params so the ledger (`class_package_transactions`) distinguishes an automatic advance-cancellation refund from a manual same-day one. New `api/src/test/package-credits.test.ts` covers all of the above plus tenant isolation and role gating.
- **#374 (done)**: Support Multi-Member Membership Plans. Migration 129 adds `membership_plans.member_limit` (`'1'`/`'2'`/`'family'`, named CHECK, default `'1'`) and a new `user_membership_members` join table (gym_id, `user_membership_id`/`member_id` FKs both `ON DELETE CASCADE`, `is_owner`, UNIQUE on the pair) recording every Member covered by a Membership; existing Memberships are backfilled so their current owner becomes their sole covered Member (equivalent to `member_limit='1'`), so no manual data migration is needed. `membership-plans.ts` validates `member_limit` on create/update, copies it on duplicate, and rejects (400) shrinking it below the covered-Member count of any active Membership already on that plan. `user-memberships.ts`: `POST /` now inserts the paying Member as `is_owner=1` into `user_membership_members` in the same transaction as the Membership row; new `GET/POST /:id/members` and `DELETE /:id/members/:memberId` (all `requireModuleWrite('PAYMENTS')` for writes) manage the covered-Member list, enforcing the plan's cap server-side and protecting the owner from removal. Per the ticket's "benefits apply to all covered Members" requirement, the plan's booking-time entitlement checks (`plan-allowances.ts` and `plan-class-types.ts`'s `registerBookingAccessHook`s) and `GET /me/schedule`'s `access_locked` flag were extended so a covered Member gets the same access as the Membership's owner, not just the paying Member. Admin UI: Plans page gets a "Members per Membership" select (Individual/Couple/Family) in the add/edit/details views; Memberships page gets a new "Members" action opening `MembershipMembersModal.tsx` (owner-first list, add via a Member picker respecting the cap, remove for non-owners). Bug fix found and fixed along the way (pre-existing, unrelated to this ticket): `user-memberships.ts`'s `effectivePrice()` selected `membership_plans.base_price`, a column dropped in migration 058 and never restored — every `POST /user-memberships` call was failing with a MySQL "Unknown column" error, apparently uncaught because this router had zero test coverage before now. New `api/src/test/user-memberships.test.ts` (the router's first test file — auth/role/tenant-isolation guards, full CRUD happy paths, and the whole `/:id/members` sub-resource including cap enforcement for each `member_limit` value, the owner-removal guard, and the duplicate-member 409) and extensions to `membership-plans.test.ts` for `member_limit` validation and the shrink-guard.
- **#375 (done)**: Aligned Membership Plans with the standard Admin Inline row CRUD pattern (frontend-only; no migration, no API changes). `apps/admin/src/app/[locale]/plans/page.tsx` no longer imports `CrudModal` — the Add Plan, Details, Price, Allowance and Centers modals were all removed. `+ Add Plan` now opens an inline creation row (`inlineNew` state, `renderInlineNewRow()`, capturing Name/Description/Status/Enrollment as in the ticket's mock; a default `billing_policies` row is created alongside it, same as before). `Details` from the row's `ContextMenu` now just expands the row read-only (`showDetails()`) instead of opening a separate modal, satisfying the ticket's "Details and Edit use the same expanded context" requirement. Billing Policy, Centers, Included Services (allowances) and Price History each gained a per-section inline `Edit`/`+ Add` toggle within the expanded row (`billingEditForPlanId`, `centersForPlanId`, `allowanceForPlanId`, `priceForPlanId`), matching the pre-existing inline Charge Benefits editor already on this page — this also newly makes Billing Policy editable after creation (previously only settable via the removed Add modal). `docs/feature-patterns.md` and `CLAUDE.md` updated: Plans is now the **Inline row CRUD** reference (alongside Sellable Items/Taxes) instead of **Modal CRUD**; Class Types is the new Modal CRUD reference. Also carries forward #374's "Members per Membership" select and Members-action wiring on the Plans/Memberships pages, reconciled with the inline-row rewrite during the merge with #374.
- **#436 (done)**: Fixed "Delete Center" (`DELETE /centers/:id`), which crashed with a 500 on almost every real-world delete. Root cause: `firstDependentTable()`'s pre-delete dependency guard queried a table called `events`, permanently dropped in migration `095_drop_events.js` (#221) — the guard only reaches that entry when a center has no dependents in the tables checked before it, so it fired on the common case (deleting a center with no remaining dependents), not the rare one. Also modernized two other stale entries left over from the #360 stage 3 consolidation: `class_sessions` → `calendar_events` (scoped to `kind = 'session'`, so one-off Activities/CalendarEvents don't block a delete) and `bookings` → `calendar_event_bookings`. Follow-up after CI on #433: `rooms` → `spaces` (migration 067 rename), which #436 had left in place and which still 500'd every clean delete; session test fixtures now set required `calendar_events.title`; `POST /me/shared-training-requests` aliases `class_session_id` again. Soft delete (`deleted_at`/`deleted_by_membership_id`/`deleted_by_name`), audit logging (`recordAudit`, entity type `center`), and recycle-bin integration (`center` was already a registered `EntityType` in `recycle-bin.ts`) were all already correct — they just needed the crash removed so the DELETE handler could reach them. First test coverage for this router: new `api/src/test/centers.test.ts` (auth/role/tenant-isolation guards, full CRUD happy paths, and the dependency-check/soft-delete/audit/recycle-bin-restore regression this fix addresses).
- **#376 (done)**: Assign Plan to Member(s) and Create Membership & Billing Events, building on #374's multi-member model. New `POST /membership-plans/:id/assign` (`api/src/api/membership-plans.ts`, admin-only) takes `{ member_ids[], owner_member_id, starts_at }`, validates the count against the plan's `member_limit` (exactly 1/2, unlimited for `family`) and that every member exists in the gym, then in one transaction: inserts the `user_memberships` row (owner as `member_id`, reusing `effectivePrice()` from `user-memberships.ts`), writes the P1.6 `status_changed` billing ledger row via `recordStatusChange`, inserts one `user_membership_members` row per selected member (owner flagged, per #374), and snapshots the plan's current `plan_charge_benefits` onto a new `user_membership_charge_benefits` table (migration 130 — same shape as `plan_charge_benefits`, `no_benefit` rows excluded, unique on `(user_membership_id, gym_charge_id)`) so a later edit to the Plan's benefits never retroactively changes an already-created Membership. After the transaction commits, any Promotion currently targeting the plan is auto-applied best-effort via the existing `applyPromotionToMembership()` (a non-stackable conflict is skipped, not fatal). Reuses the existing `ER_DUP_ENTRY` → 409 handling for "member already has an active membership." Admin Plans page gained an "Assign Plan to Member" context-menu action opening `AssignPlanModal.tsx` (member search/multi-select via the existing `MemberSearchInput`, owner radio-select, capacity warning, starts-on date).
- **#438 (done)**: Fixed feature-flag/Home sync in `apps/member/`. The "Next Booking" section on Home (`page.tsx`) rendered unconditionally regardless of the `member_web.my_bookings` flag, because it's backed by `GET /me/schedule` — deliberately left ungated at the API layer (#367) since it's shared with the un-flagged `/calendar` page — so disabling the flag left the widget (and its backend-gated `DELETE /me/bookings/:id` cancel button) visibly stuck instead of hidden; now explicitly wrapped in the same `featureEnabled('member_web.my_bookings')` check (superadmin-bypassing) already used by the nav tiles and My Membership card. Also fixed client-side staleness: `FeatureFlagsProvider` fetched flags once on mount and never again, so an admin's toggle wasn't reflected in an already-open member session without a full reload — it now polls `GET /feature-flags` every 30s, matching the backend's in-memory cache TTL (`api/src/infra/featureFlags.ts`). Scoped to `apps/member/` per the ticket; the identical staleness pattern in `apps/admin/`'s `FeatureFlagsContext.tsx`, plus impersonation-scoped flag evaluation, is covered by #439.
- **#360 (in progress — stage 3 of 5, API consolidation)**: Unify Sessions and Events under `CalendarEvent`, staged per the agreed rollout (design → schema+booking support → API consolidation → frontend consolidation → cleanup; stages 1–2 landed via #406/#407). This stage repoints every booking/session/shared-training router at the stage-2 schema with no behavior or field-name change visible to any frontend: `class-sessions.ts` merged into `calendar-events.ts` (still mounted at `/class-sessions` and `/calendar-events`, discriminated by a new `calendar_events.kind ENUM('session','event')` column so the admin Calendar page's existing dual-fetch, #326, still renders each occurrence exactly once); `bookings.ts`, `me.ts`, `shared-training-requests.ts`, `plan-allowances.ts`, and `package-credits.ts` now read/write `calendar_events`/`calendar_event_bookings`/`calendar_event_shared_training_requests` instead of `class_sessions`/`bookings`/`shared_training_requests` (now frozen, dropped in stage 5). Migration 134 closes schema gaps found while scoping this stage: `calendar_events.center_id` (was never added despite docs assuming it existed), `calendar_event_bookings.center_id`/`modified_at`/`modified_by_membership_id`, `calendar_event_shared_training_requests`'s FK recreated `ON DELETE CASCADE`, and `class_package_transactions.calendar_event_booking_id` (a mutual-exclusivity CHECK against the now-legacy `booking_id`). Also fixed a pre-existing bug found along the way: `domain/scheduleEngine.ts`'s schedule-rule materializer fetched `activity_type.max_capacity` but never wrote it to the generated `calendar_events.capacity` column — now backfilled for existing rows and fixed going forward, and the materializer also resolves `center_id` (activity type's `default_center_id`, falling back to the gym's sole center) and stamps `kind='session'`. Per the ticket's answered scoping questions (issue comment thread), this was a hard cutover with no dual-write/backfill (no production data to preserve) and no literal router-file merge deferral to stage 4. See `docs/architecture.md`'s "Planned: CalendarEvent Unification" section and `docs/decisions.md` #10. Frontend consolidation (stage 4) and dropping the old tables (stage 5) remain.
- **#417 (done — Stage 3: nutrition meal images)**: Cloudflare R2 image storage integration, per the agreed 3-stage plan on the issue thread (Stage 1: R2 client + bucket-init action, no upload yet; Stage 2: generic upload endpoint + rewire `exercises.image_url`; Stage 3: add `image_url` to nutrition meals). Credentials are platform-wide env vars only (`CLOUDFLARE_R2_ENDPOINT`/`CLOUDFLARE_R2_ACCESS_KEY_ID`/`CLOUDFLARE_R2_SECRET_ACCESS_KEY`/`CLOUDFLARE_R2_BUCKET`) — one shared bucket, folder-scoped per gym — not per-gym DB-stored secrets, per the issue's confirmed resolution to CLAUDE.md's "no hardcoded values"/"no additional stores" constraints. New `api/src/infra/storage.ts` lazily constructs an `@aws-sdk/client-s3` client (mirrors `payments/index.ts`'s `getPaymentProvider()` pattern) and exposes `isStorageConfigured()`, `sanitizeGymFolderName()`/`buildGymFolderPrefix()`, and `initializeGymBucket()` (creates the `Nutrition/Images`, `Exercises/Images`, `Exercises/Videos`, `Branding/Logo`, `Branding/Images`, `Members` folder markers). Migration 135 adds `gyms.storage_folder_prefix` (nullable, unique) and `gyms.storage_initialized_at` (nullable). New `POST /platform/gyms/:id/storage/initialize` (superadmin) — 503 if the deployment isn't configured, 502 on an R2 failure, idempotent (reuses the captured prefix on re-run). Admin **Cordel → Gyms** page gets a read-only Storage section + "Initialize Cloudflare Bucket" button. Follow-up fix: the GitHub Environment `dev` secrets/vars for R2 existed but `deploy.yml` never forwarded them to the VPS, so `isStorageConfigured()` was always false in production and the button never rendered (only the "not configured" warning did) — `deploy.yml` now passes all four `CLOUDFLARE_R2_*` values through. This also came with a deploy-mechanism change: the deploy script now writes the API's whole runtime env — DB creds, Clerk keys, and the R2 values — as inline `Environment=` lines directly in the `fitness-api.container` quadlet unit (regenerated every deploy, GitHub stays the source of truth), not a separate `fitness-api.env` file as before. **Stage 2**: new `api/src/api/storage.ts` (`storageRouter`, mounted at `/storage`) exposes `POST /storage/uploads/exercise-image` (`requireModuleWrite('TRAINING')` + `requireFeatureEnabled('training.exercises')`) — one route per upload target so each target keeps its own module/feature guard, rather than one shared gate. Raw-body upload via `express.raw({ type: image/*, limit: '6mb' })`, same pattern as the existing theme-logo upload in `themes.ts`; validates mime (png/jpeg/webp/gif, else 415) and a 5MB size cap (413); 503 if the deployment has no R2 configured; 409 with the ticket's exact copy ("Cloudflare storage has not been initialized for this gym, therefore images cannot be uploaded.") if the calling gym's `storage_folder_prefix` is still null; 502 on an R2 failure. New `infra/storage.ts` `uploadGymImage()` builds the object key as `<folderPrefix>/<folder>/<uuid>.<ext>` and returns the URL as `${CLOUDFLARE_R2_ENDPOINT}/${CLOUDFLARE_R2_BUCKET}/<key>`, per the issue thread's confirmed URL-construction answer. Frontend: new reusable `apps/admin/src/components/ImageUploadField.tsx` (thumbnail preview + Upload/Replace/Remove buttons + inline not-configured/not-initialized warning, reading two new fields — `storage_configured`/`storage_folder_prefix` — added to `GymContext`'s `GymOption`) replaces the plain URL `<input>` for `image_url` in both the Exercises inline-edit row and the Add-Exercise modal; the read-only Exercise Details modal and the expanded-row view now render the image as a thumbnail instead of a raw URL string. **Stage 3**: migration 138 adds nullable `nutrition_library_items.image_url` (`VARCHAR(500)`, mirrors `exercises.image_url`) — gym-owned items only, since system (`gym_id IS NULL`) catalog items have no per-gym storage folder to upload into. New sibling route `POST /storage/uploads/nutrition-image` (`requireModuleWrite('NUTRITION')` + `requireFeatureEnabled('nutrition.nutrition_library')`) shares `handleImageUpload()` with the exercise-image route, uploading into `Nutrition/Images`. `nutrition-library.ts`'s create/update endpoints now accept/return `image_url` (the platform/system-item router, `platform-nutrition-library.ts`, is untouched — system items stay image-less). Admin **Nutrition → Nutrition Library** page's Add/Edit modals use the shared `ImageUploadField` (`uploadPath="/storage/uploads/nutrition-image"`), and the expanded row's detail view renders the stored image as a thumbnail when set. Follow-up: the Initialize Cloudflare Bucket button is always visible in Cordel → Gyms (disabled + warning when R2 env vars aren't set, rather than hidden); `initializeGymBucket()` also writes parent folder markers (`<prefix>/`, `Nutrition/`, `Exercises/`, `Branding/`) so the R2 tree matches the ticket exactly. Second follow-up (per issue thread): the button is now always **enabled** too (not just visible) — the "not configured for this deployment" warning still shows, but clicking still works so the folder structure can be (re)created later once R2 is configured, or to add new folders in the future; a misconfigured deployment surfaces the existing 503 inline via the toast instead of being blocked ahead of time. Third follow-up (#542): the 503 from both `POST /platform/gyms/:id/storage/initialize` and `POST /storage/uploads/:target` now names exactly which `CLOUDFLARE_R2_*` env var(s) are unset (new `getMissingStorageConfigKeys()` in `infra/storage.ts`, returned as both a detailed `error` string and a `missingConfig` array) instead of a single generic "not configured" message — surfaced automatically wherever the existing toast/inline-error UI already renders `err.message`, no frontend change needed. Fourth follow-up (#542, per the issue thread's "I'd need a bit more of information/traces"): R2 failures now return structured diagnostics instead of a single toast line. `infra/storage.ts` gains `getStorageDiagnostics()` (endpoint split into protocol/host/path + `endpointMalformed`, bucket, `missingConfig`, and credential *lengths* only — never the access key or secret), `describeStorageError()` (flattens an `@aws-sdk/client-s3` rejection: error `name`/`code`, `$metadata.httpStatusCode`/`requestId`/`attempts`, and the `err.cause` chain) and a `StorageOperationError` wrapper that names the exact object key that failed. Both the 503 and the 502 from `POST /platform/gyms/:id/storage/initialize` now carry `diagnostics`; the 502 also carries `details`. The upload route's 502 carries `details` only — it is gym-staff-facing, so the deployment-config snapshot goes to the pino log rather than the response. Admin **Cordel → Gyms** pins a dismissible failure panel under the button (all fields + a "Copy diagnostics" button) so the report survives longer than a toast. Also fixes a likely root cause of the reported failure: the `S3Client` now pins `requestChecksumCalculation`/`responseChecksumValidation` to `WHEN_REQUIRED`, since aws-sdk-js v3 ≥ 3.729 defaults to `WHEN_SUPPORTED` and sends a trailing CRC32 checksum with `content-encoding: aws-chunked`, which R2 rejects.
- **#439 (done)**: Feature flag toggles didn't reach an already-open admin/member session (fetched once on mount, no polling), and a superadmin's flag-bypass incorrectly carried over while impersonating, so an impersonated user saw the superadmin's unrestricted view instead of their own. Fix: `requireFeatureEnabled` (`api/src/infra/featureFlags.ts`) now bypasses only in native capacity — `tenantCtx.isSuperadmin && !tenantCtx.impersonatedUserId` — `tenantCtx.isSuperadmin` itself is unchanged (still `true` under impersonation for other permission checks). Both `FeatureFlagsContext.tsx` (admin and member) now poll every 30s, matching the backend's in-memory cache TTL. Frontend bypass checks were updated the same way: admin's `Sidebar.tsx` computes `filterNavGroups`'s role as `(isSuperadmin && !isImpersonating) ? 'superadmin' : activeGym?.role`; member's Home/`/training`/`/nutrition`/`/schedule`/`/membership`/`/profile` use `isSuperadmin && !isImpersonating` in place of raw `isSuperadmin`, `isImpersonating` from `useImpersonation()`. `AdminBar`/`TopHeader`'s own impersonate/exit-impersonation controls and `GymSwitcher`'s pre-impersonation gym picker intentionally keep raw `isSuperadmin` — they must render in native capacity regardless of feature flags. Extended `member-web-feature-flags.test.ts` to assert the corrected precedence (impersonated superadmin gets 403 when the flag is off; native superadmin still bypasses).
- **#415 (done)**: Member-app Home froze under admin impersonation (Next Booking / My Membership stuck on Loading, Calendar and other tiles appearing unresponsive). Network evidence: `GET /me/profile` 403 and `GET /feature-flags` 401. Root cause: a bare superadmin has no member identity — `/me/profile` is `requireRole('member')` so `tenantContext` assigns `role: 'admin'` and 403s; Home's local `loading` only clears inside `load()`, which is gated on `isLinked`, so widgets spun forever and other pages bounced home. Impersonation hydration from `sessionStorage` also lagged `AppContext`'s first fetch, so a refresh while impersonating could fire the same 403 before the `x-impersonate-as` header was restored. Fix: `ImpersonationProvider` exposes `ready` and `AppContext` waits on it; skip `/me/profile` until impersonating; prefer `session.gymId`; Home shows a Support-mode prompt instead of infinite spinners; feature-flag fetch requires a Clerk token; member impersonation dialog lists members only. Tests: `GET /me/profile` 403 as bare superadmin, 200 under `x-impersonate-as: member:<id>`.
- **#559 (done — stages 1–4)**: Calendar styling section for Base Themes and Custom Themes. **Stage 4** closes the ticket with the accessibility pass: a WCAG-2.1 contrast report under the Calendar section in both theme editors (`apps/admin/src/lib/calendarContrast.ts` + `CalendarContrastReport` in `ThemeTokensEditor.tsx`) that scores every foreground/surface pair the calendar paints — advisory only, never a save blocker, since a gym's branding is its own and `validateTokens()` stays a pure format check; `:focus-visible` indicators for calendar events and nav buttons drawn from tokens guaranteed to read against their surroundings (added identically to Admin's and Member Web's `CalendarThemeStyles.tsx`); and `calendarVarValue()` in both `themeTokens.ts` files hardening the fallback so an *unusable* persisted value (empty or non-hex) falls back to the default rather than dropping the CSS declaration. Covered by `apps/admin/src/test/calendar-a11y.test.ts` and Member Web mirror checks. **Stage 3** finished the event surfaces: an event's fill no longer encodes its status — every event takes the theme's new `calendarEventBackground`/`calendarEventBorder` (joining `calendarEventText`) plus a new `calendarEventHoverBackground` advanced attribute, and the booking status moves to a pill-shaped badge rendered inside the event (`components/CalendarStatusBadge.tsx`, the shape and palette of the app's existing `StatusBadge`). That is the resolution of the conflict with #541 agreed in the thread: status still decides a color and nothing else does, it just paints the badge rather than the box, which is what frees the box to be themeable. `lib/calendarEventColors.ts` remains the single status → color mapping, now returning a `{ bg, fg }` pair and covering `full` (a `scheduled` session at capacity), and the badge labels are translated from `calendar.status_*` instead of showing the raw uppercased DB value. The admin calendar page stops setting a per-event `backgroundColor`/`borderColor` (FullCalendar writes those inline, which would beat the theme); the sheet maps the tokens onto `--fc-event-bg-color`/`--fc-event-border-color`/`--fc-event-text-color` and adds the radius (re-asserting FullCalendar's corner-flattening for continued multi-day events), a hover background and the selected/focused overlay at `opacity: .25`. Member Web is deliberately unchanged — its events are colored by availability to that member via an inline style that wins over these rules. Defaults are the `#6c63ff` purple `scheduled` events already had, so an unconfigured calendar looks as before. **Stage 2** made the stage-1 tokens visible: `applyTokens()` (both `apps/admin/src/lib/themeTokens.ts` and its Member Web mirror) writes one `--gd-calendar-*` CSS variable per calendar token — driven by the new `CALENDAR_COLOR_VARS`/`CALENDAR_ADVANCED_VARS` maps, with every key falling back to its default so a pre-#559 theme still resolves the full set — and a new `CalendarThemeStyles` component holds the FullCalendar override sheet that reads them. The sheet maps each token onto FullCalendar v6's own `--fc-*` variable where one exists (border, page/today/highlight/non-business, the whole nav-button palette), so one declaration reaches `timeGridDay`/`timeGridWeek`/`dayGridMonth` at once, and adds an explicit rule for the surfaces FullCalendar hardcodes (column headers, month-view date numbers including other-month dates, the time axis, slot height, weekend columns). Every rule is scoped to a `gd-calendar` wrapper class on the calendar container in both the admin and member calendar pages, and every `var()` repeats the default token value as its CSS fallback, so an unthemed page renders exactly as before. Event colors are untouched — the three event variables are emitted but read by no rule yet. Two intentional pixel-level changes: other-month dates now use `calendarMutedDayText` at full opacity instead of FullCalendar's `opacity: .3`, and non-business slots paint the configured `calendarDisabledSlotBackground` opaquely instead of FullCalendar's translucent grey (the stage-1 default, `#f7f7f7`, is what that grey renders as over white). Stage 1 added the token layer and the editor section only — 16 `calendar*` colors on `tokens.colors` plus 5 `calendar*` entries in the `advanced` map, their defaults (mirroring FullCalendar's own built-ins so an unconfigured theme is visually unchanged), hex validation in `api/src/domain/themeTokens.ts` (`CALENDAR_COLOR_FIELDS`), and a new `group_calendar` entry in `COLOR_GROUPS`, which gives both theme editors the section at once since they share `ThemeColorsEditor`. No migration: the tokens blob is schema-less JSON and missing keys fall back to defaults, so pre-#559 themes keep saving and rendering unchanged. Per the ticket's clarification thread the Base↔Custom relationship is deliberately untouched — cloning copies the calendar tokens along with everything else, exactly as before; no live sparse inheritance was introduced.
- **#547 (done)**: Membership Plan Pricing section — one current, VAT-inclusive price per plan plus an immutable price history, per the ticket thread's final clarification ("Price field should be fixed and aligned with the behavior that we have in sellable items… Add Price need to be removed… Save… Apply new price to assigned plans"). Earlier rounds shipped the tax model (#413) and the live net/gross recalculation (#581); this closes the ticket. Migration 161 adds `membership_plan_prices.status` (`active`/`applied`/`inactive`, named CHECK), `applied_at`, and a `tax_rate_id`/`tax_rate_percent` VAT snapshot per price row, backfilled from the window covering today and the plan's current tax rate. New `PUT /membership-plans/:id/pricing` (admin) replaces the plan's current price in one transaction: the superseded row is closed (`valid_to` = yesterday, or its own `valid_from` when it opened today) and marked `inactive`, a new open-ended `active` row opens today with the selected VAT snapshotted, and the plan's `tax_rate_id` is updated with `tax_behavior` pinned to `inclusive` — a VAT change opens a new price too, and re-saving identical values is a no-op. New `POST /membership-plans/:id/pricing/apply-to-assigned-plans` (admin) pushes the current price onto the `user_memberships` still running on the plan (`draft`/`awaiting_payment`/`active`/`paused` only) and marks the price `applied`; terminal (cancelled/expired) Assigned Plans and already-generated `billing_events` are never touched (req. 15), and an Assigned Plan with a `discount_reason` keeps its agreed `final_price` while its plan-price snapshot is refreshed — the response reports `{ updated, kept_discounted }`. `recomputePriceStatuses()` re-derives the status column from the validity windows after every price write (including the legacy `POST`/`PUT`/`DELETE /:id/prices` endpoints, kept for API compatibility), so status can never drift from the dates it describes; `effectivePrice()` and `enrichPlan()` break a same-day tie in favour of the non-history row. Admin UI: the expanded Plan row's "Add Price" button and its from/to price-window sub-form are gone, replaced by a **Pricing** section (Price (VAT included) + Tax Rate + **Save**, with the live net/gross preview) and an **Apply new price to assigned plans** button behind a `ConfirmDialog`; the **Price History** section below it is read-only and badges each row Active / Applied to assigned plans / Inactive. Tax Rate and Tax Behavior were removed from the plan's inline edit form (price and VAT are edited together in Pricing, and a plan's price is always VAT-inclusive). Duplication copies each price with its VAT snapshot but never the `applied` marker. Tests: 18 new cases in `api/src/test/membership-plans.test.ts` (auth/role, tenant isolation, cross-tenant tax rate, validation, 0%/21%/10% rates, history immutability, apply-to-assigned including the discount and cancelled-membership cases, duplication) and the updated `plans-pricing-locales.test.ts` key set.
- **Training module redesign (#60–#63, done)**: dynamic Workout Block form driven by
  `blockFieldConfig.ts` (#60); tree-grid Training Plan Template editor (#61); dependency
  awareness for shared catalog entities (#62); tree-grid Workout Template editor with
  in-place block/exercise CRUD, paginated/filtered list (`created_by_membership_id`,
  migration 053) and cross-template block drag-and-drop via `PUT …/blocks/:id/move` (#63).
  Patterns documented in `docs/feature-patterns.md` (Tree-Grid Editor, Config-Driven Fields,
  Dependency Awareness).
- **Personalized Training Plans module (#67, done)**: gym-level **Training → Training
  Plans** page (list, filters by member, source template, status, created by, and date
  ranges) with a two-step "New Training Plan" dialog (From Template / From Scratch) that
  posts to `POST /training-plans`. Existing-active-plan collisions return `409`; the UI
  swaps to a keep-vs-expire prompt whose "Expire" branch closes prior actives
  (`status='expired'`, `end_date` = new plan's start) and their `member_training_plans`
  rows atomically before the new plan is created. Editor page at
  `/training-plans/[id]` with a header form (name/desc/dates/status), drag-reorder for
  workouts, cross-parent move + duplicate for blocks and exercises via new endpoints on
  `training-plans.ts` (`…/blocks/:id/move`, `…/exercises/:id/move`,
  `…/duplicate` at each level). Templates page gains an **Assign Plan to Member** action
  on active templates that opens the same dialog with the template preselected.
  Migration 054 adds `training_plans.start_date` (backfilled from the latest
  `member_training_plans.valid_from`) and `end_date`, and updates the status CHECK to
  `draft/active/expired/deleted` (existing `inactive` rows remapped to `expired`).
  `training-plan-creation.ts` extracts the clone/write-history transaction so both
  `POST /training-plans` and the legacy `POST /members/:id/member-training-plans` share
  one implementation.
- **Training Plans UX Redesign — Inline Editing (#112, done)**: replaced the
  `/training-plans/[id]` editor page with an inline expandable card list matching the
  Training Plan Templates UX. Each card expands to show workouts/blocks/exercises with
  the existing drag-reorder and block/exercise CRUD modals. New context menu actions:
  **Edit** (inline metadata), **Details** (read-only modal with full audit info),
  **Duplicate** (clones the full plan tree to a draft), and **Complete** (transitions
  status to `completed`, stamps `end_date`, makes plan read-only). Migration 066 adds
  `completed` to the status CHECK constraint. The old `/training-plans/[id]` route now
  redirects to the list. All write endpoints on `training-plans.ts` reject completed plans
  with 403.
- **Training Plan Workout Builder Parity (#164, done)**: Training Plan workout cards now have
  full UX parity with the Workout Template builder. Extracted `WorkoutBlockBuilder.tsx`
  (`apps/admin/src/app/[locale]/workout-templates/`) — a self-contained component with its own
  dnd-kit `DndContext` that handles block reorder, exercise reorder within a block, and
  cross-block exercise moves via `PUT …/blocks/:srcBlockId/exercises/:exId/move`. Props:
  `workoutKey` (namespaces drag IDs to avoid collision across multiple open workout cards),
  `blocksUrl`, `blocks: HierBlock[]`, `canWrite`, `onChanged`. Training Plans page updated:
  block/exercise CRUD modals removed from the workout tree; each workout card now renders
  `WorkoutBlockBuilder` inline with a searchable `ExerciseCombobox`, always-editable fields
  (inline block name + type + config, inline exercise rows, result_type/target_value/unit),
  block Duplicate/Delete context menus, and drag-and-drop at both levels. Workout name
  always-editable inline (on-blur PUT, reverts on error). `+ Add Workout` replaces
  `window.prompt()` with an inline form. `canWrite` now uses `canWriteModule(role, 'TRAINING')`
  (matches Training Plan Templates pattern). No API or DB changes — backend already supported
  all required endpoints (`/blocks`, `/blocks/:id/exercises`, cross-block `/move`, etc.).
- **Billing ledger member history endpoint (#114, done)**: `GET /billing-events/member/:memberId`
  convenience alias added to `billing-events.ts` (paginated, tenant-isolated, 404 on unknown
  member). First test suite for the billing ledger (`billing-events.test.ts`): 14 tests
  covering auth, happy paths, new endpoint, and cross-gym tenant isolation.
- **Cordel platform menu + two-scope audit log (#66, done)**: superadmin-only **Cordel**
  sidebar group (Gyms, Users, platform Audit Log at `/cordel/audit`); **System** reduced to
  gym-scoped Audit Log + Customize and opened to gym admins (`requiredRole: 'admin'`).
  `GET /audit-logs?scope=all` (platform superadmins only, `tenantCtx.isSuperadmin`) lifts
  the tenant gym filter and joins `gyms.name`; shared `AuditLogView` component renders both
  scopes, adding a Gym column in platform mode.
- **Audit Log UX Improvements (#69, done)**: each audit row is now a self-contained
  historical snapshot. `actor_name` and `entity_name` are captured at write time and never
  recalculated (migration 055; existing rows truncated). Actor name comes from the Clerk user
  object already fetched in `tenantContext` (zero extra queries). Entity name is resolved by
  `infra/audit-registry.ts`: named entities use a `name`-column lookup; M-N/link entities
  get a composed label from their FK parents (e.g. `user_membership` → member name + plan
  name). FK fields in `previous_values`/`new_values` are enriched at write time from plain
  `foo_id: value` to `foo: { id, name }` objects. `GET /audit-logs/meta` returns the full
  entity-type list + action list for dropdown population. New filter params: `entity_name`
  (LIKE), `actor` (name LIKE or Clerk ID exact match), `action`, `source`. `AuditLogView`
  gains dropdowns for entity type, action and source; actor now searched by stored name;
  entity column shows `type#id` + name beneath it; Clerk uid surfaced only in the expanded
  row. `class-types.ts` and `promotions.ts` gain `recordAudit` coverage.
- **Done outside the phase plan**:
  - **Assign Members When Creating a CalendarEvent (#366)**: staff can assign one or more Members directly when creating a session or a recurring activity, instead of booking them one at a time afterward. `POST /class-sessions` accepts an optional `member_ids: number[]`, validated and booked (via the existing `bookMemberOnSession(force=true)` — same mechanism as a manual staff "Add member") inside the same transaction as the session's own INSERT. New `activity_type_schedule_rule_members` table (migration 135) lets staff assign Members to a recurring `activity_type_schedule_rules` row; `materializeScheduleRule` auto-books every assigned Member on each freshly generated `calendar_events` occurrence — including occurrences generated by a later `PUT` re-materialization — so the assignment isn't a one-time snapshot. Capacity is always advisory for this staff-driven path (never blocks create/save); Member-facing privacy needed no changes since `/me/bookings` and the member calendar were already scoped to the caller's own bookings. New `MemberMultiSelect` component (`[locale]/calendar/MemberMultiSelect.tsx`) wraps the existing `MemberSearchInput` with removable chips and a lightweight over-capacity warning, used on both the Schedule page's session-creation form and the Activity Types page's schedule-rule form.
  - **Per-gym theming (#51, extended by #68)**: `gyms.theme_key` (migration 030, presets `indigo/emerald/crimson/amber`), `ThemeProvider`, and the superadmin **System → Customize** editor (now replaced by #68).
  - **Centers UI Redesign (#74)**: Centers list now shows Email, truncated Address, Active Members count, and a ContextMenu (⋮) replacing Edit/Delete buttons. Menu items: Edit (existing modal, unchanged), Details (new read-only dialog with General / Statistics / Audit sections including `created_by`/`modified_by`/`deleted_by` resolved names), View Members (navigates to `/members?centerId=X&status=active`), Delete. Migration 064 adds `created_by_membership_id` and `deleted_by_membership_id` to `centers`. `GET /centers` and `GET /centers/:id` return `active_member_count` (subquery) and resolved modifier names via LEFT JOINs. `GET /members` gains `?centerId` filter. Members page reads `centerId` from URL params on load and shows a center dropdown filter (multi-center gyms only). `ContextMenu` component updated to `position: fixed` dropdown so it escapes `overflow: hidden` table containers.
  - **Membership Plans Redesign (#70)**: `membership_plans` gains `lifecycle_status` ENUM(`draft`/`active`/`archived`) + `enrollment_status` ENUM(`open`/`closed`) + soft-delete + audit fields (migrations 058–062). `class_types` renamed to `activity_types` (migration 059, all FK references migrated). New tables: `billing_policies` (1-per-plan billing/service cycle config), `plan_allowances` (replaces `class_type_user_memberships` + `membership_plan_benefits`; supports `unlimited` and `session_count` with recurrence), `membership_plan_centers` (optional per-plan center restriction). API: enriched `GET /membership-plans` (nested `billing_policy`, `allowances`, `centers`, `price_history`, `current_price`, `member_count`); new actions `duplicate`, `archive`, `PUT enrollment`. Booking enforcement (`plan-allowances.ts` side-effect hook) checks center restriction then allowance type/quota. Admin UI replaces DataTable with a per-plan accordion showing all five sections inline; context menu with Edit / Duplicate / Open‑Close Enrollment / Archive / Delete.
  - **Theme Management (#68)**: first-class `themes` entity (migrations 056–057 — table + gym FK). `tokens` JSON carries header/sidebar/typography design tokens. Superadmin **Cordel → Themes** CRUD (draft/active/deleted lifecycle, logo upload up to 512 KB via `POST /platform/themes/:id/logo`, `GET /themes/:id/logo` public caching endpoint). Gyms assigned a theme via `theme_id` FK replacing old `theme_key`. Admin `ThemeProvider` reads `activeGym.theme.tokens` from the gym list response (LEFT JOIN) and writes `--gd-*` CSS variables live on gym switch. Member app introduces `GET /me/gym` (no tenant context needed) so `AppContext` self-resolves gymId + theme; member-side `ThemeProvider` applies the same variables. Both `TopHeader` and `Sidebar` consume `--gd-*` vars; `BottomNav` in the member app follows suit.
  - **Separate Base Themes from Org Themes / Fix Default Logic (#144)**: `GET|PUT|DELETE /platform/themes` now filter to `gym_id IS NULL` — org-created custom themes no longer appear in the Base Themes screen. New `is_system_default TINYINT(1)` column on `themes` (migration 072, Black seeded as initial default) marks the single Base Theme used as factory default when a new organization is created. New endpoint `PUT /platform/themes/:id/set-system-default` atomically flips `is_system_default` to the target theme and clears all others; 404 if the target is not a base theme. Base Themes list response drops `is_default`/`gym_name` in favour of `is_system_default`. UI: Type column always shows "System" badge; Default badge reflects `is_system_default`; "Set as System Default" added to the context menu for non-default base themes. Org default theme logic (`gyms.theme_id`) and center inheritance are unchanged.
  - **Immediate Theme Application on Default Change (#143)**: After a successful `PUT /system/themes/:id/set-default`, the Themes page now calls `refreshGyms()` in parallel with `loadAssignments()`. `GymProvider` re-fetches the gym list (which includes the updated `theme.tokens`), causing `ThemeProvider` to re-apply `--gd-*` CSS variables immediately — no browser refresh required. No backend or migration changes.
  - **Instant Theme Reload on Any Change (#209)**: Any token modification (colors, typography, advanced) is now applied to the DOM immediately via `applyTokens()` when the edited theme is the currently active gym or center theme — no waiting for the debounced save. After the save succeeds, `refreshGyms()` + `refreshCenters()` sync the context so the `ThemeProvider` confirms the new state; on failure the pre-edit tokens are restored. Assignment changes (Assign Centers, Restore Inheritance, Set as Org Default) also call `refreshGyms()` + `refreshCenters()` so the new theme assignment propagates instantly. `CenterOption` in `CenterContext` gains `theme_id: string | null` (already in the API response via `c.*`) to let `isExpandedThemeActive()` detect when the active center has a direct theme override. No backend or migration changes.
  - **Theme Assignment Management (#120)**: Gym admins can now assign themes to the Organization and individual Centers from within the inline Theme editor. System → Themes page (`/themes`) rebuilt as the proper gym-admin page using `GET /system/themes` (was accidentally overwritten in #117 with Base Themes content). New Assignments tab in the inline editor for every theme (base or custom): org-default checkbox (`gyms.theme_id`), center list with Inherited/Assigned badges, Restore Inheritance (sets `centers.theme_id = NULL`), "+ Assign Centers…" multi-select picker, search filter, show-all when > 10. Five new endpoints on `gym-themes.ts`: `GET/:id/assignments`, `PUT/:id/set-default`, `GET/:id/unassigned-centers`, `POST/:id/assign-centers`, `DELETE/:id/centers/:centerId`. Delete guard unified to single message: "This theme is currently in use. Remove all assignments before deleting it." Theme resolution order: `centers.theme_id` → `gyms.theme_id` → first alphabetically.
  - **Base Themes rich list (#121)**: Cordel → Base Themes page gains a rich summary row: Logo (32×32), Name, Description (first line truncated), Type badges (System/Custom + Default when `gyms.theme_id` points to the theme), Usage count (distinct orgs for system themes; centers direct or inherited for custom themes), and Status. The ⋮ context menu now includes a Details action opening a read-only modal with full description, type, status, usage, owner (Cordel / gym name), org-default flag, and audit fields (Created/Modified by + at, resolved from `audit_logs`). New `description TEXT` column added to `themes` (migration 068). `GET /platform/themes` now returns all themes (system + custom) in one list; `GET /platform/themes/:id` includes `created_by_name` / `modified_by_name`.
  - **Meals Sub-Entities UX Alignment (#162)**: Dishes, Sides, and Sauces now follow the platform entity standard. Migration 077 adds `status`, `created_by_membership_id`, `modified_by_membership_id`, `deleted_at`, `deleted_by_membership_id` to all three tables. `DELETE` changed from hard-delete to soft-delete (`status='deleted'`). New `POST /:id/duplicate` endpoint. `GET /` filters out deleted items; `GET /:id` returns full audit fields. Admin UI replaced with `DataTable` with columns: Name, Description (truncated), Created At, Created By, Status badge, Actions. Context menu: Details (read-only modal with audit fields) / Edit / Duplicate / Delete. 65 new tests in `meals-catalog.test.ts`. **Note**: dishes/sides/sauces are fully removed by #163 — `meals-catalog.test.ts` and the three routers are deleted.
  - **Nutrition Library + Nutrition Plan Templates Redesign (#163)**: Replaces the gym-scoped dishes/sides/sauces catalog with a global `nutrition_library_items` table (no `gym_id`, seeded with 32 items across 6 categories: main_dish, side, sauce, drink, dessert, other — same pattern as `benefit_types`). Migration 075 drops `nutrition_plan_template_meal_dishes`, `dishes`, `sides`, `sauces`; creates `nutrition_library_items`, `nutrition_plan_template_restrictions`, `nutrition_plan_template_goals`; adds `main_dish_id`, `side_id`, `sauce_id`, `drink_id` FK columns to `nutrition_plan_template_meals`; extends weekday CHECK to 0–7 (7 = "All Days"). New `nutrition-library.ts` router: `GET /nutrition-library` (global read, optional `?category=` filter, no write endpoints). `nutrition-plan-templates.ts` rewritten: meals now have 4 direct library FK selectors instead of a sortable dish list; new CRUD endpoints for restrictions (`POST/GET/PUT/DELETE /:id/restrictions`) and goals (`POST/GET/PUT/DELETE /:id/goals`); `POST /:id/duplicate` deep-clones days→meals + restrictions + goals in one transaction. `GET /:id/hierarchy` returns `{ days, restrictions, goals }`. Admin: Nutrition Library page (read-only category view), navigation entry renamed from "Meals" to "Nutrition Library", `meals/page.tsx` redirects to `nutrition-library`. `NutritionPlanTree.tsx` rewritten: `MealRow` renders 4 library dropdowns, `RestrictionsSection` and `GoalsSection` appended below the days list. `meals-catalog.ts` router and `meals-catalog.test.ts` deleted. New test files: `nutrition-library.test.ts` (4 tests), `nutrition-plan-templates.test.ts` fully rewritten (removing all dish references).
  - **Nutrition Plan Templates (#132)**: Inline expandable nutrition module. Shared gym-scoped catalogs: `dishes`, `sides`, `sauces` (each a full CRUD router: `GET/POST/PUT/DELETE /dishes|sides|sauces`; `DELETE /dishes/:id` returns 409 if the dish is referenced in any template). Template hierarchy: `nutrition_plan_templates` → days (weekday 0–6, one per weekday per template) → meals (N per day, drag-reorder) → meal_dishes (N per meal, each with optional side + sauce, drag-reorder). New routers: `nutrition-plan-templates.ts` (template CRUD + duplicate deep-clone + hierarchy endpoint + day/meal/meal-dish CRUD + per-level reorder), `meals-catalog.ts` (factory for dishes/sides/sauces). Admin UI: **Nutrition → Meals** page (three expandable catalog sections), **Nutrition → Nutrition Plan Templates** page (expandable cards with inline tree editor using @dnd-kit at three levels). Migration 071 creates 7 new tables. 38 new tests; `cleanupTestGyms` extended with nutrition table deletions.
  - **Centers UX Redesign (#131)**: Centers page replaces the DataTable + modal editor with the standard Cordel inline expandable row pattern. Collapsed row shows Name, Email, Theme (with "(Inherited)" suffix when `center.theme_id` is null), Status inline dropdown, Created At, and a ContextMenu (Details / Delete). Expanding a row opens four inline sections: General Information (Name, Code, Status), Contact Information (Email, Phone), Location (Address), and Theme assignment. All text fields auto-save on blur/Enter; dropdowns (Status, Theme) auto-save on change. No Save/Cancel buttons. "+ Add Center" creates a record via POST then auto-expands + focuses the Name field. Theme section: dropdown of all gym-accessible themes (base + custom); when a direct theme is assigned, a "Restore inheritance" link appears to reset `center.theme_id` to null. Details modal (audit only: Created At/By, Modified At/By, Deleted At/By). The legacy "Edit Center" modal is removed. `GET /centers` and `GET /centers/:id` now join `themes` and `gyms` to return `theme_name` and `gym_theme_name`.
  - **Workout Templates Fully Inline Editing (#130)**: Replaces modal-based block/exercise editing with always-editable inline fields — no save/cancel buttons, no modals. Block header is a single row (name input + type select + rounds input) that auto-saves on blur/change. Exercise table rows are always editable; exercise selection uses a custom filter-as-you-type combobox (position: absolute listbox, closes on outside click). Type-adaptive columns: `reps` → Sets/Min–Max reps/Rest, `time` → Sets/Duration/Rest, `distance` → Distance/Unit/Rest. Rest field displayed in minutes (÷60 display, ×60 on save). New endpoints: `POST /:id/blocks/:blockId/duplicate` (deep-copies block + exercises), `POST /:id/blocks/:blockId/exercises/:exId/duplicate`. Context menus: Duplicate + Delete (no Edit). `BlockModal` and `ExerciseModal` removed. Migration 071 adds `exercise_type VARCHAR(20) NOT NULL DEFAULT 'reps'` (CHECK: reps/time/distance) to `exercises`; adds `duration_seconds INT UNSIGNED`, `distance_value DECIMAL(8,2) UNSIGNED`, `distance_unit VARCHAR(20)` to `workout_template_exercises`. `exerciseSummary()` in `summaries.ts` updated for type adaptation and rest-in-minutes.
  - **Promotions UX redesign (#127)**: Promotions page replaces the DataTable + modal editor with the standard inline expandable row pattern. Collapsed row shows Name, Description, Created, Start Date, End Date, Status badge, and a ContextMenu (Details / Duplicate / Delete). Expanding a row opens an inline form for all promotion fields plus two embedded child collections: **Charge Benefits** (charge_type, action, value) and **Period Benefits** (membership_plan, action, value, duration_months). Both collections display as compact tables with inline add-row forms and per-row delete. "+ New Promotion" creates a record and auto-expands + focuses the Name field. Search by name/description with 300ms debounce; status filter; `created_by_membership_id` tracking (migration 069). `promotion_period_benefits` table redesigned from "pay X get Y free" model to "plan + action + value + duration" model (migration 070). New API: `GET /promotions` adds `q`, `created_by`, `sort`, `dir`; `GET /promotions/created-by-options`; `POST /promotions/:id/duplicate` (deep-copies charge + period benefits); `PUT /charge-benefits/:cbId`; `PUT /period-benefits/:pbId`; `POST /period-benefits` updated to new shape. Details modal simplified to audit info only (created_by, created_at). Refined by #158: Edit moved to context menu (rows no longer expand on click), list columns restructured (Type + Value derived from first charge_benefit, Period combines starts/ends, Created By added), Details modal expanded with full promo fields + modified_at/by from audit_logs, `GET /promotions` and `GET /promotions/:id` extended with primary_action_type, primary_value, modified_at, modified_by_name.
  - **Base Themes UX unification (#117)**: Cordel → Base Themes page replaces the DataTable + modal editor with the same inline expandable row pattern used by System → Themes. Each row shows a color swatch, name, status badge, and a context menu (Clone, Details, Delete). Expanding a row opens the branding/typography/colors inline editor. New `POST /platform/themes/clone/:id` clones a base theme into a new base theme (superadmin only). A read-only Details modal surfaces name, created_at, and modified_at. Deleted themes are non-expandable.
  - **Theme Management v2 (#97)**: Separates platform-owned Base Themes from customer-owned Customer Themes. Migration 065 adds `gym_id` to `themes` (NULL = base, non-NULL = customer theme for that gym) and `theme_id` to `centers` (optional per-center override). Seeds "Black" base theme. New `POST /system/themes/clone/:id` (clone any theme to customer theme), `PUT/DELETE /system/themes/:id` (edit/soft-delete customer themes with gym/center conflict guard), logo endpoints. Cordel nav entry renamed to **Base Themes** (`nav.base_themes`); new **System → Themes** page (`/themes`) for gym admins shows System Themes (read-only, expandable) and My Themes (inline editable with ContextMenu: Clone / Activate / Edit / Delete) in separate sections.
  - **Team management (#53)**: gym-scoped admin/coach/staff CRUD via `gym-users.ts` + the **Organization → Team** page. Clerk-invitation flow with an `invited` placeholder row that links on first sign-in; self-edit and last-admin guards; audited. Added `gym_memberships` columns `status`, `email`, `name`, `invitation_id` (migrations 031–034).
  - **Platform superadmin management**: `platform/superadmins` + **System → Users**.
  - **Grouped, role-gated sidebar**: `config/navigationGroups.ts` (Membership / Organization / Training / Nutrition / Financials / System / Cordel).
  - **Staff nav rename (#77)**: Navigation-only refactor. **Team** entry renamed to **Staff** and now points to the existing Trainers page (`/trainers`). The standalone **Trainers** nav entry removed. `trainers.title` i18n key updated to "Staff" across `en`/`es`/`ca` locales. No API, DB, or business logic changes.
- **Placeholder shells (built, content pending)**: per-group Dashboard pages (`organization`, `training`, `nutrition`, `financials`, singular `membership`). Nutrition has no backend yet.
- **Phase 9 — Centers (#59, supersedes Phase 7)**: `Center` (migration 043) as the single
  location concept going forward — `gym_locations` was never built. `member_centers`
  junction (044) gives each Member one or more Centers with a default; `center_id` +
  audit-column backfill added to `rooms`/`class_sessions`/`bookings` (045–047); every
  existing Gym auto-got one default Center named after the gym, every existing Member
  defaulted onto it (046), and new gyms get the same default Center at creation. New
  `resources`/`trainer_availability`/`events` tables (048–050), each `center_id`-scoped
  from creation. `centerContext` middleware (`x-center-id` header) mirrors `tenantContext`;
  `resolveCenterId()` falls back to a gym's sole active Center so single-center gyms need
  no UI changes. Admin: **Organization → Centers/Resources/Events** pages, Trainer
  Availability inline on the Trainers page, Member-edit Assigned Centers/Default Center
  (hidden until a gym has >1 center). Member app: `GET /me/centers` + an optional
  center switcher, hidden for the single-center case. **Phase 7 (#39–#41) is superseded
  and closed** — its `gym_locations`/nullable-FK design was never implemented.
- **Phase 8 — MONEI payments (active, replacing deferred Stripe plan)**: ~~provider abstraction (#179)~~, ~~DB migrations (#180)~~, ~~API routers + isolated payment page token endpoint (#181)~~, ~~admin MemberPaymentsModal (#182)~~, ~~member payment banner + success/error pages (#183)~~, ~~isolated payment page app at `pay.vdicube.com` (#204)~~, ~~infra/Docker for the payment container (#205)~~, ~~recurring MIT billing run (#184)~~, ~~cash payment recording + fiscal receipt (#185)~~, ~~nginx IP allowlist for /billing/ endpoints (#306)~~.
- Platform naming migrated gymdesk → fitness (2026-07-10/11): DB schema `fitness`, containers/images
  `fitness-*`, VPS user `podman`, env vars `CORDEL_FITNESS_*`. See `docs/architecture.md` § Deployment.
- Legacy cleanup complete: `/fares` and `/subscriptions` routers + the old
  Subscriptions page deleted, `user_memberships.plan` column dropped
  (migrations 004, 007, 009). The member app's `/subscriptions` route was
  renamed to `/membership` (P1.8).
- **#440 (done)**: Staff Management — consolidated the expanded staff profile view in
  `apps/admin/src/app/[locale]/staff/page.tsx` from tabbed sections (General/Employment/
  Schedule/Notes/Clerk) into a single continuous scroll (tab bar + `activeSection` state
  removed). Added multi-center association for Staff, mirroring #59's `member_centers`
  pattern: migration 136 creates `staff_centers` (gym_id/staff_id/center_id, `is_default`,
  same generated-column one-default-per-staff unique index as `member_centers`); migration
  137 backfills it from the old single `staff.assigned_center_id` column (falling back to
  a gym's sole center) and then drops that column — nothing outside `staff.ts` read it.
  New `api/src/api/staff-centers.ts` (`GET`/`PUT /staff/:staffId/centers`, admin-only
  write) mirrors `member-centers.ts`, except a staff member may have zero centers (no
  access-control path depends on staff having one, unlike members). `staff.ts`'s
  create/duplicate now accept/propagate `center_ids`/`default_center_id` via
  `resolveStaffCenters()` (members' `resolveMemberCenters()` equivalent). Frontend adds a
  Centers subsection (checkboxes + default `<select>`, hidden for single-center gyms,
  matching the Members page's `showCenters` pattern) to the consolidated view.
- **#437 (done)**: UI cleanup — removed the admin `[locale]/schedule/` page (and its
  `SessionRosterPanel`/`SharedTrainingPanel` sub-components) plus its nav entry
  (`navigationGroups.ts`'s Calendar group `nav.schedule`/`calendar.schedule` item),
  per the issue's answered clarification that scheduling is now managed from the
  Calendar page rather than a separate section. Session create/edit/cancel/roster
  is already fully covered by the Calendar page's `ClassSessionDetailPanel`
  (unaffected by this change); shared-training request approval
  (`SharedTrainingPanel`'s Approve/Reject UI) has no replacement admin UI yet — see
  `docs/architecture.md`'s "Planned: CalendarEvent Unification" (#360 stage 4) note.
  Removed the `nav.schedule` key and the entire top-level `schedule.*` i18n block from
  `apps/admin/locales/base/{en,ca,es}.json`. No backend/API changes — `bookings.ts` and
  `shared-training-requests.ts` are untouched.
- **#481 (done)**: Eligible Membership Plans + Public Event on Activity Types —
  migration 139 adds `activity_types.public_event TINYINT NOT NULL DEFAULT 1`
  (defaults true for backward compatibility — every pre-existing/zero-config
  activity type stays bookable by anyone) and join table
  `activity_type_eligible_plans` (gym_id/activity_type_id/membership_plan_id,
  inverse semantics of `membership_plan_centers`: the join table only gates
  bookings when `public_event=false`). New `GET`/`PUT /activity-types/:id/eligible-plans`
  (replace-all, mirrors `spaces.ts`'s `/:id/activity-types`). New
  `api/src/api/activity-eligibility.ts` registers a `registerBookingAccessHook`
  enforcing this at booking time — independent from `plan-allowances`/
  `package-credits` entitlement checks — registered before both in `app.ts`.
  Added a generic staff-override mechanism spanning both hook families:
  `AccessHook`'s optional `opts?: { overrideAccess }`, `bookMemberOnSession`'s
  trailing `overrideAccess` param, `POST /bookings`'s `override_eligibility` body
  field (main booking hook loop only, not waitlist promotion), and matching
  bypass checks in `plan-allowances.ts` and `activity-eligibility.ts`;
  `POST /bookings`'s error response now also includes `code`. Admin Activity
  Type editor gets a "Booking Access" section (Public Event checkbox + Eligible
  Membership Plans checkbox multi-select, Spaces-style) in the inline edit
  form, read-only expanded row, and Details modal; `ClassSessionDetailPanel`
  gets a confirm-and-retry override flow for `plan_not_eligible`/`plan_required`/
  `allowance_exhausted`/`center_not_covered`, mirroring the existing
  over-capacity "book anyway" pattern. Tests added to `activity-types.test.ts`
  and `bookings.test.ts`.
- **#483 (done)**: Fix missing Sellable Item names in Membership Plan Charge
  Benefits — the backend name resolution (`membership-plans.ts`'s
  `COALESCE(gym_charges.name, charge_types.name) AS gym_charge_name`, used by
  `enrichPlan` and both `/:id/charge-benefits` endpoints) was already correct
  for custom Sellable Items (`gym_charges.charge_type_id IS NULL`, `name` set
  directly at creation). The actual bug was in the admin Charge Benefits
  editor (`apps/admin/.../plans/page.tsx`): the row-selector list (populated
  from `GET /sellable-items`, the full catalog) rendered `gc.charge_type_name`
  — a field only populated via the legacy `charge_types` LEFT JOIN — instead
  of the item's own `gc.name`, so any custom Sellable Item (no `charge_type_id`)
  showed a blank label while configuring a benefit. Fixed by adding `name` to
  the frontend `GymCharge` type and rendering it instead. Regression tests
  added to `plan-charge-benefits.test.ts` covering a custom Sellable Item's
  name resolving through `GET/PUT /charge-benefits`, the enriched
  `GET /membership-plans/:id` and `GET /membership-plans` responses, and the
  `sellable_items` catalog on the enriched plan.
- **#484 (done)**: Professional Services — new `professional_services` table
  (migration 140; global system rows with `gym_id = NULL`, or gym-owned custom
  rows) representing a stable catalog of services a gym offers (Personal
  Training Individual, Personal Training Duo, Group Class, Nutrition
  Coaching, Physiotherapy seeded as system rows), deliberately independent
  from Activities/Sellable Items/Plans/Staff Roles (all out of scope for this
  ticket); new `gym_professional_services` join table holds the per-gym
  active/inactive state so the same system service can be enabled in one gym
  and disabled in another without touching the global row, seeded for every
  existing gym by the migration and for new/duplicated gyms in `gyms.ts`; new
  `/professional-services` router under the ORGANIZATION module gate
  (GET/GET:id open to any ORGANIZATION read role, POST/PUT/activate/
  deactivate/duplicate/DELETE `requireRole('admin')`) — system records can be
  activated/deactivated/duplicated but never edited or deleted (403), custom
  records support full CRUD with soft-delete; `recycle-bin.ts` extended with
  the `professional_service` entity type (custom records only); new admin
  page at `/professional-services` (Organization nav group) using the
  expandable-row/inline-CRUD pattern (no `CrudModal`), with a System badge,
  Activate/Deactivate/Duplicate/Delete context-menu actions, and Delete
  hidden for system rows; i18n `professional_services` namespace + `nav.
  professional_services` key added in en/es/ca; `professional-services.test.ts`
  covering tenant isolation, auth/role gating, happy path, system-record
  protection, and per-gym-independent activation state.
- **#485 (done)**: Add Membership Plan Pricing and Billing Events Forecast —
  read-only, dynamically-calculated projection of a Plan's next 10 billing
  events (price + `billing_policies.recurring_billing_interval`/`_unit` +
  `plan_charge_benefits`), never persisted (no new table/migration). New pure
  module `domain/billingForecast.ts` (`computeBillingForecast`,
  `applyChargeBenefit`) reuses `billing.ts`'s existing `advanceBillingDate`
  for date math instead of a second billing-cadence implementation.
  `enrichPlan` embeds the result as `billing_forecast` on every Plan returned
  by `GET /membership-plans` and `GET /membership-plans/:id`, so it
  recalculates automatically whenever price/billing-policy/charge-benefits
  are saved; also exposed standalone as `GET /membership-plans/:id/
  billing-forecast` (thin wrapper over the same computation) per the
  ticket's API requirement. Deliberately excludes Promotions — a forecast
  with no price/billing policy configured returns `{ available: false,
  reason }` rather than guessing. Admin UI: new read-only "Billing Events
  Forecast" section at the bottom of the Plans expanded row (below Price
  History), with a "does not include promotions" disclaimer and no
  create/edit/delete controls. Unit tests for the pure forecast module
  (`billing-forecast.test.ts`) + integration tests for the new endpoint and
  the embedded field in `membership-plans.test.ts` (tenant isolation, auth,
  happy path with benefits, unavailable state, no billing-event side effects).
- **#486 (done)**: Add Pay Beforehand and Improve Promotion Forecast — new
  `pay_beforehand_months` column on `promotions` (migration 141, unsigned int,
  `NOT NULL DEFAULT 0` so existing Promotions keep their current forecast
  behavior) selects how many of `paid_months` are already paid beforehand;
  validated on POST/PUT as `0 <= pay_beforehand_months <= paid_months`
  (a PUT that patches only one of the two fields validates against the
  other's current DB value). The Free/Pay/Prepaid/Bonus/Regular forecast
  classification — previously duplicated client-side — now lives in the new
  pure module `domain/promotionTimeline.ts` (`computePromotionTimeline`,
  `validatePayBeforehandMonths`), never persisted, exposed as a config-only
  preview endpoint `GET /promotions/timeline` (query params, not tied to a
  saved Promotion id) so the admin page's live example-timeline can call the
  backend for the classification rule instead of re-implementing it. New
  statuses (stable keys, translated in the frontend): `free_promotion`,
  `pay_promotion`, `prepaid_promotion`, `bonus_promotion`, `pay_regular`
  (terminal, open-ended) — user-facing labels are "Free (promotion)" / "Pay
  (promotion)" / "Prepaid (promotion)" / "Bonus (promotion)" / "Pay
  (regular)", replacing the old "Paid (promotion)"/"Paid (regular)" wording.
  Admin UI: new "Pay Beforehand (months)" input in the Billing & Duration
  section; the example timeline is now fetched from `GET /promotions/
  timeline` (debounced on every unsaved field change) instead of computed
  inline. Unit tests for the pure timeline module
  (`promotion-timeline.test.ts`, covering the ticket's worked examples) +
  integration tests in `promotions.test.ts` (CRUD/validation for the new
  field, duplicate copies it, the new endpoint's happy path/validation/auth).
  - **Nutrition Plan Templates: training-related meal types (#502)**: `meal_type`
    on `nutrition_plan_template_meals` and `member_nutrition_plan_meals` now
    accepts 3 additional values — `two_three_hours_before_training`,
    `immediately_before_training`, `immediately_after_training` — alongside
    the existing 7 (migration 143, guarded drop-and-recreate `CHECK`
    constraint widening, same idempotent pattern as migration 123). Backend
    `MEAL_TYPES` whitelist updated in all three routers that validate it
    (`nutrition-plan-templates.ts`, `platform-nutrition-plan-templates.ts`,
    `member-nutrition-plans.ts`); admin `NutritionPlanTree.tsx` meal-type
    selector updated to match. New `tree_meal_type_*` keys added to admin
    en/es/ca locales and `meal_type.*` keys added to member en/es/ca locales.
- **#487 stage 1 (done)**: Configurable Membership Fee Benefit on Promotion
  Period Benefits — staged rollout, this PR delivers stage 1 only (schema +
  validation + inline admin UI); stages 2-4 (shared calc function, wiring
  into real billing via `computeFinalPrice`, Forecast/timeline surface) are
  separate future PRs. `promotion_period_benefits` gains nullable
  `action VARCHAR(30)` + `value DECIMAL(10,2)` (migration 144), mirroring
  `promotion_charge_benefits`'s existing action/value CHECK
  (`no_benefit`/`waive`/`percentage_discount`/`fixed_discount`/`fixed_price`);
  both columns nullable with no backfill, so existing (non-Membership-Fee)
  period benefit rows are unaffected. Validated in `validatePeriodBenefit()`
  across all four write paths in `promotion-details.ts`
  (`PUT`/`POST /period-benefits`, `PUT /period-benefits/:pbId`, `GET` just
  returns the columns) plus the duplicate endpoint in `promotions.ts` (which
  also now copies `duration_months`, a pre-existing gap). Admin UI: the
  Period Benefits row for Membership Fee gets the same inline Action
  selector + dynamic Value input as Charge Benefits (switching actions
  clears the value), reusing the existing `cb_action_*` i18n keys — no new
  locale strings needed. Extended `promotions.test.ts` with round-trip,
  validation-boundary, backward-compatibility, tenant-isolation and auth
  coverage for the new columns.
- **#487 stage 2 (done)**: shared calc function for Promotion Charge/Period
  Benefit actions, and closes the pre-existing `fixed_price` gap in real
  billing — stages 3-4 (wiring Period Benefits' `frequency`/`duration_months`
  window into `computeFinalPrice`, Forecast/timeline surface) remain separate
  future PRs. New `applyPeriodBenefit(amount, action, value)` in
  `api/src/domain/promotionBenefits.ts` is the single place that turns an
  action/value pair into a resulting amount for the 5 stable actions
  (`no_benefit`/`waive`/`percentage_discount`/`fixed_discount`/`fixed_price`),
  clamped at 0 — mirrors `billingForecast.ts`'s `applyChargeBenefit`, but that
  function is for the unrelated Plan Charge Benefits forecast (ticket #485,
  explicitly excludes promotions) so it was left untouched. `computeFinalPrice`
  in `membership-promotions.ts` now calls the shared function instead of its
  own inline `if`/`else`, which adds the `fixed_price` branch it was missing.
  While wiring this up, found and fixed two pre-existing bugs blocking any
  real use of Promotion Charge Benefits on `membership_fee`: (1)
  `computeFinalPrice`'s query selected `p.base_price` from `membership_plans`,
  a column migration 058 dropped in favor of `membership_plan_prices` —
  every call 500'd; fixed by reading `user_memberships.base_price` (the
  snapshot `effectivePrice()` always sets at assignment time) directly,
  no plan join needed. (2) `promotion_charge_benefits`'s `action` CHECK was
  never actually widened to allow `fixed_price`: migration 102 dropped
  `pcb_action_check` and re-added it with 5 values, but the constraint that
  existed at the time (from migration 092) was actually named
  `chk_prcb_action` — the misnamed `DROP` silently no-opped, leaving the
  stale 4-value constraint active alongside the new one, and MySQL enforces
  all CHECK constraints, so every `fixed_price` charge benefit was silently
  rejected since 102 shipped. New migration 145 drops the stale constraint.
  New `promotion-benefits.test.ts` (unit, no DB) covers all 5 actions.
  New `membership-promotions.test.ts` (integration) exercises the real
  `POST /user-memberships/:id/promotions` endpoint for all 4 non-trivial
  actions including `fixed_price`, plus auth/tenant-isolation coverage this
  router previously had none of; `helpers.ts`'s `cleanupTestGyms` extended
  to delete `user_membership_promotions` and `billing_events` (the former
  needed first — its `promotion_id` FK is `RESTRICT`, and it was blocking
  the existing `DELETE FROM promotions` cleanup step for any test creating
  applied promotions).
- **#487 stage 3 (done)**: wires Membership Fee Period Benefits into real
  billing — stage 4 (Forecast/timeline surface) remains a separate future
  PR. `computeFinalPrice` (`membership-promotions.ts`) now also queries
  `promotion_period_benefits` for the applied promotion(s), filtered to
  `charge_types.code = 'membership_fee'` and `enabled = 1`, and applies each
  via the shared `applyPeriodBenefit` (stage 2) after the existing Charge
  Benefit loop, so the two stack. Unlike Charge Benefits (which apply
  unconditionally for as long as the promotion is applied), a Period
  Benefit is additionally gated by its `duration_months` window, counted
  from `user_membership_promotions.applied_at`: active while
  `applied_at + duration_months months > NOW()`, or always active when
  `duration_months IS NULL`. `quantity`/`frequency_interval`/`frequency_unit`
  are intentionally not part of this gate — they describe how a count-based
  benefit (e.g. free sessions) recurs, not whether the Membership Fee action
  itself is in effect. `computeFinalPrice` only runs at existing mutation
  points (assignment, promotion apply/revoke) — there is still no scheduled
  job that reverts `final_price` on its own the moment a `duration_months`
  window lapses without a new mutation; that remains a possible future
  stage, not added here since the ticket thread's question about it wasn't
  answered either way. Extended `membership-promotions.test.ts` with
  coverage for: waive/fixed_price/fixed_discount period benefits taking
  effect, a `null duration_months` never expiring, a benefit past its
  `duration_months` window being excluded on the next recompute, a
  `enabled=0` benefit being a no-op, a benefit on a non-`membership_fee`
  charge type being ignored, and Charge + Period benefits stacking.
- **#482 (done)**: Add Recurring Availability Hours and Bookable Slots to
  Activities — 4-stage rollout agreed in the issue thread, landed as #519
  (weekly rule windows sliced into `duration_minutes`-sized bookable
  `calendar_events`, non-blocking `slot_warning` when a window doesn't
  divide evenly), #520 (rule edit/delete preserve already-booked future
  occurrences instead of blind cancel+regenerate; a booked occurrence that
  no longer fits blocks with `409 booked_occurrences_impacted` until staff
  confirms, then cancels and notifies affected members via the existing
  in-app `member_notifications` channel — no email, since the codebase has
  no mailer infrastructure), #521 (non-blocking `overlap_warning` when a
  rule's window overlaps another rule sharing the same space or trainer),
  and this stage (Admin UI): no new Availability section was needed since
  the existing generic weekly-rule form (multi-weekday + start/end time,
  reused across any Activity Type) already produces the sliced slots; the
  Activity Types admin page now surfaces `slot_warning`/`overlap_warning`
  as info toasts on save and handles the `409` rule edit/delete response
  with a `ConfirmDialog` that resubmits with `confirm_cancel_booked: true`
  once staff confirms. No frontend test added — `apps/admin` has no test
  harness in this repo (no vitest/testing-library configured).
- **#488 (done)**: Added `logo_contains_gym_name` (migration 146) — a single
  boolean on `themes`, shared by Base and Customer Themes, describing whether
  the configured logo already contains the gym's name. Threaded through every
  theme-shaped API response (`themes.ts`, `gym-themes.ts`, `me.ts`'s
  `GYM_THEME_SELECT`/`mapGymRow` used by `/me/gym` + `/me/gyms`, `gyms.ts`'s
  `THEME_SELECT`/`attachTheme` used by the admin gym list) and on the public
  `GET /payment-page/token/:token` response (`logoUrl`, `logoContainsGymName`,
  via a new `LEFT JOIN themes`). Admin UI: checkbox directly under the Logo
  field on both the Base Theme and Customer Theme editors. Per the scoping
  answer on the issue thread, this also builds first-time logo+name header
  rendering where none existed: Admin's `TopHeader.tsx` (previously a pure
  logo-XOR-name toggle, now renders both unless the flag suppresses the name),
  Member Web's `TopBar.tsx` home-screen slot (no logo/name rendering existed
  there before), and the Payment app's `checkout.html`/`checkout.js` (new
  `<img id="gym-logo">`, gated by the flag) — the Cloudflare logo-upload
  feature itself is deferred to a follow-up ticket per the same thread.
  Payment app's `nginx.conf` gained a `/themes/` proxy location (alongside
  the existing `/payment-page/` one) so the logo image stays same-origin
  under its CSP `img-src 'self'`.
- **#489 stage 1 (done — foundation)**: Improve Theme Color System —
  staged rollout agreed on the issue thread (1: foundation, 2: new semantic
  color tokens + delete the Advanced section by redistributing its settings
  into the appropriate sections, 3: wire shared components to the new
  tokens, 4: Member Web + Pay theming parity, 5: tests + docs). Extracted
  the byte-identical `validateTokens()`/`defaultTokens()`/`HEX_RE`/
  `FONT_STACKS` duplicated verbatim across `api/src/api/themes.ts` and
  `api/src/api/gym-themes.ts` into a new shared `api/src/domain/themeTokens.ts`
  (same pattern as `domain/muscles.ts`), and fixed a UI categorization bug:
  `cardBorder` is already a standard, validated color (not an Advanced one)
  but both Admin theme editors (`[locale]/themes/page.tsx` and
  `[locale]/system/themes/page.tsx`) rendered its control under the
  "Advanced" section — moved into the standard Colors section's
  `group_application` group next to `cardBackground`; no data-model change.
  The originally proposed interim "wire `tokens.advanced` to CSS variables"
  step was dropped from this stage since the agreed plan now deletes the
  Advanced section outright in stage 2 rather than keeping it wired — that
  work would have been thrown away immediately after.
- **#489 stage 2 (done — new semantic tokens + Advanced section retired)**: added the five new
  `colors.*` fields from the ticket to the shared `themeTokens.ts`
  (`secondaryTextColor`, `mutedTextColor`, `separatorColor`,
  `inputBorderColor`, `inputBackgroundColor`) and to the Admin frontend's
  `apps/admin/src/lib/themeTokens.ts`. Per §19, `textColor` is *aliased* to
  "Primary Text Color" in the editor rather than replaced — same persisted
  field, no migration needed. Per the issue thread's naming answer, the new
  generic `separatorColor` is labeled "Application Separator Color" and the
  existing header-scoped one relabeled "Header Separator Color" so the two
  are no longer ambiguous (§7). `applyTokens()` in both Admin theme editors
  now also wires the previously dead `--gd-text-muted`, `--gd-border` and
  `--gd-input-bg` CSS variables (already referenced by `cordel/feature-flags`,
  `financials/taxes`, `financials/sellable-items`, `professional-services`,
  `calendar/MemberSearchInput`, `calendar/EventDetailsPanel`) to the new
  fields, plus a new `--gd-input-border`/`--gd-text-secondary` pair for
  stage 3 to consume — with a `?? DEFAULT_TOKENS.colors.*` fallback so
  Themes/Centers persisted before this stage (missing the new keys) don't
  render `undefined` custom properties. Both Admin theme editors reorganized
  their Colors section per the ticket's §11 target structure: new **Text**
  (`textColor`, `secondaryTextColor`, `mutedTextColor`), **Separators**
  (`separatorColor`) and **Inputs** (`inputBackgroundColor`,
  `inputBorderColor`) groups, alongside the existing Application/Header/
  Sidebar/Navigation/Buttons/Status/Links groups; the theme-load merge in
  both editors now deep-merges `colors` with `DEFAULT_TOKENS.colors` so an
  old theme's color pickers show sensible values (and get migrated to the
  full field set on next save) instead of rendering blank/black. New i18n
  keys added to `themes`/`gym_themes` in en/es/ca.

  **Remainder of stage 2 (this PR)**: retired the standalone "Advanced"
  section per the issue thread's answer ("delete the advanced and add every
  setting into each appropriate section: header, calendar, side, cards,
  global, etc"). Each of the ~52 `ADVANCED_ATTRIBUTES` entries
  (`apps/admin/src/lib/themeTokens.ts`) now carries one of the same `group`
  keys the standard color groups use, instead of its own `adv_group_*` key:
  Header- and Sidebar-specific attributes (e.g. `headerHeight`,
  `sidebarWidth`) moved into **Header**/**Sidebar**; button/input/status
  attributes (e.g. `primaryBtnHoverBg`, `inputFocusBorderColor`,
  `successBg`) joined the existing **Buttons**/**Inputs**/**Status** color
  groups; dropdown-shape attributes joined **Navigation**. Two groups had no
  existing color-group home: a new **Cards** group (which also absorbs the
  existing `cardBackground`/`cardBorder` colors, previously miscategorized
  under Application) holds `cardBorderRadius`/`cardShadow`, and a new
  colorless **Tables** group holds the five table-density attributes.
  Everything else with no single-component owner (layout, spacing, global
  shape/shadow/overlay/animation settings) joined **Application**.
  `ThemeAdvancedSection` (`apps/admin/src/components/ThemeAdvancedSection.tsx`)
  now renders one group's attributes at a time via a required `group` prop,
  called from inside each `COLOR_GROUPS` iteration in both Admin theme
  editor pages right after that group's color pickers, instead of once for
  every group inside a separate collapsible section — so a component's
  colors and its fine-grained attributes now live together. No data-model
  change: `advanced` stays the same free-form JSON map, only which UI
  section renders each key changed. Removed the now-unused `section_advanced`
  and 12 `adv_group_*` i18n keys from `themes`/`gym_themes` in en/es/ca.
  Stages 3–5 remain (wiring shared components to consume these attributes,
  Member Web + Pay theming parity, tests/docs).
- **#489 stage 3 (done — shared components wired to global tokens)**: per the
  issue thread's answer ("Shared components should land into the global
  section"), wired the Admin app's shared, widely-imported component library
  to consume the global `--gd-*` CSS variables (`--gd-text`,
  `--gd-text-secondary`, `--gd-text-muted`, `--gd-border`, `--gd-card-bg`,
  `--gd-app-bg`, `--gd-input-bg`, `--gd-input-border`, and the pre-existing
  `--gd-dropdown-bg`/`--gd-dropdown-text`/`--gd-dropdown-hover-bg`) instead of
  hardcoded hex, each with a `var(--gd-x, <default-token-hex>)` fallback so
  unthemed contexts render identically to before:
  - `DataTable.tsx` (9 consumers) — header row and expanded-row background,
    row/expanded-row separators, card background, chevron and loading/empty
    text now theme-driven.
  - `ui.tsx`'s `modalStyle`, `CrudModal.tsx`'s `FormInput`/`FormLabel` (28
    consumers), `Toast.tsx` (53 consumers), `ContextMenu.tsx` (31 consumers),
    `StatusFilter.tsx` (20 consumers) and `MultiSelectFilter.tsx` (5
    consumers) — input/card/popover backgrounds, borders and secondary/muted
    text converted. `ContextMenu`'s and `MultiSelectFilter`'s dropdown panels
    use the existing Navigation-group dropdown tokens
    (`--gd-dropdown-bg`/`--gd-dropdown-text`/`--gd-dropdown-hover-bg`), which
    is a closer semantic match than the generic card tokens. Destructive/danger
    text (`#c0392b`) and status colors (toast border/icon) are intentionally
    left as-is — out of scope for this ticket's five neutral tokens.
  - Deliberately **not** touched: the long tail of one-off, page-level inline
    styling (`calendar/EventDetailsPanel`, `calendar/MemberSearchInput`,
    `financials/taxes`, `financials/sellable-items`, `professional-services`,
    `cordel/feature-flags`, plus 10+ other pages found to have 3+ repeated
    hardcoded border/background occurrences) — these are already partially
    wired from stage 2 and are not "shared components" per the ticket's own
    scoping instruction (§17: "Do not attempt to rewrite every visual
    component in the platform as part of this ticket. Focus on standard/shared
    components"). Tracked as follow-up cleanup, not part of this stage.
  - No data-model, API, or migration change — pure CSS-variable consumption
    in existing component styles. Stages 4–5 remain (Member Web + Pay theming
    parity — `apps/member`'s `ThemeProvider` doesn't yet emit these tokens at
    all; tests/docs for the full semantic system).
- **#489 stage 4 (done — Member Web + Pay theming parity)**: per the issue
  thread's answer ("Yes, changes in theme should impact members and pay
  applications"), extended the full `--gd-*` token set to the other two
  theme-consuming apps.
  - **Member Web**: `apps/member`'s `ThemeProvider` was a hand-maintained
    subset of Admin's — it only ever set ~10 of the ~30 `--gd-*` variables
    (no `--gd-card-bg`, `--gd-card-border`, `--gd-text-secondary`,
    `--gd-text-muted`, `--gd-border`, `--gd-input-*`, `--gd-dropdown-*`,
    `--gd-primary/secondary-btn*`, `--gd-status-*`, `--gd-link*`), so
    components that already referenced those names (`GymSwitcher.tsx`'s
    `--gd-card-bg`/`--gd-card-border`) were silently stuck on their hardcoded
    fallback regardless of theme. New `apps/member/src/lib/themeTokens.ts`
    mirrors `apps/admin/src/lib/themeTokens.ts`'s `ThemeTokens` shape,
    `DEFAULT_TOKENS`, and `applyTokens()` byte-for-byte (member has no editor,
    so the admin-only `tokensEqual`/`getLiveTokens` draft helpers are
    omitted); `ThemeProvider.tsx` now calls this full `applyTokens()` instead
    of its old ~10-variable subset. Member Web has no center-level theme
    override today (`/me/centers` doesn't return `theme_tokens`), so unlike
    Admin's center-then-gym-then-default chain, this only resolves
    gym-theme-then-default — that parity gap is unrelated to this ticket.
  - **Pay**: the public `GET /payment-page/token/:token` endpoint
    (`api/src/api/payment-page.ts`) previously returned only `logoUrl`/
    `logoContainsGymName` from the gym's theme. It now also returns a new
    `themeColors` field — a curated subset of `tokens.colors` (page/card
    background, card border, primary/muted text, primary button + its text,
    status error, separator, input border/background) picked to match what
    `checkout.html` actually renders; `null` when the gym has no theme or the
    theme predates tokens with a `colors` key. Deliberately not the full
    tokens blob (typography/`advanced` aren't used on this page, and this is
    an unauthenticated endpoint — no reason to widen it further than
    display needs). `apps/payment/js/checkout.js` applies these via a new
    `applyThemeColors()` that `setProperty`s the CSS variables
    `apps/payment/css/style.css` already defines; `style.css` gained
    `--card-border` (previously the same generic `--border` as the consent
    box) and `--input-border`/`--input-bg`/`--accent-text` (previously a
    hardcoded `#fff` button text color) so the Monei card-input container and
    the card's own border can be themed independently of the page's generic
    divider color, matching Admin/Member's existing
    cardBorder/separatorColor/inputBorderColor/inputBackgroundColor
    distinction. Every new variable keeps its current hardcoded value as the
    CSS default, so an unthemed or legacy-theme checkout page renders
    identically to before.
  - No migration — `tokens` is an existing JSON column on `themes`; no schema
    change. Stage 5 (tests + docs for the full semantic system) remains.
- **#489 stage 5 (done — tests + docs; epic complete)**: closed out the
  ticket's §20 testing checklist for the pieces this repo can actually
  exercise. `api/src/domain/themeTokens.ts` (validation of the five stage-2
  semantic fields, partial/legacy tokens shapes) already had unit coverage
  from stages 1–2. This stage adds HTTP-level integration coverage in
  `api/src/test/themes.test.ts` (Base Themes) and
  `api/src/test/gym-themes.test.ts` (Customer Themes): the new
  `secondaryTextColor`/`mutedTextColor`/`separatorColor`/
  `inputBorderColor`/`inputBackgroundColor` fields persist and round-trip
  through POST/PUT/GET; `separatorColor` and `headerSeparatorColor` persist
  as independent values (§7); an existing `tokens.advanced` map round-trips
  byte-for-byte through an update that doesn't touch it, confirming the
  data layer neither duplicates nor drops promoted/legacy Advanced values;
  and a legacy/partial `tokens.colors` shape (missing the newer fields)
  still validates, persists and reads back without error, matching how a
  pre-stage-2 Theme actually looks in the database (§19's migration
  requirement — no backfill needed since the API never required
  completeness). No frontend test harness exists in this repo to
  automate Admin/Member Web/Pay's *visual* consumption of these tokens
  (consistent with prior UI-only PRs in this series, e.g. #526/#528/#529);
  that verification remains manual, as called out in each stage's PR test
  plan. Docs were already kept current stage-by-stage (this roadmap entry
  and `docs/architecture.md`'s Theming section); no further doc changes were
  needed beyond this entry. All five stages of the staged plan agreed on the
  issue thread are now merged — closes #489.
- **#492 (done)**: Add Explicit Save / Cancel Workflow to Theme Editor with
  Live Preview — per the issue thread's scoping answers, consolidated Base
  Themes (`[locale]/system/themes/`) and Customer Themes (`[locale]/themes/`)
  onto one shared editor implementation (`apps/admin/src/components/
  ThemeTokensEditor.tsx`: `COLOR_GROUPS`, `ThemeColorsEditor`,
  `ThemeTypographyEditor`, replacing the byte-identical Colors/Typography
  JSX each page had duplicated) rather than building a second, different
  mechanism. Base Themes already had an explicit draft with Save/Cancel but
  no live preview; Customer Themes' Colors/Typography/Advanced instead
  auto-saved via a 600ms-debounced `PUT` per field change (`#188`) — both
  are gone, replaced by: every field edits an in-memory draft only;
  Colors/Typography/Advanced changes call `applyTokens()` directly against
  the live app chrome for immediate feedback (per the thread's answer, no
  sandboxed preview surface); a single Save persists the whole draft
  (branding + `tokens`) in one request; Cancel discards the draft and
  restores whatever tokens are actually live right now via the new
  `getLiveTokens()`/`tokensEqual()` helpers in `lib/themeTokens.ts` (same
  center-then-gym-then-default resolution `ThemeProvider` already uses),
  independent of which theme was being edited. Per the thread's answer,
  logo removal ("clear logo") is now deferred the same way: it only sets a
  pending-removal flag on the draft, and the `DELETE /logo` call fires on
  Save (previously an immediate delete on click, the one other
  immediate-persistence gap on both pages). Save is disabled while the
  draft matches the last-persisted state; both pages gained the same
  `beforeunload` + confirm-discard-before-switching-theme guard already
  used by Members/Training Plans. Fixed a latent bug found while unifying
  the two pages: Customer Themes rendered Base Themes' color pickers as
  interactive and wired to the same autosave path, but `gym-themes.ts`'s
  `PUT` only matches `gym_id`-owned rows, so every edit 404'd silently —
  those pickers are now `readOnly`, matching the existing "read-only" hint
  text. No migration, no new endpoints — reuses the existing Theme update
  API per the ticket.
- **#513 (done)**: Add NIF/NIE/Passport Field to Members with Document
  Validation — migration 147 adds nullable `members.nif_nie_passport
  VARCHAR(20)` (no uniqueness constraint, no index — #515 will add search/
  filter on it later). New pure-function validator
  `api/src/domain/documentId.ts` (`validateDocumentId`, `maskDocumentId`)
  implements NIF (8 digits + mod-23 control letter against
  `TRWAGMYFPDXBNJZSQVHLCKE`), NIE (X/Y/Z→0/1/2 prefix + 7 digits, same
  control-letter algorithm), and Passport (alphanumeric, 3–20 chars, no
  control letter) with type auto-detection (NIE shape → NIF shape →
  Passport) — a value with NIF/NIE shape but a bad control letter is
  rejected outright, never silently downgraded to Passport; a
  whitespace-only value is rejected, an empty/`null` value clears the
  field. Mirrored verbatim at `apps/admin/src/lib/documentId.ts` for
  client-side feedback, following the existing no-shared-package
  mirroring convention (`themeTokens.ts`, `permissions.ts`) since this
  repo has no built package between `api/` and `apps/admin/`. `POST`/`PUT
  /members` validate and persist the normalized value, returning
  `400 { error }` on invalid input without writing it; `GET /members` and
  `GET /members/:id` already returned it via their existing `SELECT *`.
  Audit: reuses the existing `recordAudit()` create/update calls, but
  masks the value (`maskDocumentId` — all but the last 4 characters) in
  the `next` payload so the raw document number is never written to
  `audit_logs`. Admin UI: field added to the Members Add modal, the
  inline `MemberEditForm` (with immediate inline validation error/help
  text), and `MemberDetailModal`, labeled "NIF/NIE/Passport"; i18n keys
  added to en/es/ca. New unit test file `document-id.test.ts` (24 tests,
  no DB) plus `members.test.ts` additions covering create/update with
  valid NIF/NIE/Passport values, rejection of bad control letters and
  whitespace-only input with a structured error and no persisted row, and
  round-trip through GET.
- **#515 (done)**: Display and Filter by NIF/NIE/Passport in Members —
  builds on #513's `members.nif_nie_passport` column (no index added: the
  filter is a leading-wildcard `LIKE '%term%'` contains-search, which a
  B-tree index can't serve any better than a full scan, matching the
  existing unindexed `q` name/email filter). `GET /members` gains a
  `nif_nie_passport` query param — trimmed, matched via a plain
  `LIKE '%...%'` against the column (MySQL 8's default
  `utf8mb4_0900_ai_ci` collation is already case-insensitive, so no
  `LOWER()` wrapping is needed, mirroring the existing `q` filter); no
  document validation is applied to the search term, so incomplete or
  invalid fragments still match, and leading zeros are preserved (the
  value is never parsed as a number). Unlike `q`, this filter isn't capped
  at 20 rows — it behaves like the existing `payment_status`/
  `enrollment_status` filters, returning the full matching set. Admin
  Members page: new fixed "NIF/NIE/Passport" column (`—` when absent,
  matching the existing empty-value convention) and a new text filter
  input next to the name/email search box, wired through the same
  `buildParams`/`syncUrl` URL-persistence pattern as the other filters;
  reuses the existing `label_document` translation ("NIF/NIE/Passport")
  for both the column header and the filter's placeholder/`aria-label`
  rather than introducing a separate label. No new permission model —
  `GET /members` was already gated behind `requireModuleAccess('MEMBERS')`
  and already returned the raw field via `SELECT m.*`. `members.test.ts`
  gains a 9-test `nif_nie_passport filter (#515)` block (absent filter,
  case-insensitive partial match, fragment match, leading-zero
  preservation, no control-letter/length requirement, no-match empty
  array, tenant isolation, combining with `q`, and the auth guard).
- **#516 (done)**: Add NIF/NIE/Passport to Assigned Plans — extends #410/
  #411's `GET /user-memberships` (already `JOIN`s `members AS m`) rather
  than introducing a duplicate field: `LIST_SELECT` adds
  `m.nif_nie_passport AS member_nif_nie_passport`, and the list endpoint
  gains a `nif_nie_passport` query param — trimmed, matched via
  `m.nif_nie_passport LIKE '%term%'` in the filter's inner `WHERE`
  (unlike `lifecycle_status`, this doesn't need the outer derived-table
  indirection since it's a plain joined column, not a `CASE` alias) —
  mirroring #515's `members.ts` filter verbatim: no document validation
  applied, leading zeros preserved, case-insensitive via MySQL 8's default
  collation. No migration: reuses the `members.nif_nie_passport` column
  from #513. Admin `financials/assigned-plans` page: the Member column
  now shows the related Member's `NIF/NIE/Passport` as a secondary line
  under the name (`—` when absent), and a new text filter input added to
  the existing filter bar (`nif_nie_passport` param, wired through
  `load()`/`hasFilters`/`clearFilters` alongside the other filters); new
  `label_document`/`filter_document` i18n keys in en/es/ca reusing the
  existing "NIF/NIE/Passport" wording. `user-memberships.test.ts` gains a
  6-test `nif_nie_passport filter (#516)` block (case-insensitive partial
  match, fragment match, absent-filter passthrough, no-match empty array,
  combining with `member_id`, and the joined field appearing in the
  response).
- **#511 stage 4 of 5 (done — Assigned Plans page frontend)**: builds on
  stages 1-3 above; the last stage with user-visible scope for this epic
  (stage 5's wrap-up work was already folded into stages 1-3, as noted
  below). `financials/assigned-plans/page.tsx` rows are now expandable via
  the shared `DataTable` component (the Members-page pattern) instead of a
  flat table; expanding a row mounts the new `AssignedPlanExpandedRow.tsx`,
  which fetches stage 3's `GET /user-memberships/:id` expanded detail and
  renders members, pricing/billing config, benefit usage, applied
  promotions, and the Billing Events view directly in the card — Created/
  Modified audit metadata is deliberately left out of the card and shown
  only in the new `AssignedPlanDetailsModal.tsx` (mirrors
  `PromotionDetailModal`'s field/value + Audit-section layout, fed from the
  already-loaded detail rather than a second fetch). A per-row
  `ContextMenu` exposes Details (always), Edit (`draft`/`awaiting_payment`
  only — extended past the ticket's original Awaiting-Payment-only scope
  since `draft` didn't exist when §6's matrix was written, and the issue
  thread's Q1 answer explicitly allows configuring a draft before Submit),
  Submit (`draft` only), Pause (`active` only), Reactivate (`paused` only),
  and Close (`awaiting_payment`/`active`/`paused`, admin-only). Edit
  switches the card into an inline Save/Cancel form (starts_at/ends_at/
  final_price/discount_reason/discount_expires_at — the fields `PUT /:id`
  actually accepts), matching `MemberExpandedRow`'s inline-edit shape
  rather than a modal. Close always confirms first, then — on a `409
  unused_value_impacted` response — shows a second confirmation listing the
  returned `warnings` before resending with `confirm: true`. `StatusBadge`
  and the `status.*` locale namespace gained an `awaiting_payment` entry
  (en/es/ca); a new `apps/admin/src/test/assigned-plans-locales.test.ts`
  (mirrors `member-locales.test.ts` from #540/#563) guards the
  `assigned_plans_page.*` key set staying complete across all three
  locales. This closes out Assigned Plans Management (#511) end to end.
- **#511 stage 3 of 5 (done — Expanded detail + Billing Events endpoint)**:
  builds on stages 1-2 below. `GET /user-memberships/:id` is now the
  "expanded card" endpoint (mirroring `enrichPlan()`'s pattern in
  `membership-plans.ts`), additionally embedding `members`, `billing_policy`,
  the assignment-time `charge_benefits` snapshot, `activity_allowances`
  (allocated/used/remaining, aggregated across covered Members), `promotions`
  (via the same `fetchAppliedPromotions()` now also used by
  `GET /:id/promotions`), and a `billing_events` Billing Events view — also
  exposed on its own via `GET /:id/billing-events`. The range rule (issue
  thread Q2): all billing events affected by a promotion applied to the
  plan, plus the events covering the following two calendar months after the
  last one; with no applicable promotion, the next two calendar months from
  the plan's `starts_at`; clamped to `ends_at`. Computed by the new, pure
  `domain/assignedPlanBillingEvents.ts` (unit-tested, no DB dependency):
  `draft` plans (which never write to `billing_events`) get a non-persisted
  projection from the plan's billing cadence + currently-applied promotions
  (capped at 36 months for an indefinite, never-revoked promotion); every
  other status queries the real, persisted ledger and only ever tags
  (`promotion_affected`) and filters it, preserving historical values. A
  top-level (and per-event, for drafts) `projected: true/false` flag lets the
  frontend distinguish a draft's preview from a submitted plan's persisted
  events. Migration 150 adds `user_membership_promotions.revoked_at`
  (nullable `DATETIME`, stamped on revoke) — together with `applied_at`, the
  `[applied_at, revoked_at]` window used to tag whether a billing event/cycle
  was promotion-affected — plus a `billing_events (user_membership_id,
  created_at)` composite index for the range query. The Close action's
  unused-value check (stage 1) now reuses the same `activity_allowances`
  usage helper instead of only its original pending-`next_billing_date`
  check. Stage 4 (the Assigned Plans page frontend) remains; stage 5's
  wrap-up work was folded into stages 1-3 as it was for #560/#561.
- **#511 stage 2 of 5 (done — Promotion snapshot + audit-derived
  Modified by/at)**: builds on stage 1 below. `user_membership_promotions`
  gains a nullable `snapshot` JSON column (migration 149), populated by a new
  `buildPromotionSnapshot()` in `membership-promotions.ts` at the moment a
  promotion is applied (both `applyPromotionToMembership()` and
  `POST /user-memberships/:id/promotions`) — it captures the promotion's
  name/description/stackable/dates/free-paid-bonus months plus its full set
  of `promotion_charge_benefits`/`promotion_period_benefits`/
  `promotion_included_benefits` rows (joined to `charge_types` for a stable
  code/name), mirroring the existing `user_membership_charge_benefits`
  assignment-time snapshot pattern (#376) as a single JSON column rather than
  relational rows, since this is purely a display/history concern. A new
  `withSnapshot()` overlays the snapshot (when present) onto
  `GET /user-memberships/:id/promotions` rows in place of the live
  `promotions` join, so renaming a promotion or changing its benefits after
  the fact no longer rewrites what an already-applied Assigned Plan shows;
  legacy pre-migration rows (`snapshot IS NULL`) keep falling back to the
  live join. Separately, `GET /user-memberships/:id` gains audit-derived
  `created_by_name`/`modified_by_name`/`modified_at` (new `loadAuditMetadata`
  helper querying `audit_logs`, mirroring the existing `promotions.ts`/
  `themes.ts` `:id` pattern) — "modified" is the latest action of any kind
  after creation (edit, submit, close, pause, reactivate, apply/revoke
  promotion, add/remove member), `null` until the first one happens.
  Deliberately not added to `LIST_SELECT`/the list endpoint or the expanded
  card: the ticket requires this audit metadata only in the Details modal.
  Stage 3 (expanded detail + Billing Events endpoint) landed next, see above.
- **#511 stage 1 of 5 (done — Status model + backend transitions)**:
  first stage of Assigned Plans Management (see the issue thread for the
  audited 5-stage plan and its Q&A). `user_memberships.status` gains two
  pre-activation values, `draft` and `awaiting_payment` (migration 148 widens
  the `user_memberships_status_check` CHECK constraint and adds a nullable
  `closed_at DATETIME`, stamped by the new Close action independently of the
  pre-existing, admin-settable `ends_at`); the ticket's "Closed" action maps
  onto the existing `cancelled` value rather than a new terminal status. A new
  `ALLOWED_TRANSITIONS` table in `user-memberships.ts` enforces the ticket's
  §10 rules (`draft`→`awaiting_payment`/`cancelled`,
  `awaiting_payment`→`active`/`cancelled`, `active`↔`paused`/→`cancelled`,
  `paused`→`cancelled`; `expired` has no forward transitions — only ever
  reached by `assign-new-plan`'s supersede logic) — `PUT /:id` now validates
  any direct `status` flip against it (400 on an invalid transition) on top
  of its existing admin-only `cancelled` guard. Four new action endpoints on
  `/user-memberships/:id`: `POST /submit` (`draft`→`awaiting_payment`,
  `requireModuleWrite('PAYMENTS')`), `POST /pause` (`active`→`paused`),
  `POST /reactivate` (`paused`→`active`), and `POST /close`
  (`requireRole('admin')`, mirroring `DELETE`'s existing cancel restriction —
  `awaiting_payment`/`active`/`paused`→`cancelled`, stamps `closed_at`).
  Close first checks for unused value that would be lost and returns
  `409 {error: 'unused_value_impacted', message, warnings}` unless the
  request body includes `confirm: true` (same warn-then-confirm contract as
  `activity-type-schedule-rules.ts`'s `confirm_cancel_booked`) — today that
  check is limited to a scheduled MIT charge (`next_billing_date >=
  CURDATE()`), since per-membership benefit/usage accounting doesn't exist
  yet. Deliberately out of scope for this stage (left for later stages per
  the issue thread): submitting a draft doesn't materialize persisted
  Billing Events yet (nothing in the running system pre-creates future
  `billing_events` rows today), no UI or creation-flow entry point creates a
  `draft` row yet, and full Benefits/Usage accounting for the Close warning
  and the expanded card lands with the Billing Events/Benefits stage.
- **#543 (done)**: Fix Sellable Items showing a blank name + `type_null` for
  the 6 charge-type-based system items (Registration Fee, Insurance Fee,
  etc.) on every gym created or duplicated since migration 102 added
  `gym_charges.name`/`type` — that migration's backfill only ran once, on
  existing rows, but the runtime provisioning inserts in `gyms.ts`
  (`POST /platform/gyms` and `POST /platform/gyms/:id/duplicate`) were never
  updated to also populate `name`/`type` from `charge_types` when seeding
  those rows for new/duplicated gyms, so they shipped with both columns
  NULL. `sellable-items.ts` passes `name`/`type` straight through without a
  fallback, and the admin Sellable Items page's `t(\`type_${item.type}\`)`
  falls back to next-intl's default missing-key string `type_null` when
  `item.type` is `null` (the blank name reads as "System" only because the
  adjacent System badge sits right next to it). Fixed both `gyms.ts` inserts
  to select `name`/`'fee'` from `charge_types` alongside `charge_type_id`
  (mirroring migration 102's original backfill), and added migration 151 to
  repair the already-affected rows (scoped to `name IS NULL`/
  `type IS NULL AND charge_type_id IS NOT NULL`, so custom Sellable Items are
  untouched). Regression tests added to `gyms.test.ts` (both provisioning
  paths) and `gym-charges.test.ts` (`GET /sellable-items` response).
- **#503 (done — all 9 stages of the plan agreed on the issue thread have
  landed)**: Members App — Align Calendar Event Information with Admin
  Calendar. Stage 1, "drop the `kind` distinction": migration 152 removes
  `calendar_events.kind ENUM('session','event')` (added by the #360
  unification) and both its CHECK constraints. `bookMemberOnSession`
  (`bookings.ts`) no longer gates bookings on `kind` — every `calendar_events`
  row is bookable at the primitive/API level, per the issue thread's explicit
  "no separate logic for sessions versus events." All dependent queries
  updated: `me.ts` (`/me/schedule`, `/me/upcoming`, `/me/activity-history`),
  `shared-training-requests.ts`, `scheduleEngine.ts`, `centers.ts`
  (`DEPENDENT_TABLES` center-delete guard), `audit-registry.ts`, and
  `calendar-events.ts`. The admin-facing `classSessionsRouter`/
  `calendarEventsRouter` split is *not* merged by this stage — it now
  partitions by `activity_type_id` (`IS NOT NULL` vs `IS NULL`) instead of
  `kind`, an exhaustive, mutually-exclusive split equivalent to the old one,
  so the admin Calendar page's dual-fetch needed no change. See
  `docs/decisions.md` #11 and `docs/architecture.md`'s "Planned: CalendarEvent
  Unification" section for the full rationale, including the two explicitly
  deferred gaps: merging the two admin detail panels (`ClassSessionDetailPanel`
  vs `EventDetailsPanel` — no UI design has been specified for this yet), and
  extending member-facing discovery to rows without an `activity_type_id`
  (the plan's stage 5, "Unified member read model"). Stage 2, "waitlist mode":
  migration 154 adds `activity_types.waitlist_mode`
  (`disabled`/`open`/`closed`, CHECK-constrained) with a nullable
  per-occurrence override on `calendar_events.waitlist_mode`;
  `bookMemberOnSession` resolves `COALESCE(ce.waitlist_mode,
  at.waitlist_mode)` and now returns 409 instead of silently waitlisting an
  over-capacity booking unless the effective mode is `open` — `force` still
  books past capacity, and promoting someone already queued is deliberately
  never gated. Pre-existing activity types are migrated to `open` (today's
  behavior); new ones default to `disabled` per the issue thread. Admin
  Activity Types page gets the selector in its Booking & Access section
  (en/es/ca), the admin session panel hides "Add to waiting list" when the
  waitlist isn't open, and `/me/schedule` reports `FULL` rather than
  `WAITLIST_AVAILABLE` so members aren't offered a waitlist that would 409.
  See `docs/decisions.md` #12. Stage 3, "activity → future-event field
  propagation": `PUT /activity-types/:id` now pushes edits to
  `default_space_id`/`default_trainer_membership_id`/`default_center_id`/
  `color`/`max_capacity` onto every not-yet-started `calendar_events` row for
  that activity type (past occurrences and existing bookings on affected
  future rows are left untouched — only the listed columns change). No
  schema change: these are exactly the fields `scheduleEngine.
  materializeScheduleRule()` already copies onto `calendar_events` at
  creation time. Since propagation can silently move/reassign already-visible
  future events, it's gated the same way stage 2's booked-occurrence
  protection is: `409 {error: 'future_events_impacted', fields,
  impacted_events, booked_events}` unless the request carries
  `confirm_propagate: true`, surfaced in the Admin UI via the same
  `ConfirmDialog` pattern used for the schedule-rules 409. `waitlist_mode` is
  excluded — it already reaches every event live via the existing
  `COALESCE` fallback, so there's nothing to propagate. See
  `docs/architecture.md`'s Activity types row for the full behavior. Stage 4,
  "activity end-date special case": `PUT /activity-types/:id/schedule-rules/
  :ruleId` now diffs surgically when only a recurring rule's `end_date`
  changes, instead of the general #482 cancel-everything-and-regenerate path.
  Occurrences that stay within the new range — booked or not — are never
  touched; extending the end date only adds the newly in-range occurrences,
  and shortening it removes only the now-out-of-range ones, 409ing with the
  existing `booked_occurrences_impacted` shape (and notifying affected
  members the same way #482 already does) when any of them are booked. No
  schema change and no frontend change were needed: the Admin UI's existing
  generic 409 `ConfirmDialog` and the member app's existing in-app
  notifications center (`docs/architecture.md`'s Member Notifications row)
  already cover this. Stage 5, "unified member read model": `GET /me/schedule`
  gains three fields, additive next to the existing `availability_state`
  (not replaced — the booking-action UI keeps reading that). `status`
  (`scheduled`/`running`/`completed`/`cancelled`) mirrors `calendar_events
  .status` except `running`, which is derived live from `now` falling inside
  `[starts_at, ends_at)` rather than stored, so it never needs a manual
  staff update. `occupancy_status` (`available`/`few_spots_left`/`full`/
  `unavailable`) is capacity-only — `few_spots_left` at ≤20% of capacity
  remaining with a minimum of 1 (so small-capacity activities still report
  it), `unavailable` when access is locked or the row's own `status` isn't
  `scheduled`, matching exactly the gate `bookMemberOnSession` enforces so
  this field never reports a bookable state the booking endpoint would
  reject. `waitlist_status` is the same `effective_waitlist_mode`
  (`disabled`/`open`/`closed`) `/me/schedule` already computed, now also
  exposed under its own name; a new `waitlist_count` aggregate (COUNT of
  `waitlisted` bookings) is added next to it — an aggregate only, per the
  issue thread's privacy requirement that waitlist identities are never
  exposed to members. Pure `computeCalendarEventStatus`/
  `computeOccupancyStatus` helpers in `me.ts` are unit-tested in
  `me-calendar-event-status.test.ts`. **Left open**: `/me/schedule`'s `JOIN
  activity_types` is still an inner join, so manually created
  `calendar_events` rows (`activity_type_id IS NULL`) remain invisible to
  members, and `bookMemberOnSession` still can't book them either (same
  inner join) — closing that gap needs booking-logic changes beyond a read
  model and isn't part of the agreed stage 5 scope on the issue thread; it's
  the discovery gap `docs/architecture.md`'s CalendarEvent Unification
  section flagged against the plan's earlier numbering, now confirmed still
  open. Stage 6, "local filters": `GET /me/schedule` gains two optional
  query params, additive next to the existing `x-center-id`-header-driven
  scoping rather than replacing it — `center_id` narrows results to one
  center the member is already assigned to (still keeping `center_id IS
  NULL` rows visible, per #478), returning 403 if the requested center falls
  outside the header-derived `allowedCenterIds`/`centerId`; `trainer_
  membership_id` narrows to one trainer's occurrences. Per the issue
  thread's explicit requirement that this filter stay local to the calendar
  and never move the global `CenterSwitcher`, both params only affect this
  one request — no header/session state changes. New `GET /me/trainers`
  gives members a read-only `{id, name}` list of coach-role
  `gym_memberships` for the filter panel's trainer dropdown (the existing
  `/trainers` router is staff-only via `requireModuleAccess('ORGANIZATION')`,
  so it can't be reused as-is). No schema or frontend changes: this stage is
  API-only, per the plan — the member calendar UI stage (stage 7) is what
  will wire up the actual filter panel. Stage 7, "member calendar UI":
  `GET /me/schedule` gains an additive `LEFT JOIN centers` returning
  `center_id`/`center_name` (stage 6 could already filter by center but
  never returned it for display). The `/calendar` page gets a compact
  Filter button (collapsed by default, per the thread's mobile-space
  requirement) opening a dropdown panel with independent, combinable
  Center/Trainer selects wired to stage 6's `center_id`/
  `trainer_membership_id` query params — the center select defaults once to
  `AppContext.activeCenterId` (the global `CenterSwitcher`'s current value)
  but never writes back to it, matching the thread's "calendar-local, never
  affects other sections" requirement; a dot on the button flags an active
  filter. The event grid shows an aggregate occupancy count
  (`booked/capacity`) per cell, and the bottom-sheet detail panel gains the
  three-badge breakdown from the thread's Q3 answer — `status`,
  `occupancy_status` (+ count), and (only when not `disabled`)
  `waitlist_status` with an aggregate `waitlist_count` — additive alongside
  the existing `availability_state`-keyed action buttons, which are
  unchanged. Stage 8, "Home + My Bookings, and the Notifications entry
  point": the plan's own premise — that Home/My Bookings hardcode
  `kind: 'session'` — turned out to be stale; both already consume `/me
  /schedule` (stage 5's unified read model) and have no `kind` reference
  left anywhere. The real remaining gaps were (a) neither screen displayed
  which center a booking is at, even though stage 7 already added
  `center_id`/`center_name` to `/me/schedule` for the calendar UI, and (b)
  the "Notifications" entry point the thread asked for already existed as
  a full feature (#194 — `member_notifications` table, `/me/notifications`
  endpoints, the `/notifications` page, `AppContext`'s unread count) but
  had no working nav entry: `TopBar`'s unread dot sat on the Profile
  button, which links to `/profile`. Fixed both: Home
  (`app/[locale]/page.tsx`) and My Bookings (`app/[locale]/schedule
  /page.tsx`) now render `center_name` alongside the existing room/space
  name; `TopBar.tsx` gets its own bell button (using the previously-unused
  `nav.alerts` i18n key) linking to `/notifications`, with the unread
  badge moved onto it. Also filled two missing `notifications.type_*` i18n
  keys (`event_cancelled`/`event_updated`) that stage 4's cancellation
  notifications had been firing since #575 but which rendered as a raw,
  untranslated type string in both Home's alert banner and the
  Notifications page, in all three locales. No schema or API changes. See
  `docs/decisions.md` #13 for why the pre-existing `member_notifications`
  feature needed no rebuilding. Stage 9, "tests + docs": added backend
  regression coverage in `member-calendar.test.ts` proving no `/me/*`
  response can leak another member's booking identity (only aggregate
  `booked_count`/`waitlist_count`) and that one member's booking never
  flips `availability_state`/`status` for another member on the same
  event; added the Member app's first test file
  (`apps/member/src/test/locales.test.ts`, with a new `vitest` devDependency
  and `test` script for `apps/member`) asserting every `en`/`es`/`ca`
  translation key referenced anywhere in the app resolves in all three
  locales and that the three locale files carry an identical key set.
  Corrected a stale `docs/decisions.md` #9 bullet that had said
  `member_notifications`'s `event_cancelled`/`event_updated` types would
  never be written again — stage 4 (#575) already reactivated them — and
  added #14 recording the plan's completion, including the two gaps this
  stage deliberately leaves open rather than silently dropping: the
  `INNER JOIN activity_types` member-discovery gap (flagged since stage 1)
  and the calendar detail panel's missing explicit `Booked` badge (booking
  is currently implied only by which action button renders, while My
  Bookings already shows one). This closes #503.
- **#555 (done)**: Aligned Membership Plans' look and feel with Promotions —
  presentation only, no migration, no API/business-logic changes. List
  header now carries a lifecycle-status `StatusFilter` (reusing the
  already-supported `GET /membership-plans?lifecycle_status=`, same as
  Promotions), and the row's expand chevron moved from the first column to
  just before the actions menu, matching Promotions' column order. Card
  border/radius, the row/column-header padding, and each expanded section's
  divider now match Promotions' `cardSt`/`rowSt`/`subSectionSt`/
  `sectionLabelSt` styling (a border above each section instead of below its
  label); the purple "editing" card highlight is now reserved for the
  not-yet-saved new-plan row only, mirroring Promotions' own new-row
  treatment. Wording aligned with Promotions for the shared charge-benefit
  action labels (`no_benefit`/`waive`/`fixed_discount`, same enum on both
  routers) and the "no charge benefits" empty state, in en/es/ca. Also fixed
  a few pre-existing i18n bugs surfaced along the way: the Included Services
  section header referenced a missing `plans.section_services` key (raw key
  shown to English users; es/ca happened to still carry old duplicate
  entries) — repointed to the correct `plans.section_allowances`; the
  Activity picker's label and "Select…" placeholder were hardcoded English
  instead of translated; en.json was missing `plans.add_price` entirely.
- **Done (continued)**: #550 stage 1 of 4 (Redistribute Promotion Benefits by
  Sellable Item Type and Frequency — schema + classification helper only, no
  routers/frontend yet). New `promotion_session`/`promotion_oneoff`/
  `promotion_periodical` tables (migration 155), each `gym_id`+`promotion_id`+
  `gym_charge_id FK→gym_charges` (real Sellable Items, not the old
  `charge_types` pseudo-catalog) + `quantity`, replacing the "quantity
  granted" half of the legacy `promotion_period_benefits`/
  `promotion_included_benefits` tables; shared pure
  `classifySellableItem()`/`isRecurringFrequency()` helper in
  `api/src/domain/sellableItemClassification.ts` (sessions type → session;
  else recurring `billing_frequency` → periodical; else → oneoff), unit
  tests in `sellable-item-classification.test.ts`. Skeleton snapshot tables
  (migration 156, `user_membership_promotion_{session,oneoff,periodical}_snapshot`)
  created schema-only for a future assignment-flow ticket — not yet written
  to or read, kept as real rows (not JSON) alongside the existing
  `user_membership_promotions.snapshot` JSON column (#511 stage 2) so a
  member's granted benefits stay inspectable per-row. Follow-up migration 158
  hard-deletes (`TRUNCATE`) the stale `promotion_period_benefits`/
  `promotion_included_benefits` data per the issue owner's explicit
  "start from scratch, no historical preservation" clarification — the two
  tables are truncated, not dropped, since `promotion-details.ts`/
  `promotions.ts`/the admin Promotions page still read/write them until the
  API cutover lands in a later stage; `promotion_charge_benefits` (the
  discount mechanism) is untouched, out of scope. Stages 2–4 (backend
  validation/endpoints, three-section frontend, tests/docs polish) remain.
- **Done (continued)**: #550 stage 2 of 4 (backend validation + endpoints).
  Three new sub-resources on `promotion-details.ts` —
  `GET`/`PUT /promotions/:id/session-benefits`, `/oneoff-benefits`,
  `/periodical-benefits` — backed by stage 1's `promotion_session`/
  `promotion_oneoff`/`promotion_periodical` tables; same one-time-grant shape
  as `/included-benefits` but keyed to a real Sellable Item (`gym_charge_id`),
  with `PUT` enforcing tenant ownership, active/non-deleted status, no
  duplicate `gym_charge_id` per request, a positive integer `quantity`, and
  section-classification (`classifySellableItem()` must match the endpoint's
  category). `POST /:id/duplicate` now also copies all three tables.
  Integration tests added to `promotions.test.ts` (tenant isolation, auth,
  happy path, classification/active-status/duplicate-id rejection, 403 for
  non-admin, duplicate). The legacy `/period-benefits`
  (non-Membership-Fee)/`/included-benefits` endpoints from stage 1 are left
  untouched and still live alongside the three new ones — retiring them lands
  with the stage-3 frontend cutover, not before, so the currently-live
  Promotions UI never breaks mid-migration. Stages 3 (three-section frontend)
  and 4 (tests/docs polish) remain.
- **Done**: #550 stage 3 of 4 (three-section frontend cutover), with stage 4
  (tests/docs polish) folded in rather than landing separately — this
  completes the ticket. Admin Promotions page: Included Benefits and the
  generic (non-Membership-Fee) Period Benefits sections are replaced by three
  Sellable-Item-keyed sections — Session Benefits, One-off Benefits, Period
  Benefits — reading/writing stage 2's `/session-benefits`/`/oneoff-benefits`/
  `/periodical-benefits`. `GET /sellable-items` now returns a server-computed
  `benefit_category` field (`classifySellableItem()` via a new
  `attachBenefitCategory()` in `sellable-items.ts`) so the item pickers group
  by that field instead of re-deriving the type/frequency rule client-side, per
  CLAUDE.md's "no duplicated business logic in the frontend." Period Benefits'
  Frequency column is read-only, sourced live from the Sellable Item's own
  `billing_frequency` (never a per-benefit field, matching stage 1's schema).
  New selections come from active, tenant-scoped Sellable Items only; an item
  already selected on a promotion stays visible/editable even after going
  inactive (fixed a stage-2 gap: `PUT` on the three endpoints now only
  requires *newly* added `gym_charge_id`s to be active, mirroring the Suitable
  Membership Plans `/plans` PUT pattern) — this was the ticket's explicit
  "existing selected items must remain visible when editing" requirement.
  Backend: the legacy `charge_types`-pseudo-catalog-driven `/period-benefits`
  (bulk + single-row) and `/included-benefits` endpoints are removed outright;
  `promotion_period_benefits` stays (still backs the untouched
  `/membership-fee-benefit` singleton, #551) and `promotion_included_benefits`
  stays but is now unused (no remaining write path; no data loss beyond what
  migration 158 already reset, per the issue owner's "start from scratch"
  clarification). New/updated tests in `promotions.test.ts` (removed tests for
  the retired endpoints, added the "already-selected item stays selectable
  after going inactive" case for all three categories) and `gym-charges.test.ts`
  (`benefit_category` classification + presence on list/detail/create/update).

## Decisions

- **Database (updated 2026-07-03)**: migrate from Neon PostgreSQL to **Oracle HeatWave MySQL**
  (paid tier, ~€50/mo) for predictable pricing and single-vendor infra. Tracked as
  **Phase M ([#45](https://github.com/cordel-app/gymdesk/issues/45)–[#49](https://github.com/cordel-app/gymdesk/issues/49))**, which **blocks Phase 1**. MySQL consequences for later tickets:
  partial unique indexes (P1.5, P2.5) become generated column + unique index; `jsonb`/`inet`
  (P6.1) become `JSON`/`VARCHAR(45)`; `RETURNING` is replaced by insert + select helpers.
- **Multi-location (updated 2026-07-15)**: shipped as **Phase 9 — Centers (#59)**, superseding
  the deferred Phase 7 `gym_locations` design (never built). `Center` is the single location
  concept; `Gym` remains the tenant boundary.
- **Payments**: internal `billing_events` ledger first (staff-recorded); MONEI is the active PSP for Phase 8 (replaced Stripe/Paycomet). Gymdesk owns all subscription logic; MONEI only tokenizes and charges. PCI scope minimized via isolated `fitness-pay` container at `pay.vdicube.com`.
- `fares` → `membership_plans` and `subscriptions` → `user_memberships` **evolve in place**
  with data-carrying migrations; old routes/pages are replaced.
- Trainers are existing `coach`-role rows in `gym_memberships`; trainer data (specialities)
  attaches there.
- Lookup vocabularies (`benefit_types`, `charge_types`, `action_types`) are global tables
  (no `gym_id`), seeded in their migrations. Statuses are `text` + CHECK constraints.
- Exercises/muscles catalog is **per-gym**, with an idempotent `import-defaults` seed endpoint.
- "Replaces" tickets keep the old route mounted until the phase's frontend ticket lands,
  then delete it.

## Conventions

Every feature ticket follows `docs/feature-patterns.md`: migration → Express router
(`requireRole` guards, `gym_id` filter, `ER_DUP_ENTRY`→409) → register in `api/src/index.ts` →
admin page (Members page as staff-level/soft-delete template, Plans page as admin-only
template) → Sidebar → i18n (en/es/ca).

## Phases

### Phase M — MySQL migration (blocks Phase 1)
| Ticket | Issue | Size | Depends on |
|---|---|---|---|
| M1 Provision HeatWave MySQL on OCI (backups + PITR) | [#45](https://github.com/cordel-app/gymdesk/issues/45) | S | — |
| M2 Replace node-pg-migrate with Knex targeting MySQL | [#46](https://github.com/cordel-app/gymdesk/issues/46) | M | — |
| M3 Port backend data layer and queries to mysql2 | [#47](https://github.com/cordel-app/gymdesk/issues/47) | L | #46 |
| M4 Migrate data from Neon to HeatWave | [#48](https://github.com/cordel-app/gymdesk/issues/48) | M | #45 #47 |
| M5 Cutover deploy, CI on MySQL, roadmap amendments | [#49](https://github.com/cordel-app/gymdesk/issues/49) | M | #48 |

### Phase 0 — Foundation
| Ticket | Issue | Size | Depends on |
|---|---|---|---|
| P0.1 Extract shared DataTable, CrudModal, ConfirmDialog | [#2](https://github.com/cordel-app/gymdesk/issues/2) | M | — |
| P0.2 StatusBadge + status-filter convention | [#3](https://github.com/cordel-app/gymdesk/issues/3) | S | #2 |
| P0.3 Member app navigation shell | [#4](https://github.com/cordel-app/gymdesk/issues/4) | M | — |

### Phase 0a — Structured logging (pino) ✅
| Ticket | Issue | Size | Depends on |
|---|---|---|---|
| Install pino structured logging in the API | [#177](https://github.com/cordel-app/gymdesk/issues/177) | S | — |

### Phase 0b — Log shipping (Grafana Alloy → Grafana Cloud Loki) ✅
| Ticket | Issue | Size | Depends on |
|---|---|---|---|
| Set up Alloy on VPS → Grafana Cloud Loki | [#178](https://github.com/cordel-app/gymdesk/issues/178) | M | #177 |

Config files: `infra/alloy/config-corback.alloy`, `infra/alloy/config-corfront.alloy`, `infra/alloy/alloy.service`.
Deploy: push to `main` triggers `.github/workflows/deploy-alloy.yml`; write `GRAFANA_CLOUD_API_KEY=<token>` to `~/.config/alloy/secrets` on each VPS on first install.
Grafana Cloud: Loki instance `xavieregea-logs`, user `969010`, push URL `https://logs-prod-012.grafana.net/loki/api/v1/push`.

### Phase 1 — Membership plans & billing core (replaces fares, subscriptions)
| Ticket | Issue | Size | Depends on |
|---|---|---|---|
| P1.1 ⚠️ Migrate fares → membership_plans | [#5](https://github.com/cordel-app/gymdesk/issues/5) | M | — |
| P1.2 Replace Fares page with Plans page | [#6](https://github.com/cordel-app/gymdesk/issues/6) | M | #5 #2 #3 |
| P1.3 membership_plan_prices with validity windows | [#7](https://github.com/cordel-app/gymdesk/issues/7) | M | #6 |
| P1.4 benefit_types + membership_plan_benefits | [#8](https://github.com/cordel-app/gymdesk/issues/8) | M | #7 |
| P1.5 ⚠️ Migrate subscriptions → user_memberships | [#9](https://github.com/cordel-app/gymdesk/issues/9) | L | #5 |
| P1.6 charge_types + billing_events ledger | [#10](https://github.com/cordel-app/gymdesk/issues/10) | M | #9 |
| P1.7 Replace Subscriptions page with Memberships page | [#11](https://github.com/cordel-app/gymdesk/issues/11) | L | #9 #10 #2 |
| P1.8 Member app: my membership + payment history | [#12](https://github.com/cordel-app/gymdesk/issues/12) | M | #11 #4 |

### Phase 2 — Classes v2 (replaces classes, bookings v1)
| Ticket | Issue | Size | Depends on |
|---|---|---|---|
| P2.1 Rooms CRUD | [#13](https://github.com/cordel-app/gymdesk/issues/13) | S | #2 |
| ~~P2.2 Specialities + trainer_specialities~~ | [#14](https://github.com/cordel-app/gymdesk/issues/14) | M | #13 | _(removed in #219)_ |
| P2.3 class_types CRUD | [#15](https://github.com/cordel-app/gymdesk/issues/15) | M | #14 |
| P2.4 ⚠️ Migrate classes → class_sessions | [#16](https://github.com/cordel-app/gymdesk/issues/16) | L | #15 |
| P2.5 ⚠️ Bookings v2: waitlist, capacity, attendance | [#17](https://github.com/cordel-app/gymdesk/issues/17) | L | #16 |
| P2.6 Replace Classes/Bookings pages with Schedule | [#18](https://github.com/cordel-app/gymdesk/issues/18) | L | #17 #2 |
| P2.7 class_type_user_memberships plan-access control | [#19](https://github.com/cordel-app/gymdesk/issues/19) | M | #17 #9 |
| P2.8 Member app: schedule, booking, waitlist | [#20](https://github.com/cordel-app/gymdesk/issues/20) | L | #19 #4 |

### Phase 3 — Class packages
| Ticket | Issue | Size | Depends on |
|---|---|---|---|
| P3.1 class_packages catalog CRUD | [#21](https://github.com/cordel-app/gymdesk/issues/21) | S | #2 |
| P3.2 user_class_packages + credit transactions | [#22](https://github.com/cordel-app/gymdesk/issues/22) | L | #21 #10 |
| P3.3 Consume/refund credits on booking lifecycle | [#23](https://github.com/cordel-app/gymdesk/issues/23) | L | #22 #19 |
| P3.4 Member app: my packages + credit balance | [#24](https://github.com/cordel-app/gymdesk/issues/24) | M | #23 #12 |

### Phase 4 — Promotions
| Ticket | Issue | Size | Depends on |
|---|---|---|---|
| P4.1 action_types + promotions CRUD | [#25](https://github.com/cordel-app/gymdesk/issues/25) | M | #2 |
| P4.2 Promotion plan targeting + charge benefits | [#26](https://github.com/cordel-app/gymdesk/issues/26) | M | #25 #10 |
| P4.3 promotion_period_benefits | [#27](https://github.com/cordel-app/gymdesk/issues/27) | S | #25 |
| P4.4 Apply promotions to memberships (price calc) | [#28](https://github.com/cordel-app/gymdesk/issues/28) | L | #26 #11 |
| P4.5 Backoffice + member app promotion surfaces | [#29](https://github.com/cordel-app/gymdesk/issues/29) | M | #28 #12 |

### Phase 5 — Workouts & training
| Ticket | Issue | Size | Depends on |
|---|---|---|---|
| P5.1 muscles, exercises, exercise_muscles | [#30](https://github.com/cordel-app/gymdesk/issues/30) | L | #2 |
| P5.2 workouts + workout_exercises builder | [#31](https://github.com/cordel-app/gymdesk/issues/31) | L | #30 |
| P5.3 training_plan_templates | [#32](https://github.com/cordel-app/gymdesk/issues/32) | M | #31 |
| P5.4 Assign training_plans to members | [#33](https://github.com/cordel-app/gymdesk/issues/33) | M | #32 |
| P5.5 workout_logs + member training endpoints | [#34](https://github.com/cordel-app/gymdesk/issues/34) | M | #33 |
| P5.6 Member app: Training tab + set logging | [#35](https://github.com/cordel-app/gymdesk/issues/35) | L | #34 #4 |

### Phase 6 — Audit log
| Ticket | Issue | Size | Depends on |
|---|---|---|---|
| P6.1 audit_logs table + recordAudit helper | [#36](https://github.com/cordel-app/gymdesk/issues/36) | M | — |
| P6.2 Instrument high-value mutation routes | [#37](https://github.com/cordel-app/gymdesk/issues/37) | M | #36 + phases 1–4 |
| P6.3 Backoffice audit viewer | [#38](https://github.com/cordel-app/gymdesk/issues/38) | M | #37 #2 |

### Phase 7 — Multi-location (superseded by Phase 9 / #59, 2026-07-15)
`gym_locations` was never built — closed in favor of `Center` (see Phase 9 above).

| Ticket | Issue | Size | Depends on |
|---|---|---|---|
| ~~P7.1 gym_locations CRUD~~ | [#39](https://github.com/cordel-app/gymdesk/issues/39) (closed) | S | — |
| ~~P7.2 Locations on rooms, trainers, class types~~ | [#40](https://github.com/cordel-app/gymdesk/issues/40) (closed) | M | #39 |
| ~~P7.3 Per-location plan pricing + member filter~~ | [#41](https://github.com/cordel-app/gymdesk/issues/41) (closed) | M | #40 #7 |

### Phase 9 — Centers (#59)
| Ticket | Issue | Size | Depends on |
|---|---|---|---|
| Center + member_centers + center_id backfill (backend) | [#59](https://github.com/cordel-app/gymdesk/issues/59) | L | — |
| Centers/Resources/Events admin UI + Member center assignment | [#59](https://github.com/cordel-app/gymdesk/issues/59) | L | backend |
| Member app center plumbing | [#59](https://github.com/cordel-app/gymdesk/issues/59) | S | backend |

### Phase 8 — MONEI payments
| Ticket | Issue | Size | Depends on |
|---|---|---|---|
| ~~P8.1 Payment provider abstraction layer (MoneiProvider)~~ | [#179](https://github.com/cordel-app/gymdesk/issues/179) | M | — | ✅ done |
| ~~P8.2 DB migrations — payment_methods + payment_requests~~ | [#180](https://github.com/cordel-app/gymdesk/issues/180) | S | #179 | ✅ done |
| ~~P8.3 payment-requests + payment-page token + webhooks API~~ | [#181](https://github.com/cordel-app/gymdesk/issues/181) | M | #179 #180 | ✅ done |
| ~~P8.4 Isolated payment page app (vanilla JS + Monei iframe)~~ | [#204](https://github.com/cordel-app/gymdesk/issues/204) | M | #179 #181 | ✅ done |
| ~~P8.5 Infra — Docker + deploy for fitness-pay container~~ | [#205](https://github.com/cordel-app/gymdesk/issues/205) | S | #204 | ✅ done |
| ~~P8.6 Admin app — MemberPaymentsModal + payment-providers page~~ | [#182](https://github.com/cordel-app/gymdesk/issues/182) | M | #181 | ✅ done |
| ~~P8.7 Member app — payment banner, success and error pages~~ | [#183](https://github.com/cordel-app/gymdesk/issues/183) | M | #181 | ✅ done |
| ~~P8.8 Recurring billing run — MIT charges~~ | [#184](https://github.com/cordel-app/gymdesk/issues/184) | M | #179 #180 #181 | ✅ done |
| ~~P8.9 Cash payment recording + fiscal receipt generation~~ | [#185](https://github.com/cordel-app/gymdesk/issues/185) | M | #182 | ✅ done |
| ~~P8.10 nginx IP allowlist for /billing/ endpoints~~ | [#306](https://github.com/cordel-app/gymdesk/issues/306) | S | #184 | ✅ done |

_(Old Stripe tickets #42–#44 are closed and superseded by this phase.)_

## Member app follow-ups (spike #85, 2026-07-19)

**Already shipped** (member app parity with roadmap items): P2.8 schedule/booking (#20 closed), P1.8 membership + billing history (#12 closed), P5.6 training tab + set logging (#35 closed), P3.4 my packages (#24 closed).

**Gaps identified and new issues created** (prioritised by member value vs. backend readiness):

| Ticket | Issue | Size | Depends on |
|---|---|---|---|
| Member home dashboard (upcoming booking + membership snapshot) | [#105](https://github.com/cordel-app/gymdesk/issues/105) | S | — |
| Member app: my class packages & credit balance | [#106](https://github.com/cordel-app/gymdesk/issues/106) | M | — |
| Member app: profile page (view & edit personal data) | [#107](https://github.com/cordel-app/gymdesk/issues/107) | M | — |

## Critical path

- **Billing**: P1.1 → P1.5 → P1.6 — the ledger unblocks packages (Phase 3), promotions (Phase 4), and MONEI payments (Phase 8).
- **Classes**: P2.3 → P2.4 → P2.5 — the session model unblocks everything class-related.
- Phases 3, 4, and 5 are parallelizable once their Phase 1/2 dependencies land.
